// PostgreSQL — replication
// Structured interview content — run: node scripts/generate-db-tracks.mjs
// 15 questions

export const questions = [
  {
    id: "pg-streaming-replication-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "How does PostgreSQL streaming replication work between primary and standby?",
    answer: "## Interview Answer\nStandby connects to primary walsender process; primary streams WAL records as they are generated. Standby walreceiver writes to pg_wal and startup process replays via recovery. Synchronous mode waits for standby flush/apply per synchronous_standby_names. Replication uses physical WAL — byte-for-byte replica.\n\n## Detailed Explanation\nPrimary pg_stat_replication shows connected standbys, sent_lsn, write_lag, flush_lag, replay_lag. Standby hot_standby=on allows read queries during recovery. recovery_min_apply_delay can lag apply intentionally. Cascading replication: standby feeds downstream standbys.\n\n## Production Perspective\nNew standby provisioned via pg_basebackup -R -X stream -C -S standby1; slot ensured WAL retention during catch-up. pg_stat_replication replay_lag stabilized under 200ms after initial sync.\n\n## Common Follow-up Questions\n• Difference sent_lsn vs replay_lsn?\n• How to add standby without downtime?\n• Hot standby feedback purpose?\n• Cascading vs direct replication?\n\n## Common Mistakes\n• No replication slot during pg_basebackup catch-up — WAL removed\n• promote standby without checking timeline divergence\n• Monitoring only flush_lag ignoring replay_lag on hot standby queries\n• max_wal_senders too low blocking new standbys\n\n## Senior Engineer Insights\nAlways create physical replication slot named per standby. replay_lag is what users feel on read replicas.\n\n## Key Commands\n• SELECT application_name, state, sync_state, sent_lsn, replay_lsn, replay_lag FROM pg_stat_replication;\n• pg_basebackup -h primary -D /var/lib/pgsql/standby -U replicator -Fp -Xs -P -R\n• SELECT pg_create_physical_replication_slot('standby1');\n\n## Best Practices\n• Use replication slots for each standby\n• Monitor replay_lag not just connection state\n• Automate standby provisioning with pg_basebackup\n• Test promotion procedure quarterly",
    command: "SELECT application_name, state, sync_state, sent_lsn, replay_lsn, replay_lag FROM pg_stat_replication;\npg_basebackup -h primary -D /var/lib/pgsql/standby -U replicator -Fp -Xs -P -R\nSELECT pg_create_physical_replication_slot('standby1');",
    tags: ["postgresql","streaming-replication","easy","very-common"],
  },
  {
    id: "pg-streaming-replication-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "Diagnose and reduce replication lag on a hot standby.",
    answer: "## Interview Answer\nMeasure write_lag, flush_lag, replay_lag in pg_stat_replication. Causes: standby IO slower than primary WAL generation, long queries blocking recovery, insufficient wal_receiver/wal_writer, network bandwidth, or hot standby conflicts. Check pg_stat_database_conflicts and standby pg_is_in_recovery().\n\n## Detailed Explanation\nReplay is single-threaded for WAL apply — CPU rarely bottleneck; IO and lock conflicts are. standby max_standby_streaming_delay cancels queries blocking recovery. wal_compression on primary reduces network. pg_wal_receiver_status on standby shows receive rate.\n\n## Production Perspective\nReplica replay_lag climbed to 30s during primary checkpoint spikes. Standby on gp2 with low IOPS; migrated to io2 with 16k IOPS, added recovery_prefetch (PG 15), lag dropped to sub-second.\n\n## Common Follow-up Questions\n• recovery_prefetch and wal_decode in PG 15+?\n• Impact of large transactions on replay lag?\n• Parallel apply for logical vs physical?\n• Synchronous rep latency vs async lag?\n\n## Common Mistakes\n• Read-heavy workload on standby without conflict monitoring\n• Standby disk tier lower than primary\n• Ignoring checkpoint-induced lag spikes\n• max_standby_streaming_delay=-1 forever\n\n## Senior Engineer Insights\nLag spikes at checkpoint = standby IO problem. Steady lag = WAL generation exceeds replay — IO or conflicts.\n\n## Key Commands\n• SELECT * FROM pg_stat_replication;\n• SELECT * FROM pg_stat_database_conflicts;\n• SELECT pg_is_in_recovery();\n• SHOW max_standby_streaming_delay;\n\n## Best Practices\n• Match or exceed primary storage IOPS on standbys\n• Monitor pg_stat_database_conflicts on replicas\n• Use dedicated replication network if cross-AZ\n• Set sensible max_standby_streaming_delay with alerting",
    command: "SELECT * FROM pg_stat_replication;\nSELECT * FROM pg_stat_database_conflicts;\nSELECT pg_is_in_recovery();\nSHOW max_standby_streaming_delay;",
    tags: ["postgresql","streaming-replication","medium","common"],
  },
  {
    id: "pg-streaming-replication-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "What are replication slots and why should every standby use one?",
    answer: "## Interview Answer\nReplication slots track consumer LSN on primary — WAL segments not recycled until consumed. Physical slots tied to standby restart_lsn; logical slots to confirmed_flush_lsn. pg_replication_slots shows retained WAL. Without slot, standby falling behind loses WAL and needs rebuild.\n\n## Detailed Explanation\npg_create_physical_replication_slot creates durable slot surviving restarts. inactive slot still retains WAL — operational hazard. max_replication_slots limits count. max_slot_wal_keep_size bounds disk risk per slot.\n\n## Production Perspective\nStandby rebuild required after 4h network partition — no slot, WAL gone. Post-incident: mandatory slot per standby via Patroni config, alert on pg_wal_lsn_diff > 1GB.\n\n## Common Follow-up Questions\n• Drop slot safely when decommissioning standby?\n• Logical slot pgoutput vs wal2json?\n• Temporary vs permanent slots?\n• Slot sync with pg_basebackup -C?\n\n## Common Mistakes\n• Orphan slots after failed experiments\n• Creating logical slots without consumer monitoring\n• max_replication_slots default too low for HA + CDC\n• Deleting slot while standby still running\n\n## Senior Engineer Insights\nSlot = contract that primary keeps WAL for consumer. Orphan slot is a disk-fill time bomb.\n\n## Key Commands\n• SELECT * FROM pg_replication_slots;\n• SELECT pg_create_physical_replication_slot('replica1', true);\n• SELECT pg_drop_replication_slot('old_slot');\n• SELECT pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) FROM pg_replication_slots;\n\n## Best Practices\n• One named slot per standby and logical consumer\n• Alert inactive slots with retained WAL\n• Document slot ownership in infrastructure registry\n• Set max_slot_wal_keep_size as safety valve",
    command: "SELECT * FROM pg_replication_slots;\nSELECT pg_create_physical_replication_slot('replica1', true);\nSELECT pg_drop_replication_slot('old_slot');\nSELECT pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) FROM pg_replication_slots;",
    tags: ["postgresql","streaming-replication","medium","very-common"],
  },
  {
    id: "pg-streaming-replication-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "Configure synchronous replication with quorum for RPO zero across multiple AZs.",
    answer: "## Interview Answer\nsynchronous_standby_names = 'FIRST 2 (sync1, sync2, sync3)' requires two listed standbys to acknowledge commit. ANY 1 (...) allows any one of set. pg_stat_replication sync_state=sync on acknowledged standbys. Trade-off: commit latency = slowest sync standby RTT + flush time.\n\n## Detailed Explanation\nQuorum protects against single AZ loss without blocking on one slow replica. priority in synchronous_standby_names orders preference. synchronous_commit=remote_apply strongest — waits apply not just flush. Cloud: cross-AZ latency 1-3ms adds to commit p99.\n\n## Production Perspective\nFinancial ledger required sync rep; FIRST 1 caused commits blocked when single AZ standby maintenance. Switched to FIRST 2 of 3 sync standbys — survived AZ outage with zero committed transaction loss.\n\n## Common Follow-up Questions\n• Failover when sync standby down — commits block?\n• synchronous_commit remote_write vs on?\n• Patroni synchronous_mode vs PostgreSQL native?\n• Aurora synchronous replica semantics?\n\n## Common Mistakes\n• FIRST 1 with only one sync standby — no AZ redundancy\n• All standbys async — believing HA without RPO guarantee\n• Not testing commit latency impact before enabling sync\n• Forgetting to update synchronous_standby_names after topology change\n\n## Senior Engineer Insights\nQuorum sync is the sweet spot for multi-AZ — explain FIRST 2 of 3 with commit latency math.\n\n## Key Commands\n• ALTER SYSTEM SET synchronous_standby_names = 'FIRST 2 (az1, az2, az3)';\n• SELECT application_name, sync_state, sync_priority FROM pg_stat_replication;\n• SELECT pg_reload_conf();\n\n## Best Practices\n• Use quorum sync for multi-AZ RPO zero\n• Load test commit latency with sync enabled\n• Automate synchronous_standby_names updates in failover tooling\n• Monitor sync_state and commit latency p99",
    command: "ALTER SYSTEM SET synchronous_standby_names = 'FIRST 2 (az1, az2, az3)';\nSELECT application_name, sync_state, sync_priority FROM pg_stat_replication;\nSELECT pg_reload_conf();",
    tags: ["postgresql","streaming-replication","hard","common"],
  },
  {
    id: "pg-streaming-replication-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Recover a standby that has fallen behind or has timeline divergence after promotion.",
    answer: "## Interview Answer\nIf WAL missing: rebuild via pg_basebackup or pgBackRest restore. Timeline divergence after promote/failback: pg_rewind re-syncs diverged standby to new primary timeline using WAL overlap. pg_upgrade and pg_wal_replay_resume for controlled catch-up. Never manually edit pg_control without expert guidance.\n\n## Detailed Explanation\npg_rewind requires wal_log_hints or data_checksums enabled at init. Standby rejoins as replica after rewind + restart with primary_conninfo. Multiple promotions create timeline history files in pg_wal/archive. pg_switch_wal before promotion ensures clean segment boundary.\n\n## Production Perspective\nFailed back to old primary after split-brain — timelines diverged. pg_rewind --target-pgdata=/var/lib/pgsql/data --source-server=connstr succeeded; rejoined as replica in 20 min vs 6h full rebuild.\n\n## Common Follow-up Questions\n• When pg_rewind fails?\n• pg_verifybackup role?\n• Rebuild vs rewind decision tree?\n• pg_resetwal danger scenarios?\n\n## Common Mistakes\n• Manual copy of data directory without matching timeline\n• Skipping data_checksums then needing rewind later\n• Promoting multiple standbys without fencing old primary\n• pg_resetwal as first troubleshooting step\n\n## Senior Engineer Insights\nEnable data_checksums + wal_log_hints at cluster birth — enables pg_rewind and corruption detection.\n\n## Key Commands\n• pg_rewind --target-pgdata=$PGDATA --source-server=\"host=primary dbname=postgres user=rewind\"\n• pg_controldata $PGDATA | grep Timeline\n• pg_basebackup -h newprimary -D $PGDATA -Fp -Xs -P -R\n\n## Best Practices\n• Enable data_checksums on new clusters\n• Document rewind vs rebuild criteria in runbook\n• Fence old primary before any promotion\n• Test pg_rewind in staging after simulated split-brain",
    command: "pg_rewind --target-pgdata=$PGDATA --source-server=\"host=primary dbname=postgres user=rewind\"\npg_controldata $PGDATA | grep Timeline\npg_basebackup -h newprimary -D $PGDATA -Fp -Xs -P -R",
    tags: ["postgresql","streaming-replication","hard","rare"],
  },
  {
    id: "pg-logical-replication-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "What is logical replication and how does it differ from streaming replication?",
    answer: "## Interview Answer\nLogical replication decodes WAL to row-level changes via replication slot and publishes to subscriber via pgoutput plugin. Supports table-level publication, cross-version upgrade, and selective replication. Physical streaming replicates entire cluster byte-identical; logical allows heterogeneous targets and transformations.\n\n## Detailed Explanation\nPublisher: CREATE PUBLICATION, wal_level=logical. Subscriber: CREATE SUBSCRIPTION connects to publisher slot. Apply workers insert/update/delete on subscriber. Initial sync copies existing data. Conflicts on subscriber possible with concurrent writes. pg_stat_subscription tracks apply lag.\n\n## Production Perspective\nMigrating PG 13 → 16: logical replication publication on old primary, subscription on new cluster, cutover after lag zero — near-zero downtime vs pg_dump.\n\n## Common Follow-up Questions\n• wal_level logical WAL overhead?\n• Publication FOR ALL TABLES vs specific?\n• Bidirectional logical replication pitfalls?\n• DDL replication support PG 15+?\n\n## Common Mistakes\n• wal_level not logical before enabling publication\n• No primary key on published table — UPDATE/DELETE cannot apply\n• Writing to subscriber tables also receiving replication — conflicts\n• Ignoring replication slot disk on publisher\n\n## Senior Engineer Insights\nLogical rep is the modern upgrade path and CDC mechanism — know publication/subscription/slot triangle.\n\n## Key Commands\n• CREATE PUBLICATION orders_pub FOR TABLE orders, order_items;\n• CREATE SUBSCRIPTION orders_sub CONNECTION 'host=pub dbname=app' PUBLICATION orders_pub;\n• SELECT * FROM pg_stat_subscription;\n\n## Best Practices\n• Ensure PRIMARY KEY on all published tables\n• Monitor publisher slot lag and retained WAL\n• Use logical rep for major version upgrades\n• Avoid concurrent writes on subscriber without conflict strategy",
    command: "CREATE PUBLICATION orders_pub FOR TABLE orders, order_items;\nCREATE SUBSCRIPTION orders_sub CONNECTION 'host=pub dbname=app' PUBLICATION orders_pub;\nSELECT * FROM pg_stat_subscription;",
    tags: ["postgresql","logical-replication","easy","very-common"],
  },
  {
    id: "pg-logical-replication-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do you handle logical replication conflicts and apply errors on the subscriber?",
    answer: "## Interview Answer\nSubscriber apply errors pause subscription — pg_stat_subscription shows worker state. Conflicts when subscriber modified same row: default uses ERROR and stops. Options: disable subscription, fix data, skip LSN with pg_replication_origin_advance (dangerous), or use conflict-free subscriber (read-only). PG 16+ improved conflict policies.\n\n## Detailed Explanation\nCommon errors: duplicate key, missing row for UPDATE, FK violations. subscriber_disabled stops apply. ALTER SUBSCRIPTION ... DISABLE/ENABLE. pg_logical_emit_message for custom signaling. Transformations via BEFORE triggers on subscriber risky.\n\n## Production Perspective\nCDC subscriber had manual hotfix UPDATE conflicting with replication — subscription stopped 2h. Disabled subscription, reconciled 47 rows via diff script, resumed from pg_stat_subscription latest_lsn after verification.\n\n## Common Follow-up Questions\n• COPY vs INSERT for initial sync?\n• Replication identity FULL vs DEFAULT?\n• Filter rows in publication?\n• Logical decoding plugin comparison?\n\n## Common Mistakes\n• Manual subscriber writes on replicated tables\n• Advancing LSN without understanding skipped transactions\n• No alerting on pg_stat_subscription worker errors\n• REPLICA IDENTITY FULL on wide tables — WAL bloat\n\n## Senior Engineer Insights\nSubscriber is read-only unless you have explicit bidirectional design with conflict resolution.\n\n## Key Commands\n• ALTER SUBSCRIPTION orders_sub DISABLE;\n• SELECT subname, received_lsn, latest_end_lsn, last_msg_receipt_time FROM pg_stat_subscription;\n• ALTER SUBSCRIPTION orders_sub ENABLE;\n• ALTER TABLE orders REPLICA IDENTITY FULL; -- only if needed\n\n## Best Practices\n• Keep subscriber apply tables read-only for CDC\n• Alert on subscription worker not running\n• Document conflict reconciliation procedure\n• Use REPLICA IDENTITY DEFAULT unless UPDATE on non-PK columns",
    command: "ALTER SUBSCRIPTION orders_sub DISABLE;\nSELECT subname, received_lsn, latest_end_lsn, last_msg_receipt_time FROM pg_stat_subscription;\nALTER SUBSCRIPTION orders_sub ENABLE;\nALTER TABLE orders REPLICA IDENTITY FULL; -- only if needed",
    tags: ["postgresql","logical-replication","medium","common"],
  },
  {
    id: "pg-logical-replication-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "Monitor logical replication lag and slot health on the publisher.",
    answer: "## Interview Answer\npg_stat_replication for logical walsender shows sent_lsn; pg_replication_slots confirmed_flush_lsn vs pg_current_wal_lsn measures lag bytes. pg_stat_subscription on subscriber shows apply lag. inactive logical slot retains WAL indefinitely — monitor pg_wal_lsn_diff.\n\n## Detailed Explanation\nLogical slots named in CREATE SUBSCRIPTION create_slot option. pg_stat_replication.application_name matches subscription. Consumer lag in Debezium/Kafka visible separately — DB slot lag is authoritative for WAL retention risk.\n\n## Production Perspective\nDebezium connector paused weekend — logical slot retained 180GB WAL. Added alert: pg_wal_lsn_diff > 10GB on any logical slot. Consumer auto-restart policy added.\n\n## Common Follow-up Questions\n• pgoutput vs test_decoding?\n• Slot export snapshot for consistent CDC start?\n• Multiple subscriptions one publication?\n• Drop subscription slot cleanup?\n\n## Common Mistakes\n• Logical slot without active consumer\n• Not naming slots — hard to trace ownership\n• Monitoring Kafka lag only not PG slot lag\n• max_replication_slots exhausted by orphaned logical slots\n\n## Senior Engineer Insights\nCDC pipeline health starts at pg_replication_slots on publisher — Kafka lag is downstream.\n\n## Key Commands\n• SELECT slot_name, plugin, active, pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), confirmed_flush_lsn)) AS lag FROM pg_replication_slots WHERE slot_type='logical';\n• SELECT * FROM pg_stat_replication WHERE application_name LIKE 'sub_%';\n\n## Best Practices\n• Tag slots with owner and consumer system in name\n• Page on slot lag bytes not just active=false\n• Drop subscription WITH (slot_name) on decommission\n• Capacity plan max_replication_slots for HA + CDC",
    command: "SELECT slot_name, plugin, active, pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), confirmed_flush_lsn)) AS lag FROM pg_replication_slots WHERE slot_type='logical';\nSELECT * FROM pg_stat_replication WHERE application_name LIKE 'sub_%';",
    tags: ["postgresql","logical-replication","medium","very-common"],
  },
  {
    id: "pg-logical-replication-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "Database Engineer",
    question: "Perform zero-downtime major version upgrade using logical replication.",
    answer: "## Interview Answer\nPublisher on old version, subscriber on new: CREATE PUBLICATION on source, CREATE SUBSCRIPTION with copy_data=true on target. Wait sync lag zero. Stop writes briefly, verify counts, redirect apps to new cluster, drop old subscription. pglogical or native logical rep depending on version jump.\n\n## Detailed Explanation\nLimitations: sequences not replicated — sync via pg_dump --data-only or setval. Large objects need separate handling. Extensions may differ between versions. DDL on PG 15+ can replicate via publication. Cutover window for final LSN catch-up typically seconds to minutes.\n\n## Production Perspective\nPG 12→16 upgrade: logical rep to new RDS instance, pg_dump --data-only for sequences, 30-second write freeze at cutover, rollback plan kept old cluster read-only 48h.\n\n## Common Follow-up Questions\n• Sequence sync strategies?\n• Schema drift before cutover?\n• Filter publication for unsupported types?\n• Rollback if cutover fails?\n\n## Common Mistakes\n• Forgetting sequence synchronization\n• Different extension versions on subscriber\n• Cutover without row count verification\n• Dropping old cluster before soak period\n\n## Senior Engineer Insights\nLogical upgrade is standard for large DBs where pg_upgrade downtime unacceptable — rehearse twice.\n\n## Key Commands\n• CREATE PUBLICATION upgrade_pub FOR ALL IN SCHEMA public;\n• CREATE SUBSCRIPTION upgrade_sub CONNECTION '...' PUBLICATION upgrade_pub WITH (copy_data = true, create_slot = true);\n• SELECT pg_size_pretty(pg_wal_lsn_diff(received_lsn, latest_end_lsn)) FROM pg_stat_subscription;\n\n## Best Practices\n• Rehearse full upgrade on clone\n• Sync sequences explicitly at cutover\n• Verify row counts per table before switch\n• Keep old cluster available for rollback window",
    command: "CREATE PUBLICATION upgrade_pub FOR ALL IN SCHEMA public;\nCREATE SUBSCRIPTION upgrade_sub CONNECTION '...' PUBLICATION upgrade_pub WITH (copy_data = true, create_slot = true);\nSELECT pg_size_pretty(pg_wal_lsn_diff(received_lsn, latest_end_lsn)) FROM pg_stat_subscription;",
    tags: ["postgresql","logical-replication","hard","common"],
  },
  {
    id: "pg-logical-replication-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Cloud Engineer",
    question: "Design logical replication architecture for multi-region read replicas with selective table sync.",
    answer: "## Interview Answer\nRegional publisher in home region; subscribers in EU/APAC with publications filtered to regional-relevant tables. Avoid bidirectional unless using conflict-free CRDT patterns. Network: VPN/private link for replication connection. Slot per subscriber per region. RPO = replication lag; regional subscriber for read locality not DR alone.\n\n## Detailed Explanation\nCREATE PUBLICATION pub_eu FOR TABLE eu_customers, eu_orders. Row filters PG 15+ WHERE clause in publication. Subscriber connection string with SSL. Cross-region lag higher — async by nature. Combine with physical WAL archive for regional DR.\n\n## Production Perspective\nGlobal app replicated 200 tables to EU subscriber — unnecessary WAL and 45s lag. Split publications: global_read_pub (20 tables) per region; cut lag to 3s and halved slot WAL retention.\n\n## Common Follow-up Questions\n• Publication row filters PG 15+?\n• Security for replication connection cross-cloud?\n• Citus vs logical for sharding?\n• Subscriber connection failover?\n\n## Common Mistakes\n• Replicating entire database cross-region unnecessarily\n• Bidirectional without conflict strategy\n• Public replication port on internet\n• Single subscription for unrelated SLA tiers\n\n## Senior Engineer Insights\nSelective publication is performance and compliance — replicate only what region needs.\n\n## Key Commands\n• CREATE PUBLICATION eu_pub FOR TABLE customers, orders WITH (publish = 'insert,update,delete');\n• CREATE SUBSCRIPTION eu_sub CONNECTION 'host=primary sslmode=require' PUBLICATION eu_pub;\n\n## Best Practices\n• Filter publications to regional data needs\n• Use private networking for replication traffic\n• Separate slots and subscriptions per region\n• Combine logical reads with WAL DR for RPO",
    command: "CREATE PUBLICATION eu_pub FOR TABLE customers, orders WITH (publish = 'insert,update,delete');\nCREATE SUBSCRIPTION eu_sub CONNECTION 'host=primary sslmode=require' PUBLICATION eu_pub;",
    tags: ["postgresql","logical-replication","hard","rare"],
  },
  {
    id: "pg-physical-replication-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "Explain physical (binary) replication and what it copies.",
    answer: "## Interview Answer\nPhysical replication ships raw WAL bytes to standby replaying identical changes at page level. Entire cluster replicated — all databases, roles, tablespaces. Standby is block-level clone evolving with primary. Base backup + continuous WAL archive/stream completes replica.\n\n## Detailed Explanation\nRequires wal_level=replica or logical. Standby recovery.conf/standby.signal configures primary_conninfo. hot_standby allows reads. Cannot replicate single database only — use logical for selective. pg_basebackup creates consistent snapshot using WAL.\n\n## Production Perspective\nDR standby in secondary region initialized with pgBackRest backup + stream WAL; RPO 30s async. Quarterly promote test validates RTO.\n\n## Common Follow-up Questions\n• File-level vs block-level replication?\n• Tablespace replication paths?\n• Standby promotion steps?\n• pg_receivewal use case?\n\n## Common Mistakes\n• Expecting selective DB replication with physical rep\n• wal_level=minimal on primary with standbys\n• Not testing promotion procedure\n• Different major PG version on standby without compatibility check\n\n## Senior Engineer Insights\nPhysical rep = full cluster clone. Interviewers ask when you would choose logical instead — selective tables, upgrades, CDC.\n\n## Key Commands\n• pg_basebackup -h primary -U repl -D /standby -Fp -Xs -P -R\n• SELECT pg_is_in_recovery();\n• SHOW wal_level;\n\n## Best Practices\n• wal_level=replica minimum for any standby\n• Automate base backup + replication setup\n• Test promotion regularly\n• Match major version on standbys",
    command: "pg_basebackup -h primary -U repl -D /standby -Fp -Xs -P -R\nSELECT pg_is_in_recovery();\nSHOW wal_level;",
    tags: ["postgresql","physical-replication","easy","very-common"],
  },
  {
    id: "pg-physical-replication-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "What is timeline history and how does it affect failover?",
    answer: "## Interview Answer\nEach promotion increments timeline ID. WAL filenames encode timeline. History file records parent timeline and switchpoint LSN. Standby must follow new timeline after failover — pg_rewind or rebuild. pg_controldata shows current timeline.\n\n## Detailed Explanation\nFailover: pg_ctl promote or trigger_file on standby. Old primary must be fenced — split-brain if still accepts writes. Patroni/etcd manages timeline and leader election. pg_waldump inspects WAL records across timelines.\n\n## Production Perspective\nManual promotion during outage forgot to fence old primary — split-brain 8 minutes until network isolation. Post-incident: Patroni with STONITH via cloud API disable old primary NIC.\n\n## Common Follow-up Questions\n• pg_ctl promote vs pg_promote()?\n• Timeline switch WAL record?\n• Failback procedure after false promotion?\n• etcd role in Patroni failover?\n\n## Common Mistakes\n• Two primaries accepting writes simultaneously\n• Not updating application connection strings after promotion\n• Promoting lagging standby without acknowledging data loss\n• Ignoring timeline in WAL archive during PITR\n\n## Senior Engineer Insights\nTimeline is PostgreSQL HA vocabulary — explain promotion creates new timeline branch.\n\n## Key Commands\n• pg_controldata $PGDATA | grep -E 'Timeline|checkpoint'\n• pg_ctl promote -D $PGDATA\n• cat $PGDATA/pg_wal/*.history\n• patronictl failover\n\n## Best Practices\n• Automate failover with fencing (Patroni/k8s operator)\n• Never allow dual-primary writes\n• Update DNS/proxy on timeline change\n• Archive timeline history files with WAL",
    command: "pg_controldata $PGDATA | grep -E 'Timeline|checkpoint'\npg_ctl promote -D $PGDATA\ncat $PGDATA/pg_wal/*.history\npatronictl failover",
    tags: ["postgresql","physical-replication","medium","common"],
  },
  {
    id: "pg-physical-replication-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "Provision a new physical standby using pg_basebackup best practices.",
    answer: "## Interview Answer\npg_basebackup -D target -Fp -Xs -P -R -C -S slotname from primary. -Xs streams WAL during backup for consistency. -R writes standby.signal and primary_conninfo. -C creates replication slot. Run during low IO if possible; parallel backup with -j for pgBackRest alternative.\n\n## Detailed Explanation\nBackup takes base copy lock briefly at start checkpoint. Ensure enough disk on standby. recovery parameters in postgresql.auto.conf. Start standby without initdb — uses backup directory. Verify pg_stat_replication on primary shows connected standby.\n\n## Production Perspective\nNew replica added with pg_basebackup -j 4 via pgBackRest stanza — 800GB in 45 min vs 3h single-threaded. Slot prevented WAL loss during long backup.\n\n## Common Follow-up Questions\n• pg_basebackup vs pgBackRest vs wal-g?\n• Backup from replica allowed?\n• Encrypt base backup in transit?\n• Verify backup integrity before start?\n\n## Common Mistakes\n• No replication slot during multi-hour basebackup\n• Starting standby without standby.signal in PG 12+\n• Insufficient disk for WAL accumulation during backup\n• Wrong primary_conninfo SSL settings\n\n## Senior Engineer Insights\nAlways -C -S slotname on pg_basebackup — textbook production answer.\n\n## Key Commands\n• pg_basebackup -h primary -U replicator -D /var/lib/pgsql/16/data -Fp -Xs -P -R -C -S replica2\n• pg_verifybackup /path/to/backup\n• SELECT * FROM pg_stat_replication;\n\n## Best Practices\n• Create replication slot during basebackup\n• Verify connectivity before cutover to standby\n• Use parallel backup tools for large datasets\n• Document replica provisioning in runbook",
    command: "pg_basebackup -h primary -U replicator -D /var/lib/pgsql/16/data -Fp -Xs -P -R -C -S replica2\npg_verifybackup /path/to/backup\nSELECT * FROM pg_stat_replication;",
    tags: ["postgresql","physical-replication","medium","very-common"],
  },
  {
    id: "pg-physical-replication-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "Compare async vs sync physical replication for RPO/RTO in cloud deployments.",
    answer: "## Interview Answer\nAsync: primary commits without waiting — RPO = last replicated WAL on failure (seconds to minutes). Sync: commit waits for standby persist — RPO zero for acknowledged commits. RTO depends on promotion automation — manual minutes, Patroni ~30-60s. Cross-region async common; sync usually same-region multi-AZ.\n\n## Detailed Explanation\nCloud RDS: Multi-AZ synchronous to standby in another AZ — RPO zero within region. Read replicas async — RPO > 0. Aurora storage replication different model — shared storage layer. Network partition with sync rep blocks commits if quorum lost.\n\n## Production Perspective\nChose same-region sync Multi-AZ for OLTP (RPO 0) + cross-region async read replica for DR (RPO 5 min acceptable). Documented data loss window for regional disaster explicitly.\n\n## Common Follow-up Questions\n• Aurora vs RDS Postgres replication model?\n• Quorum sync across 3 AZs?\n• RTO for manual vs automated promotion?\n• Synchronous rep and pgbouncer transaction pooling?\n\n## Common Mistakes\n• Believing cross-region async replica gives RPO zero\n• No documented acceptable data loss for async DR\n• Sync rep across regions — commit latency unacceptable\n• Read replica promoted without measuring lag at failure time\n\n## Senior Engineer Insights\nDraw RPO/RTO matrix: same-AZ sync, cross-AZ async, cross-region async — different numbers each.\n\n## Key Commands\n• SELECT application_name, sync_state, flush_lag, replay_lag FROM pg_stat_replication;\n• SHOW synchronous_standby_names;\n\n## Best Practices\n• Document RPO/RTO per replica tier explicitly\n• Same-region sync for zero RPO OLTP\n• Cross-region async for DR with tested promotion\n• Automate promotion to meet RTO SLO",
    command: "SELECT application_name, sync_state, flush_lag, replay_lag FROM pg_stat_replication;\nSHOW synchronous_standby_names;",
    tags: ["postgresql","physical-replication","hard","common"],
  },
  {
    id: "pg-physical-replication-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Troubleshoot a standby that cannot connect or stays in recovery not catching up.",
    answer: "## Interview Answer\nCheck primary pg_hba for replication user, pg_stat_replication for connection attempts, standby postgresql.auto.conf primary_conninfo, network/firewall port 5432, replication slot existence, and pg_log on both sides. pg_is_in_recovery() true expected; pg_last_wal_receive_lsn vs pg_last_wal_replay_lsn on standby shows catch-up.\n\n## Detailed Explanation\nCommon failures: wrong repl user password, missing REPLICATION privilege, SSL mismatch, slot dropped while standby down, primary max_wal_senders exhausted, standby data directory from wrong timeline. walreceiver process absent in pg_stat_activity on standby.\n\n## Production Perspective\nStandby stuck: primary_conninfo pointed to decommissioned ELB after failover. Updated auto.conf via Patroni, restarted standby — reconnected in 30s. Added Patroni DCS-managed conninfo to prevent drift.\n\n## Common Follow-up Questions\n• pg_hba replication keyword line format?\n• recovery_min_apply_delay misconfiguration?\n• Standby readonly queries blocking replay?\n• Reinit standby without full cluster rebuild?\n\n## Common Mistakes\n• Replication user with LOGIN but not REPLICATION attribute\n• Firewall allows 5432 app but blocks standby IP\n• Manual auto.conf edits overwritten by Patroni\n• Deleting standby data dir partial files corrupt state\n\n## Senior Engineer Insights\nSplit troubleshooting: connection (hba, network, auth) vs catch-up (slot, lag, conflicts) — different symptoms.\n\n## Key Commands\n• # pg_hba.conf: host replication repl 10.0.0.0/16 scram-sha-256\n• SELECT rolreplication FROM pg_roles WHERE rolname='repl';\n• SELECT pg_last_wal_receive_lsn(), pg_last_wal_replay_lsn();\n• grep primary_conninfo $PGDATA/postgresql.auto.conf\n\n## Best Practices\n• Automate conninfo management via Patroni/operator\n• Test replication auth from standby host before backup\n• Monitor walreceiver process on standby\n• Keep slot when standby intentionally stopped",
    command: "# pg_hba.conf: host replication repl 10.0.0.0/16 scram-sha-256\nSELECT rolreplication FROM pg_roles WHERE rolname='repl';\nSELECT pg_last_wal_receive_lsn(), pg_last_wal_replay_lsn();\ngrep primary_conninfo $PGDATA/postgresql.auto.conf",
    tags: ["postgresql","physical-replication","hard","rare"],
  }
];
