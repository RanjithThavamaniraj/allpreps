// PostgreSQL — highAvailability
// Structured interview content — run: node scripts/generate-db-tracks.mjs
// 20 questions

export const questions = [
  {
    id: "pg-high-availability-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "What are the core components of PostgreSQL high availability?",
    answer: "## Interview Answer\nPrimary-replica streaming replication for data redundancy. Automatic failover via Patroni/repmgr/Stolon with DCS (etcd/Consul). Connection routing via PgBouncer/HAProxy/VIP. WAL archiving for PITR. Monitoring replication lag, slot health, and leader status. Quorum sync for RPO zero when required.\n\n## Detailed Explanation\nHA stack: app → pooler → Patroni-managed nodes → etcd quorum. Load balancer health checks read-only vs primary. Watchdog/fencing prevents split-brain. Backup independent of HA for logical errors. Multi-AZ placement for AZ failure tolerance.\n\n## Production Perspective\nPatroni + etcd 3-node cluster on K8s, PgBouncer service, sync rep to 2 replicas in different AZs. Quarterly failover drill RTO 45s measured.\n\n## Common Follow-up Questions\n• Patroni vs repmgr vs manual?\n• etcd quorum sizing?\n• HA without automatic failover acceptable when?\n• Aurora HA vs Patroni?\n\n## Common Mistakes\n• Two nodes only for etcd — no quorum\n• HAProxy without read/write split sending writes to replica\n• No fencing on old primary after failover\n• Treating read replica as DR without promotion test\n\n## Senior Engineer Insights\nDraw full stack diagram in interview — pooler, DCS, sync/async tiers, backup side channel.\n\n## Key Commands\n• patronictl list\n• SELECT * FROM pg_stat_replication;\n• curl http://etcd:2379/v2/keys/patroni/leader\n\n## Best Practices\n• Odd-number etcd cluster for quorum\n• Automate failover with fencing\n• Separate read and write endpoints in app config\n• Quarterly failover and PITR drills",
    command: "patronictl list\nSELECT * FROM pg_stat_replication;\ncurl http://etcd:2379/v2/keys/patroni/leader",
    tags: ["postgresql","high-availability","easy","very-common"],
  },
  {
    id: "pg-high-availability-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "Design multi-AZ PostgreSQL HA on Kubernetes with Patroni operator.",
    answer: "## Interview Answer\nZalando Postgres Operator or CrunchyData PGO deploys Patroni StatefulSets across AZs. Pod anti-affinity spreads replicas. PersistentVolume per pod on fast storage. Services: primary (-rw) and replica (-ro). Sync rep via Patroni synchronous_mode or native sync_standby_names.\n\n## Detailed Explanation\nOperator handles failover, backup sidecars (WAL-G), connection pooling optional PgBouncer sidecar. etcd as Patroni DCS or Kubernetes endpoints. Upgrades: rolling pod restart. Monitor via Prometheus postgres_exporter. Network policies restrict replication traffic.\n\n## Production Perspective\nGKE 3-AZ Patroni cluster, 1 primary 2 sync replicas, PgBouncer sidecar 1000 app connections → 30 PG connections. Failover tested via pod delete — 38s leader election.\n\n## Common Follow-up Questions\n• Local PV vs network storage for PG on K8s?\n• Operator upgrade strategy?\n• Cross-cluster DR on K8s?\n• Resource limits for PostgreSQL pods?\n\n## Common Mistakes\n• Single AZ node pool — defeats multi-AZ\n• Shared RWO volume across pods\n• No podDisruptionBudget — voluntary eviction causes outage\n• Under-provisioned etcd on same nodes as PG\n\n## Senior Engineer Insights\nK8s PG HA answer must mention storage class IOPS and anti-affinity — not just Patroni logo.\n\n## Key Commands\n• kubectl get postgresql -n db\n• kubectl exec -it cluster-0 -- patronictl list\n• kubectl get pods -o wide -n db\n\n## Best Practices\n• Pod anti-affinity across AZs\n• Dedicated fast storage class for WAL/data\n• PodDisruptionBudget minAvailable 1 for primary\n• Monitor Patroni leader via Prometheus",
    command: "kubectl get postgresql -n db\nkubectl exec -it cluster-0 -- patronictl list\nkubectl get pods -o wide -n db",
    tags: ["postgresql","high-availability","medium","common"],
  },
  {
    id: "pg-high-availability-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "Monitor PostgreSQL HA health: replication, lag, and leader election.",
    answer: "## Interview Answer\nMetrics: pg_stat_replication replay_lag, pg_replication_slots retained WAL, Patroni /health and /patroni endpoints, pg_is_in_recovery() on each node, connection count per role. Alerts: lag > SLO, slot inactive, no primary in DCS, split-brain detection via timeline.\n\n## Detailed Explanation\npostgres_exporter custom queries for lag bytes. Patroni exposes REST API cluster state. Alert on synchronous_standby_names unfulfilled — commits blocking. Grafana dashboards: lag p99, connections, checkpoints, autovacuum. Synthetic write+read probe through pooler.\n\n## Production Perspective\nDatadog monitors: replay_lag > 5s page, patroni_has_leader=0 critical, pg_wal_lsn_diff slot > 5GB warning. Reduced undetected replica failure MTTR from 45min to 3min.\n\n## Common Follow-up Questions\n• Lag alert threshold tuning?\n• False positive lag during vacuum?\n• Monitor etcd health separately?\n• CloudWatch RDS replica lag vs replay_lag?\n\n## Common Mistakes\n• Only monitoring primary CPU not replica lag\n• No alert on missing sync standby\n• Patroni REST not scraped by monitoring\n• Treating flush_lag as user-visible lag on hot standby\n\n## Senior Engineer Insights\nHA monitoring minimum: leader exists, replay_lag SLO, slot retention, sync standby count.\n\n## Key Commands\n• SELECT application_name, state, replay_lag, sync_state FROM pg_stat_replication;\n• curl -s http://patroni:8008/patroni | jq .\n• SELECT pg_is_in_recovery();\n\n## Best Practices\n• Alert on replication lag exceeding SLO\n• Monitor Patroni leader and member state\n• Track replication slot WAL retention\n• Synthetic transaction probes through HA endpoint",
    command: "SELECT application_name, state, replay_lag, sync_state FROM pg_stat_replication;\ncurl -s http://patroni:8008/patroni | jq .\nSELECT pg_is_in_recovery();",
    tags: ["postgresql","high-availability","medium","very-common"],
  },
  {
    id: "pg-high-availability-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "DBA",
    question: "Compare Patroni, repmgr, and cloud-managed HA for PostgreSQL.",
    answer: "## Interview Answer\nPatroni: DCS-based leader election, REST API, flexible, industry standard self-managed. repmgr: simpler, witness server, good for smaller deployments. RDS Multi-AZ: managed sync failover same region. Aurora: storage-layer replication, faster failover, different consistency model.\n\n## Detailed Explanation\nPatroni supports sync rep, cascading, K8s native. repmgr less active development vs Patroni. Cloud managed trades control for ops reduction. pg_auto_failover (Microsoft) alternative with monitor node. Choose based on team skill, compliance, multi-cloud need.\n\n## Production Perspective\nMigrated repmgr to Patroni for K8s compatibility and better REST integration. Kept RDS Multi-AZ for internal tools tier with different RTO/RPO SLA.\n\n## Common Follow-up Questions\n• pg_auto_failover vs Patroni?\n• Aurora storage vs WAL replication?\n• Custom HA without DCS risks?\n• License/support considerations?\n\n## Common Mistakes\n• DIY heartbeat script without fencing in 2024\n• Patroni without etcd TLS in production\n• Assuming Aurora identical to PostgreSQL semantically\n• repmgr without witness on 2-node cluster\n\n## Senior Engineer Insights\nPick HA tool matching platform: Patroni for K8s/bare metal, RDS for managed tier, hybrid common at scale.\n\n## Key Commands\n• patronictl list\n• repmgr cluster show\n• aws rds describe-db-instances --query 'DBInstances[*].[DBInstanceIdentifier,MultiAZ]'\n\n## Best Practices\n• Standardize on Patroni for self-managed clusters\n• Use cloud managed HA for appropriate tier SLAs\n• Never run custom failover scripts without fencing\n• Document HA tool choice per environment tier",
    command: "patronictl list\nrepmgr cluster show\naws rds describe-db-instances --query 'DBInstances[*].[DBInstanceIdentifier,MultiAZ]'",
    tags: ["postgresql","high-availability","hard","common"],
  },
  {
    id: "pg-high-availability-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Cloud Engineer",
    question: "Architect cross-region DR with RPO/RTO tiers alongside same-region HA.",
    answer: "## Interview Answer\nSame-region: sync Patroni replicas RPO 0, RTO ~60s. Cross-region: async physical replica or WAL archive to DR region RPO minutes-hours. DNS/global load balancer routes after regional failover. Application dual-write or read-only DR until promotion. Compliance data residency may restrict region.\n\n## Detailed Explanation\nDR promotion: Patroni in DR as independent cluster restored from archive or replica lag accepted. Route53 health check fails over write endpoint. Test regional failure game-day annually. Split-brain across regions prevented by never auto-promoting both.\n\n## Production Perspective\nUS-East Patroni primary + sync replicas; US-West async replica + daily wal-g copy. Regional DR runbook promotes West replica with 15-min RPO acceptance; RTO 90 min last drill.\n\n## Common Follow-up Questions\n• Global database product comparison?\n• Cross-region sync rep latency impact?\n• Data residency blocking cross-region DR?\n• Active-active multi-region PG?\n\n## Common Mistakes\n• Cross-region sync rep killing commit latency\n• No DR drill — West replica 7 days lag at real outage\n• DNS TTL too high slowing cutover\n• Assuming same-region HA equals regional DR\n\n## Senior Engineer Insights\nSeparate same-region HA (RPO 0) from cross-region DR (RPO > 0) — different architecture and cost.\n\n## Key Commands\n• SELECT application_name, replay_lag FROM pg_stat_replication;\n• pgbackrest --stanza=main info\n• patronictl list --role=master\n\n## Best Practices\n• Document distinct RPO/RTO for HA vs DR tiers\n• Annual cross-region failover drill\n• Automate WAL shipping to DR region\n• Keep DR runbook independent of primary tooling",
    command: "SELECT application_name, replay_lag FROM pg_stat_replication;\npgbackrest --stanza=main info\npatronictl list --role=master",
    tags: ["postgresql","high-availability","hard","rare"],
  },
  {
    id: "pg-failover-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "Describe manual PostgreSQL failover steps and risks.",
    answer: "## Interview Answer\nOn standby: pg_ctl promote or touch trigger_file (legacy). Old primary must stop accepting writes — fence via Patroni, STONITH, or network isolation. Update application connection strings or VIP. Verify timeline, pg_stat_replication on new primary, recreate slots for remaining replicas.\n\n## Detailed Explanation\nUnplanned promotion with lag = data loss equal to replay lag at failure. Split-brain if old primary not fenced — dual writes corrupt data. pg_rewind reintegrates old primary as replica. Synchronous rep reduces loss window. Document promotion checklist.\n\n## Production Perspective\nPrimary hardware failure — Patroni auto-promoted replica in 42s. Manual intervention only to decommission dead node. Previous manual runbook took 15 min before Patroni adoption.\n\n## Common Follow-up Questions\n• pg_ctl promote vs pg_promote()?\n• Failover vs switchover?\n• How much data loss with 30s replay lag?\n• Reintegrate old primary after failover?\n\n## Common Mistakes\n• Promoting replica without stopping old primary\n• Not measuring lag before manual promotion\n• Forgetting to recreate replication slots\n• Applications cache old primary DNS\n\n## Senior Engineer Insights\nManual failover interview: always mention fencing old primary first — split-brain is the disaster.\n\n## Key Commands\n• pg_ctl promote -D $PGDATA\n• SELECT pg_is_in_recovery();\n• patronictl failover\n• pg_rewind --target-pgdata=$PGDATA --source-server=...\n\n## Best Practices\n• Automate failover with Patroni where possible\n• Always fence old primary before promotion\n• Measure replay lag before accepting manual promotion\n• Maintain written failover checklist",
    command: "pg_ctl promote -D $PGDATA\nSELECT pg_is_in_recovery();\npatronictl failover\npg_rewind --target-pgdata=$PGDATA --source-server=...",
    tags: ["postgresql","failover","easy","very-common"],
  },
  {
    id: "pg-failover-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "How does Patroni perform automatic failover and leader election?",
    answer: "## Interview Answer\nPatroni agents on each node heartbeat to DCS (etcd). Leader holds leader lock key with TTL. On primary failure, lock expires, replica acquires lock, runs postgresql promote callback, updates leader key. Other replicas reconfigure primary_conninfo to new leader. REST API exposes role.\n\n## Detailed Explanation\nloop_wait and ttl tune failover speed vs false positives. synchronous_mode waits for sync standby before responding. callbacks: on_role_change, reload config. bootstrap initializes new cluster. Watchdog optional for split-brain on primary isolation.\n\n## Production Perspective\nNetwork partition isolated primary — Patroni primary demoted itself when could not renew DCS lock; replica promoted. 35s outage. postgresql.conf managed by Patroni — manual edits overwritten.\n\n## Common Follow-up Questions\n• Patroni synchronous_mode vs PostgreSQL native sync?\n• etcd split-brain impact?\n• Patroni pause mode maintenance?\n• Custom failover scripts via callbacks?\n\n## Common Mistakes\n• Editing postgresql.conf outside Patroni scope\n• etcd on same failed AZ as only primary\n• ttl too high — slow failover\n• No application retry on connection failure during failover\n\n## Senior Engineer Insights\nPatroni failover time = ttl + promote + DNS update — know default ~30-60s.\n\n## Key Commands\n• patronictl list\n• patronictl show-config\n• curl http://node:8008/patroni\n• patronictl pause --role=master\n\n## Best Practices\n• Run etcd in odd quorum across AZs\n• Configure application connection retry with backoff\n• Use Patroni for config management not manual edits\n• Test Patroni failover in staging monthly",
    command: "patronictl list\npatronictl show-config\ncurl http://node:8008/patroni\npatronictl pause --role=master",
    tags: ["postgresql","failover","medium","common"],
  },
  {
    id: "pg-failover-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "Troubleshoot split-brain and dual-primary scenarios after failover.",
    answer: "## Interview Answer\nSymptoms: writes succeed on two nodes, divergent data, replication broken both directions. Cause: old primary not fenced during promotion. Fix: stop writes on wrong primary immediately, pg_rewind or rebuild wrong node as replica, audit divergent data manually if both accepted writes.\n\n## Detailed Explanation\nPrevention: STONITH (shutdown old primary), Patroni DCS lock, load balancer health check only on DCS leader, iptables/cloud API isolate. Detection: compare pg_current_wal_lsn and timelines on suspected nodes. Never merge divergent PostgreSQL writes automatically.\n\n## Production Perspective\nMisconfigured load balancer sent writes to both nodes 3 minutes during failover — 847 conflicting order rows. Old primary rewound via pg_rewind; conflicts reconciled from application audit log over 4 hours.\n\n## Common Follow-up Questions\n• Patroni watchdog mode?\n• Cloud API fencing example?\n• Timeline divergence detection?\n• Application idempotency during failover?\n\n## Common Mistakes\n• Promoting without confirming old primary down\n• Load balancer TCP check only — both appear healthy\n• Delaying fencing hoping old primary recovers\n• No audit log to reconcile dual-write period\n\n## Senior Engineer Insights\nSplit-brain reconciliation is manual pain — prevention via fencing is the only production answer.\n\n## Key Commands\n• SELECT pg_current_wal_lsn(), pg_is_in_recovery(), pg_control_checkpoint();\n• patronictl list\n• # STONITH: aws ec2 stop-instances --instance-ids i-oldprimary\n\n## Best Practices\n• Implement automatic fencing in failover tooling\n• Load balancer must check Patroni leader role not just TCP\n• Alert on multiple nodes accepting writes\n• Maintain audit trail for post-failover reconciliation",
    command: "SELECT pg_current_wal_lsn(), pg_is_in_recovery(), pg_control_checkpoint();\npatronictl list\n# STONITH: aws ec2 stop-instances --instance-ids i-oldprimary",
    tags: ["postgresql","failover","medium","very-common"],
  },
  {
    id: "pg-failover-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "Perform zero-downtime switchover (planned failover) with Patroni.",
    answer: "## Interview Answer\npatronictl switchover demotes current primary gracefully, promotes chosen replica, minimal connection drop. Requires healthy sync replica. Application connection pools recycle connections on error. switchover vs failover: no data loss, controlled timing during maintenance.\n\n## Detailed Explanation\nSwitchover steps: verify lag zero on target, patronictl switchover --master old --candidate new, verify patronictl list, rolling app pool reload. Maintenance: primary OS patches via switchover to upgraded standby. Schedule during low traffic; still test app retry.\n\n## Production Perspective\nMonthly OS kernel patch: patronictl switchover to patched replica, patch old primary, switchover back. Total user-visible errors: 12 connection resets caught by app retry — zero failed transactions.\n\n## Common Follow-up Questions\n• Switchover with synchronous_mode required?\n• PgBouncer behavior during switchover?\n• Cancel switchover mid-flight?\n• RDS reboot vs switchover?\n\n## Common Mistakes\n• Switchover to lagging replica\n• No sync standby available — data loss risk on crash mid-switch\n• Applications without connection retry\n• Switchover during active long transactions without notice\n\n## Senior Engineer Insights\nPlanned switchover proves HA works — run monthly, measure connection error rate.\n\n## Key Commands\n• patronictl switchover --master cluster-0 --candidate cluster-1\n• patronictl list\n• SHOW POOLS; -- pgbouncer reload\n\n## Best Practices\n• Monthly planned switchover drill\n• Verify sync replica caught up before switchover\n• Ensure app connection retry logic\n• Document switchover in maintenance runbook",
    command: "patronictl switchover --master cluster-0 --candidate cluster-1\npatronictl list\nSHOW POOLS; -- pgbouncer reload",
    tags: ["postgresql","failover","hard","common"],
  },
  {
    id: "pg-failover-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Lead post-failover validation checklist before declaring all-clear.",
    answer: "## Interview Answer\nVerify: pg_is_in_recovery false on new primary only, pg_stat_replication shows connected replicas, replay lag nominal, application write/read smoke test, sequences advanced correctly, cron jobs running, replication slots recreated, backup jobs targeting new primary, monitoring updated leader tag.\n\n## Detailed Explanation\nCheck for orphaned prepared transactions, invalid indexes from interrupted CONCURRENTLY, subscription lag on logical replicas. Compare row counts critical tables vs pre-failover snapshot if available. Re-enable synchronous_standby_names if disabled during crisis.\n\n## Production Perspective\nPost-failover checklist caught missing replication slot for analytics replica — CDC silent 2h until slot recreated. Added automated post-failover Ansible playbooks.\n\n## Common Follow-up Questions\n• Sequence jump after failover?\n• Logical replication re-point subscription?\n• Patroni post-failover config drift?\n• When to pg_rewind old primary vs rebuild?\n\n## Common Mistakes\n• Declaring resolved after promote without replica connectivity\n• Backup still pointing at old hostname\n• Forgotten logical replication subscription update\n• Not checking invalid indexes after crash\n\n## Senior Engineer Insights\nFailover is not done at promote — validation checklist is senior DBA differentiator.\n\n## Key Commands\n• SELECT pg_is_in_recovery();\n• SELECT * FROM pg_stat_replication;\n• SELECT indexrelid::regclass FROM pg_index WHERE NOT indisvalid;\n• SELECT pg_create_physical_replication_slot('replica1');\n\n## Best Practices\n• Maintain post-failover checklist in runbook\n• Automate slot and subscription recreation\n• Run smoke tests before all-clear announcement\n• Verify backup and monitoring target new primary",
    command: "SELECT pg_is_in_recovery();\nSELECT * FROM pg_stat_replication;\nSELECT indexrelid::regclass FROM pg_index WHERE NOT indisvalid;\nSELECT pg_create_physical_replication_slot('replica1');",
    tags: ["postgresql","failover","hard","rare"],
  },
  {
    id: "pg-connection-pooling-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "Why is connection pooling essential for PostgreSQL applications?",
    answer: "## Interview Answer\nPostgreSQL uses process-per-connection — each backend consumes memory and file descriptors. Hundreds of app threads should not map 1:1 to backends. Pooler multiplexes many clients onto fewer server connections. Reduces memory pressure, speeds connection establishment, protects max_connections limit.\n\n## Detailed Explanation\nWithout pool: app server 200 workers = 200 PG backends × ~10MB. With pool: 200 clients → 20 server connections in transaction mode. Connection time ~ms vs fork backend ~20ms. Pool also centralizes SSL, auth, and routing to primary/replica.\n\n## Production Perspective\nJava app with HikariCP pool_size=100 against PG max_connections=100 — exhausted connections during spike. Moved to PgBouncer transaction mode 1000 clients / 50 servers — stable under 3× traffic.\n\n## Common Follow-up Questions\n• App-side pool vs external PgBouncer?\n• max_connections sizing formula?\n• Pool after failover behavior?\n• RDS Proxy vs PgBouncer?\n\n## Common Mistakes\n• max_connections=2000 on PG without pool\n• Double pooling app + PgBouncer without understanding mode\n• Each microservice opening 100 connections directly\n• Ignoring connection count in load tests\n\n## Senior Engineer Insights\nProcess-per-connection is THE reason pooler exists — quantify memory at interview.\n\n## Key Commands\n• SELECT count(*), state FROM pg_stat_activity GROUP BY state;\n• SHOW max_connections;\n• SELECT setting FROM pg_settings WHERE name='superuser_reserved_connections';\n\n## Best Practices\n• Always pool at scale — external PgBouncer or RDS Proxy\n• Size server connections << client connections\n• Monitor pg_stat_activity connection count\n• Reserve superuser connections for admin",
    command: "SELECT count(*), state FROM pg_stat_activity GROUP BY state;\nSHOW max_connections;\nSELECT setting FROM pg_settings WHERE name='superuser_reserved_connections';",
    tags: ["postgresql","connection-pooling","easy","very-common"],
  },
  {
    id: "pg-connection-pooling-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "Compare transaction pooling, session pooling, and statement pooling modes.",
    answer: "## Interview Answer\nSession mode: client owns server connection for session lifetime — compatible with all PG features, limited multiplexing. Transaction mode: server connection assigned per transaction only — high multiplexing, breaks session-scoped state (SET, temp tables, prepared stmts without DEALLOCATE). Statement mode: rare, per statement, most restrictions.\n\n## Detailed Explanation\nTransaction mode: must not use SET without SET LOCAL, advisory locks problematic, LISTEN/NOTIFY broken, cursors dont survive. Session mode for ORMs needing prepared statements persistent. PgBouncer default transaction for web apps. RDS Proxy session pinning on SET variable.\n\n## Production Perspective\nDjango with transaction pooling failed — used SET search_path in middleware. Fixed: SET LOCAL search_path in transaction or switched affected services to session mode pool.\n\n## Common Follow-up Questions\n• Prepared statements in transaction mode?\n• PgBouncer max_prepared_statements PG 14+?\n• HikariCP + PgBouncer double pool?\n• Session mode connection count benefit?\n\n## Common Mistakes\n• Transaction mode with app using SET SESSION\n• Prepared statement leak across transactions\n• Advisory locks in transaction pool — lock lost between txns\n• LISTEN/NOTIFY through transaction pooler\n\n## Senior Engineer Insights\nTransaction mode caveat list is interview staple — SET, temp table, prepared stmt, advisory lock.\n\n## Key Commands\n• # pgbouncer.ini pool_mode = transaction\n• SHOW CONFIG; -- pgbouncer admin\n• SET LOCAL search_path TO app, public;\n\n## Best Practices\n• Use transaction mode for stateless web apps\n• Audit app for session-scoped features before transaction pooling\n• Use SET LOCAL not SET SESSION in pooled apps\n• Session mode for admin and migration tools only",
    command: "# pgbouncer.ini pool_mode = transaction\nSHOW CONFIG; -- pgbouncer admin\nSET LOCAL search_path TO app, public;",
    tags: ["postgresql","connection-pooling","medium","common"],
  },
  {
    id: "pg-connection-pooling-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "Diagnose connection pool exhaustion and \"too many clients\" errors.",
    answer: "## Interview Answer\nError: FATAL remaining connection slots reserved or too many clients. Check pg_stat_activity count vs max_connections. Identify source via application_name, client_addr. Causes: pool leak, connection storm deploy, long idle sessions, missing pooler, replication connections consuming slots.\n\n## Detailed Explanation\nsuperuser_reserved_connections leaves slots for admin. idle in transaction holds connection. pg_stat_activity backend_start shows connection age. Terminate idle: pg_terminate_backend. PgBouncer wait queue SHOW POOLS cl_waiting. App connection leak shows steady count growth.\n\n## Production Perspective\nDeploy doubled pod count each opening 50 connections — hit max_connections 300. Short-term: raised max_connections to 400. Fix: PgBouncer with default_pool_size=30, app pods reduced to 10 direct max.\n\n## Common Follow-up Questions\n• idle_in_transaction_session_timeout?\n• Connection leak detection in app?\n• Replication slots vs connection limit?\n• PgBouncer queue timeout?\n\n## Common Mistakes\n• Raising max_connections indefinitely\n• Killing replication backends to free slots\n• No application_name — cannot trace source\n• Ignoring idle in transaction as connection consumer\n\n## Senior Engineer Insights\nConnection incident triage: pg_stat_activity group by application_name, client_addr — find noisy neighbor.\n\n## Key Commands\n• SELECT application_name, client_addr, count(*) FROM pg_stat_activity GROUP BY 1,2 ORDER BY 3 DESC;\n• SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE state='idle' AND query_start < now()-interval '1 hour';\n• SHOW POOLS;\n\n## Best Practices\n• Set idle_in_transaction_session_timeout globally\n• Tag all connections with application_name\n• Alert connection count > 80% max_connections\n• Use pooler to bound server connection count",
    command: "SELECT application_name, client_addr, count(*) FROM pg_stat_activity GROUP BY 1,2 ORDER BY 3 DESC;\nSELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE state='idle' AND query_start < now()-interval '1 hour';\nSHOW POOLS;",
    tags: ["postgresql","connection-pooling","medium","very-common"],
  },
  {
    id: "pg-connection-pooling-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "Design connection pooling architecture for microservices accessing shared PostgreSQL.",
    answer: "## Interview Answer\nCentral PgBouncer cluster or sidecar per AZ. Per-service database roles and pool_size limits. Transaction mode for stateless APIs. Read/write split: separate pools to primary vs replicas. Avoid each microservice unlimited HikariCP — cap total server connections across fleet.\n\n## Detailed Explanation\nFormula: sum(default_pool_size per service) + replication + admin < max_connections × 0.8. PgBouncer user auth via auth_query from pg_shadow. TLS termination at pooler. Kubernetes: PgBouncer Deployment as shared service in mesh.\n\n## Production Perspective\n40 microservices shared one RDS — connection chaos. Deployed PgBouncer per AZ with auth_query, per-service user pool_mode=transaction max 15 server conns each. Total server connections capped 120 on max_connections=150 RDS.\n\n## Common Follow-up Questions\n• auth_user in PgBouncer?\n• RDS Proxy multi-tenant?\n• Circuit breaker on pool exhaustion?\n• Separate pool per database vs per user?\n\n## Common Mistakes\n• Each microservice 50 Hikari connections no central cap\n• Shared DB user for all services — no attribution\n• Single PgBouncer SPOF without HA\n• No pool sizing math documented\n\n## Senior Engineer Insights\nFleet connection budget spreadsheet — service × pool_size = must fit max_connections.\n\n## Key Commands\n• # pgbouncer.ini auth_query = SELECT usename, passwd FROM pg_shadow WHERE usename=$1\n• auth_user = pgbouncer_auth\n• SHOW DATABASES;\n• SHOW CLIENTS;\n\n## Best Practices\n• Central connection budget across microservices\n• Per-service DB user and pool limits\n• HA PgBouncer with multiple instances + LB\n• Monitor PgBouncer SHOW STATS wait times",
    command: "# pgbouncer.ini auth_query = SELECT usename, passwd FROM pg_shadow WHERE usename=$1\nauth_user = pgbouncer_auth\nSHOW DATABASES;\nSHOW CLIENTS;",
    tags: ["postgresql","connection-pooling","hard","common"],
  },
  {
    id: "pg-connection-pooling-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Database Engineer",
    question: "Handle prepared statements, advisory locks, and LISTEN/NOTIFY with pooling.",
    answer: "## Interview Answer\nTransaction pooling: prepared statements must be DEALLOCATE ALL before connection return or use PgBouncer max_prepared_statements (PG14+). Advisory locks dont survive transaction end in transaction mode — use session mode or DB-level locking. LISTEN/NOTIFY requires dedicated session mode connection outside pool.\n\n## Detailed Explanation\nPgBouncer tracks prepared stmts per client-server mapping in newer versions. ORMs (Rails, Hibernate) often incompatible with transaction pool without config. NOTIFY listener long-lived connection bypasses pooler to dedicated channel. pg_advisory_xact_lock preferred over session lock in transaction mode.\n\n## Production Perspective\nRails prepared statements broke under PgBouncer transaction mode — disabled prepared_statements in database.yml OR moved to session pool for Rails tier only while Node APIs stayed transaction pool.\n\n## Common Follow-up Questions\n• PgBouncer 1.21 prepared statement support?\n• Skip locked with transaction pool?\n• Temp tables in transaction mode?\n• RDS Proxy session pinning?\n\n## Common Mistakes\n• ORM prepared statements + transaction pool without testing\n• pg_advisory_lock session lock in transaction pool\n• LISTEN through PgBouncer transaction pool\n• Not documenting which services need session mode\n\n## Senior Engineer Insights\nPool mode compatibility matrix per app framework — real production gotcha.\n\n## Key Commands\n• DEALLOCATE ALL;\n• SELECT pg_advisory_xact_lock(12345);\n• LISTEN order_events; -- dedicated session connection\n• SHOW CONFIG; | grep max_prepared\n\n## Best Practices\n• Test ORM with target pool mode in staging\n• Use pg_advisory_xact_lock in transaction pooled apps\n• Dedicated session connection for LISTEN/NOTIFY\n• Document per-service pool mode requirements",
    command: "DEALLOCATE ALL;\nSELECT pg_advisory_xact_lock(12345);\nLISTEN order_events; -- dedicated session connection\nSHOW CONFIG; | grep max_prepared",
    tags: ["postgresql","connection-pooling","hard","rare"],
  },
  {
    id: "pg-pgbouncer-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "What is PgBouncer and how do you configure basic pool settings?",
    answer: "## Interview Answer\nPgBouncer lightweight connection pooler. pgbouncer.ini: [databases] maps alias to connection string, [pgbouncer] pool_mode, default_pool_size, max_client_conn, listen_port 6432. Auth via auth_file or auth_query. Admin console: psql -p 6432 pgbouncer.\n\n## Detailed Explanation\ndefault_pool_size server connections per user/database pair. max_client_conn total clients. reserve_pool for burst. server_lifetime recycles connections. unix socket or TCP. log_connections for audit.\n\n## Production Perspective\nStandard config: transaction mode, default_pool_size=25, max_client_conn=1000, auth_query for credential rotation without ini reload.\n\n## Common Follow-up Questions\n• PgBouncer vs pgpool-II?\n• Unix socket vs TCP latency?\n• Multiple PgBouncer behind LB?\n• Version compatibility with PG 16?\n\n## Common Mistakes\n• pool_mode=session defeating multiplexing purpose\n• auth_file plaintext passwords in repo\n• default_pool_size equal to max_client_conn\n• No max_db_connections limit per database\n\n## Senior Engineer Insights\nKnow pgbouncer.ini sections: databases, pgbouncer, users — admin question frequent.\n\n## Key Commands\n• psql -h localhost -p 6432 -U pgbouncer pgbouncer -c \"SHOW POOLS;\"\n• RELOAD; -- admin console\n• # pool_mode = transaction\ndefault_pool_size = 25\nmax_client_conn = 1000\n\n## Best Practices\n• Start with transaction mode for web workloads\n• Use auth_query for credential management\n• Set max_client_conn >> default_pool_size\n• Monitor via SHOW POOLS and SHOW STATS",
    command: "psql -h localhost -p 6432 -U pgbouncer pgbouncer -c \"SHOW POOLS;\"\nRELOAD; -- admin console\n# pool_mode = transaction\ndefault_pool_size = 25\nmax_client_conn = 1000",
    tags: ["postgresql","pgbouncer","easy","very-common"],
  },
  {
    id: "pg-pgbouncer-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "Configure PgBouncer auth_query and integrate with SCRAM authentication.",
    answer: "## Interview Answer\nauth_query executes on PostgreSQL to fetch password hash for client user. auth_user connects to run query: SELECT usename, passwd FROM pg_shadow WHERE usename=$1. Supports SCRAM hashes from PG 10+. Avoids duplicating passwords in auth_file.\n\n## Detailed Explanation\nCreate pgbouncer_auth role with limited privileges executing security definer function if not direct pg_shadow access. SCRAM in PostgreSQL 14+ default. PgBouncer 1.17+ SCRAM client auth. Rotate passwords in PG only — pooler picks up on next auth.\n\n## Production Perspective\nMigrated from auth_file MD5 to auth_query SCRAM — security audit finding closed. pgbouncer_auth user cannot login interactively, only via auth_query path.\n\n## Common Follow-up Questions\n• security definer function vs pg_shadow?\n• Client cert auth through PgBouncer?\n• auth_hba_file option?\n• Password rotation without disconnect?\n\n## Common Mistakes\n• auth_user with superuser privileges\n• auth_file MD5 in 2024 production\n• auth_query function SQL injection in username\n• Not TLS between PgBouncer and PostgreSQL\n\n## Senior Engineer Insights\nauth_query is production standard — explain auth_user minimal privilege pattern.\n\n## Key Commands\n• CREATE USER pgbouncer_auth PASSWORD '...';\nGRANT pg_read_all_settings TO pgbouncer_auth; -- or custom fn\n• auth_query = SELECT usename, passwd FROM pg_shadow WHERE usename=$1\n• auth_type = scram-sha-256\n\n## Best Practices\n• Use auth_query not static auth_file in production\n• SCRAM between client-PgBouncer and PgBouncer-PG\n• Minimal privilege auth_user\n• TLS for all pooler connections",
    command: "CREATE USER pgbouncer_auth PASSWORD '...';\nGRANT pg_read_all_settings TO pgbouncer_auth; -- or custom fn\nauth_query = SELECT usename, passwd FROM pg_shadow WHERE usename=$1\nauth_type = scram-sha-256",
    tags: ["postgresql","pgbouncer","medium","common"],
  },
  {
    id: "pg-pgbouncer-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "Monitor PgBouncer with SHOW commands and troubleshoot pool saturation.",
    answer: "## Interview Answer\nSHOW POOLS: cl_active, cl_waiting, sv_active, sv_idle, maxwait. SHOW STATS: totals and averages. SHOW CLIENTS/SERVERS: connection detail. cl_waiting > 0 sustained = pool too small or slow queries holding server connections. maxwait shows longest wait seconds.\n\n## Detailed Explanation\nSaturation fixes: increase default_pool_size, optimize long queries, ensure transaction mode releases quickly, add PgBouncer instances. query_wait_timeout kills waiting clients. server_idle_timeout closes unused server connections.\n\n## Production Perspective\ncl_waiting peaked 847 during sale — default_pool_size 20 insufficient. Raised to 50, optimized 3 slow queries holding connections 30s+, cl_waiting zero during next sale.\n\n## Common Follow-up Questions\n• Export PgBouncer metrics to Prometheus?\n• pgbouncer_exporter Grafana?\n• Pool saturation vs PG CPU saturation?\n• reserve_pool configuration?\n\n## Common Mistakes\n• Ignoring cl_waiting metric until users timeout\n• Increasing pool_size without checking PG max_connections headroom\n• Long transactions holding pooled server connection\n• No query_wait_timeout — infinite client wait\n\n## Senior Engineer Insights\nSHOW POOLS cl_waiting is the PgBouncer panic metric — dashboard it.\n\n## Key Commands\n• SHOW POOLS;\n• SHOW STATS;\n• SHOW CLIENTS;\n• SET query_wait_timeout = 120; -- pgbouncer.ini\n\n## Best Practices\n• Dashboard cl_waiting and maxwait continuously\n• Size pool considering query duration P99\n• Set query_wait_timeout to fail fast\n• Alert sv_active approaching default_pool_size sustained",
    command: "SHOW POOLS;\nSHOW STATS;\nSHOW CLIENTS;\nSET query_wait_timeout = 120; -- pgbouncer.ini",
    tags: ["postgresql","pgbouncer","medium","very-common"],
  },
  {
    id: "pg-pgbouncer-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "Deploy highly available PgBouncer tier with failover routing to PostgreSQL primary.",
    answer: "## Interview Answer\nMultiple PgBouncer instances behind HAProxy/NLB. Patroni REST or consul-template updates backend primary address in pgbouncer.ini or use host=all in database line with separate routing layer. On failover: RELOAD PgBouncer or automated config push. Health check PgBouncer not just PostgreSQL.\n\n## Detailed Explanation\nPgBouncer HA not built-in — stateless pooler instances scale horizontally. primary_conninfo change on failover requires pgbouncer database host update — automate via Patroni callback script calling RELOAD. Avoid single PgBouncer pod SPOF. Co-locate pooler with app in same AZ for latency with cross-AZ PG replica routing for reads.\n\n## Production Perspective\n3 PgBouncer instances behind NLB, Patroni on_failover callback runs sed + pgbouncer -R on all poolers. Failover connection blip 2s average with app retry.\n\n## Common Follow-up Questions\n• PgBouncer on same host as PG?\n• K8s headless service for PgBouncer?\n• Read/write split in PgBouncer config?\n• AWS RDS Proxy HA built-in comparison?\n\n## Common Mistakes\n• Single PgBouncer instance production SPOF\n• Manual pgbouncer.ini host update during failover\n• HAProxy checking PG through wrong pooler backend\n• Not reloading all PgBouncer instances after primary change\n\n## Senior Engineer Insights\nPgBouncer HA = stateless N instances + automated primary tracking — not Patroni for pooler itself.\n\n## Key Commands\n• # Patroni callback: sed -i \"s/host=.*/host=newprimary/\" /etc/pgbouncer/pgbouncer.ini && pgbouncer -R\n• SHOW DATABASES;\n• patronictl list\n\n## Best Practices\n• Run minimum 2 PgBouncer instances behind LB\n• Automate primary address update on failover\n• Health check each PgBouncer instance\n• Test failover end-to-end including pooler reload",
    command: "# Patroni callback: sed -i \"s/host=.*/host=newprimary/\" /etc/pgbouncer/pgbouncer.ini && pgbouncer -R\nSHOW DATABASES;\npatronictl list",
    tags: ["postgresql","pgbouncer","hard","common"],
  },
  {
    id: "pg-pgbouncer-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Advanced PgBouncer tuning: pool sizing, timeouts, and multi-tenant isolation.",
    answer: "## Interview Answer\nPer-database pool_size override in [databases] section. max_user_connections limit noisy tenant. server_round_robin for load spread. pkt_buf/max_packet_size for large payloads. disable_pqexec for simple protocol. Application naming via connect_query SET application_name.\n\n## Detailed Explanation\nMulti-tenant: separate database alias per tenant same PG database different pool_size. track_extra_parameters for SET tracking PG 14+. client_idle_timeout disconnects idle clients. query_timeout kills long server queries protecting pool.\n\n## Production Perspective\nSaaS 200 tenants shared pool — one tenant batch job exhausted pool. Implemented per-tenant PgBouncer database alias with max_user_connections=5 for free tier, 50 for enterprise.\n\n## Common Follow-up Questions\n• PgBouncer vs RDS Proxy feature matrix?\n• Pool per tenant vs per tier?\n• admin_users security?\n• PgBouncer connection to multiple PG clusters?\n\n## Common Mistakes\n• Single pool_size for heterogeneous tenant SLAs\n• No query_timeout — one slow query blocks pool\n• connect_query SET on every checkout overhead ignored\n• admin password default pgbouncer\n\n## Senior Engineer Insights\nMulti-tenant pool isolation at PgBouncer layer — enterprise SaaS architecture pattern.\n\n## Key Commands\n• # [databases]\ntenant_a = host=pg dbname=shared pool_size=5 max_user_connections=5\ntenant_b = host=pg dbname=shared pool_size=50\n• connect_query = SET application_name='via-pgbouncer'\n• SHOW CONFIG;\n\n## Best Practices\n• Tier pool limits by tenant SLA\n• Set query_timeout to protect shared pools\n• Secure admin console with strong auth_users\n• Use connect_query for application_name attribution",
    command: "# [databases]\ntenant_a = host=pg dbname=shared pool_size=5 max_user_connections=5\ntenant_b = host=pg dbname=shared pool_size=50\nconnect_query = SET application_name='via-pgbouncer'\nSHOW CONFIG;",
    tags: ["postgresql","pgbouncer","hard","rare"],
  }
];
