// PostgreSQL — mvcc
// Structured interview content — run: node scripts/generate-db-tracks.mjs
// 20 questions

export const questions = [
  {
    id: "pg-mvcc-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "Explain PostgreSQL MVCC: xmin, xmax, and tuple visibility rules.",
    answer: "## Interview Answer\nEach row version carries xmin (inserting xact) and xmax (deleting/updating xact). Snapshots compare active xids to determine visibility. Readers never block writers; UPDATE creates new tuple version leaving dead row until vacuum. Transaction isolation uses snapshot export at statement or transaction start.\n\n## Detailed Explanation\nHeap tuples store t_xmin/t_xmax in tuple header. CLOG (pg_xact) and commit timestamps track commit status. SnapshotTooOld can occur on long snapshots with aggressive vacuum settings. Repeatable Read and Serializable use same snapshot for transaction duration. HOT updates avoid index changes when updated columns are not indexed and page has space.\n\n## Production Perspective\nSupport ticket: duplicate key on unique index despite SELECT showing no row — uncommitted INSERT held lock; concurrent session saw invisible tuple. pg_locks showed waiting on RowExclusiveLock. Resolved by shortening idle transactions holding open xacts.\n\n## Common Follow-up Questions\n• What is HOT update and when does it fail?\n• How does pg_snapshot_xmin() relate to vacuum?\n• Difference between Read Committed and Repeatable Read visibility?\n• What causes xmin horizon to stall vacuum?\n\n## Common Mistakes\n• Assuming DELETE frees space immediately without vacuum\n• Confusing MVCC with undo logs like Oracle\n• Long idle in transaction blocking vacuum on entire cluster\n• Not checking xmax for update conflicts in Serializable\n\n## Senior Engineer Insights\nInterview gold: trace SELECT visibility for one row through concurrent UPDATE and COMMIT. Mention freeze xmin and wraparound — separate but related vacuum concern.\n\n## Key Commands\n• SELECT xmin, xmax, * FROM orders WHERE id = 1;\n• SELECT txid_current();\n• SELECT relname, n_dead_tup, last_vacuum FROM pg_stat_user_tables ORDER BY n_dead_tup DESC LIMIT 10;\n• SELECT pg_current_snapshot();\n\n## Best Practices\n• Keep transactions short to limit dead tuple accumulation\n• Monitor n_dead_tup via pg_stat_user_tables\n• Use READ COMMITTED default unless app needs RR semantics\n• Alert on idle in transaction sessions > 5 minutes",
    command: "SELECT xmin, xmax, * FROM orders WHERE id = 1;\nSELECT txid_current();\nSELECT relname, n_dead_tup, last_vacuum FROM pg_stat_user_tables ORDER BY n_dead_tup DESC LIMIT 10;\nSELECT pg_current_snapshot();",
    tags: ["postgresql","mvcc","easy","very-common"],
  },
  {
    id: "pg-mvcc-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do transaction isolation levels affect locking and anomalies in PostgreSQL?",
    answer: "## Interview Answer\nDefault READ COMMITTED takes new snapshot per statement — prevents dirty read but allows non-repeatable read. REPEATABLE READ prevents non-repeatable read; phantoms blocked differently than Oracle RR. SERIALIZABLE uses SSI detecting rw-conflicts with 40001 errors.\n\n## Detailed Explanation\nRC: each statement sees rows committed before statement start. RR: snapshot at transaction start; concurrent updates to seen rows fail with serialization failure on write. Serializable tracks rw-dependencies. SELECT FOR UPDATE takes row locks regardless of isolation. Advisory locks are application-level.\n\n## Production Perspective\nPayment service used RR but did not retry on 40001 — rare double-charge during flash sale. Added ORM retry on serialization_failure. pg_stat_database deadlocks unrelated — conflict was on primary write path.\n\n## Common Follow-up Questions\n• When does SELECT FOR UPDATE SKIP LOCKED help?\n• SSI predicate locking vs traditional 2PL?\n• How does deferrable READ ONLY avoid snapshot conflicts?\n• Lock modes for UPDATE vs DELETE?\n\n## Common Mistakes\n• Using SERIALIZABLE globally without app retry logic\n• Expecting RR to prevent all phantoms without understanding PG RR\n• FOR UPDATE on non-indexed column causing seq scan + row lock storm\n• Mixing isolation levels in same connection pool without SET per transaction\n\n## Senior Engineer Insights\nMost web apps are fine with READ COMMITTED + explicit locking for inventory. Serializable is for financial invariants — always pair with exponential backoff retry.\n\n## Key Commands\n• SHOW transaction_isolation;\n• BEGIN ISOLATION LEVEL SERIALIZABLE;\n• SELECT * FROM pg_locks WHERE NOT granted;\n• SELECT datname, deadlocks FROM pg_stat_database;\n\n## Best Practices\n• Document isolation level per service in architecture docs\n• Implement retry on SQLSTATE 40001 for Serializable\n• Use SELECT FOR UPDATE for explicit pessimistic paths\n• Monitor deadlocks via pg_stat_database and log_lock_waits",
    command: "SHOW transaction_isolation;\nBEGIN ISOLATION LEVEL SERIALIZABLE;\nSELECT * FROM pg_locks WHERE NOT granted;\nSELECT datname, deadlocks FROM pg_stat_database;",
    tags: ["postgresql","mvcc","medium","common"],
  },
  {
    id: "pg-mvcc-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "What causes \"could not serialize access due to concurrent update\" and how do you resolve it?",
    answer: "## Interview Answer\nError SQLSTATE 40001 in Serializable or when RR transaction re-reads row modified by concurrent commit. SSI detects dangerous structures between concurrent transactions. Resolution: application retry with backoff, reduce transaction scope, order lock acquisition consistently, or downgrade isolation where invariants allow.\n\n## Detailed Explanation\nIn RR, UPDATE/DELETE of row changed since snapshot raises 40001. In Serializable, rw-conflicts between concurrent read/write sets trigger rollback of one transaction. High conflict rate signals hot row contention — consider advisory lock per entity or queue serialization.\n\n## Production Perspective\nInventory decrement under Serializable caused 15% transaction failure rate during peak. Switched hot SKU path to SELECT FOR UPDATE in READ COMMITTED — conflicts dropped to near zero, latency predictable.\n\n## Common Follow-up Questions\n• Difference between deadlock and serialization failure?\n• How to log serialization failures?\n• Does advisory lock participate in SSI?\n• Impact of prepared statements on retry?\n\n## Common Mistakes\n• Infinite retry without jitter causing thundering herd\n• Catching generic Exception instead of 40001 specifically\n• Using Serializable for read-heavy reporting\n• Not reducing transaction touch set on hot rows\n\n## Senior Engineer Insights\nMeasure conflict rate before choosing Serializable. Often explicit row lock + RC is simpler and faster than SSI rollback lottery.\n\n## Key Commands\n• SET log_lock_waits = on;\n• SELECT pg_advisory_xact_lock(hashtext('sku:123'));\n• BEGIN ISOLATION LEVEL REPEATABLE READ;\n\n## Best Practices\n• Retry 40001 with exponential backoff capped at 3-5 attempts\n• Serialize hot row updates via FOR UPDATE or advisory locks\n• Keep Serializable transactions read-only when possible\n• Load test isolation choice under peak concurrency",
    command: "SET log_lock_waits = on;\nSELECT pg_advisory_xact_lock(hashtext('sku:123'));\nBEGIN ISOLATION LEVEL REPEATABLE READ;",
    tags: ["postgresql","mvcc","medium","very-common"],
  },
  {
    id: "pg-mvcc-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "Database Engineer",
    question: "Explain HOT chains, line pointer redirection, and index bloat from non-HOT updates.",
    answer: "## Interview Answer\nHOT (Heap Only Tuple): when UPDATE does not change indexed columns and same page has free space, new row version stays on page with HOT chain via ctid redirect. Index entries still point to chain head — no new index tuples. Non-HOT UPDATE when indexed column changes or page full causes index bloat.\n\n## Detailed Explanation\nHOT reduces index write amplification on frequently updated non-indexed columns. pg_stat_user_tables n_tup_hot_upd tracks HOT ratio. REINDEX needed when index bloat exceeds benefit. fillfactor < 100 reserves page space for HOT.\n\n## Production Perspective\nUser sessions table updated last_active every 30s on indexed user_id PK — HOT ratio 0%, indexes 3× table size. Dropped redundant index, SET fillfactor=80, moved last_active to side table. n_tup_hot_upd rose to 85%.\n\n## Common Follow-up Questions\n• How to detect HOT failure in pageinspect?\n• fillfactor trade-offs for insert-heavy tables?\n• BRIN and HOT interaction?\n• Does VACUUM FULL rebuild HOT chains?\n\n## Common Mistakes\n• Indexing every column on high-update tables\n• fillfactor=100 on session/heartbeat tables\n• Ignoring n_tup_hot_upd metric\n• REINDEX CONCURRENTLY without fixing update pattern\n\n## Senior Engineer Insights\nSchema design beats tuning: separate volatile columns to side table without indexes on hot path.\n\n## Key Commands\n• SELECT relname, n_tup_upd, n_tup_hot_upd FROM pg_stat_user_tables WHERE relname = 'sessions';\n• ALTER TABLE sessions SET (fillfactor = 80);\n• SELECT indexrelname, pg_size_pretty(pg_relation_size(indexrelid)) FROM pg_stat_user_indexes WHERE relname = 'sessions';\n\n## Best Practices\n• Monitor HOT update ratio on high-churn tables\n• Avoid indexing columns updated every request\n• Use fillfactor 70-90 on update-heavy heaps\n• Split volatile attributes to narrow side tables",
    command: "SELECT relname, n_tup_upd, n_tup_hot_upd FROM pg_stat_user_tables WHERE relname = 'sessions';\nALTER TABLE sessions SET (fillfactor = 80);\nSELECT indexrelname, pg_size_pretty(pg_relation_size(indexrelid)) FROM pg_stat_user_indexes WHERE relname = 'sessions';",
    tags: ["postgresql","mvcc","hard","common"],
  },
  {
    id: "pg-mvcc-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "How does PostgreSQL handle transaction ID wraparound and freeze operations?",
    answer: "## Interview Answer\nXIDs are 32-bit; vacuum FREEZE marks old tuples frozen so they remain visible forever without xid comparison. autovacuum triggers anti-wraparound when relfrozenxid ages. Emergency autovacuum runs at high cost delay zero. Failure to freeze before limit causes shutdown to protect data integrity.\n\n## Detailed Explanation\npg_class.relfrozenxid tracks oldest unfrozen xid per relation. age(datfrozenxid) in pg_database shows cluster-wide risk. vacuum freeze updates relfrozenxid. multixact has separate wraparound via pg_multixact.\n\n## Production Perspective\nMonitoring alert: datfrozenxid age 1.8B on 9-year-old cluster — emergency VACUUM FREEZE on largest tables during maintenance window. autovacuum_freeze_max_age lowered proactively post-incident.\n\n## Common Follow-up Questions\n• vacuum freeze vs vacuum full?\n• Impact of long-running transactions on freeze cutoff?\n• How do prepared transactions affect freeze?\n• Replication slots holding xmin preventing freeze?\n\n## Common Mistakes\n• Disabling autovacuum on high-churn tables\n• Ignoring age(relfrozenxid) in monitoring\n• Assuming pg_repack eliminates wraparound risk without freeze\n• Replication slots holding xmin preventing freeze progress\n\n## Senior Engineer Insights\nWraparound is rare on modern autovacuum but catastrophic — include datfrozenxid age in every dashboard.\n\n## Key Commands\n• SELECT datname, age(datfrozenxid) FROM pg_database ORDER BY 2 DESC;\n• SELECT relname, age(relfrozenxid) FROM pg_class WHERE relkind = 'r' ORDER BY 2 DESC LIMIT 10;\n• VACUUM (FREEZE, VERBOSE) large_table;\n• SELECT * FROM pg_prepared_xacts;\n\n## Best Practices\n• Alert datfrozenxid age > 500M\n• Never disable autovacuum globally\n• Resolve orphaned prepared transactions promptly\n• Include freeze progress in major maintenance runbooks",
    command: "SELECT datname, age(datfrozenxid) FROM pg_database ORDER BY 2 DESC;\nSELECT relname, age(relfrozenxid) FROM pg_class WHERE relkind = 'r' ORDER BY 2 DESC LIMIT 10;\nVACUUM (FREEZE, VERBOSE) large_table;\nSELECT * FROM pg_prepared_xacts;",
    tags: ["postgresql","mvcc","hard","rare"],
  },
  {
    id: "pg-wal-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "What is PostgreSQL WAL and why is it critical for durability and recovery?",
    answer: "## Interview Answer\nWrite-Ahead Log records all data changes before reaching data files — crash recovery replays WAL from last checkpoint. WAL enables PITR, streaming replication, and logical decoding. pg_wal stores 16MB segments; wal_level controls content (minimal, replica, logical).\n\n## Detailed Explanation\nEvery commit waits for WAL flush to disk (wal_sync_method, synchronous_commit). Checkpoint records consistent recovery start point. full_page_writes protects against partial page writes after crash. wal_compression reduces segment size. archive_command ships completed segments to durable storage.\n\n## Production Perspective\nPrimary disk full on pg_wal — 500GB WAL accumulated because archive_command failed silently. Replication slots held segments. Fixed IAM role, added alert on pg_wal directory size > 100GB.\n\n## Common Follow-up Questions\n• Difference between wal_level replica and logical?\n• What triggers checkpoint?\n• How does synchronous_commit=off affect RPO?\n• WAL vs heap write ordering?\n\n## Common Mistakes\n• Placing pg_wal on slow network storage without testing\n• wal_level=minimal on replicas needing hot standby\n• Ignoring archive_command non-zero exit in logs\n• Manual rm on pg_wal files\n\n## Senior Engineer Insights\nWAL is the spine of PostgreSQL HA — trace commit → WAL insert → flush → bgwriter dirty pages.\n\n## Key Commands\n• SELECT pg_current_wal_lsn();\n• SELECT * FROM pg_stat_wal;\n• SHOW wal_level;\n\n## Best Practices\n• Monitor pg_wal disk separately from data\n• Test archive_command recovery monthly\n• Set wal_level=replica minimum for HA\n• Alert on pg_stat_archiver failed_count",
    command: "SELECT pg_current_wal_lsn();\nSELECT * FROM pg_stat_wal;\nSHOW wal_level;",
    tags: ["postgresql","wal","easy","very-common"],
  },
  {
    id: "pg-wal-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "Explain checkpoint tuning: max_wal_size, checkpoint_timeout, and checkpoint_completion_target.",
    answer: "## Interview Answer\nCheckpoints flush dirty buffers and create recovery starting point. max_wal_size triggers checkpoint when WAL generated exceeds threshold. checkpoint_timeout is time-based cap. checkpoint_completion_target spreads dirty writes over checkpoint interval reducing IO spikes.\n\n## Detailed Explanation\nDefault max_wal_size 1GB often too low for write-heavy workloads — frequent checkpoints increase WAL volume via full_page_writes. Raising max_wal_size to 4-8GB spreads checkpoints; pair with checkpoint_completion_target=0.9. pg_stat_bgwriter buffers_checkpoint reveals spike severity.\n\n## Production Perspective\nReplica lag spiked every 5 min correlating with checkpoint. Raised max_wal_size from 1GB to 8GB, checkpoint_completion_target 0.9; spike IO dropped 60%, replay lag stabilized.\n\n## Common Follow-up Questions\n• Impact of checkpoint on replication lag?\n• What is immediate checkpoint vs scheduled?\n• How does full_page_writes interact with checkpoint frequency?\n• pg_checkpoint view in PG 15+?\n\n## Common Mistakes\n• max_wal_size huge without recovery time analysis\n• checkpoint_completion_target=0 without spreading writes\n• Ignoring pg_stat_bgwriter after storage migration\n• Disabling full_page_writes to reduce WAL\n\n## Senior Engineer Insights\nCheckpoint tuning is IO smoothing — graph buffers_checkpoint against replica lag.\n\n## Key Commands\n• SELECT * FROM pg_stat_bgwriter;\n• SELECT name, setting FROM pg_settings WHERE name LIKE 'checkpoint%' OR name LIKE '%wal_size%';\n\n## Best Practices\n• Tune max_wal_size based on write throughput benchmarks\n• Use checkpoint_completion_target 0.9 on cloud disks\n• Monitor checkpoint duration after major tuning changes\n• Document expected recovery time with chosen WAL sizes",
    command: "SELECT * FROM pg_stat_bgwriter;\nSELECT name, setting FROM pg_settings WHERE name LIKE 'checkpoint%' OR name LIKE '%wal_size%';",
    tags: ["postgresql","wal","medium","common"],
  },
  {
    id: "pg-wal-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "How do you diagnose and fix WAL disk full conditions?",
    answer: "## Interview Answer\nWAL segments accumulate when archive_command fails, replication slots lag without consumption, or long pg_basebackup holds xmin. PostgreSQL stops accepting writes when pg_wal partition fills. Check pg_ls_waldir size, pg_replication_slots retained_wal, pg_stat_archiver, and standby replay lag.\n\n## Detailed Explanation\nEach slot prevents removal of WAL until consumer advances confirmed_flush_lsn. inactive slot on dropped replica is classic leak. wal_keep_size retains extra WAL. max_slot_wal_keep_size limits retention per slot.\n\n## Production Perspective\nDev left replication slot \"test_slot\" after local replica deleted — 400GB WAL in 48h, primary read-only. Dropped slot; WAL recycled within hour.\n\n## Common Follow-up Questions\n• Difference between pg_current_wal_lsn and insert_lsn?\n• How does wal_keep_size differ from slot retention?\n• Can you relocate pg_wal to separate mount?\n• pg_wal_summary view usage?\n\n## Common Mistakes\n• Creating slots without monitoring or TTL policy\n• Ignoring pg_stat_archiver.last_failed_time\n• Deleting WAL files manually to free space\n• No separate filesystem for pg_wal on write-heavy systems\n\n## Senior Engineer Insights\nRunbook: WAL full → check slots → check archiver → check lagging standby → never rm WAL.\n\n## Key Commands\n• SELECT slot_name, active, pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) AS retained FROM pg_replication_slots;\n• SELECT * FROM pg_stat_archiver;\n\n## Best Practices\n• Separate pg_wal mount with 20%+ headroom\n• Alert on replication slot inactive with retained WAL\n• Automate archive failure paging\n• Set max_slot_wal_keep_size to bound slot bloat",
    command: "SELECT slot_name, active, pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) AS retained FROM pg_replication_slots;\nSELECT * FROM pg_stat_archiver;",
    tags: ["postgresql","wal","medium","very-common"],
  },
  {
    id: "pg-wal-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "Compare synchronous_commit settings and their impact on RPO and latency.",
    answer: "## Interview Answer\nsynchronous_commit=on (default): commit waits for WAL flush — durable on single node. remote_write waits for standby to receive WAL; remote_apply waits for apply. off/local: commit returns after WAL written to buffer — faster but RPO window on crash.\n\n## Detailed Explanation\nsynchronous_standby_names controls which standbys count for quorum (FIRST n or ANY n). pg_stat_replication sync_state shows sync/async. Group commit batches multiple commits per WAL flush even with sync on.\n\n## Production Perspective\nAnalytics batch loader set synchronous_commit=local in session — 40% throughput gain acceptable for staging table truncated nightly. Production OLTP kept default on via role GUC.\n\n## Common Follow-up Questions\n• Quorum vs ANY in synchronous_standby_names?\n• Impact on logical replication?\n• Measuring commit latency?\n• Difference remote_write vs sync rep replay lag?\n\n## Common Mistakes\n• Global synchronous_commit=off on financial OLTP\n• Expecting sync replica without synchronous_standby_names configured\n• Not testing failover RPO after sync policy change\n• Confusing remote_write with sync rep replay lag SLA\n\n## Senior Engineer Insights\nMap business RPO to GUC: financial ledger = on + sync standby; clickstream buffer = local acceptable.\n\n## Key Commands\n• SHOW synchronous_commit;\n• SELECT application_name, sync_state, sync_priority FROM pg_stat_replication;\n• SELECT name, setting FROM pg_settings WHERE name LIKE 'synchronous%';\n\n## Best Practices\n• Document RPO per workload and matching commit settings\n• Use sync standbys for zero-RPO failover targets\n• Never set synchronous_commit=off globally without review\n• Test primary crash recovery with chosen settings",
    command: "SHOW synchronous_commit;\nSELECT application_name, sync_state, sync_priority FROM pg_stat_replication;\nSELECT name, setting FROM pg_settings WHERE name LIKE 'synchronous%';",
    tags: ["postgresql","wal","hard","common"],
  },
  {
    id: "pg-wal-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Design WAL archiving and monitoring for cross-region disaster recovery.",
    answer: "## Interview Answer\nArchive completed WAL via archive_command to S3/GCS with immutable versioning. Combine nightly pg_basebackup + continuous WAL for PITR. Monitor lag between pg_current_wal_lsn and last archived segment. DR restore: fetch base backup, replay WAL to target time via recovery.signal and restore_command.\n\n## Detailed Explanation\narchive_mode=on, archive_command copies segments to object store. wal-g and pgBackRest automate base backup + WAL push. Cross-region: async replication for RPO minutes + WAL archive for RPO seconds on regional loss.\n\n## Production Perspective\nRegional outage simulation: restored to DR region using wal-g backup 6h old + WAL replay to T-2min. RTO 90 min dominated by parallel restore of 2TB base.\n\n## Common Follow-up Questions\n• pg_combinebackup vs traditional restore?\n• How to validate WAL integrity before restore?\n• Impact of encryption on archive throughput?\n• recovery.signal vs recovery.conf PG 12+?\n\n## Common Mistakes\n• Archive to same region/account as primary — loses DR value\n• No restore drills — discovered corrupt base backup at real DR\n• Missing timeline history files in archive\n• Underestimating WAL replay time in RTO calc\n\n## Senior Engineer Insights\nDR architecture answer needs numbers: RPO from sync+archive, RTO from last restore drill.\n\n## Key Commands\n• SELECT * FROM pg_stat_archiver;\n• pgbackrest info\n• touch $PGDATA/recovery.signal;\n\n## Best Practices\n• Automate restore drills to secondary region quarterly\n• Store WAL in immutable object storage separate from primary\n• Monitor archive lag and failed_count continuously\n• Document timeline and target LSN recovery procedures",
    command: "SELECT * FROM pg_stat_archiver;\npgbackrest info\ntouch $PGDATA/recovery.signal;",
    tags: ["postgresql","wal","hard","rare"],
  },
  {
    id: "pg-vacuum-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "What does VACUUM do in PostgreSQL and why is it not optional?",
    answer: "## Interview Answer\nVACUUM reclaims dead tuple space from UPDATE/DELETE, updates visibility map for index-only scans, advances freeze cutoff, and updates planner statistics (with ANALYZE option). It does not block normal reads/writes unlike VACUUM FULL which rewrites tables exclusively.\n\n## Detailed Explanation\nDead tuples remain until vacuum marks space reusable within pages. Visibility map bits enable index-only scans when all tuples on page visible. vacuum_cost_delay throttles IO. Manual VACUUM needed when autovacuum cannot keep up or before DDL requiring low bloat.\n\n## Production Perspective\nOrders table 80GB with 40% dead tuples — sequential scans 10× slower. pg_stat_user_tables showed n_dead_tup=32M, last_autovacuum NULL for 2 weeks (autovacuum disabled on table). VACUUM ANALYZE orders; latency normalized.\n\n## Common Follow-up Questions\n• VACUUM vs VACUUM FULL vs pg_repack?\n• What is visibility map?\n• Does vacuum reclaim space to OS?\n• Impact of vacuum on replication slots xmin?\n\n## Common Mistakes\n• Running VACUUM FULL on production without maintenance window\n• Disabling autovacuum on \"critical\" tables — opposite of intent\n• Expecting VACUUM to shrink disk file size to OS\n• Not vacuuming after massive DELETE before peak traffic\n\n## Senior Engineer Insights\nVacuum is garbage collection — treat n_dead_tup ratio as SLA metric. VACUUM FULL is last resort; pg_repack preferred for online rewrite.\n\n## Key Commands\n• VACUUM (VERBOSE, ANALYZE) orders;\n• SELECT relname, n_live_tup, n_dead_tup, round(100.0*n_dead_tup/nullif(n_live_tup+n_dead_tup,0),2) AS dead_pct FROM pg_stat_user_tables ORDER BY n_dead_tup DESC LIMIT 10;\n• SELECT * FROM pg_stat_progress_vacuum;\n\n## Best Practices\n• Never disable autovacuum globally or on high-churn tables\n• Monitor dead tuple ratio per table\n• Use pg_repack instead of VACUUM FULL when online shrink needed\n• Schedule manual VACUUM after bulk deletes",
    command: "VACUUM (VERBOSE, ANALYZE) orders;\nSELECT relname, n_live_tup, n_dead_tup, round(100.0*n_dead_tup/nullif(n_live_tup+n_dead_tup,0),2) AS dead_pct FROM pg_stat_user_tables ORDER BY n_dead_tup DESC LIMIT 10;\nSELECT * FROM pg_stat_progress_vacuum;",
    tags: ["postgresql","vacuum","easy","very-common"],
  },
  {
    id: "pg-vacuum-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "Explain vacuum freeze, visibility map, and free space map (FSM).",
    answer: "## Interview Answer\nFreeze marks tuples with xmin older than vacuum_freeze_min_age as frozen — immune to xid wraparound comparison. Visibility map tracks all-visible and all-frozen pages enabling index-only scans and skipping freeze checks. FSM tracks free space within pages for INSERT/UPDATE placement.\n\n## Detailed Explanation\nAll-visible pages allow index-only scans without heap visit when visibility map bit set. FSM stored in separate fork; VACUUM updates FSM with reclaimable space. pg_visibility extension inspects map. Aggressive freeze during anti-wraparound vacuum can increase IO.\n\n## Production Perspective\nIndex-only scan never chosen on events table — visibility map never set because long-running reporting transaction held snapshot. pg_stat_activity showed 6h idle in transaction on read replica connection to primary. Killed session; next vacuum set all-visible bits; IO dropped 30%.\n\n## Common Follow-up Questions\n• How does vacuum_freeze_table_age trigger aggressive freeze?\n• Can FSM be corrupted and how to detect?\n• Index-only scan prerequisites?\n• Impact of fillfactor on FSM?\n\n## Common Mistakes\n• Long snapshots preventing all-visible page marking\n• Ignoring index-only scan opportunities after vacuum\n• Assuming freeze eliminates need for regular vacuum\n• Not monitoring pg_stat_all_tables last_vacuum freeze stats\n\n## Senior Engineer Insights\nIndex-only scan is free performance when VM bits set — debug why vacuum cannot set them (long xact, lock).\n\n## Key Commands\n• CREATE EXTENSION pg_visibility;\n• SELECT * FROM pg_visibility_map('events'::regclass) LIMIT 5;\n• EXPLAIN SELECT id FROM events WHERE created_at > now()-interval '1 day';\n\n## Best Practices\n• Kill or timeout idle in transaction sessions\n• Verify index-only scans on large indexed tables post-vacuum\n• Monitor wraparound age alongside regular vacuum\n• Use pg_stat_progress_vacuum during long runs",
    command: "CREATE EXTENSION pg_visibility;\nSELECT * FROM pg_visibility_map('events'::regclass) LIMIT 5;\nEXPLAIN SELECT id FROM events WHERE created_at > now()-interval '1 day';",
    tags: ["postgresql","vacuum","medium","common"],
  },
  {
    id: "pg-vacuum-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "When should you use VACUUM FULL versus pg_repack versus routine VACUUM?",
    answer: "## Interview Answer\nRoutine VACUUM reclaims space within pages without exclusive lock — use continuously via autovacuum. VACUUM FULL rewrites entire table with ACCESS EXCLUSIVE lock — shrinks file to OS but blocks all access. pg_repack rewrites online using triggers/logs with brief lock at end.\n\n## Detailed Explanation\nBloat from non-HOT updates leaves pages underfilled — routine vacuum cannot compact pages, only reuse space. pg_repack needs primary key and free disk ~ table size. VACUUM FULL simpler but unacceptable downtime on large OLTP tables. pg_squeeze and pg_repack are alternatives.\n\n## Production Perspective\n30GB table bloat after archive delete — VACUUM reduced dead tuples but file size unchanged at 30GB. pg_repack -t archive_events during low traffic; file shrunk to 8GB with 2-minute exclusive lock at swap.\n\n## Common Follow-up Questions\n• Does pg_repack work on partitioned tables?\n• REINDEX CONCURRENTLY vs vacuum for index bloat?\n• How to measure bloat without pgstattuple?\n• Cloud managed PG bloat options?\n\n## Common Mistakes\n• VACUUM FULL on multi-TB table during business hours\n• pg_repack without disk space for copy\n• Routine vacuum expecting OS-level shrink\n• Ignoring toast table bloat separately\n\n## Senior Engineer Insights\nPresent decision tree: dead tuples → VACUUM; page density → pg_repack; emergency shrink + downtime OK → VACUUM FULL.\n\n## Key Commands\n• CREATE EXTENSION pgstattuple;\n• SELECT * FROM pgstattuple('archive_events');\n• pg_repack -d appdb -t archive_events\n• VACUUM FULL VERBOSE archive_events; -- maintenance only\n\n## Best Practices\n• Prefer pg_repack for online bloat remediation\n• Measure bloat with pgstattuple before choosing tool\n• Ensure 2× table size free disk for repack\n• Schedule VACUUM FULL only with explicit downtime approval",
    command: "CREATE EXTENSION pgstattuple;\nSELECT * FROM pgstattuple('archive_events');\npg_repack -d appdb -t archive_events\nVACUUM FULL VERBOSE archive_events; -- maintenance only",
    tags: ["postgresql","vacuum","medium","very-common"],
  },
  {
    id: "pg-vacuum-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "DBA",
    question: "How does vacuum interact with replication slots, hot standby, and conflict resolution?",
    answer: "## Interview Answer\nVacuum on primary removes dead tuples still needed by standby queries — catalog_xmin held by slot or hot standby feedback prevents cleanup causing bloat. Standby max_standby_streaming_delay can cancel long queries conflicting with vacuum cleanup. Logical slots hold xmin until consumed.\n\n## Detailed Explanation\nReplication slot pg_replication_slots.catalog_xmin shows oldest xmin slot needs. hot_standby_feedback sends standby xmin to primary delaying vacuum. Conflicts on standby: snapshot too old if vacuum removed rows standby query needs. Monitor pg_stat_database_conflicts on standby.\n\n## Production Perspective\nLogical replication slot on CDC pipeline stalled 48h — primary orders table bloat 60GB. catalog_xmin frozen by unconsumed slot. Consumer caught up; VACUUM reclaimed space. Added max_slot_wal_keep_size and consumer lag alert.\n\n## Common Follow-up Questions\n• hot_standby_feedback trade-offs?\n• vacuum_defer_cleanup_age purpose?\n• Logical vs physical slot xmin behavior?\n• Canceling standby queries vs primary bloat?\n\n## Common Mistakes\n• hot_standby_feedback=on without monitoring primary bloat\n• Orphan logical replication slots after consumer decommission\n• Ignoring pg_stat_database_conflicts on replicas\n• max_standby_streaming_delay=-1 allowing unlimited bloat\n\n## Senior Engineer Insights\nCDC slots are vacuum enemies — treat consumer lag as primary storage incident. Cap slot retention and page on catalog_xmin age.\n\n## Key Commands\n• SELECT slot_name, catalog_xmin, pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), confirmed_flush_lsn)) FROM pg_replication_slots;\n• SELECT * FROM pg_stat_database_conflicts WHERE datname = current_database();\n• SHOW hot_standby_feedback;\n\n## Best Practices\n• Monitor catalog_xmin age per replication slot\n• Set max_slot_wal_keep_size and consumer lag SLOs\n• Use hot_standby_feedback judiciously with bloat alerts\n• Drop unused slots immediately",
    command: "SELECT slot_name, catalog_xmin, pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), confirmed_flush_lsn)) FROM pg_replication_slots;\nSELECT * FROM pg_stat_database_conflicts WHERE datname = current_database();\nSHOW hot_standby_feedback;",
    tags: ["postgresql","vacuum","hard","common"],
  },
  {
    id: "pg-vacuum-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Database Engineer",
    question: "Troubleshoot a table where vacuum never completes or constantly restarts.",
    answer: "## Interview Answer\nCheck pg_stat_progress_vacuum for phase and dead tuples remaining. Causes: infinite update churn on same pages, lock conflicts, wraparound vacuum competing, very large table with high cost delay, or blocked by long transaction xmin horizon. pg_locks shows blocking.\n\n## Detailed Explanation\nVacuum cannot truncate line pointers if xmin horizon blocked by idle transaction or slot. Constant updates on same rows create churn — vacuum never catches up. Reduce autovacuum_vacuum_cost_delay or increase autovacuum_max_workers temporarily. Consider partitioning hot append-only tables.\n\n## Production Perspective\nHeartbeat table updated every second — autovacuum ran continuously, never finishing, CPU 30% vacuum. Fixed by UNLOGGED side table for heartbeats + periodic merge, and fillfactor=50 on main table.\n\n## Common Follow-up Questions\n• What are vacuum phases in pg_stat_progress_vacuum?\n• autovacuum_work_mem impact on large tables?\n• Partitioning to isolate vacuum scope?\n• Deadlock during vacuum freeze?\n\n## Common Mistakes\n• Increasing cost delay when vacuum cannot keep up — wrong direction\n• Not identifying update churn root cause\n• Killing vacuum thinking it helps — worsens bloat\n• Single monolithic table for high-frequency updates\n\n## Senior Engineer Insights\nIf vacuum always active on one table, schema is wrong — partition or offload volatile columns before tuning GUCs forever.\n\n## Key Commands\n• SELECT * FROM pg_stat_progress_vacuum;\n• SELECT pid, state, xact_start, query FROM pg_stat_activity WHERE state != 'idle' ORDER BY xact_start LIMIT 10;\n• SELECT pg_blocking_pids(pid) FROM pg_stat_activity WHERE query LIKE '%vacuum%';\n\n## Best Practices\n• Architect away from single-row high-frequency update antipattern\n• Watch pg_stat_progress_vacuum during incidents\n• Resolve xmin horizon blockers before forcing vacuum\n• Partition largest churn tables",
    command: "SELECT * FROM pg_stat_progress_vacuum;\nSELECT pid, state, xact_start, query FROM pg_stat_activity WHERE state != 'idle' ORDER BY xact_start LIMIT 10;\nSELECT pg_blocking_pids(pid) FROM pg_stat_activity WHERE query LIKE '%vacuum%';",
    tags: ["postgresql","vacuum","hard","rare"],
  },
  {
    id: "pg-autovacuum-1",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "How does autovacuum decide when to vacuum and analyze a table?",
    answer: "## Interview Answer\nAutovacuum launcher forks workers when dead tuple count exceeds autovacuum_vacuum_scale_factor × reltuples + autovacuum_vacuum_threshold (similar formula for analyze). Anti-wraparound vacuum triggers regardless when relfrozenxid age exceeds autovacuum_freeze_max_age. Per-table settings via ALTER TABLE storage parameters override globals.\n\n## Detailed Explanation\nDefault scale factor 0.2 means 20% dead tuples before vacuum — too lazy for large tables (10M rows → 2M dead). Set autovacuum_vacuum_scale_factor=0.01 on hot tables. autovacuum_analyze_scale_factor controls stats refresh. pg_stat_user_tables last_autovacuum shows activity.\n\n## Production Perspective\n10M row inventory table waited for 2M dead tuples before vacuum — caused periodic latency spikes. ALTER TABLE inventory SET (autovacuum_vacuum_scale_factor = 0.02, autovacuum_analyze_scale_factor = 0.01); spikes eliminated.\n\n## Common Follow-up Questions\n• How many autovacuum workers default?\n• autovacuum_vacuum_insert_scale_factor PG 13+?\n• Does autovacuum run during replication?\n• log_autovacuum_min_duration usage?\n\n## Common Mistakes\n• Leaving default 0.2 scale factor on million-row OLTP tables\n• Disabling autovacuum globally via autovacuum=off\n• Not setting per-table params on append-heavy fact tables\n• Ignoring anti-wraparound vacuum in monitoring\n\n## Senior Engineer Insights\nFormula interview answer: threshold + scale_factor × n_live_tup. Always give numeric example for 50M row table.\n\n## Key Commands\n• SELECT relname, reloptions FROM pg_class WHERE relname = 'inventory';\n• SELECT name, setting FROM pg_settings WHERE name LIKE 'autovacuum%';\n• ALTER TABLE inventory SET (autovacuum_vacuum_scale_factor = 0.02);\n\n## Best Practices\n• Lower scale factor on large high-churn tables\n• Enable log_autovacuum_min_duration to log slow vacuums\n• Never disable autovacuum except brief debugging with ticket\n• Document per-table autovacuum overrides in schema",
    command: "SELECT relname, reloptions FROM pg_class WHERE relname = 'inventory';\nSELECT name, setting FROM pg_settings WHERE name LIKE 'autovacuum%';\nALTER TABLE inventory SET (autovacuum_vacuum_scale_factor = 0.02);",
    tags: ["postgresql","autovacuum","easy","very-common"],
  },
  {
    id: "pg-autovacuum-2",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "Tune autovacuum for high-write OLTP workloads without starving the database.",
    answer: "## Interview Answer\nIncrease autovacuum_max_workers (requires max_worker_processes headroom), reduce autovacuum_vacuum_cost_delay (or set to 0 for aggressive), raise autovacuum_vacuum_cost_limit, and set maintenance_work_mem higher for vacuum sort phases. Per-table scale factors more impactful than global knobs.\n\n## Detailed Explanation\nVacuum cost based on page reads/writes/delay; defaults throttle heavily on busy systems. autovacuum_naptime controls launcher frequency. Too aggressive autovacuum competes with OLTP IO — monitor pg_stat_bgwriter and disk latency. RDS Parameter groups expose same GUCs with limits.\n\n## Production Perspective\nFlash sale write storm — dead tuples piled faster than autovacuum. Temporarily set autovacuum_vacuum_cost_delay=0 and autovacuum_max_workers=6 on primary; reverted post-event. Added per-table tuning on orders and order_items.\n\n## Common Follow-up Questions\n• maintenance_work_mem vs autovacuum_work_mem PG 16+?\n• IO concurrency and autovacuum on SSD?\n• Does autovacuum hold locks that block OLTP?\n• autovacuum worker per database limit?\n\n## Common Mistakes\n• autovacuum_max_workers=10 without max_worker_processes increase\n• Permanent cost_delay=0 on IO-saturated disk\n• Tuning globals instead of per-table on one problematic relation\n• Ignoring autovacuum duration in logs\n\n## Senior Engineer Insights\nAutovacuum tuning is table-specific first. Global aggression is for incidents only — revert and fix scale factors.\n\n## Key Commands\n• ALTER SYSTEM SET autovacuum_max_workers = 5;\n• ALTER SYSTEM SET autovacuum_vacuum_cost_delay = 2;\n• SELECT pg_reload_conf();\n• SELECT * FROM pg_stat_progress_vacuum;\n\n## Best Practices\n• Tune per-table before global GUC changes\n• Correlate autovacuum with disk IO metrics\n• Revert temporary aggressive settings post-incident\n• Size maintenance_work_mem for largest table vacuum needs",
    command: "ALTER SYSTEM SET autovacuum_max_workers = 5;\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = 2;\nSELECT pg_reload_conf();\nSELECT * FROM pg_stat_progress_vacuum;",
    tags: ["postgresql","autovacuum","medium","common"],
  },
  {
    id: "pg-autovacuum-3",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "Diagnose \"autovacuum not keeping up\" on a production cluster.",
    answer: "## Interview Answer\nCheck pg_stat_user_tables n_dead_tup vs n_live_tup, last_autovacuum timestamps, pg_stat_progress_vacuum active workers, blocked xmin from pg_stat_activity and pg_replication_slots, and autovacuum settings. Compare vacuum rate to update/delete rate from pg_stat_user_tables n_tup_upd/n_tup_del.\n\n## Detailed Explanation\nCommon root causes: autovacuum disabled on table, scale factor too high, all workers busy on other tables, replication slot blocking cleanup, vacuum cost throttling, or table too large for naptime cycle. pg_stat_bgwriter and OS IO wait indicate resource starvation.\n\n## Production Perspective\nDashboard showed autovacuum workers always busy but events table n_dead_tup growing. Five workers vacuuming small dimension tables; events had no per-table tuning. Set storage params on events, increased max_workers to 4, dead ratio stabilized under 5%.\n\n## Common Follow-up Questions\n• How to prioritize which table to tune first?\n• pg_prewarm interaction with autovacuum?\n• Autovacuum on standby PG 13+?\n• toast table autovacuum separately?\n\n## Common Mistakes\n• Adding indexes instead of fixing vacuum backlog\n• Manual VACUUM FULL as first response\n• Not checking replication slot catalog_xmin\n• Assuming more CPU fixes IO-bound vacuum\n\n## Senior Engineer Insights\nRank tables by n_dead_tup × seq_scan count from pg_stat_user_tables — highest impact first.\n\n## Key Commands\n• SELECT relname, n_dead_tup, n_live_tup, last_autovacuum, last_autoanalyze FROM pg_stat_user_tables ORDER BY n_dead_tup DESC LIMIT 15;\n• SELECT count(*) FROM pg_stat_progress_vacuum;\n• SELECT slot_name, catalog_xmin FROM pg_replication_slots;\n\n## Best Practices\n• Dashboard dead tuple ratio top 20 tables\n• Alert last_autovacuum > 24h on high-churn tables\n• Review slot xmin during vacuum incidents\n• Capacity plan autovacuum workers with table count",
    command: "SELECT relname, n_dead_tup, n_live_tup, last_autovacuum, last_autoanalyze FROM pg_stat_user_tables ORDER BY n_dead_tup DESC LIMIT 15;\nSELECT count(*) FROM pg_stat_progress_vacuum;\nSELECT slot_name, catalog_xmin FROM pg_replication_slots;",
    tags: ["postgresql","autovacuum","medium","very-common"],
  },
  {
    id: "pg-autovacuum-4",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Common",
    role: "DBA",
    question: "Explain anti-wraparound autovacuum and emergency vacuum behavior.",
    answer: "## Interview Answer\nWhen relfrozenxid age approaches autovacuum_freeze_max_age (default 200M), autovacuum triggers aggressive anti-wraparound vacuum with zero cost delay, even on large tables, to advance relfrozenxid. If age exceeds vacuum_freeze_table_age, vacuum marks table urgently. Catastrophic age forces shutdown.\n\n## Detailed Explanation\nAnti-wraparound vacuum can IO-saturate disk and contend with OLTP. vacuum_freeze_min_age and vacuum_freeze_table_age control urgency. multixact has parallel autovacuum_multixact_freeze_max_age. Monitor age(relfrozenxid) proactively — do not wait for emergency.\n\n## Production Perspective\nAlert at age 150M triggered planned VACUUM FREEZE on top 5 tables over weekend. Avoided emergency autovacuum during Monday peak that previously caused 40% latency increase.\n\n## Common Follow-up Questions\n• Difference vacuum_freeze_min_age vs autovacuum_freeze_max_age?\n• Can anti-wraparound be deferred?\n• Impact on logical replication during freeze vacuum?\n• pg_resetxlog never for wraparound — why?\n\n## Common Mistakes\n• No datfrozenxid monitoring until emergency\n• Long prepared xacts blocking freeze\n• Assuming regular vacuum always advances relfrozenxid enough\n• Disabling autovacuum on large old tables\n\n## Senior Engineer Insights\nWraparound vacuum is the database saving itself — your job is never reaching it via proactive freeze monitoring.\n\n## Key Commands\n• SELECT relname, age(relfrozenxid) AS freeze_age FROM pg_class WHERE relkind='r' ORDER BY 2 DESC LIMIT 10;\n• SELECT datname, age(datfrozenxid) FROM pg_database;\n• VACUUM (FREEZE) critical_table;\n\n## Best Practices\n• Alert relfrozenxid age at 50% of autovacuum_freeze_max_age\n• Schedule proactive freeze vacuums on largest tables\n• Clear prepared xacts and stale slots blocking freeze\n• Include wraparound metrics in weekly DBA review",
    command: "SELECT relname, age(relfrozenxid) AS freeze_age FROM pg_class WHERE relkind='r' ORDER BY 2 DESC LIMIT 10;\nSELECT datname, age(datfrozenxid) FROM pg_database;\nVACUUM (FREEZE) critical_table;",
    tags: ["postgresql","autovacuum","hard","common"],
  },
  {
    id: "pg-autovacuum-5",
    technology: "postgresql",
    category: "postgresql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Cloud Engineer",
    question: "Design autovacuum monitoring and alerting strategy for 500+ table RDS PostgreSQL fleet.",
    answer: "## Interview Answer\nExport pg_stat_user_tables metrics via postgres_exporter: n_dead_tup ratio, last_autovacuum age, autovacuum_count, table size. Alert thresholds per size tier. Track datfrozenxid age cluster-wide. Correlate with Performance Insights IO spikes and replication slot lag. Automate per-table tuning via Terraform postgrestablers.\n\n## Detailed Explanation\nFleet-wide: Prometheus recording rules for max dead ratio, tables without autovacuum in 7 days, freeze age max. RDS lacks pg_repack — plan table maintenance windows. Use pg_cron for off-peak VACUUM on known problem tables. CloudWatch alarms on FreeStorageSpace and ReplicationSlotDiskUsage.\n\n## Production Perspective\nImplemented Datadog monitors on custom query max(dead_pct) by database — paged when >15% on any table >10GB. Reduced Sev2 incidents 70%. Per-table autovacuum params stored in schema migration repo.\n\n## Common Follow-up Questions\n• postgres_exporter autovacuum metrics?\n• RDS autovacuum limits by instance class?\n• Aurora vs RDS autovacuum differences?\n• Automating ALTER TABLE SET autovacuum via migration?\n\n## Common Mistakes\n• Single global dead tuple threshold for 1KB vs 1TB tables\n• No freeze age monitoring on managed PG\n• Ignoring toast bloat in fleet dashboards\n• Alert fatigue without severity tiers by table criticality\n\n## Senior Engineer Insights\nFleet strategy: tier tables by size and business criticality — different thresholds and runbooks per tier.\n\n## Key Commands\n• SELECT relname, pg_size_pretty(pg_total_relation_size(relid)), n_dead_tup FROM pg_stat_user_tables WHERE pg_total_relation_size(relid) > 10737418240 ORDER BY n_dead_tup DESC;\n• SELECT datname, age(datfrozenxid) FROM pg_database;\n\n## Best Practices\n• Tiered alerting by table size and criticality\n• Automate autovacuum params in schema migrations\n• Weekly report top 10 bloated tables per database\n• Integrate slot lag with autovacuum dashboards",
    command: "SELECT relname, pg_size_pretty(pg_total_relation_size(relid)), n_dead_tup FROM pg_stat_user_tables WHERE pg_total_relation_size(relid) > 10737418240 ORDER BY n_dead_tup DESC;\nSELECT datname, age(datfrozenxid) FROM pg_database;",
    tags: ["postgresql","autovacuum","hard","rare"],
  }
];
