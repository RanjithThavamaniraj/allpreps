// MySQL — architecture
// Structured interview content — run: node scripts/generate-db-tracks.mjs
// 17 questions

export const questions = [
  {
    id: "mysql-arch-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "Describe the MySQL server architecture: connection layer, SQL layer, and storage engine layer.",
    answer: "## Interview Answer\nMySQL separates client connections, SQL parsing/optimization, and pluggable storage engines. The connection layer handles auth and thread-per-connection (or thread pool); the SQL layer parses, optimizes, and executes; storage engines (InnoDB default) own data files, locking, and crash recovery.\n\n## Detailed Explanation\nConnection layer: listener, max_connections, auth plugins, connection buffers.\nSQL layer: parser, optimizer, cache (query cache removed in 8.0), privilege checks.\nStorage engine API: handler interface; InnoDB is default; MyISAM, MEMORY, etc. are plugins.\nBackground threads: InnoDB IO, purge, page cleaner, redo log writer.\nInformation schema and performance_schema expose runtime state.\n\n## Production Perspective\nSize max_connections against RAM (thread stack + per-connection buffers). Use ProxySQL or app pooling to avoid thread explosion. Monitor Threads_connected vs max_connections. After upgrades, verify default_storage_engine=InnoDB and performance_schema=ON.\n\n## Common Follow-up Questions\n• What changed in MySQL 8.0 architecture?\n• How does the thread pool plugin differ from one-thread-per-connection?\n• Where does the optimizer live relative to storage engines?\n\n## Common Mistakes\n• Confusing MySQL Server with InnoDB only — replication and binlog live above engines.\n• Ignoring performance_schema overhead on tiny instances.\n• Setting max_connections to 10,000 without memory math.\n\n## Senior Engineer Insights\nInterviewers want you to trace a SELECT from TCP accept → parse → optimize → handler API → InnoDB buffer pool → disk. Mention that DDL metadata is in InnoDB data dictionary (8.0+) not legacy .frm files.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'max_connections';\n• SHOW GLOBAL STATUS LIKE 'Threads_%';\n• SELECT * FROM information_schema.engines;\n• SHOW ENGINE INNODB STATUS\\G\n\n## Best Practices\n• Default InnoDB for all new tables.\n• Enable performance_schema in production with sensible consumers.\n• Use connection pooling at the app or ProxySQL layer.\n• Document thread and memory model in runbooks.",
    command: "SHOW VARIABLES LIKE 'max_connections';\nSHOW GLOBAL STATUS LIKE 'Threads_%';\nSELECT * FROM information_schema.engines;",
    tags: ["mysql","MySQL Architecture","easy","very-common"],
  },
  {
    id: "mysql-arch-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do the MySQL data dictionary and InnoDB system tablespace work in MySQL 8.0?",
    answer: "## Interview Answer\nMySQL 8.0 stores metadata in InnoDB data dictionary tables inside the ibdata1 (or file-per-table) system tablespace. DDL is transactional — no more .frm files. Table definitions, indexes, and stats are visible via information_schema and data dictionary views.\n\n## Detailed Explanation\nAtomic DDL: CREATE/ALTER/DROP logged in redo; rollback on failure.\nData dictionary persisted in mysql schema tables backed by InnoDB.\ninnodb_system tablespace holds DD, change buffer, undo tablespace pointers.\nPer-table .ibd files for file-per-table; general tablespace option for shared files.\nUpgrade path: mysql_upgrade replaced by server auto-upgrade on restart.\n\n## Production Perspective\nBackup tools must capture ibdata1 + all .ibd files consistently (XtraBackup). DDL during peak can cause metadata locks — use pt-online-schema-change or gh-ost for large tables. Monitor innodb_ddl_log progress for long alters.\n\n## Common Follow-up Questions\n• What is instant ADD COLUMN in InnoDB?\n• How do you recover if ibdata1 is corrupted?\n• Compare DD in 8.0 vs legacy .frm.\n\n## Common Mistakes\n• Deleting .ibd without DISCARD/IMPORT TABLE sequence.\n• Assuming mysqldump captures physical files — it is logical.\n• Running multiple concurrent ALGORITHM=INPLACE alters on hot tables without lock monitoring.\n\n## Senior Engineer Insights\nStrong answers tie DD atomicity to GTID-safe failover: half-applied DDL on a replica is recoverable from binlog replay. Mention mysql.ibd and how clone plugin copies DD consistently.\n\n## Key Commands\n• SELECT NAME, SPACE_TYPE FROM information_schema.innodb_tablespaces LIMIT 20;\n• SHOW CREATE TABLE orders\\G\n• SELECT * FROM performance_schema.metadata_locks;\n• ALTER TABLE t ADD COLUMN c INT, ALGORITHM=INSTANT;\n\n## Best Practices\n• Use file-per-table (innodb_file_per_table=ON).\n• Plan large DDL with online tools and MDL monitoring.\n• Validate backups include system tablespace.\n• Test major version upgrades on clone first.",
    command: "SELECT NAME, SPACE_TYPE FROM information_schema.innodb_tablespaces LIMIT 20;\nSHOW CREATE TABLE orders\\G\nSELECT * FROM performance_schema.metadata_locks;\nALTER TABLE t ADD COLUMN c INT, ALGORITHM=INSTANT;",
    tags: ["mysql","MySQL Architecture","medium","common"],
  },
  {
    id: "mysql-arch-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "Explain the role of performance_schema and sys schema in production troubleshooting.",
    answer: "## Interview Answer\nperformance_schema is low-overhead instrumentation inside the server — waits, stages, statements, memory, replication. The sys schema wraps it with readable views (schema_tables_with_full_table_scans, innodb_lock_waits). Use it before guessing from SHOW PROCESSLIST alone.\n\n## Detailed Explanation\nConsumers filter what is collected — enable only needed instruments on busy systems.\nevents_statements_* captures normalized SQL, latency, rows examined.\nevents_transactions_* shows trx state and lock time.\nmemory_summary_* tracks allocator usage by user/host.\nsys views add opinionated defaults for DBAs (statement_analysis, user_summary).\n\n## Production Perspective\nEnable setup_instruments selectively; full enable on small prod can cost 1–3% CPU. Use sys.statements_with_full_table_scans for missing index hunts. Pair with slow query log for historical trending. Reset counters after incidents for clean baselines.\n\n## Common Follow-up Questions\n• How do you find the top wait events?\n• What is the difference between events_statements_current and history?\n• How does sys schema differ from performance_schema?\n\n## Common Mistakes\n• Leaving all consumers ON on OLTP with millions of QPS micro-queries.\n• Not correlating digest with EXPLAIN — same digest, different plans after stats change.\n• Ignoring memory instruments until OOM.\n\n## Senior Engineer Insights\nPresent a workflow: digest from performance_schema → EXPLAIN FORMAT=JSON → index change → verify digest latency drop. Mention performance_schema vs Performance Insights on RDS/Aurora.\n\n## Key Commands\n• SELECT * FROM sys.statements_with_full_table_scans LIMIT 10;\n• SELECT * FROM sys.innodb_lock_waits\\G\n• SELECT EVENT_NAME, COUNT_STAR, SUM_TIMER_WAIT/1e12 AS sec FROM performance_schema.events_waits_summary_global_by_event_name ORDER BY SUM_TIMER_WAIT DESC LIMIT 10;\n• SELECT * FROM sys.user_summary;\n\n## Best Practices\n• Standardize on sys views in runbooks.\n• Export digest stats to Prometheus via mysqld_exporter.\n• Snapshot performance_schema before killing sessions.\n• Document which consumers are enabled per environment.",
    command: "SELECT * FROM sys.statements_with_full_table_scans LIMIT 10;\nSELECT * FROM sys.innodb_lock_waits\\G\nSELECT EVENT_NAME, COUNT_STAR, SUM_TIMER_WAIT/1e12 AS sec FROM performance_schema.events_waits_summary_global_by_event_name ORDER BY SUM_TIMER_WAIT DESC LIMIT 10;\nSELECT * FROM sys.user_summary;",
    tags: ["mysql","MySQL Architecture","medium","common"],
  },
  {
    id: "mysql-arch-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "Database Engineer",
    question: "How does a DML statement flow through InnoDB redo log, undo log, and doublewrite buffer?",
    answer: "## Interview Answer\nCommitted changes: update buffer pool pages, write undo for rollback/MVCC, append redo (physical) for crash recovery, flush via doublewrite against partial page writes, then commit and async flush data files. Binlog write happens at storage-engine commit coordination (2PC between InnoDB and binlog).\n\n## Detailed Explanation\nRedo log (ib_logfile*): circular WAL of page changes; innodb_flush_log_at_trx_commit controls durability vs latency.\nUndo log: multi-version rows; purge thread reclaims after no active read view needs old versions.\nDoublewrite: shared buffer writes full pages twice — staging area then data files.\nGroup commit batches fsync for redo + binlog.\nChange buffer merges secondary index updates asynchronously.\n\n## Production Perspective\nNever disable doublewrite on production unless vendor documents safe alternative (some NVMe arrays). Tune innodb_log_file_size and innodb_flush_log_at_trx_commit against RPO. Monitor Innodb_os_log_written and log waits. SSD: set innodb_io_capacity appropriately.\n\n## Common Follow-up Questions\n• What is the order of fsync in group commit?\n• How does sync_binlog interact with innodb_flush_log_at_trx_commit?\n• When does purge lag cause undo tablespace growth?\n\n## Common Mistakes\n• Setting innodb_flush_log_at_trx_commit=2 on primary without understanding 1-second RPO window.\n• Confusing redo (crash recovery) with binlog (replication/PITR).\n• Running with innodb_doublewrite=OFF to \"gain performance\".\n\n## Senior Engineer Insights\nDraw the mini 2PC: InnoDB prepare → write binlog → InnoDB commit. Explain why replicas can lag on commit-heavy workloads even with fast disks — binlog dump + apply pipeline.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'innodb_%log%';\nSHOW VARIABLES LIKE 'innodb_doublewrite%';\nSHOW ENGINE INNODB STATUS\\G\n-- Look for LOG, TRANSACTIONS, PURGE sections\n• SELECT * FROM performance_schema.file_summary_by_instance WHERE FILE_NAME LIKE '%ib_logfile%';\n\n## Best Practices\n• Keep redo on separate fast volume from data.\n• Monitor purge lag and history list length.\n• Use ROW binlog format for deterministic replication.\n• Test crash recovery time after tuning log sizes.",
    command: "SHOW VARIABLES LIKE 'innodb_%log%';\nSHOW VARIABLES LIKE 'innodb_doublewrite%';\nSHOW ENGINE INNODB STATUS\\G\n-- Look for LOG, TRANSACTIONS, PURGE sections\nSELECT * FROM performance_schema.file_summary_by_instance WHERE FILE_NAME LIKE '%ib_logfile%';",
    tags: ["mysql","MySQL Architecture","hard","common"],
  },
  {
    id: "mysql-arch-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "What are the main background threads in InnoDB and what does each do?",
    answer: "## Interview Answer\nKey InnoDB threads: master thread (scheduling), IO threads (read/write), page cleaner (flush dirty pages), log writer (redo), purge (undo cleanup), and insert buffer merge. Monitoring SHOW ENGINE INNODB STATUS shows their activity and backlog signals.\n\n## Detailed Explanation\nRead/write IO threads: async read-ahead and page flush (innodb_read_io_threads, innodb_write_io_threads).\nPage cleaner: reduces dirty page ratio before checkpoint pressure.\nLog writer: writes redo buffer to log files.\nPurge: removes undo history no longer needed for MVCC.\nFull status output includes SEMAPHORES, BUFFER POOL, INSERT BUFFER AND ADAPTIVE HASH INDEX.\n\n## Production Perspective\nHigh purge lag → growing undo, slower secondary index scans. Many pending flushes → tune innodb_io_capacity and buffer pool. On NUMA systems, consider innodb_numa_interleave. Cloud RDS hides some knobs — use Performance Insights wait events.\n\n## Common Follow-up Questions\n• What is adaptive hash index and when disable it?\n• How do you detect IO capacity saturation?\n• What triggers aggressive checkpointing?\n\n## Common Mistakes\n• Misreading \"history list length\" in status output.\n• Adding CPU without fixing IO-bound flush bottleneck.\n• Ignoring read-only replicas purge differences.\n\n## Senior Engineer Insights\nConnect background thread health to user-visible symptoms: purge lag ↔ long-running transactions holding views; flush pressure ↔ write latency spikes at checkpoint.\n\n## Key Commands\n• SHOW ENGINE INNODB STATUS\\G\n• SELECT NAME, STATUS FROM information_schema.innodb_metrics WHERE STATUS != 'disabled' LIMIT 30;\n• SHOW VARIABLES LIKE 'innodb_%io%';\nSHOW VARIABLES LIKE 'innodb_%purge%';\n\n## Best Practices\n• Review INNODB STATUS monthly on primaries.\n• Alert on history list length thresholds.\n• Right-size innodb_io_capacity for storage type.\n• Kill long idle transactions blocking purge.",
    command: "SHOW ENGINE INNODB STATUS\\G\nSELECT NAME, STATUS FROM information_schema.innodb_metrics WHERE STATUS != 'disabled' LIMIT 30;\nSHOW VARIABLES LIKE 'innodb_%io%';\nSHOW VARIABLES LIKE 'innodb_%purge%';",
    tags: ["mysql","MySQL Architecture","easy","common"],
  },
  {
    id: "mysql-arch-006",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Design a multi-tier MySQL deployment architecture for a global e-commerce platform.",
    answer: "## Interview Answer\nRegional primaries or single primary with read replicas per region; ProxySQL/Router for RW/RO routing; semi-sync or Group Replication for RPO; XtraBackup + binlog for PITR; cache (Redis) in front; schema per domain with connection pools sized per service.\n\n## Detailed Explanation\nWrite path: single primary (or GR single-primary) in home region; async replicas globally for read scaling.\nFailover: Orchestrator/MHA/InnoDB Cluster with GTID; DNS or proxy tier switches writes.\nData residency: separate clusters per jurisdiction; avoid cross-border replication of PII without legal review.\nObservability: PMM, Prometheus, slow log aggregation, replication lag dashboards.\nSharding last resort: Vitess or application sharding when single instance limits hit.\n\n## Production Perspective\nDocument RPO/RTO per tier. Game-day failover quarterly. Cap replica lag SLO (e.g., p99 < 5s). Use read-after-write consistency patterns (stick session to primary after checkout). Plan binlog retention for PITR across regions.\n\n## Common Follow-up Questions\n• When would you choose Vitess over vertical scale?\n• How handle cross-region write latency?\n• Compare Aurora global database vs self-managed GR.\n\n## Common Mistakes\n• Multi-primary GR with app assuming single-writer semantics — write conflicts.\n• Read replicas for critical read-your-writes without routing logic.\n• No tested PITR runbook.\n\n## Senior Engineer Insights\nPresent a diagram: App → ProxySQL → Primary / Replicas → Backup pipeline → DR region. Call out metadata locks during schema migrations on global catalog tables.\n\n## Key Commands\n• SELECT * FROM performance_schema.replication_group_members;\n• SHOW REPLICA STATUS\\G\n• SELECT * FROM sys.schema_table_statistics ORDER BY rows_full_scanned DESC LIMIT 10;\n\n## Best Practices\n• Single writer unless conflict-aware app.\n• Automate failover with external orchestrator.\n• Separate OLTP from analytics (CDC to warehouse).\n• IaC for all topology changes.",
    command: "SELECT * FROM performance_schema.replication_group_members;\nSHOW REPLICA STATUS\\G\nSELECT * FROM sys.schema_table_statistics ORDER BY rows_full_scanned DESC LIMIT 10;",
    tags: ["mysql","MySQL Architecture","hard","rare"],
  },
  {
    id: "mysql-innodb-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "Why is InnoDB the default storage engine and what core features does it provide?",
    answer: "## Interview Answer\nInnoDB provides ACID transactions, row-level locking, MVCC for consistent reads, crash recovery via redo/undo, and foreign keys. It stores rows in clustered primary key order and is required for replication durability patterns in production.\n\n## Detailed Explanation\nTransactions: BEGIN/COMMIT/ROLLBACK with isolation levels (RR default, RC common in web apps).\nRow locks: record and gap locks under RR prevent phantoms; next-key locks.\nMVCC: snapshot reads without blocking writers; Read View per transaction.\nClustered index: PK is the table; secondary indexes leaf nodes store PK values.\nBuffer pool caches data and index pages in memory.\n\n## Production Perspective\nAll new tables InnoDB; audit for ENGINE=MyISAM in legacy schemas. Match isolation level to app expectations (ORM defaults vs RR). Monitor lock waits via performance_schema.data_locks (8.0).\n\n## Common Follow-up Questions\n• Compare RR vs RC in InnoDB.\n• What happens without a primary key?\n• How does InnoDB handle TEXT/BLOB off-page storage?\n\n## Common Mistakes\n• Using SELECT ... FOR UPDATE without index — locks escalate to gap/next-key on large ranges.\n• Wide UUID v4 PK causing fragmented inserts.\n• Assuming FK checks are free on bulk loads.\n\n## Senior Engineer Insights\nMention hidden DB_ROW_ID if no PK defined — bad for performance. Prefer surrogate BIGINT or ordered UUID for PK.\n\n## Key Commands\n• SHOW TABLE STATUS WHERE Name='orders'\\G\n• SELECT engine, support FROM information_schema.engines;\n• ALTER TABLE legacy ENGINE=InnoDB;\n• SELECT * FROM performance_schema.data_locks LIMIT 10;\n\n## Best Practices\n• Explicit PRIMARY KEY on every table.\n• Use RC if app handles consistency and wants fewer gap locks.\n• Size buffer pool to 70–80% RAM on dedicated DB nodes.\n• Review FK cascade impact before production DDL.",
    command: "SHOW TABLE STATUS WHERE Name='orders'\\G\nSELECT engine, support FROM information_schema.engines;\nALTER TABLE legacy ENGINE=InnoDB;\nSELECT * FROM performance_schema.data_locks LIMIT 10;",
    tags: ["mysql","InnoDB","easy","very-common"],
  },
  {
    id: "mysql-innodb-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "DBA",
    question: "How do InnoDB isolation levels affect locking behavior and phantom reads?",
    answer: "## Interview Answer\nREAD COMMITTED sees committed rows per statement; fewer gap locks. REPEATABLE READ (default) holds consistent read view for transaction lifetime and uses next-key locks on writes to prevent phantoms. SERIALIZABLE locks every read. Most SaaS apps set transaction_isolation=READ-COMMITTED globally.\n\n## Detailed Explanation\nRC: gap locks minimized except FK and unique checks; each statement new snapshot.\nRR: consistent reads share Read View; locking reads (FOR UPDATE) use next-key locks.\nPhantom: prevented in RR via gap locks on index ranges; possible in RC.\nSemi-consistent read optimization in RC for UPDATE statements.\nBinlog_format=ROW replicates row images regardless of isolation nuances.\n\n## Production Perspective\nChanging global isolation requires app regression tests. ORMs may assume RR repeatable reads within @Transactional. Debug deadlocks: SHOW ENGINE INNODB STATUS — LATEST DETECTED DEADLOCK. Consider index order for lock contention hot spots.\n\n## Common Follow-up Questions\n• Why does Amazon RDS default RC?\n• What locks does INSERT intend on duplicate unique key?\n• How does gap lock interact with REPLICA READ ONLY?\n\n## Common Mistakes\n• Mixing isolation levels across connection pools unpredictably.\n• Long RR transactions causing undo bloat and slow purge.\n• Using LOCK TABLES on InnoDB — use transactions instead.\n\n## Senior Engineer Insights\nExplain the 2019+ change: INSERT ... SELECT under RC may still need gap locks for binlog consistency when statement-based — with ROW binlog impact is reduced.\n\n## Key Commands\n• SELECT @@transaction_isolation;\n• SET SESSION transaction_isolation='READ-COMMITTED';\n• SHOW ENGINE INNODB STATUS\\G\n-- LATEST DETECTED DEADLOCK\n• SELECT * FROM performance_schema.data_locks WHERE OBJECT_NAME='orders';\n\n## Best Practices\n• Document isolation level in service README.\n• Use ROW binlog format in production.\n• Keep transactions short on hot rows.\n• Add covering indexes to narrow lock ranges.",
    command: "SELECT @@transaction_isolation;\nSET SESSION transaction_isolation='READ-COMMITTED';\nSHOW ENGINE INNODB STATUS\\G\n-- LATEST DETECTED DEADLOCK\nSELECT * FROM performance_schema.data_locks WHERE OBJECT_NAME='orders';",
    tags: ["mysql","InnoDB","medium","very-common"],
  },
  {
    id: "mysql-innodb-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "How do you diagnose and resolve InnoDB deadlocks in production?",
    answer: "## Interview Answer\nInnoDB detects circular lock waits and rolls back the smaller transaction (by rows modified). Capture LATEST DETECTED DEADLOCK from SHOW ENGINE INNODB STATUS or enable innodb_print_all_deadlocks. Fix by consistent lock ordering, shorter transactions, and better indexes.\n\n## Detailed Explanation\nDeadlock graph shows two transactions waiting on each other's locks.\nTypical causes: concurrent UPDATE on overlapping index ranges, FK checks locking parent/child in different order, triggers amplifying lock footprint.\ninnodb_lock_wait_timeout is for lock wait timeout, not deadlock — deadlocks resolve instantly.\nperformance_schema.data_lock_waits shows live waits in 8.0.\nApplication should retry deadlock errors (1213 / 40001).\n\n## Production Perspective\nLog deadlock dumps to error log in staging; in prod use PMM or scrape STATUS periodically. pt-deadlock-logger for history. After index fix, verify deadlock rate drop in application metrics.\n\n## Common Follow-up Questions\n• Why did InnoDB pick transaction 2 as victim?\n• Can deadlocks occur on replicas?\n• Difference between lock wait timeout and deadlock?\n\n## Common Mistakes\n• Retrying without exponential backoff causing thundering herd.\n• Adding random sleep without fixing lock order.\n• Using SELECT ... FOR UPDATE on non-indexed column scanning entire table.\n\n## Senior Engineer Insights\nWalk through a checkout example: txn A locks user row then inventory; txn B locks inventory then user — classic ordering fix: always lock parent then child by PK order.\n\n## Key Commands\n• SHOW ENGINE INNODB STATUS\\G\n• SET GLOBAL innodb_print_all_deadlocks=ON;\n• SELECT * FROM sys.innodb_lock_waits\\G\n• SELECT * FROM performance_schema.events_errors_summary_global_by_error WHERE ERROR_NAME='ER_LOCK_DEADLOCK';\n\n## Best Practices\n• Retry deadlocks in application layer.\n• Access tables in consistent order.\n• Index all columns used in WHERE/JOIN for updates.\n• Load test concurrent update paths before launch.",
    command: "SHOW ENGINE INNODB STATUS\\G\nSET GLOBAL innodb_print_all_deadlocks=ON;\nSELECT * FROM sys.innodb_lock_waits\\G\nSELECT * FROM performance_schema.events_errors_summary_global_by_error WHERE ERROR_NAME='ER_LOCK_DEADLOCK';",
    tags: ["mysql","InnoDB","medium","common"],
  },
  {
    id: "mysql-innodb-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "DBA",
    question: "Explain InnoDB buffer pool management, LRU, and how to tune for OLTP workloads.",
    answer: "## Interview Answer\nThe buffer pool caches data and index pages. InnoDB uses a split LRU (young/old sublists) to avoid full scans polluting cache. Tune innodb_buffer_pool_size to 70–80% of RAM, multiple instances for large pools, and monitor hit ratio via Innodb_buffer_pool_read_requests vs reads.\n\n## Detailed Explanation\ninnodb_buffer_pool_instances reduces mutex contention (automatically scaled in 8.0).\nMidpoint insertion: first touch goes to old sublist; re-access promotes to young.\ninnodb_old_blocks_pct controls scan resistance threshold.\nAdaptive hash index speeds hot secondary lookups — disable if CPU contention (innodb_adaptive_hash_index=OFF on some workloads).\nBuffer pool dump/load (innodb_buffer_pool_dump_at_shutdown) warms cache after restart.\n\n## Production Perspective\nMiss rate spike after restart → plan buffer pool warm-up or staggered rolling restart. Cloud: right-size instance RAM before CPU. Watch evictions vs read requests; not every workload needs huge pool if working set exceeds RAM — focus on index efficiency.\n\n## Common Follow-up Questions\n• What is innodb_buffer_pool_chunk_size?\n• How does clone plugin affect buffer pool?\n• When does read-ahead hurt performance?\n\n## Common Mistakes\n• Setting buffer pool to 95% RAM starving OS and connection memory.\n• Ignoring innodb_buffer_pool_size on replicas with different read patterns.\n• Equating hit ratio 99% with \"no tuning needed\" while queries examine millions of rows.\n\n## Senior Engineer Insights\nCorrelate buffer pool reads with EXPLAIN rows examined — cache hit on inefficient plan still wastes CPU. Mention innodb_buffer_pool_stats for per-table cache footprint.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'innodb_buffer_pool%';\nSHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_%';\n• SELECT POOL_ID, POOL_SIZE, FREE_BUFFERS, DATABASE_PAGES FROM information_schema.innodb_buffer_pool_stats;\n• SET GLOBAL innodb_buffer_pool_dump_now=ON;\n\n## Best Practices\n• Monitor read_requests/reads ratio with alerting.\n• Align pool size across fleet for predictable performance.\n• Combine pool tuning with query/index optimization.\n• Use dedicated DB nodes — avoid sharing with app heap.",
    command: "SHOW VARIABLES LIKE 'innodb_buffer_pool%';\nSHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_%';\nSELECT POOL_ID, POOL_SIZE, FREE_BUFFERS, DATABASE_PAGES FROM information_schema.innodb_buffer_pool_stats;\nSET GLOBAL innodb_buffer_pool_dump_now=ON;",
    tags: ["mysql","InnoDB","hard","common"],
  },
  {
    id: "mysql-innodb-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "What are InnoDB tablespaces, file-per-table, and general tablespaces?",
    answer: "## Interview Answer\nSystem tablespace (ibdata1) holds DD and optionally undo/change buffer. file-per-table (default) creates table.ibd per table — easier backup, truncate, and transport. General tablespaces (CREATE TABLESPACE) share ibd files across tables for management or placement on specific storage.\n\n## Detailed Explanation\ninnodb_file_per_table=ON: DROP TABLE reclaims OS space; ALTER TABLE copy cost isolated.\nShared ibdata growth from file-per-table off is hard to reclaim.\nGeneral tablespace: CREATE TABLESPACE ts1 ADD DATAFILE 'ts1.ibd';\nTransportable tablespace: DISCARD/IMPORT for fast bulk load or migration.\nUndo tablespaces (8.0): separate from system tablespace; truncate when empty.\n\n## Production Perspective\nUse file-per-table unless strong reason for shared. Place hot tablespaces on NVMe via DATA DIRECTORY or symlink strategy (documented per OS). Monitor ibd size vs buffer pool for working set fit. XtraBackup handles per-table files with --tables.\n\n## Common Follow-up Questions\n• How does innodb_undo_tablespaces work?\n• What is ALTER TABLE ... IMPORT TABLESPACE?\n• Impact of innodb_directories on recovery?\n\n## Common Mistakes\n• Copying .ibd without FLUSH TABLES ... FOR EXPORT.\n• Deleting ibd file manually corrupting catalog.\n• One giant general tablespace preventing granular restore.\n\n## Senior Engineer Insights\nDiscuss instant DDL and whether operation rebuilds table — affects tablespace size and replication time. Mention Aurora storage layer differences if cloud interview.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'innodb_file_per_table';\nSHOW VARIABLES LIKE 'innodb_undo_tablespaces';\n• SELECT NAME, PATH, SPACE_TYPE FROM information_schema.innodb_tablespaces;\n• ALTER TABLE orders DISCARD TABLESPACE;\nALTER TABLE orders IMPORT TABLESPACE;\n\n## Best Practices\n• Keep file-per-table enabled.\n• Monitor table size growth with retention policies.\n• Use partition drop for archival instead of DELETE.\n• Document transportable tablespace procedures.",
    command: "SHOW VARIABLES LIKE 'innodb_file_per_table';\nSHOW VARIABLES LIKE 'innodb_undo_tablespaces';\nSELECT NAME, PATH, SPACE_TYPE FROM information_schema.innodb_tablespaces;\nALTER TABLE orders DISCARD TABLESPACE;\nALTER TABLE orders IMPORT TABLESPACE;",
    tags: ["mysql","InnoDB","medium","common"],
  },
  {
    id: "mysql-innodb-006",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Lead an incident where InnoDB corruption was detected — what is your recovery approach?",
    answer: "## Interview Answer\nStop writes, assess scope (page checksum error vs tablespace corruption). Restore from latest validated XtraBackup + binlog PITR to clean point. For single table, restore from partial backup or logical dump. Avoid continuing on corrupted pages — innodb_force_recovery is read-only triage only.\n\n## Detailed Explanation\nError log shows InnoDB: Database page corruption or assertion failures.\ninnodb_force_recovery levels 1–6 allow SELECT-only salvage — never level >4 without expert.\nPage checksums (innodb_checksum_algorithm=crc32) detect silent corruption.\nRestore workflow: provision new instance, xtrabackup --prepare, restore, apply binlog to target time, cutover.\nRoot causes: hardware, fsync bugs, killed restore, full disk.\n\n## Production Perspective\nMonthly restore drills catch bad backups before incidents. Keep binlog on separate volume with retention matching RPO. Document force_recovery data export scripts. Communicate data loss window if PITR gap exists.\n\n## Common Follow-up Questions\n• When is logical mysqldump preferred over physical restore?\n• How does clone plugin help rebuild replica?\n• What does innodb_force_recovery=6 mean?\n\n## Common Mistakes\n• Running force_recovery=6 on primary for \"quick fix\".\n• Restarting without identifying underlying disk failure.\n• Skipping binlog apply after physical restore — data drift.\n\n## Senior Engineer Insights\nTimeline narrative wins interviews: detect → isolate → restore path decision → stakeholder comms → postmortem with backup validation automation.\n\n## Key Commands\n• SHOW ENGINE INNODB STATUS\\G\n• SELECT * FROM information_schema.innodb_metrics WHERE NAME LIKE '%corrupt%';\n• xtrabackup --prepare --target-dir=/restore\nmysqlbinlog --stop-datetime='2024-06-15 14:00:00' binlog.000045 | mysql\n\n## Best Practices\n• Validate backups with automated restore tests.\n• Monitor disk health and SMART alerts.\n• Separate binlog and data volumes.\n• Run checksum-enabled backups (XtraBackup default).",
    command: "SHOW ENGINE INNODB STATUS\\G\nSELECT * FROM information_schema.innodb_metrics WHERE NAME LIKE '%corrupt%';\nxtrabackup --prepare --target-dir=/restore\nmysqlbinlog --stop-datetime='2024-06-15 14:00:00' binlog.000045 | mysql",
    tags: ["mysql","InnoDB","hard","rare"],
  },
  {
    id: "mysql-myisam-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Common",
    role: "Database Engineer",
    question: "Compare InnoDB and MyISAM storage engines for production workloads.",
    answer: "## Interview Answer\nInnoDB: transactions, row locks, crash recovery, FK — use for all OLTP. MyISAM: table-level locks, no transactions, fast read-only tables, full-text before InnoDB FT — legacy only. Production standard is 100% InnoDB except rare read-only archives.\n\n## Detailed Explanation\nMyISAM files: .frm (pre-8.0), .MYD data, .MYI indexes — no redo log.\nCrash recovery: REPAIR TABLE may lose data; no automatic recovery.\nTable locks block writes during SELECT on mixed workloads.\nMyISAM key cache (key_buffer_size) separate from InnoDB buffer pool.\nInnoDB full-text and spatial indexes matured — MyISAM advantages largely gone.\n\n## Production Perspective\nInventory: SELECT table_schema, table_name, engine FROM information_schema.tables WHERE engine='MyISAM'. Migration: ALTER TABLE ... ENGINE=InnoDB during maintenance window (rewrites table). Watch disk space 2× during conversion.\n\n## Common Follow-up Questions\n• Does MyISAM support foreign keys?\n• When is MEMORY engine appropriate?\n• How migrate MyISAM full-text to InnoDB?\n\n## Common Mistakes\n• Choosing MyISAM for \"faster inserts\" on concurrent write workloads.\n• Running MyISAM on replicated primary — crash inconsistency on replica.\n• Ignoring metadata locks during ENGINE conversion on large tables.\n\n## Senior Engineer Insights\nFrame MyISAM as technical debt — migration plan with pt-online-schema-change if rename/conversion needed online. Mention MyISAM in mysql system tables history only.\n\n## Key Commands\n• SELECT engine, COUNT(*) FROM information_schema.tables GROUP BY engine;\n• SHOW TABLE STATUS WHERE Engine='MyISAM'\\G\n• ALTER TABLE legacy_log ENGINE=InnoDB, ALGORITHM=COPY;\n\n## Best Practices\n• Ban MyISAM in coding standards.\n• Automate CI check for ENGINE=InnoDB in migrations.\n• Convert remaining MyISAM during major upgrades.\n• Use InnoDB compressed for read-heavy archives if size matters.",
    command: "SELECT engine, COUNT(*) FROM information_schema.tables GROUP BY engine;\nSHOW TABLE STATUS WHERE Engine='MyISAM'\\G\nALTER TABLE legacy_log ENGINE=InnoDB, ALGORITHM=COPY;",
    tags: ["mysql","MyISAM","easy","common"],
  },
  {
    id: "mysql-myisam-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "DBA",
    question: "How would you migrate a legacy MyISAM application to InnoDB with minimal downtime?",
    answer: "## Interview Answer\nAssess size, locks, full-text, and repair status. Use mysqldump reload, ALTER ENGINE=InnoDB, or pt-online-schema-change trigger-based migration for zero-downtime. Validate query plans after — MyISAM table scans may have hidden missing indexes. Switch in maintenance window with read-only gate.\n\n## Detailed Explanation\nPre-check: CHECK TABLE, row counts, dependent apps.\nSmall tables: direct ALTER ENGINE=InnoDB (blocking copy in older versions).\nLarge tables: pt-osc or gh-ost creates shadow InnoDB table, swaps.\nFull-text: recreate InnoDB FT indexes — syntax differs slightly.\nReplication: row format already ROW; test replica apply time for large DDL.\n\n## Production Perspective\nRun on staging with production-sized data. Capture slow query baseline before/after. Monitor MDL during cutover. Rollback plan: keep MyISAM snapshot until validation window passes.\n\n## Common Follow-up Questions\n• Does ALGORITHM=INPLACE work for ENGINE change?\n• Impact on auto_increment behavior?\n• How handle MyISAM-specific SELECT LOCK?\n\n## Common Mistakes\n• Converting during peak without throttling pt-osc.\n• Forgetting to rebuild stats ANALYZE TABLE after migration.\n• Assuming identical performance without EXPLAIN comparison.\n\n## Senior Engineer Insights\nDiscuss business case: support cost of MyISAM vs one-time migration investment. Quantify table-lock incidents if metrics exist.\n\n## Key Commands\n• CHECK TABLE legacy_orders EXTENDED;\n• pt-online-schema-change --alter 'ENGINE=InnoDB' D=shop,t=legacy_orders --execute\n• ANALYZE TABLE legacy_orders;\nEXPLAIN SELECT * FROM legacy_orders WHERE status='OPEN';\n\n## Best Practices\n• Test on clone with production stats.\n• Schedule during low-traffic window.\n• Validate app integration tests post-migration.\n• Update documentation and ER diagrams.",
    command: "CHECK TABLE legacy_orders EXTENDED;\npt-online-schema-change --alter 'ENGINE=InnoDB' D=shop,t=legacy_orders --execute\nANALYZE TABLE legacy_orders;\nEXPLAIN SELECT * FROM legacy_orders WHERE status='OPEN';",
    tags: ["mysql","MyISAM","medium","rare"],
  },
  {
    id: "mysql-myisam-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Rare",
    role: "Production Support",
    question: "What is MyISAM table-level locking and why does it hurt concurrent OLTP?",
    answer: "## Interview Answer\nMyISAM grants READ locks shared across readers or WRITE locks exclusive for all access. A long SELECT blocks writes; bulk INSERT blocks reads. InnoDB row-level locks allow concurrent reads during writes — essential for OLTP.\n\n## Detailed Explanation\nLOCK TABLES explicit locks still exist for InnoDB but discouraged.\nMyISAM concurrent insert feature allows INSERT with SELECT on tail only — niche.\nKey buffer caches index blocks only — data read from OS cache.\nNo MVCC — readers may see partial updates without transactions.\nMixing engines in JOIN prevents transactional consistency.\n\n## Production Perspective\nIf legacy MyISAM remains, schedule writes off-peak; avoid long reporting queries on live tables — copy to InnoDB replica or warehouse. Monitor Key_read_requests vs Key_reads for key cache efficiency.\n\n## Common Follow-up Questions\n• What is concurrent_insert setting?\n• Compare to InnoDB metadata locks.\n• Can MyISAM participate in transactions via handlerton?\n\n## Common Mistakes\n• Running mysqldump without --single-transaction on MyISAM-heavy DB — inconsistent dump.\n• Using MyISAM for session store with concurrent updates.\n• REPAIR TABLE as routine maintenance instead of fixing root cause.\n\n## Senior Engineer Insights\nUse table-lock pain as migration justification metric — count blocked threads during peak if historical processlist logs exist.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'concurrent_insert';\n• SHOW OPEN TABLES WHERE In_use > 0;\n• SHOW STATUS LIKE 'Table_locks_%';\n\n## Best Practices\n• Eliminate MyISAM from OLTP paths.\n• Use read replicas for reporting.\n• Never mix transactional and non-transactional engines in one business transaction.\n• Monitor Table_locks_waited.",
    command: "SHOW VARIABLES LIKE 'concurrent_insert';\nSHOW OPEN TABLES WHERE In_use > 0;\nSHOW STATUS LIKE 'Table_locks_%';",
    tags: ["mysql","MyISAM","easy","rare"],
  },
  {
    id: "mysql-myisam-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "Database Engineer",
    question: "How do MyISAM repair operations work and what are the risks?",
    answer: "## Interview Answer\nMyISAM corruption from crashes requires REPAIR TABLE or myisamchk offline. REPAIR may rebuild indexes and lose rows marked corrupt. Always backup before repair. InnoDB eliminates this class of operational toil with redo-based recovery.\n\n## Detailed Explanation\nmyisamchk -r repairs index chains; must stop mysqld or lock table.\nREPAIR TABLE USE_FRM when .MYI missing metadata edge cases.\nOPTIMIZE TABLE for MyISAM reclaims space by rebuild.\nReplication: repairing primary does not fix replica — resync replica.\nNo crash-safe guarantee — last writes may vanish.\n\n## Production Perspective\nIf still on MyISAM logs, mirror to InnoDB via ETL. Schedule myisamchk from cron only on read-only replicas with mysqld stopped — rare pattern. Document row loss acceptance for repair ops.\n\n## Common Follow-up Questions\n• Difference REPAIR vs OPTIMIZE for MyISAM?\n• Can you repair on a running replica?\n• Why is myisam_recover_options relevant at startup?\n\n## Common Mistakes\n• Running myisamchk while mysqld active on same files.\n• Assuming REPAIR fixes root disk issue.\n• Not verifying row counts post-repair against application audit.\n\n## Senior Engineer Insights\nContrast with InnoDB crash recovery narrative — business prefers paying migration cost once vs recurring repair fire drills.\n\n## Key Commands\n• REPAIR TABLE corrupt_log USE_FRM;\n• myisamchk -r /var/lib/mysql/db/log.MYI\n• CHECK TABLE corrupt_log EXTENDED;\n\n## Best Practices\n• Migrate off MyISAM instead of tuning repair cron.\n• Backup before any repair.\n• Validate checksums and counts after repair.\n• Root-cause hardware/filesystem checks.",
    command: "REPAIR TABLE corrupt_log USE_FRM;\nmyisamchk -r /var/lib/mysql/db/log.MYI\nCHECK TABLE corrupt_log EXTENDED;",
    tags: ["mysql","MyISAM","medium","rare"],
  },
  {
    id: "mysql-myisam-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "When might MyISAM still appear in a MySQL 8.0 environment and how do you handle it?",
    answer: "## Interview Answer\nRare: legacy app tables, vendor schemas, read-only archive on isolated instance, or internal temp patterns (prefer InnoDB internal tmp). Policy: discover via information_schema, risk-rate, schedule conversion, block new MyISAM in CI.\n\n## Detailed Explanation\nmysql system tables were MyISAM historically — now InnoDB in 8.0.\nSome ISV products still ship MyISAM — vendor escalation path.\nRead-only stat tables: convert or export to object storage + Athena pattern.\nMyISAM not supported in Group Replication — blocker for HA adoption.\nFull-text legacy: InnoDB FT with ngram parser for CJK.\n\n## Production Perspective\nGovernance: Liquibase/Flyway check rejects CREATE without ENGINE=InnoDB. Quarterly audit report to engineering leadership. For immovable vendor MyISAM, isolate on non-HA read instance with frequent logical backups.\n\n## Common Follow-up Questions\n• Is MyISAM removed entirely in future MySQL?\n• Impact on mysqlbinlog flashback?\n• MyISAM vs Aria in MariaDB context?\n\n## Common Mistakes\n• Installing new MyISAM for \"simple log table\" without team review.\n• Enabling Group Replication with MyISAM tables present.\n• Ignoring vendor upgrade that converts to InnoDB — test anyway.\n\n## Senior Engineer Insights\nPosition as architecture governance: engine choice is HA and ops contract, not micro-optimization.\n\n## Key Commands\n• SELECT table_schema, table_name, engine, table_rows\nFROM information_schema.tables\nWHERE engine='MyISAM' AND table_schema NOT IN ('mysql','sys','performance_schema');\n• SHOW CREATE TABLE vendor_stats\\G\n\n## Best Practices\n• CI gate on ENGINE=InnoDB.\n• Track MyISAM inventory in config management.\n• Require exception ticket for new MyISAM.\n• Plan vendor upgrades for InnoDB-native schemas.",
    command: "SELECT table_schema, table_name, engine, table_rows\nFROM information_schema.tables\nWHERE engine='MyISAM' AND table_schema NOT IN ('mysql','sys','performance_schema');\nSHOW CREATE TABLE vendor_stats\\G",
    tags: ["mysql","MyISAM","hard","rare"],
  }
];
