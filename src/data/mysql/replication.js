// MySQL — replication
// Structured interview content — run: node scripts/generate-db-tracks.mjs
// 21 questions

export const questions = [
  {
    id: "mysql-binlog-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "What is the MySQL binary log and why is it essential in production?",
    answer: "## Interview Answer\nThe binary log records committed changes for replication and point-in-time recovery. Unlike InnoDB redo (crash recovery), binlog is logical/physical change events consumed by replicas and mysqlbinlog. Production requires log_bin=ON with ROW format for deterministic replication.\n\n## Detailed Explanation\nEvents: GTID, Query, Table_map, Write_rows, Update_rows, Delete_rows, Xid.\nRotation by max_binlog_size; expire_logs_days replaced by binlog_expire_logs_seconds.\nSync: sync_binlog=1 fsyncs each commit — durable but slower; group commit mitigates.\nRow image: binlog_row_image FULL vs MINIMAL vs NOBLOB affects size and flashback capability.\nReplica and backup tools depend on consistent binlog chain.\n\n## Production Perspective\nMonitor disk on binlog volume — full disk stops writes. Separate mount for /var/lib/mysql-bin. Retention must cover PITR window + replica catch-up. Never rm binlog files manually — PURGE BINARY LOGS.\n\n## Common Follow-up Questions\n• Compare ROW vs STATEMENT binlog format.\n• What is binlog_row_image MINIMAL?\n• How does binlog encryption work in 8.0?\n\n## Common Mistakes\n• PURGE BINARY LOGS before replica caught up — breaks replication.\n• sync_binlog=0 on financial primary.\n• Statement-based replication with non-deterministic UDFs.\n\n## Senior Engineer Insights\nExplain dual logging cost: InnoDB redo + binlog — group commit is why both can be durable without 2× fsync latency.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'log_bin%';\nSHOW VARIABLES LIKE 'binlog%';\nSHOW BINARY LOGS;\n• SHOW BINLOG EVENTS IN 'binlog.000101' LIMIT 20;\n• SHOW MASTER STATUS\\G\n\n## Best Practices\n• ROW + FULL/MINIMAL row image per compliance needs.\n• Automated binlog disk alerting.\n• sync_binlog=1 on production primaries.\n• Document purge policy aligned with backup retention.",
    command: "SHOW VARIABLES LIKE 'log_bin%';\nSHOW VARIABLES LIKE 'binlog%';\nSHOW BINARY LOGS;\nSHOW BINLOG EVENTS IN 'binlog.000101' LIMIT 20;\nSHOW MASTER STATUS\\G",
    tags: ["mysql","Binary Logs","easy","very-common"],
  },
  {
    id: "mysql-binlog-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "Compare STATEMENT, ROW, and MIXED binary log formats.",
    answer: "## Interview Answer\nROW (recommended): logs row changes — safe for non-deterministic SQL, larger logs. STATEMENT: logs original SQL — compact but unsafe with UUID(), NOW(), triggers. MIXED: statement default with row for unsafe statements. Production OLTP uses binlog_format=ROW globally.\n\n## Detailed Explanation\nSTATEMENT risks: different row order on replica, cascade trigger differences.\nROW captures before/after images — higher volume on wide tables.\nMIXED still surprises — treat as legacy.\nReplicas apply row events via SQL thread (traditional) or applier workers (parallel).\nCDC tools (Debezium) prefer ROW with minimal overhead tuning.\n\n## Production Perspective\nAfter switching to ROW, monitor binlog growth — adjust expire retention and disk. Bulk updates generate large row events — batch during maintenance. Use binlog_row_image=MINIMAL if PITR allows and no flashback need.\n\n## Common Follow-up Questions\n• How does ROW format affect trigger execution on replica?\n• Impact on temporary tables in stored procedures?\n• What is binlog_checksum?\n\n## Common Mistakes\n• Leaving STATEMENT on 5.7 legacy without audit of non-deterministic queries.\n• Wide BLOB columns with FULL row image filling disk.\n• Assuming MIXED is \"best of both\".\n\n## Senior Engineer Insights\nMention replica lag spike when switching STATEMENT→ROW on UPDATE-heavy workload — plan disk and parallel apply workers.\n\n## Key Commands\n• SET GLOBAL binlog_format='ROW';\n• SHOW VARIABLES LIKE 'binlog_row_image';\n• mysqlbinlog -vv --base64-output=DECODE-ROWS binlog.000105 | head -100\n\n## Best Practices\n• Standardize ROW format in IaC.\n• Size binlog volume for peak write rate.\n• Test format change on staging replica first.\n• Use MINIMAL row image when audit requires only changed columns.",
    command: "SET GLOBAL binlog_format='ROW';\nSHOW VARIABLES LIKE 'binlog_row_image';\nmysqlbinlog -vv --base64-output=DECODE-ROWS binlog.000105 | head -100",
    tags: ["mysql","Binary Logs","medium","common"],
  },
  {
    id: "mysql-binlog-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "How do you manage binary log retention and prevent disk-full incidents?",
    answer: "## Interview Answer\nSet binlog_expire_logs_seconds (8.0) for automatic purge; ensure retention exceeds replica lag and backup RPO. Monitor filesystem usage; never delete binlogs by hand. Use PURGE BINARY LOGS TO or BEFORE only after verifying replicas and backups.\n\n## Detailed Explanation\nexpire_logs_auto_purge must be ON (default).\nReplica without relay log catch-up needs oldest binlog preserved.\nBackup tools (XtraBackup --backup --stream) may need binlog coordinates noted.\nCloud RDS: automated retention parameter; still monitor free storage.\nBinlog on separate EBS volume — expand without touching data volume.\n\n## Production Perspective\nAlert at 75% binlog volume. Runbook: identify lagging replica → fix lag → purge. For PITR, archive binlogs to S3 with mysqlbinlog or binlog streaming before local purge.\n\n## Common Follow-up Questions\n• What happens when binlog disk is full?\n• How archive binlogs to object storage?\n• Difference expire_logs_days vs binlog_expire_logs_seconds?\n\n## Common Mistakes\n• Manual rm binlog.* breaking replication coordinates.\n• Short retention vs long-weekend replica outage.\n• PURGE on primary while backup job still needs old logs.\n\n## Senior Engineer Insights\nQuantify retention: max replica lag (72h) + last backup age (24h) + margin = minimum binlog retention policy.\n\n## Key Commands\n• SHOW BINARY LOGS;\n• SHOW VARIABLES LIKE 'binlog_expire%';\nSHOW VARIABLES LIKE 'expire_logs%';\n• PURGE BINARY LOGS BEFORE '2024-06-01 00:00:00';\n• SELECT file, format, encrypted FROM performance_schema.binary_log_files;\n\n## Best Practices\n• Dedicated binlog volume with autoscaling/monitoring.\n• Archive to S3 before aggressive local purge.\n• Automate replica lag checks before purge jobs.\n• Include binlog space in capacity planning.",
    command: "SHOW BINARY LOGS;\nSHOW VARIABLES LIKE 'binlog_expire%';\nSHOW VARIABLES LIKE 'expire_logs%';\nPURGE BINARY LOGS BEFORE '2024-06-01 00:00:00';\nSELECT file, format, encrypted FROM performance_schema.binary_log_files;",
    tags: ["mysql","Binary Logs","medium","common"],
  },
  {
    id: "mysql-binlog-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "DBA",
    question: "Explain sync_binlog and innodb_flush_log_at_trx_commit interaction for durability.",
    answer: "## Interview Answer\nFull durability: sync_binlog=1 and innodb_flush_log_at_trx_commit=1 — each transaction fsyncs binlog and InnoDB redo (group commit batches). Trading durability: sync_binlog=0 or flush_log=2 risks losing last seconds of commits on OS crash. Match settings to RPO SLA.\n\n## Detailed Explanation\nGroup commit: multiple sessions share one fsync sequence — order binlog write then InnoDB commit.\nflush_log=2: redo written each commit, fsync every second — OS crash window.\nsync_binlog=0: OS buffers binlog — faster, risky on power loss.\nSemi-sync replication adds replica ack before client commit — different layer than local durability.\nCloud managed DB often exposes these as parameter groups.\n\n## Production Perspective\nPayment workloads: both=1. Analytics staging: may relax with stakeholder sign-off. Document crash RPO in architecture decision record. Benchmark TPS before/after change on representative load.\n\n## Common Follow-up Questions\n• What is the 2-phase commit between InnoDB and binlog?\n• Does sync_binlog=1 guarantee no data loss on failover?\n• How semi-sync differs from sync_binlog?\n\n## Common Mistakes\n• Setting flush_log=0 on primary \"for performance\".\n• Assuming replica sync replaces local binlog durability.\n• Changing both knobs simultaneously without measurement.\n\n## Senior Engineer Insights\nDraw commit timeline: trx prepare → binlog write → fsync binlog → InnoDB commit → client OK. Interviewers test if you know group commit prevents naive double fsync cost.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'sync_binlog';\nSHOW VARIABLES LIKE 'innodb_flush_log_at_trx_commit';\n• SHOW GLOBAL STATUS LIKE 'Binlog_%';\n• SELECT * FROM performance_schema.replication_connection_status\\G\n\n## Best Practices\n• Document durability settings per environment tier.\n• Load test after parameter changes.\n• Use semi-sync or GR for cross-node RPO.\n• Never relax durability on sole primary without backup gap acceptance.",
    command: "SHOW VARIABLES LIKE 'sync_binlog';\nSHOW VARIABLES LIKE 'innodb_flush_log_at_trx_commit';\nSHOW GLOBAL STATUS LIKE 'Binlog_%';\nSELECT * FROM performance_schema.replication_connection_status\\G",
    tags: ["mysql","Binary Logs","hard","common"],
  },
  {
    id: "mysql-binlog-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "Cloud Engineer",
    question: "How do you use mysqlbinlog for forensic analysis and CDC debugging?",
    answer: "## Interview Answer\nmysqlbinlog decodes binlog files to SQL or row events. Use --start-datetime/--stop-datetime for time windows, -v for row detail, --database filter. Essential for tracing who changed a row, debugging replica errors, and validating CDC pipelines.\n\n## Detailed Explanation\nOutput modes: SQL replay vs RBR verbose with --base64-output=DECODE-ROWS.\nGTID events show transaction boundaries for skip/replicate.\nBinlog encryption (8.0): decrypt with --read-from-remote-master and keys.\nFlashback patterns: inverse events with pt-flashback or custom scripts — ROW only.\nPoint to original thread_id and server_id for audit.\n\n## Production Perspective\nPull binlogs from S3 archive for incident timeline. Large files: pipe to less or split by time. Never replay onto production without sandbox — use restore instance. Correlate with general_log only if temporarily enabled (performance cost).\n\n## Common Follow-up Questions\n• How find GTID of errant transaction?\n• Can mysqlbinlog apply to running server?\n• What is BINLOG statement in decoded output?\n\n## Common Mistakes\n• Replaying full binlog onto wrong server — double apply.\n• Missing --skip-gtids when building seed replica.\n• Decoding encrypted binlog without keyring access.\n\n## Senior Engineer Insights\nWalk through row-change investigation: identify time → mysqlbinlog window → table_map → before/after images → map to application user via connection audit plugin if enabled.\n\n## Key Commands\n• mysqlbinlog --start-datetime='2024-06-15 10:00:00' --stop-datetime='2024-06-15 10:05:00' -vv binlog.000200 > /tmp/incident.sql\n• mysqlbinlog --read-from-remote-source=repl_user@primary:3306 --stop-never binlog.000200\n• SHOW BINLOG EVENTS IN 'binlog.000200' FROM 4 LIMIT 50;\n\n## Best Practices\n• Archive binlogs with checksum verification.\n• Use read-only sandbox for replay tests.\n• Enable audit plugin for user attribution complement.\n• Document mysqlbinlog procedures in security incident runbook.",
    command: "mysqlbinlog --start-datetime='2024-06-15 10:00:00' --stop-datetime='2024-06-15 10:05:00' -vv binlog.000200 > /tmp/incident.sql\nmysqlbinlog --read-from-remote-source=repl_user@primary:3306 --stop-never binlog.000200\nSHOW BINLOG EVENTS IN 'binlog.000200' FROM 4 LIMIT 50;",
    tags: ["mysql","Binary Logs","medium","rare"],
  },
  {
    id: "mysql-repl-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "Explain MySQL asynchronous replication architecture: IO thread and SQL thread.",
    answer: "## Interview Answer\nPrimary writes binlog; replica IO thread pulls events into relay log; SQL thread (applier) applies them. Traditional async replication has no primary wait for replica — fast but RPO > 0 on failover. Monitor lag via performance_schema or SHOW REPLICA STATUS.\n\n## Detailed Explanation\nCHANGE REPLICATION SOURCE TO defines host, user, log file/position or AUTO_POSITION (GTID).\nRelay logs stored under relay-log directory; auto purge after apply.\nReplication filter: replicate-do-db, replicate-ignore-table — fragile, prefer separate instances.\nMulti-source replication: one replica, multiple channels.\nRead-only replicas: super_read_only=ON prevents accidental writes.\n\n## Production Perspective\nROW format, dedicated repl user with REPLICATION SLAVE priv. Network: private link, TLS (SOURCE_CONNECTION_AUTO_FAILOVER with GR). Alert on Replica_SQL_Running=No. Use read replicas for reporting, not session consistency without routing.\n\n## Common Follow-up Questions\n• What is replication lag composed of?\n• Compare async vs semi-sync.\n• How parallel replication works in 8.0?\n\n## Common Mistakes\n• Writing to replica breaking replication chain.\n• Using statement-based replication with triggers.\n• Ignoring replicate_wild_ignore_table typos.\n\n## Senior Engineer Insights\nDecompose lag: network (IO) vs apply (SQL) vs lock contention on replica hot tables.\n\n## Key Commands\n• SHOW REPLICA STATUS\\G\n• SHOW PROCESSLIST;\n-- Replica_IO_running, Replica_SQL_running\n• SELECT * FROM performance_schema.replication_applier_status_by_worker;\n\n## Best Practices\n• super_read_only on all replicas.\n• Monitor SQL thread errors immediately.\n• Dedicated replication network path.\n• Test promote replica runbook quarterly.",
    command: "SHOW REPLICA STATUS\\G\nSHOW PROCESSLIST;\n-- Replica_IO_running, Replica_SQL_running\nSELECT * FROM performance_schema.replication_applier_status_by_worker;",
    tags: ["mysql","Replication","easy","very-common"],
  },
  {
    id: "mysql-repl-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "DBA",
    question: "How do you troubleshoot a stopped replica SQL thread after a replication error?",
    answer: "## Interview Answer\nCheck Last_SQL_Error and Last_SQL_Errno in SHOW REPLICA STATUS. Common: duplicate key (1062), missing row (1032), DDL drift. Fix root cause on replica or skip single GTID/event with caution; resync from backup if widespread drift. Never skip blindly in production without impact analysis.\n\n## Detailed Explanation\nsql_slave_skip_counter deprecated — use GTID skip: STOP REPLICA; SET GTID_NEXT='uuid:N'; BEGIN; COMMIT; START REPLICA.\nsql_replica_skip_errors=1062 dangerous global skip — avoid.\nERROr 1396: user missing on replica.\nSchema drift: pt-table-checksum compare primary vs replica.\nRebuild: clone plugin or xtrabackup + CHANGE REPLICATION SOURCE.\n\n## Production Perspective\nAfter skip, verify data with checksum job. Document errant transaction source — app double-write or manual change on replica. Enable replication_applier_status monitoring. For 1032, investigate if replica missed events due to backup restore gap.\n\n## Common Follow-up Questions\n• When is clone faster than skip?\n• What is multi-threaded slave?\n• How handle circular replication?\n\n## Common Mistakes\n• sql_slave_skip_counter=1 in loop masking systemic drift.\n• Manual DELETE on replica then skip.\n• Promoting lagging replica without calculating data loss.\n\n## Senior Engineer Insights\nPresent decision tree: single event + known benign → GTID skip; unknown drift → rebuild replica from clone; schema mismatch → fix DDL then resume.\n\n## Key Commands\n• SHOW REPLICA STATUS\\G\n-- Last_SQL_Error, Retrieved_Gtid_Set, Executed_Gtid_Set\n• STOP REPLICA;\nSET GTID_NEXT='aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee:12345';\nBEGIN; COMMIT;\nSET GTID_NEXT='AUTOMATIC';\nSTART REPLICA;\n• pt-table-checksum --replicate=percona.checksums h=primary,D=app,t=orders\n\n## Best Practices\n• Automate checksum validation weekly.\n• Prohibit manual DML on replicas.\n• IaC for schema changes on all nodes.\n• Clone-based rebuild runbook under 30 min RTO.",
    command: "SHOW REPLICA STATUS\\G\n-- Last_SQL_Error, Retrieved_Gtid_Set, Executed_Gtid_Set\nSTOP REPLICA;\nSET GTID_NEXT='aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee:12345';\nBEGIN; COMMIT;\nSET GTID_NEXT='AUTOMATIC';\nSTART REPLICA;\npt-table-checksum --replicate=percona.checksums h=primary,D=app,t=orders",
    tags: ["mysql","Replication","medium","very-common"],
  },
  {
    id: "mysql-repl-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "How do you monitor and reduce MySQL replication lag?",
    answer: "## Interview Answer\nLag = time between commit on primary and apply on replica. Causes: slow SQL apply, large transactions, replica hardware weaker than primary, parallel worker limits, lock contention on replica. Mitigate with parallel replicas, better indexes on replica, binlog transaction compression, and right-sized hardware.\n\n## Detailed Explanation\nSeconds_Behind_Source (formerly Seconds_Behind_Master) approximate — can be zero while lagging on large trx.\nBetter: compare binlog positions or heartbeat table pt-heartbeat.\nreplica_parallel_workers + LOGICAL_CLOCK (8.0) applies committed primary transactions in parallel.\nHot table on replica: reporting queries blocking applier.\nNetwork: SOURCE_RETRY_COUNT, CONNECT_RETRY.\n\n## Production Perspective\nDashboard: lag p99, IO vs SQL thread state, worker queue depth. Split analytics to separate replica with delayed replication for safety. Alert lag > 60s for payment read models. During batch jobs, pause non-critical replica queries.\n\n## Common Follow-up Questions\n• Why Seconds_Behind_Source lies?\n• What is replica_parallel_type LOGICAL_CLOCK?\n• Delayed replica use cases?\n\n## Common Mistakes\n• Scaling primary CPU while replica stays small.\n• Single massive UPDATE on primary — hours lag.\n• Using lagging replica for promotion without data audit.\n\n## Senior Engineer Insights\nMention binlog_transaction_compression (8.0) for network-bound lag — trade CPU on primary. Heartbeat table gives sub-second lag metrics Prometheus-friendly.\n\n## Key Commands\n• SHOW REPLICA STATUS\\G\n-- Seconds_Behind_Source\n• SELECT * FROM performance_schema.replication_applier_status_by_coordinator\\G\n• SHOW VARIABLES LIKE 'replica_parallel%';\nSET GLOBAL replica_parallel_workers=8;\n\n## Best Practices\n• Match replica CPU/IO to primary write rate.\n• Enable parallel appliers on 8.0.\n• Heartbeat monitoring over SHOW STATUS alone.\n• Isolate heavy reads from applier contention.",
    command: "SHOW REPLICA STATUS\\G\n-- Seconds_Behind_Source\nSELECT * FROM performance_schema.replication_applier_status_by_coordinator\\G\nSHOW VARIABLES LIKE 'replica_parallel%';\nSET GLOBAL replica_parallel_workers=8;",
    tags: ["mysql","Replication","medium","common"],
  },
  {
    id: "mysql-repl-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "DBA",
    question: "Compare semi-synchronous replication vs asynchronous replication for RPO.",
    answer: "## Interview Answer\nSemi-sync waits for at least one replica to receive and ack binlog event before primary commits (after_sync mode in 8.0). RPO improves — no committed trx lost if one sync replica survives. Cost: write latency and availability if sync replica down — falls back to async per rpl_semi_sync_master_timeout.\n\n## Detailed Explanation\nPlugins: rpl_semi_sync_source (8.0) / rpl_semi_sync_master (legacy).\nrpl_semi_sync_source_wait_for_replica_count=N for multiple acks.\nAfter sync: wait after binlog flush, before InnoDB commit — stronger than after_commit.\nGroup Replication uses different consensus — not semi-sync plugin.\nCloud: Aurora sync replica, RDS async default with cross-region lag.\n\n## Production Perspective\nEnable semi-sync on sync replica in same AZ; async DR replica cross-region. Tune timeout vs availability — 1–3s typical. Monitor Rpl_semi_sync_source_status. Failover: ensure new primary has sync replica before enforcing semi-sync again.\n\n## Common Follow-up Questions\n• after_sync vs after_commit semi-sync?\n• Does semi-sync prevent split brain?\n• Compare to Galera/GR quorum commit?\n\n## Common Mistakes\n• Semi-sync to lagging replica — every commit waits.\n• No async fallback replica when sync partner dies.\n• Confusing received ack with applied on replica.\n\n## Senior Engineer Insights\nClarify semi-sync RPO: protects against primary crash, not replica apply lag — promoted replica may still need catch-up for applied data.\n\n## Key Commands\n• INSTALL PLUGIN rpl_semi_sync_source SONAME 'semisync_source.so';\n• SHOW VARIABLES LIKE 'rpl_semi_sync%';\nSHOW STATUS LIKE 'Rpl_semi_sync%';\n\n• SELECT * FROM performance_schema.replication_connection_status\\G\n\n## Best Practices\n• Same-AZ sync replica for RPO.\n• Cross-region async for DR only.\n• Monitor semi-sync fallbacks to async.\n• Load test write latency impact before enforcing.",
    command: "INSTALL PLUGIN rpl_semi_sync_source SONAME 'semisync_source.so';\nSHOW VARIABLES LIKE 'rpl_semi_sync%';\nSHOW STATUS LIKE 'Rpl_semi_sync%';\n\nSELECT * FROM performance_schema.replication_connection_status\\G",
    tags: ["mysql","Replication","hard","common"],
  },
  {
    id: "mysql-repl-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Database Engineer",
    question: "How do you perform a controlled primary failover using traditional replication?",
    answer: "## Interview Answer\nSteps: verify replica caught up (Exec_Master_Log_Pos / executed GTID set), stop writes on primary (read-only), wait lag zero, promote replica (STOP REPLICA; RESET REPLICA ALL; read_only=OFF), repoint other replicas and apps via Orchestrator/ProxySQL/MHA. Handle errant GTIDs on old primary before rejoin.\n\n## Detailed Explanation\nOrchestrator automates topology discovery and promotion.\nMHA: master failover with VIP or app config push.\nGTID: SELECT @@GLOBAL.gtid_executed on candidate vs primary subset check.\nSplit brain if old primary not isolated — use STONITH or fence writes.\nReverse replication: old primary becomes replica of new primary after reset.\n\n## Production Perspective\nGame day: measure RTO. App retry on connection errors. DNS TTL vs proxy config push trade-off. Document binlog coordinate method as fallback if GTID disabled (legacy).\n\n## Common Follow-up Questions\n• What is errant GTID cleanup?\n• Orchestrator vs MHA vs InnoDB Cluster?\n• How ProxySQL handles failover?\n\n## Common Mistakes\n• Promoting replica with replication filters.\n• Forgotten read_only on new primary.\n• Two writable primaries during network partition.\n\n## Senior Engineer Insights\nEmphasize external orchestrator + fencing — MySQL replication alone does not prevent split brain.\n\n## Key Commands\n• STOP REPLICA;\nSHOW REPLICA STATUS\\G\n-- until Executed_Gtid_Set catches Retrieved\n• SET GLOBAL super_read_only=OFF;\nSET GLOBAL read_only=OFF;\n• SELECT GTID_SUBTRACT(@@GLOBAL.gtid_executed, 'source-uuid:1-100') AS errant;\n\n## Best Practices\n• Automate failover with Orchestrator or GR.\n• STONITH old primary before promotion.\n• Quarterly failover drills.\n• GTID everywhere for simpler repointing.",
    command: "STOP REPLICA;\nSHOW REPLICA STATUS\\G\n-- until Executed_Gtid_Set catches Retrieved\nSET GLOBAL super_read_only=OFF;\nSET GLOBAL read_only=OFF;\nSELECT GTID_SUBTRACT(@@GLOBAL.gtid_executed, 'source-uuid:1-100') AS errant;",
    tags: ["mysql","Replication","hard","rare"],
  },
  {
    id: "mysql-gtid-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "What is GTID in MySQL and why does it simplify replication management?",
    answer: "## Interview Answer\nGlobal Transaction Identifier assigns each committed transaction a unique UUID:sequence pair cluster-wide. Failover uses AUTO_POSITION without guessing binlog file/offset. Chain replication and Orchestrator depend on GTID for consistent topology changes.\n\n## Detailed Explanation\ngtid_mode=ON, enforce_gtid_consistency=ON required.\nSET contains executed transactions on server — gtid_executed, gtid_purged.\nCHANGE REPLICATION SOURCE TO SOURCE_AUTO_POSITION=1.\nErrant transactions: GTIDs on server not originating from replication stream.\nOnline enable: gtid_mode=OFF_PERMISSIVE → OFF → ON_PERMISSIVE → ON.\n\n## Production Perspective\nEnable GTID on greenfield; migrate legacy with planned outage. Never mix gtid_mode OFF nodes in same prod chain. Backup/restore preserves gtid_executed for PITR alignment.\n\n## Common Follow-up Questions\n• What is gtid_purged?\n• How handle errant GTID?\n• GTID vs binlog coordinates fallback?\n\n## Common Mistakes\n• Creating errant trx on replica with sql_log_bin=0 oversight.\n• Disabling GTID mid-topology.\n• Assuming GTID eliminates need for binlog retention.\n\n## Senior Engineer Insights\nExplain GTID lifecycle: generated on primary → replicated → applied on replica executed set grows monotonically (with purged exceptions).\n\n## Key Commands\n• SELECT @@gtid_mode, @@enforce_gtid_consistency;\n• SHOW MASTER STATUS\\G\n-- Executed_Gtid_Set\n• SHOW REPLICA STATUS\\G\n-- Retrieved_Gtid_Set, Executed_Gtid_Set\n\n## Best Practices\n• GTID on all production nodes.\n• Monitor errant GTID after failover drills.\n• Document ONLINE GTID migration procedure.\n• Use AUTO_POSITION for new replicas.",
    command: "SELECT @@gtid_mode, @@enforce_gtid_consistency;\nSHOW MASTER STATUS\\G\n-- Executed_Gtid_Set\nSHOW REPLICA STATUS\\G\n-- Retrieved_Gtid_Set, Executed_Gtid_Set",
    tags: ["mysql","GTID","easy","very-common"],
  },
  {
    id: "mysql-gtid-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do you detect and resolve errant GTIDs after a failover mishap?",
    answer: "## Interview Answer\nErrant GTIDs exist on a server but not in the replication stream from current primary — often from old primary taking writes during split brain or manual session on replica. Detect with GTID_SUBTRACT(local_executed, primary_executed). Fix: inject empty transaction or rebuild node.\n\n## Detailed Explanation\nmysql.gtid_executed table persists set (8.0).\nRESET MASTER on errant node destructive — avoid on primary with data.\nInject empty: SET GTID_NEXT='errant-uuid:N'; BEGIN; COMMIT; then replicate from primary.\nOrchestrator gtid-errant-count check before rejoin.\nPrevention: super_read_only on replicas and old primary after failover.\n\n## Production Perspective\nAutomate errant check post-failover script. If errant trx modified data, logical compare or rebuild — empty inject only if transaction was benign metadata. Audit split-brain window for data reconciliation.\n\n## Common Follow-up Questions\n• Can pt-table-sync fix errant data?\n• What does RESET REPLICA ALL do to GTID?\n• gtid_executed vs gtid_purged interaction?\n\n## Common Mistakes\n• RESET MASTER to \"fix\" errant without backup — loses binlog history.\n• Ignoring errant GTID blocking CHANGE REPLICATION SOURCE.\n• Rejoin old primary writable without read_only.\n\n## Senior Engineer Insights\nWalk Orchestrator recovery: old master diverged → isolate → errant inject or discard node → resync from new primary.\n\n## Key Commands\n• SELECT GTID_SUBTRACT(@@GLOBAL.gtid_executed, 'primary-uuid:1-99999') AS errant;\n• SET GTID_NEXT='bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb:500';\nBEGIN; COMMIT;\nSET GTID_NEXT='AUTOMATIC';\n• SELECT * FROM mysql.gtid_executed ORDER BY interval_end DESC LIMIT 10;\n\n## Best Practices\n• super_read_only everywhere except single writer.\n• Post-failover errant GTID script in runbook.\n• Fence old primary before promotion completes.\n• Prefer clone rebuild over manual GTID surgery.",
    command: "SELECT GTID_SUBTRACT(@@GLOBAL.gtid_executed, 'primary-uuid:1-99999') AS errant;\nSET GTID_NEXT='bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb:500';\nBEGIN; COMMIT;\nSET GTID_NEXT='AUTOMATIC';\nSELECT * FROM mysql.gtid_executed ORDER BY interval_end DESC LIMIT 10;",
    tags: ["mysql","GTID","medium","common"],
  },
  {
    id: "mysql-gtid-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "What is the procedure to enable GTID on an existing non-GTID replication topology?",
    answer: "## Interview Answer\nRolling upgrade: set gtid_mode=OFF_PERMISSIVE on all nodes, then ON_PERMISSIVE, then ON on all, with enforce_gtid_consistency=ON. Ensure no non-transactional statements break consistency. Brief write pause may be needed; verify executed sets match across topology.\n\n## Detailed Explanation\nOFF_PERMISSIVE: generates GTIDs but accepts non-GTID trx.\nON_PERMISSIVE: requires GTID for new trx mostly.\nON: full GTID; CHANGE REPLICATION SOURCE AUTO_POSITION=1.\nBlockers: CREATE TABLE ... SELECT without ROW, temporary tables in STATEMENT mode.\nPercona documentation step-by-step for order: replicas first or primary first per version notes.\n\n## Production Perspective\nMaintenance window with app deploy freeze. Validate on staging clone full size. After enable, switch replicas to AUTO_POSITION. Monitor replication for 24h before declaring success.\n\n## Common Follow-up Questions\n• Can you disable GTID after enabling?\n• Impact on mysqlbinlog --include-gtids?\n• How mysqldump preserves GTID?\n\n## Common Mistakes\n• Skipping OFF_PERMISSIVE causing inconsistent sets.\n• enforce_gtid_consistency=OFF during ON transition.\n• Mixed 5.7 and 8.0 without compatibility matrix.\n\n## Senior Engineer Insights\nMention mysqldump --set-gtid-purged=ON for seeding new replica into GTID topology without duplicate UUID conflicts.\n\n## Key Commands\n• SET GLOBAL gtid_mode=OFF_PERMISSIVE;\n-- all nodes, then ON_PERMISSIVE, then ON\n• SELECT @@GLOBAL.gtid_mode, @@GLOBAL.gtid_executed;\n• mysqldump --single-transaction --set-gtid-purged=ON --all-databases > seed.sql\n\n## Best Practices\n• Follow vendor order for all nodes.\n• Test on full-size staging.\n• Enable enforce_gtid_consistency early.\n• Communicate freeze window to all teams.",
    command: "SET GLOBAL gtid_mode=OFF_PERMISSIVE;\n-- all nodes, then ON_PERMISSIVE, then ON\nSELECT @@GLOBAL.gtid_mode, @@GLOBAL.gtid_executed;\nmysqldump --single-transaction --set-gtid-purged=ON --all-databases > seed.sql",
    tags: ["mysql","GTID","medium","common"],
  },
  {
    id: "mysql-gtid-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "DBA",
    question: "How does GTID interact with backup, restore, and point-in-time recovery?",
    answer: "## Interview Answer\nPhysical backup records GTID executed at snapshot; restore sets gtid_purged or gtid_executed to avoid re-applying trx. PITR via mysqlbinlog must align GTID sets — skip duplicates with SET GTID_NEXT. mysqldump with --set-gtid-purged exports purged set for replica seeding.\n\n## Detailed Explanation\nXtraBackup --backup copies xtrabackup_binlog_info with GTID.\nAfter restore: RESET BINARY LOGS AND GTIDS (8.0) on fresh primary carefully.\nmysqlbinlog --exclude-gtids for partial replay.\nClone plugin copies donor gtid_executed to recipient — fast replica rebuild.\nMismatch causes \"duplicate entry\" or \"worker failed\" on replication start.\n\n## Production Perspective\nRestore runbook lists GTID verification queries before START REPLICA. Keep binlog archive with GTID event order. Test PITR to staging including GTID realignment step.\n\n## Common Follow-up Questions\n• When use RESET BINARY LOGS AND GTIDS?\n• Clone vs XtraBackup for GTID replica?\n• gtid_purged on restored slave?\n\n## Common Mistakes\n• Restoring backup without adjusting gtid_executed — double apply.\n• PITR replay crossing already-applied GTIDs.\n• mixing gtid_purged incorrectly in dump import.\n\n## Senior Engineer Insights\nExplain SET @@GLOBAL.gtid_purged — dangerous on primary if wrong — only for seeding empty replica from backup+dump combo.\n\n## Key Commands\n• xtrabackup --prepare --target-dir=/backup/full\n• cat xtrabackup_binlog_info\n-- binlog file, pos, GTID\n• mysqlbinlog --exclude-gtids='uuid:1-500' binlog.000300 | mysql\n\n## Best Practices\n• Document GTID state after every restore test.\n• Automate gtid_executed comparison in CI restore.\n• Use clone for GTID-safe replica rebuild.\n• Archive binlogs with GTID metadata.",
    command: "xtrabackup --prepare --target-dir=/backup/full\ncat xtrabackup_binlog_info\n-- binlog file, pos, GTID\nmysqlbinlog --exclude-gtids='uuid:1-500' binlog.000300 | mysql",
    tags: ["mysql","GTID","hard","common"],
  },
  {
    id: "mysql-gtid-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "Database Engineer",
    question: "Explain gtid_executed, gtid_purged, and gtid_owned system variables.",
    answer: "## Interview Answer\ngtid_executed: set of all GTIDs committed on this server. gtid_purged: GTIDs removed from binlog but still in executed set — restore must account to avoid re-fetch. gtid_owned: GTIDs currently in transaction on this session — useful for debugging hung trx.\n\n## Detailed Explanation\nPURGE BINARY LOGS adds to gtid_purged automatically.\nRESET MASTER adds all binlog GTIDs to purged before clear.\nExecuted set union purged = complete history picture for seeding.\nOwned GTIDs visible in performance_schema on 8.0.\nReplication applier adds to executed as transactions commit.\n\n## Production Perspective\nBefore seeding replica: SET GLOBAL gtid_purged on empty replica matches backup dump header. Monitor owned GTIDs during long transactions blocking purge. Document purged set after aggressive binlog retention policy.\n\n## Common Follow-up Questions\n• Can two servers have same gtid_executed?\n• Impact of RESET REPLICA on executed set?\n• How Group Replication uses GTID sets?\n\n## Common Mistakes\n• Manually SET gtid_purged without understanding duplicate risk.\n• Assuming purged GTIDs reappear in binlog.\n• Ignoring gtid_executed compression table growth.\n\n## Senior Engineer Insights\nUse gtid_purged when restoring logical dump onto fresh instance — classic interview scenario for seed replica.\n\n## Key Commands\n• SELECT @@GLOBAL.gtid_executed, @@GLOBAL.gtid_purged;\n• SELECT * FROM mysql.gtid_executed;\n• SELECT * FROM performance_schema.replication_applier_status_by_worker\\G\n\n## Best Practices\n• Never manual gtid_purged on live primary.\n• Include GTID vars in backup verification checklist.\n• Monitor mysql.gtid_executed table size.\n• Align purge policy with purged set documentation.",
    command: "SELECT @@GLOBAL.gtid_executed, @@GLOBAL.gtid_purged;\nSELECT * FROM mysql.gtid_executed;\nSELECT * FROM performance_schema.replication_applier_status_by_worker\\G",
    tags: ["mysql","GTID","medium","rare"],
  },
  {
    id: "mysql-gtid-006",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Lead a postmortem where GTID mismatch blocked replica provisioning — what happened?",
    answer: "## Interview Answer\nRoot cause often: restored backup with gtid_purged not set, or errant GTIDs from manual fix, or mixing binlog position with AUTO_POSITION. Present timeline, detection (START REPLICA failed), fix (clone rebuild vs gtid inject), and prevention (automated GTID validation in provisioning pipeline).\n\n## Detailed Explanation\nError examples: \"Duplicate GTID\", \"Cannot replicate because transaction was executed\".\nProvisioning pipeline should: restore → SELECT gtid_executed → compare donor → START REPLICA.\nInfrastructure as code: Terraform user-data runs validation before marking healthy.\nHuman factor: DBA ran RESET MASTER on staging clone copied to prod template.\n\n## Production Perspective\nAdd CI gate: replica must replicate heartbeat within 60s. Immutable AMI with validated GTID bootstrap. Runbook downgrade path: temporary file+position only with leadership approval.\n\n## Common Follow-up Questions\n• How clone plugin avoids GTID mismatch?\n• Can Orchestrator fix automatically?\n• Impact on blue-green deploys?\n\n## Common Mistakes\n• Copying VM disk image with stale gtid_executed.\n• Skipping START REPLICA test in automation.\n• Using mysqldump without --set-gtid-purged on seed.\n\n## Senior Engineer Insights\nFrame as pipeline quality issue — GTID is unforgiving of manual steps; automation must be idempotent and verified.\n\n## Key Commands\n• START REPLICA;\nSHOW REPLICA STATUS\\G\n-- Last_Error\n• SELECT GTID_SUBTRACT(@@gtid_executed, @@gtid_purged);\n• CLONE INSTANCE FROM 'donor'@'host':3306;\n\n## Best Practices\n• Automate GTID validation in replica bootstrap.\n• Ban manual RESET MASTER in prod policy.\n• Use clone for new replicas.\n• Post-incident: add integration test.",
    command: "START REPLICA;\nSHOW REPLICA STATUS\\G\n-- Last_Error\nSELECT GTID_SUBTRACT(@@gtid_executed, @@gtid_purged);\nCLONE INSTANCE FROM 'donor'@'host':3306;",
    tags: ["mysql","GTID","hard","rare"],
  },
  {
    id: "mysql-gr-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Common",
    role: "Database Engineer",
    question: "What is MySQL Group Replication and how does it differ from traditional async replication?",
    answer: "## Interview Answer\nGroup Replication (GR) is multi-primary or single-primary consensus replication using Paxos group. Members certify transactions before commit — all-or-nothing for group. Provides automatic membership, failure detection, and certified commit — foundation of InnoDB Cluster.\n\n## Detailed Explanation\nRequires GTID, ROW binlog, InnoDB only.\ngroup_replication_group_name UUID identifies group.\nSingle-primary mode: one writer elected; multi-primary: all write with conflict detection.\nCertification: transaction conflicts rolled back on other members if concurrent write same row.\nMySQL Router routes RW to primary, RO to secondaries.\n\n## Production Perspective\nMinimum 3 nodes for quorum odd count. Network latency between members affects commit latency. Not drop-in for all apps — conflict errors on multi-primary. Prefer single-primary mode for traditional OLTP.\n\n## Common Follow-up Questions\n• What is InnoDB Cluster vs GR alone?\n• How does certification conflict work?\n• Compare GR to Galera?\n\n## Common Mistakes\n• Two-node GR without arbitrator — no quorum on split.\n• MyISAM tables in group — blocked.\n• WAN latency GR expecting local async performance.\n\n## Senior Engineer Insights\nPosition GR as \"managed HA product path\" (InnoDB Cluster + Shell + Router) vs DIY Orchestrator + async.\n\n## Key Commands\n• SELECT * FROM performance_schema.replication_group_members;\n• SHOW STATUS LIKE 'group_replication%';\n\n• mysqlsh -- cluster describe\n\n## Best Practices\n• Three AZ members minimum.\n• Single-primary for most apps.\n• Use MySQL Router for transparent failover.\n• Odd number of voting members.",
    command: "SELECT * FROM performance_schema.replication_group_members;\nSHOW STATUS LIKE 'group_replication%';\n\nmysqlsh -- cluster describe",
    tags: ["mysql","Group Replication","easy","common"],
  },
  {
    id: "mysql-gr-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do you set up and bootstrap a MySQL InnoDB Cluster with Group Replication?",
    answer: "## Interview Answer\nUse MySQL Shell dba.createCluster(), addInstance() with clone recovery, then MySQL Router bootstrap. Configure group_replication variables, local_address, group_seeds, consistent IP/DNS. Bootstrap single node first, add others with clone from donor.\n\n## Detailed Explanation\nclone_recovery_donor for new members — faster than incremental catch-up.\ncluster.createCluster({gtidAssignOnFailover: true}) options vary by version.\nRouter metadata cache refreshes primary role on failover.\nSSL required for production group communication.\nSandbox vs production: OS tuning, vm.swappiness, transparent huge pages off.\n\n## Production Perspective\nAutomate with Terraform + cloud-init + mysqlsh JS API. Store cluster metadata in mysql_innodb_cluster_metadata schema. Test failover with chaos — kill primary VM. Document Router bootstrap credentials rotation.\n\n## Common Follow-up Questions\n• What is clone vs incremental recovery?\n• How rejoin expelled member?\n• Router split read/write routing rules?\n\n## Common Mistakes\n• Bootstrap with mismatched server_id or gtid_mode.\n• Adding member without clone on large dataset — days catch-up.\n• Firewall blocking group_replication port 33061.\n\n## Senior Engineer Insights\nDemonstrate mysqlsh cluster.status() interpretation: MEMBER_STATE, RECOVERY status, INSTANCE_ROLE.\n\n## Key Commands\n• mysqlsh root@primary:3306\n\ndba.createCluster('prodCluster')\ncluster.addInstance('root@node2:3306', {recoveryMethod: 'clone'})\n\n• mysqlrouter --bootstrap root@primary:3306 --account mysqlrouter_user\n• SELECT MEMBER_HOST, MEMBER_STATE, MEMBER_ROLE FROM performance_schema.replication_group_members;\n\n## Best Practices\n• Clone for new members.\n• MySQL Router for app connectivity.\n• Automate cluster status monitoring.\n• Document bootstrap break-glass procedure.",
    command: "mysqlsh root@primary:3306\n\ndba.createCluster('prodCluster')\ncluster.addInstance('root@node2:3306', {recoveryMethod: 'clone'})\n\nmysqlrouter --bootstrap root@primary:3306 --account mysqlrouter_user\nSELECT MEMBER_HOST, MEMBER_STATE, MEMBER_ROLE FROM performance_schema.replication_group_members;",
    tags: ["mysql","Group Replication","medium","common"],
  },
  {
    id: "mysql-gr-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "What causes a Group Replication member to enter ERROR or UNREACHABLE state?",
    answer: "## Interview Answer\nUNREACHABLE: network partition or member not responding to group communication — may expel if quorum lost. ERROR: applier failure, certification failure, or incompatible config. Check performance_schema.replication_group_members, error log, and group_replication_communication_stack.\n\n## Detailed Explanation\nExpelled member must rejoin via clone or full resync if data diverged.\ngroup_replication_member_expel_timeout controls sensitivity.\nFlow control throttles primary when secondaries lag (group_replication_flow_control_mode).\nIncompatible member: lower version, missing plugin, wrong group name.\nPartial transaction on expelled node — verify gtid_executed before rejoin.\n\n## Production Perspective\nAlert on MEMBER_STATE != ONLINE. Network: security group allows 33061/tcp between members. After AZ outage, verify quorum before accepting writes. Runbook rejoin: STOP GROUP_REPLICATION; reset if needed; START GROUP_REPLICATION with clone.\n\n## Common Follow-up Questions\n• What is flow control in GR?\n• Can you run GR across regions?\n• Difference ERROR vs RECOVERING?\n\n## Common Mistakes\n• Force-starting group without quorum — split brain writes.\n• Ignoring flow control as \"just warnings\".\n• Rejoining diverged member without clone — certification failures loop.\n\n## Senior Engineer Insights\nMap MEMBER_STATE transitions to user impact: RECOVERING = read possible on Router RO; primary election = brief write outage.\n\n## Key Commands\n• SELECT * FROM performance_schema.replication_group_members\\G\n• SELECT * FROM performance_schema.replication_group_member_stats;\n• SHOW ENGINE INNODB STATUS\\G\n-- GROUP replication section if present\n\n## Best Practices\n• Monitor member state continuously.\n• Low-latency private network between members.\n• Clone rejoin after long outage.\n• Chaos test network partition quarterly.",
    command: "SELECT * FROM performance_schema.replication_group_members\\G\nSELECT * FROM performance_schema.replication_group_member_stats;\nSHOW ENGINE INNODB STATUS\\G\n-- GROUP replication section if present",
    tags: ["mysql","Group Replication","medium","common"],
  },
  {
    id: "mysql-gr-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Compare single-primary vs multi-primary Group Replication modes for write-heavy OLTP.",
    answer: "## Interview Answer\nSingle-primary: one writer, no cross-member write conflicts, matches traditional app assumptions. Multi-primary: writes on all nodes; certification detects conflicts — concurrent updates same row fail on loser. Multi-primary for geo write distribution with conflict-aware app or partitioned access patterns only.\n\n## Detailed Explanation\ngroup_replication_single_primary_mode=ON default in InnoDB Cluster.\nMulti-primary: SELECT group_replication_primary_member for current primary still applies in single mode.\nConflict error: ER_LOCK_DEADLOCK or certification failure depending on scenario.\nForeign keys and DDL restrictions stricter in multi-primary.\nWorkload split: user sharded by region to different writers reduces conflicts.\n\n## Production Perspective\nStart single-primary; evaluate multi-primary only with write partitioning proof. Benchmark certification rollback rate under load test. Router primary election: wait for stable primary before app retry storm subsides.\n\n## Common Follow-up Questions\n• How certification queue depth monitored?\n• DDL behavior in multi-primary?\n• Galera wsrep certification vs GR?\n\n## Common Mistakes\n• Multi-primary with global hot row (inventory) — constant conflicts.\n• App not retrying certification failures.\n• Assuming multi-primary doubles write throughput linearly.\n\n## Senior Engineer Insights\nUse inventory example: multi-primary without app partitioning fails — single-primary + read scale-out is default winning architecture.\n\n## Key Commands\n• SELECT @@group_replication_single_primary_mode;\n• SHOW STATUS LIKE 'Certification_%';\n• mysqlsh -- cluster options cluster.setOption('clusterType', 'MM')  -- only if deliberate\n\n## Best Practices\n• Default single-primary InnoDB Cluster.\n• Partition writes before multi-primary.\n• Monitor conflict/error rate.\n• Load test certification under peak concurrency.",
    command: "SELECT @@group_replication_single_primary_mode;\nSHOW STATUS LIKE 'Certification_%';\nmysqlsh -- cluster options cluster.setOption('clusterType', 'MM')  -- only if deliberate",
    tags: ["mysql","Group Replication","hard","rare"],
  },
  {
    id: "mysql-gr-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Cloud Engineer",
    question: "Design HA architecture using InnoDB Cluster, MySQL Router, and cross-AZ placement.",
    answer: "## Interview Answer\nThree members across three AZs in one region; MySQL Router on app tier or sidecar; single-primary GR; async read replicas optional for analytics outside cluster. Router handles primary failover metadata refresh. Cross-region DR: async replica or second cluster with manual promotion.\n\n## Detailed Explanation\nQuorum survives one AZ loss. Router bootstrap users need minimal privileges.\nApp: mysql://router:6446/ schema — RW port 6446, RO 6447 classic setup.\nHealth checks: cluster.status() in cron; Prometheus exporter for GR metrics.\nPatching: rolling restart members one at a time — cluster maintains quorum.\nDR: not automatic cross-region GR — latency and split risk.\n\n## Production Perspective\nDocument RTO for Router cache refresh (~seconds). Connection storm on failover — app pool retry with backoff. Keep orchestration outside killed AZ. Test AZ failure game day.\n\n## Common Follow-up Questions\n• Router vs ProxySQL with GR?\n• How patch GR cluster rolling?\n• Second region strategy?\n\n## Common Mistakes\n• All three GR members in same AZ.\n• Router single instance — SPOF.\n• Expecting cross-region GR for DR without understanding latency.\n\n## Senior Engineer Insights\nPresent layered diagram: App → Router pool → GR quorum → optional async analytics replica → backup pipeline.\n\n## Key Commands\n• mysqlrouter --bootstrap root@member1:3306 --directory /etc/mysqlrouter\n• SELECT MEMBER_HOST, MEMBER_PORT, MEMBER_STATE FROM performance_schema.replication_group_members;\n• mysqlsh -- cluster status --extended=1\n\n## Best Practices\n• One member per AZ.\n• Redundant Router instances.\n• Rolling upgrade procedure documented.\n• Cross-region async DR separate from GR quorum.",
    command: "mysqlrouter --bootstrap root@member1:3306 --directory /etc/mysqlrouter\nSELECT MEMBER_HOST, MEMBER_PORT, MEMBER_STATE FROM performance_schema.replication_group_members;\nmysqlsh -- cluster status --extended=1",
    tags: ["mysql","Group Replication","hard","rare"],
  }
];
