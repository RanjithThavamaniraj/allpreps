// MySQL — performance
// Structured interview content — run: node scripts/generate-db-tracks.mjs
// 12 questions

export const questions = [
  {
    id: "mysql-perf-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "DBA",
    question: "What are the most important InnoDB parameters to tune for OLTP performance?",
    answer: "## Interview Answer\ninnodb_buffer_pool_size (70–80% RAM), innodb_log_file_size, innodb_flush_log_at_trx_commit, innodb_io_capacity, and innodb_thread_concurrency ( largely deprecated — usually 0). Right-size before micro-tuning — bad queries dominate over knob tweaking.\n\n## Detailed Explanation\nBuffer pool: primary cache — instances auto or manual.\nRedo log size: larger reduces checkpoint churn — 8.0 dynamic resize.\nflush_log=1 durability vs latency trade-off.\nio_capacity: SSD 2000–10000 depending on storage.\ninnodb_read_io_threads / write_io_threads: usually default OK on SSD.\n\n## Production Perspective\nChange one variable at a time with benchmark. PMM dashboards: buffer pool hit, log waits, row lock time. Cloud RDS: some params fixed — focus on instance class and queries.\n\n## Common Follow-up Questions\n• Dynamic buffer pool resize?\n• innodb_log_file_size vs redo log capacity?\n• When flush_log=2 acceptable?\n\n## Common Mistakes\n• Tuning buffers before fixing missing index.\n• Copying my.cnf from blog without workload context.\n• innodb_buffer_pool_size > available RAM.\n\n## Senior Engineer Insights\n80/20 rule: index + query fixes first; buffer pool second; exotic knobs last.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'innodb_buffer_pool%';\nSHOW VARIABLES LIKE 'innodb_flush%';\nSHOW VARIABLES LIKE 'innodb_io_capacity%';\n• SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_%';\n• SELECT * FROM sys.memory_global_by_current_bytes WHERE event_name LIKE 'memory/innodb%';\n\n## Best Practices\n• Fix queries before global tuning.\n• Benchmark parameter changes.\n• Monitor buffer pool hit ratio.\n• Document production my.cnf rationale.",
    command: "SHOW VARIABLES LIKE 'innodb_buffer_pool%';\nSHOW VARIABLES LIKE 'innodb_flush%';\nSHOW VARIABLES LIKE 'innodb_io_capacity%';\nSHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_%';\nSELECT * FROM sys.memory_global_by_current_bytes WHERE event_name LIKE 'memory/innodb%';",
    tags: ["mysql","Performance Tuning","easy","very-common"],
  },
  {
    id: "mysql-perf-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "How do you tune InnoDB redo log and checkpoint behavior under heavy write load?",
    answer: "## Interview Answer\nSmall redo logs cause aggressive checkpointing — flush storm and IO latency spikes. Increase innodb_redo_log_capacity (8.0.30+) or innodb_log_file_size. Monitor Innodb_os_log_written, pending flushes, and log buffer wait events.\n\n## Detailed Explanation\nCheckpoint age vs max checkpoint age in INNODB STATUS.\nLarge log delays crash recovery time — balance.\ninnodb_log_buffer_size if many large transactions.\nPage cleaner threads flush dirty pages proactively.\nSudden write latency spikes often correlate checkpoint not buffer pool miss.\n\n## Production Perspective\nGraph redo log utilization PMM plugin. After increasing log capacity, monitor recovery time estimate in DR drill. SSD required for large log sustained write rates.\n\n## Common Follow-up Questions\n• Redo log capacity vs file size 8.0?\n• Crash recovery time vs log size?\n• What triggers sharp checkpoint?\n\n## Common Mistakes\n• Tiny log on write-heavy primary.\n• Increasing log without IO capacity headroom.\n• Ignoring history list length with large log.\n\n## Senior Engineer Insights\nCorrelate p99 write latency graph with checkpoint metrics — compelling incident narrative.\n\n## Key Commands\n• SHOW ENGINE INNODB STATUS\\G\n-- LOG section\n• SHOW VARIABLES LIKE 'innodb_redo_log_capacity';\n• SHOW GLOBAL STATUS LIKE 'Innodb_os_log%';\nSHOW GLOBAL STATUS LIKE 'Innodb_%checkpoint%';\n\n## Best Practices\n• Size redo for write burst absorption.\n• Monitor checkpoint age.\n• Match io_capacity to storage.\n• Test recovery after log resize.",
    command: "SHOW ENGINE INNODB STATUS\\G\n-- LOG section\nSHOW VARIABLES LIKE 'innodb_redo_log_capacity';\nSHOW GLOBAL STATUS LIKE 'Innodb_os_log%';\nSHOW GLOBAL STATUS LIKE 'Innodb_%checkpoint%';",
    tags: ["mysql","Performance Tuning","medium","very-common"],
  },
  {
    id: "mysql-perf-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "How do you identify and tune temp table and sort memory usage in MySQL?",
    answer: "## Interview Answer\nCreated_tmp_disk_tables and Sort_merge_passes global status indicate memory pressure — tmp_table_size, max_heap_table_size, sort_buffer_size exceeded per connection. Fix queries first (GROUP BY index, reduce DISTINCT); raise session buffers cautiously — global max × connections = RAM risk.\n\n## Detailed Explanation\nInternal temp tables: MEMORY engine until size threshold then disk MyISAM/InnoDB temp.\nSort buffer allocated per sort operation per connection.\nEXPLAIN Extra Using temporary / Using filesort signals.\n8.0 temptable engine in performance_schema memory.\nPer-session SET sort_buffer_size for batch job only.\n\n## Production Perspective\nAlert Created_tmp_disk_tables rate spike. Top digest with temp — rewrite or index. Avoid raising global tmp_table_size to 1GB — 500 connections catastrophic.\n\n## Common Follow-up Questions\n• Memory vs disk temp threshold?\n• temptable_max_mmap?\n• Per-session buffer best practice?\n\n## Common Mistakes\n• Global sort_buffer_size=10M on 2000 connections.\n• Ignoring GROUP BY rewrite opportunity.\n• Disk temp on NVMe assumed free — still slow.\n\n## Senior Engineer Insights\nCalculate worst-case RAM: sort_buffer × max_connections × concurrent sorts — often exceeds server RAM if globals naive.\n\n## Key Commands\n• SHOW GLOBAL STATUS LIKE 'Created_tmp%';\nSHOW GLOBAL STATUS LIKE 'Sort_%';\n• SHOW VARIABLES LIKE '%tmp%size';\nSHOW VARIABLES LIKE 'sort_buffer_size';\n• EXPLAIN SELECT a, COUNT(*) FROM t GROUP BY a;\n\n## Best Practices\n• Fix queries causing disk temp.\n• Session-level buffers for batch jobs.\n• Monitor Created_tmp_disk_tables.\n• Cap global buffers conservatively.",
    command: "SHOW GLOBAL STATUS LIKE 'Created_tmp%';\nSHOW GLOBAL STATUS LIKE 'Sort_%';\nSHOW VARIABLES LIKE '%tmp%size';\nSHOW VARIABLES LIKE 'sort_buffer_size';\nEXPLAIN SELECT a, COUNT(*) FROM t GROUP BY a;",
    tags: ["mysql","Performance Tuning","medium","common"],
  },
  {
    id: "mysql-perf-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "DBA",
    question: "How do you conduct systematic performance baseline and regression testing for MySQL?",
    answer: "## Interview Answer\nEstablish baseline: QPS, p95 latency, buffer pool hit, replication lag, top digests, EXPLAIN snapshots. Use sysbench/hammerDB for synthetic; capture AWR-like PMM period. Regression: compare canary deploy metrics, EXPLAIN CI, pt-query-digest weekly delta.\n\n## Detailed Explanation\nMetrics: Questions, Com_select/update, Innodb_rows_read, lock waits.\nWorkload replay: tcpdump or slow log replay tools — careful on prod.\nChange control: one variable or one release at a time.\nDocument environment: row count, hardware, my.cnf version controlled.\nFalse positive: cache warm vs cold — note in baseline doc.\n\n## Production Perspective\nGrafana dashboards per service tier. Block release if p95 regression >20% on canary. Post-upgrade baseline within 24h of deploy.\n\n## Common Follow-up Questions\n• sysbench oltp_read_write profile?\n• Warm cache methodology?\n• PMM vs custom Prometheus?\n\n## Common Mistakes\n• Benchmark empty database.\n• Changing ten parameters simultaneously.\n• No baseline before upgrade blaming MySQL.\n\n## Senior Engineer Insights\nPresent baseline report template: workload description, metrics table, top 10 queries, config snapshot — ops maturity.\n\n## Key Commands\n• sysbench oltp_read_write --mysql-host=... --tables=10 --table-size=1000000 prepare\nsysbench ... run\n• SELECT * FROM sys.user_summary ORDER BY total_latency DESC;\n• pt-query-digest slow.log --since 7d ago > weekly_digest.txt\n\n## Best Practices\n• Document baseline before changes.\n• Synthetic + real digest monitoring.\n• Canary with automated rollback.\n• Version control my.cnf.",
    command: "sysbench oltp_read_write --mysql-host=... --tables=10 --table-size=1000000 prepare\nsysbench ... run\nSELECT * FROM sys.user_summary ORDER BY total_latency DESC;\npt-query-digest slow.log --since 7d ago > weekly_digest.txt",
    tags: ["mysql","Performance Tuning","hard","common"],
  },
  {
    id: "mysql-perf-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "Cloud Engineer",
    question: "What OS and filesystem tuning matters for MySQL on Linux production servers?",
    answer: "## Interview Answer\nDisable transparent huge pages (THP) — InnoDB recommendation. vm.swappiness=1 or 0 to avoid swap latency. noop/none scheduler less relevant on NVMe; focus on XFS/ext4 with noatime. NUMA: innodb_numa_interleave on multi-socket. File descriptor limits for max_connections.\n\n## Detailed Explanation\nTHP causes latency jitter — set never in systemd unit.\nSwap: zero preferred on dedicated DB; OOM killer vs swap thrash trade-off.\nMount options: noatime reduces metadata writes.\nnoflush sync mount dangerous — don't use.\nirqbalance, NIC offloads — cloud provider handles mostly.\n\n## Production Perspective\nAutomate THP disable in AMI/cloud-init. Monitor swap usage alert >0 on DB nodes. ulimit -n 65535 in mysql systemd LimitNOFILE.\n\n## Common Follow-up Questions\n• XFS vs ext4 for MySQL?\n• NUMA binding mysql pinned?\n• Cloud managed DB OS tuning?\n\n## Common Mistakes\n• THP enabled causing mysterious latency spikes.\n• Swap enabled on buffer-pool-heavy node.\n• nofile limit 1024 with max_connections 500.\n\n## Senior Engineer Insights\nLatency outlier debugging: check dmesg THP, swap, disk await in same timeline as slow query.\n\n## Key Commands\n• cat /sys/kernel/mm/transparent_hugepage/enabled\n# always madvise or never\n• sysctl vm.swappiness\nsysctl -w vm.swappiness=1\n• ulimit -n\nSHOW VARIABLES LIKE 'open_files_limit';\n\n## Best Practices\n• Disable THP on DB nodes.\n• Minimize swap usage.\n• Raise open files limit.\n• Document OS tuning in AMI build.",
    command: "cat /sys/kernel/mm/transparent_hugepage/enabled\n# always madvise or never\nsysctl vm.swappiness\nsysctl -w vm.swappiness=1\nulimit -n\nSHOW VARIABLES LIKE 'open_files_limit';",
    tags: ["mysql","Performance Tuning","medium","rare"],
  },
  {
    id: "mysql-perf-006",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Database Engineer",
    question: "Lead optimization of a write-heavy ledger table hitting InnoDB insert bottleneck.",
    answer: "## Interview Answer\nDiagnose: random PK UUID causing page splits, secondary index count, sync_binlog+flush_log durability, redo log size, disk IO saturation. Fixes: sequential PK (auto_increment or ordered UUID), reduce indexes, batch inserts, partition by time, tune redo/io_capacity, semi-sync off for throughput tier if RPO allows.\n\n## Detailed Explanation\nInsert hotspot: monotonic PK appends right edge of B-tree — efficient.\nMany secondary indexes multiply insert cost linearly.\nGroup commit batching — high concurrent small commits still fsync bound.\nConsider async replica and relaxed sync on non-financial tier.\nShard when single instance IO exhausted after tuning.\n\n## Production Perspective\nMeasure inserts/sec before/after PK change — major migration. Batch INSERT multi-row reduces round trips. Monitor innodb_data_pending_writes and disk util.\n\n## Common Follow-up Questions\n• Ordered UUID strategies?\n• Insert buffer role?\n• When shard ledger?\n\n## Common Mistakes\n• Adding indexes on ledger for rare reports.\n• UUID v4 PK on 100K inserts/sec.\n• Relaxing durability without approval.\n\n## Senior Engineer Insights\nFinancial ledger: durability non-negotiable — optimize PK and index count not flush_log=2 without compliance sign-off.\n\n## Key Commands\n• SHOW GLOBAL STATUS LIKE 'Innodb_rows_inserted';\n• SHOW ENGINE INNODB STATUS\\G\n-- INSERT BUFFER, LOG\n• INSERT INTO ledger VALUES (...), (...), (...);  -- batch pattern\n\n## Best Practices\n• Monotonic PK for high insert.\n• Minimal secondary indexes.\n• Batch multi-row inserts.\n• Shard when IO saturated post-tune.",
    command: "SHOW GLOBAL STATUS LIKE 'Innodb_rows_inserted';\nSHOW ENGINE INNODB STATUS\\G\n-- INSERT BUFFER, LOG\nINSERT INTO ledger VALUES (...), (...), (...);  -- batch pattern",
    tags: ["mysql","Performance Tuning","hard","rare"],
  },
  {
    id: "mysql-conn-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "How does MySQL handle connections and why is connection pooling essential?",
    answer: "## Interview Answer\nDefault one thread per connection consumes memory (thread_stack, per-connection buffers). max_connections caps concurrent sessions; beyond limit new connections rejected. App-side pooling (HikariCP) or ProxySQL multiplexes — avoids opening thousands of threads on MySQL.\n\n## Detailed Explanation\nthread_handling=one-thread-per-connection default.\nEnterprise thread pool plugin: pool-of-threads model — rare in cloud.\nConnection errors: Too many connections, Aborted_connects.\nwait_timeout disconnects idle sessions — app pool should validate connection.\nEach connection may allocate sort_buffer, join_buffer on demand per query.\n\n## Production Perspective\nSize max_connections = app_pool_size × app_instances + admin headroom. Monitor Threads_connected peak vs max. ProxySQL multiplexes thousands app conns to hundreds MySQL conns.\n\n## Common Follow-up Questions\n• thread_pool vs app pool?\n• wait_timeout vs interactive_timeout?\n• How ProxySQL multiplexing works?\n\n## Common Mistakes\n• max_connections=10000 without RAM calculation.\n• No pool — app opens connection per HTTP request.\n• Idle connections never released blocking wait_timeout needed.\n\n## Senior Engineer Insights\nRAM formula interview answer: max_connections × (thread_stack + read_buffer + per-session overhead) + buffer pool ≤ RAM.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'max_connections';\nSHOW VARIABLES LIKE '%timeout%';\n• SHOW GLOBAL STATUS LIKE 'Threads_%';\nSHOW GLOBAL STATUS LIKE 'Max_used_connections';\n• SHOW PROCESSLIST;\n\n## Best Practices\n• App connection pooling mandatory.\n• Right-size max_connections.\n• Set reasonable wait_timeout.\n• Use ProxySQL at scale.",
    command: "SHOW VARIABLES LIKE 'max_connections';\nSHOW VARIABLES LIKE '%timeout%';\nSHOW GLOBAL STATUS LIKE 'Threads_%';\nSHOW GLOBAL STATUS LIKE 'Max_used_connections';\nSHOW PROCESSLIST;",
    tags: ["mysql","Connection Management","easy","very-common"],
  },
  {
    id: "mysql-conn-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "How do you troubleshoot \"Too many connections\" errors in production?",
    answer: "## Interview Answer\nImmediate: SHOW PROCESSLIST count, check Max_used_connections vs max_connections, identify connection leak (app not closing, pool misconfig), long sleeping connections, or traffic spike. Mitigate: kill sleeping threads cautiously, temporarily raise max_connections if RAM allows, scale ProxySQL, restart leaky app tier.\n\n## Detailed Explanation\nFlapping app deploy opens connections without pool drain.\nORM connection leak in error paths.\nMonitoring tools opening new connection each check — use pooled collector.\nReserved connections: super user +1 for admin emergency SUPER privilege.\nCloud RDS max_connections tied to instance class formula.\n\n## Production Perspective\nRunbook: identify top user/host from PROCESSLIST GROUP BY. Fix app not band-aid max_connections forever. connection_control plugin rate-limits failed auth brute force — separate from legit limit.\n\n## Common Follow-up Questions\n• super_reserved_connections?\n• Kill vs increase max?\n• RDS max_connections formula?\n\n## Common Mistakes\n• Raising max_connections to 50000 — OOM crash.\n• Killing replication threads.\n• No app fix after incident.\n\n## Senior Engineer Insights\nDistinguish legitimate spike (Black Friday) vs leak (steady climb) from Grafana Threads_connected trend.\n\n## Key Commands\n• SHOW GLOBAL STATUS LIKE 'Max_used_connections';\n• SELECT user, host, COUNT(*) FROM information_schema.processlist GROUP BY user, host ORDER BY 3 DESC;\n• KILL 12345;  -- sleeping connection after confirming safe\n\n## Best Practices\n• Fix pool leaks at source.\n• Alert at 80% max_connections.\n• Reserve admin connection path.\n• Load test pool sizing before events.",
    command: "SHOW GLOBAL STATUS LIKE 'Max_used_connections';\nSELECT user, host, COUNT(*) FROM information_schema.processlist GROUP BY user, host ORDER BY 3 DESC;\nKILL 12345;  -- sleeping connection after confirming safe",
    tags: ["mysql","Connection Management","medium","very-common"],
  },
  {
    id: "mysql-conn-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do you configure ProxySQL for read/write splitting and connection pooling?",
    answer: "## Interview Answer\nProxySQL sits between apps and MySQL: multiplexes connections, routes SELECT to replica hostgroups, writes to primary, handles failover detection. Configure mysql_servers, mysql_query_rules, mysql_users. Apps connect to ProxySQL not directly to MySQL — reduces connection count and enables zero-downtime backend changes.\n\n## Detailed Explanation\nHostgroup 0 writer, 1 readers — query rules regex or digest based.\nmax_connections on ProxySQL separate from MySQL.\nGalera/GR integration: monitor writer status variable.\nPrepared statement caching and multiplexing caveats — test ORM compatibility.\nStats table stats_mysql_connection_pool for monitoring.\n\n## Production Perspective\nTwo ProxySQL instances behind VIP/NLB for HA. Sync config to disk admin layer. Failover: writer offline → promote reader hostgroup. Test JDBC prepared statements with multiplexing rules.\n\n## Common Follow-up Questions\n• Multiplexing breaks prepared statements when?\n• Query rules vs router?\n• ProxySQL vs MySQL Router?\n\n## Common Mistakes\n• Routing writes to reader hostgroup — replication lag reads stale.\n• Single ProxySQL SPOF.\n• Multiplexing without ORM testing.\n\n## Senior Engineer Insights\nArchitecture slide: App → ProxySQL pool → Primary + Replicas with hostgroup rules — standard SaaS pattern.\n\n## Key Commands\n• INSERT INTO mysql_servers(hostgroup_id, hostname, port) VALUES (0,'primary',3306),(1,'replica1',3306);\n• INSERT INTO mysql_query_rules(rule_id, active, match_pattern, destination_hostgroup, apply) VALUES (1,1,'^SELECT',1,1);\n• LOAD MYSQL SERVERS TO RUNTIME; SAVE MYSQL SERVERS TO DISK;\n• SELECT hostgroup, srv_host, status, ConnUsed FROM stats_mysql_connection_pool;\n\n## Best Practices\n• HA ProxySQL pair.\n• Test ORM with multiplexing.\n• Monitor pool saturation.\n• Automate backend server updates.",
    command: "INSERT INTO mysql_servers(hostgroup_id, hostname, port) VALUES (0,'primary',3306),(1,'replica1',3306);\nINSERT INTO mysql_query_rules(rule_id, active, match_pattern, destination_hostgroup, apply) VALUES (1,1,'^SELECT',1,1);\nLOAD MYSQL SERVERS TO RUNTIME; SAVE MYSQL SERVERS TO DISK;\nSELECT hostgroup, srv_host, status, ConnUsed FROM stats_mysql_connection_pool;",
    tags: ["mysql","Connection Management","medium","common"],
  },
  {
    id: "mysql-conn-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "What causes connection storms during failover and how do you mitigate them?",
    answer: "## Interview Answer\nFailover drops all connections — apps simultaneously reconnect exhausting max_connections and CPU on new primary. Mitigate: exponential backoff in app driver, ProxySQL/Router buffering, pre-warmed pools, circuit breakers, staggered consumer restart, adequate max_connections headroom for burst.\n\n## Detailed Explanation\nThundering herd: 500 app pods × 20 pool each reconnect at once.\nJDBC mysql connector reconnect params: connectTimeout, retries.\nProxySQL absorbs reconnect storm to smaller MySQL conn set.\nKubernetes rolling restart all pods same time — coordinate.\nWarmup endpoint on app before marking ready post-failover.\n\n## Production Perspective\nGame day failover measuring connection peak. Tune HikariCP maximumPoolSize × instances < max_connections × 0.8. MySQL Router metadata cache refresh delay — apps should retry writes briefly.\n\n## Common Follow-up Questions\n• JDBC reconnect best practices?\n• Router vs direct reconnect?\n• Headroom percentage rule?\n\n## Common Mistakes\n• Zero backoff retry loop hammering primary.\n• max_connections exactly pool size no headroom.\n• All microservices restart simultaneously after AZ failure.\n\n## Senior Engineer Insights\nQuantify: 10K connection attempts in 5 seconds — need ProxySQL + backoff + headroom trifecta.\n\n## Key Commands\n• SHOW GLOBAL STATUS LIKE 'Connections';\nSHOW GLOBAL STATUS LIKE 'Aborted_connects';\n• SHOW PROCESSLIST;\n• -- App: HikariCP connectionTimeout, initializationFailTimeout\n\n## Best Practices\n• Exponential backoff in drivers.\n• ProxySQL fronting apps.\n• 80% max_connections headroom.\n• Failover game days measure storm.",
    command: "SHOW GLOBAL STATUS LIKE 'Connections';\nSHOW GLOBAL STATUS LIKE 'Aborted_connects';\nSHOW PROCESSLIST;\n-- App: HikariCP connectionTimeout, initializationFailTimeout",
    tags: ["mysql","Connection Management","hard","common"],
  },
  {
    id: "mysql-conn-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "Database Engineer",
    question: "How do wait_timeout, interactive_timeout, and connection validation interact with connection pools?",
    answer: "## Interview Answer\nwait_timeout closes idle non-interactive connections after N seconds; interactive_timeout for interactive CLI sessions. Pools should set testOnBorrow or keepalive below wait_timeout to avoid stale connections — \"Communications link failure\" errors in app logs.\n\n## Detailed Explanation\nDefault wait_timeout 28800 (8h) — many pools lower MySQL to 600s.\nPool maxLifetime should be < wait_timeout.\nMySQL 8.0 tcp_keepalive reduces silent disconnect detection time.\nCLOSE_WAIT from app not closing — OS level leak distinct from MySQL timeout.\nPerformance_schema accounts with idle time in processlist.\n\n## Production Perspective\nAlign: MySQL wait_timeout=600, pool maxLifetime=550, pool keepalive query SELECT 1 every 300s. Document in platform runbook for all microservices.\n\n## Common Follow-up Questions\n• maxLifetime vs idleTimeout in HikariCP?\n• tcp_keepalive variables?\n• CLOSE_WAIT vs MySQL timeout?\n\n## Common Mistakes\n• MySQL timeout 60s pool lifetime 30min — stale surprises.\n• No validation query in pool config.\n• Lowering wait_timeout without app team coordination.\n\n## Senior Engineer Insights\nPlatform team owns the triangle: MySQL timeout, pool lifetime, keepalive interval — document invariant relationship.\n\n## Key Commands\n• SHOW VARIABLES LIKE '%timeout%';\n• SELECT id, user, time, state FROM information_schema.processlist WHERE command='Sleep' ORDER BY time DESC LIMIT 20;\n• SET GLOBAL wait_timeout=600;\nSET GLOBAL interactive_timeout=600;\n\n## Best Practices\n• Pool maxLifetime < wait_timeout.\n• Periodic validation query in pool.\n• Coordinate timeout changes with apps.\n• Monitor Aborted_clients rate.",
    command: "SHOW VARIABLES LIKE '%timeout%';\nSELECT id, user, time, state FROM information_schema.processlist WHERE command='Sleep' ORDER BY time DESC LIMIT 20;\nSET GLOBAL wait_timeout=600;\nSET GLOBAL interactive_timeout=600;",
    tags: ["mysql","Connection Management","medium","rare"],
  },
  {
    id: "mysql-conn-006",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Design connection architecture for 500 microservices accessing shared MySQL cluster.",
    answer: "## Interview Answer\nMandatory ProxySQL tier with multiplexing; per-service MySQL users with least privilege; hostgroup read/write split; aggregate pool limits per service in config; max_connections on MySQL sized to ProxySQL backend connections not 500×pool; circuit breakers; separate admin path; monitor per-user connection counts.\n\n## Detailed Explanation\n500 services × 10 pool = 5000 potential — multiplex to 200 MySQL conns.\nRate limiting at ProxySQL mysql_users max_connections per user.\nSchema-per-service vs shared schema — connection count same, isolation differs.\nRead replicas scale reads; writes single primary.\nEmergency: kill user connection storm via CONNECTION_CONTROL or KILL by user.\n\n## Production Perspective\nService catalog documents max pool size per tier. CI rejects new service without DBA-reviewed connection budget. Grafana per mysql user Threads_connected.\n\n## Common Follow-up Questions\n• Per-service user vs shared user?\n• Connection budget approval process?\n• Vitess for many services?\n\n## Common Mistakes\n• Each service max pool 100 — aggregate exceeds max_connections.\n• Shared root credential across services.\n• No ProxySQL — direct 5000 connections.\n\n## Senior Engineer Insights\nGovernance story impresses interviewers — technology plus process: connection budget as platform product.\n\n## Key Commands\n• SELECT user, COUNT(*) conns FROM information_schema.processlist GROUP BY user ORDER BY 2 DESC;\n• CREATE USER 'svc_orders'@'10.%' IDENTIFIED BY '...';\nGRANT SELECT,INSERT,UPDATE,DELETE ON orders_db.* TO 'svc_orders'@'10.%';\n• INSERT INTO mysql_users(username, password, default_hostgroup, max_connections) VALUES ('svc_orders','...',0,50);\n\n## Best Practices\n• ProxySQL multiplexing mandatory.\n• Per-service credentials and limits.\n• Connection budget in service onboarding.\n• Monitor per-user connection counts.",
    command: "SELECT user, COUNT(*) conns FROM information_schema.processlist GROUP BY user ORDER BY 2 DESC;\nCREATE USER 'svc_orders'@'10.%' IDENTIFIED BY '...';\nGRANT SELECT,INSERT,UPDATE,DELETE ON orders_db.* TO 'svc_orders'@'10.%';\nINSERT INTO mysql_users(username, password, default_hostgroup, max_connections) VALUES ('svc_orders','...',0,50);",
    tags: ["mysql","Connection Management","hard","rare"],
  }
];
