// MySQL — indexing
// Structured interview content — run: node scripts/generate-db-tracks.mjs
// 20 questions

export const questions = [
  {
    id: "mysql-idx-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "How do B-tree indexes work in InnoDB and what is the clustered index?",
    answer: "## Interview Answer\nInnoDB stores table data in PK B-tree order — clustered index is the table. Secondary indexes leaf nodes store PK values as row pointers. Lookups on secondary index may require double read (index + PK). Choose short, selective PK.\n\n## Detailed Explanation\nPrimary key required for best performance — hidden row_id if absent.\nSecondary index entry: index columns + PK columns.\nCardinality estimates from innodb_stats — ANALYZE refreshes.\nPrefix indexes: INDEX(col(20)) saves space, less selective.\nInvisible indexes (8.0): test drop impact without removing.\n\n## Production Perspective\nAudit PK types — avoid random UUID v4 on high insert tables. Monitor unused indexes via sys.schema_unused_indexes. Invisible index test before DROP in prod.\n\n## Common Follow-up Questions\n• Secondary index leaf structure?\n• Prefix index trade-offs?\n• Invisible index workflow?\n\n## Common Mistakes\n• No PK on large table.\n• Duplicate indexes on same prefix.\n• Over-indexing slowing writes.\n\n## Senior Engineer Insights\nWide PK bloats every secondary index — composite secondary indexes include PK implicitly at leaf.\n\n## Key Commands\n• SHOW INDEX FROM orders;\n• SELECT * FROM sys.schema_unused_indexes;\n• ALTER TABLE orders ALTER INDEX idx_old INVISIBLE;\n\n## Best Practices\n• Explicit PK every table.\n• Audit redundant indexes quarterly.\n• Use invisible index for drop validation.\n• Right-size prefix indexes only when needed.",
    command: "SHOW INDEX FROM orders;\nSELECT * FROM sys.schema_unused_indexes;\nALTER TABLE orders ALTER INDEX idx_old INVISIBLE;",
    tags: ["mysql","Indexes","easy","very-common"],
  },
  {
    id: "mysql-idx-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "DBA",
    question: "What is index selectivity and how does it affect optimizer index choice?",
    answer: "## Interview Answer\nSelectivity = distinct values / total rows. Low selectivity (gender column) — optimizer may skip index for full scan cheaper. High selectivity (email, order_id) — index ref/range optimal. Combine low-selectivity filters with high-selectivity columns in composite index.\n\n## Detailed Explanation\nCardinality in SHOW INDEX / STATS_persistent.\nHistograms improve estimates for skewed distributions.\nIndex skip scan (8.0) uses index when first column missing from WHERE.\nFull table scan chosen when >30% rows estimated — sometimes correct.\nFake index stats rare — prefer ANALYZE over manual stats unless expert.\n\n## Production Perspective\nDrop indexes with cardinality 1 on large tables unless composite prefix. Validate optimizer choice with EXPLAIN ANALYZE not assumptions. Gender+created_at composite: gender alone useless, composite with range on created_at useful.\n\n## Common Follow-up Questions\n• When optimizer ignores index?\n• Index skip scan requirements?\n• How histograms differ from index stats?\n\n## Common Mistakes\n• Indexing every column \"just in case\".\n• Assuming index always faster than scan.\n• Manual stats without documentation.\n\n## Senior Engineer Insights\nCoupon code example: low selectivity until status filter added — composite (campaign_id, status, user_id) tells the story.\n\n## Key Commands\n• SELECT INDEX_NAME, CARDINALITY FROM information_schema.STATISTICS WHERE TABLE_NAME='orders';\n• ANALYZE TABLE orders;\n• EXPLAIN SELECT * FROM users WHERE gender='M' AND email='x@y.com';\n\n## Best Practices\n• Composite indexes for multi-filter queries.\n• Remove unused low-value indexes.\n• Histograms on skewed non-indexed filters.\n• Measure before adding index.",
    command: "SELECT INDEX_NAME, CARDINALITY FROM information_schema.STATISTICS WHERE TABLE_NAME='orders';\nANALYZE TABLE orders;\nEXPLAIN SELECT * FROM users WHERE gender='M' AND email='x@y.com';",
    tags: ["mysql","Indexes","medium","very-common"],
  },
  {
    id: "mysql-idx-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "How do you find and remove redundant or duplicate indexes in production?",
    answer: "## Interview Answer\nsys.schema_redundant_indexes and schema_duplicate_indexes identify indexes where one index prefix covers another. Duplicate indexes waste write IO and buffer pool. Drop redundant after validation period with invisible index or staging load test.\n\n## Detailed Explanation\nRedundant: (a) and (a,b) — (a) alone redundant if no covering-only need.\nDuplicate: identical definitions from migration mistakes.\nFK columns need index — don't drop only index on FK child column.\nOnline drop: ALGORITHM=INPLACE, LOCK=NONE in 8.0 for most secondary drops.\nMonitor write latency improvement post-drop.\n\n## Production Perspective\nChange ticket per drop with rollback CREATE INDEX script. pt-osc not needed for index drop usually. Verify query plans on all replicas before drop — read replica plan may differ if stats differ.\n\n## Common Follow-up Questions\n• FK index requirements?\n• Drop index online lock impact?\n• Redundant vs duplicate difference?\n\n## Common Mistakes\n• Dropping only index supporting FK constraint.\n• Dropping index used by unique constraint enforcement.\n• No rollback script prepared.\n\n## Senior Engineer Insights\nQuantify: 5 redundant indexes × 10K writes/sec = measurable write amplification reduction.\n\n## Key Commands\n• SELECT * FROM sys.schema_redundant_indexes;\n• SELECT * FROM sys.schema_duplicate_indexes;\n• ALTER TABLE orders DROP INDEX idx_redundant, ALGORITHM=INPLACE, LOCK=NONE;\n\n## Best Practices\n• Quarterly index audit.\n• Keep CREATE INDEX rollback in ticket.\n• Use invisible index before drop.\n• Validate FK coverage after audit.",
    command: "SELECT * FROM sys.schema_redundant_indexes;\nSELECT * FROM sys.schema_duplicate_indexes;\nALTER TABLE orders DROP INDEX idx_redundant, ALGORITHM=INPLACE, LOCK=NONE;",
    tags: ["mysql","Indexes","medium","common"],
  },
  {
    id: "mysql-idx-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "Database Engineer",
    question: "How do functional indexes and expression indexes work in MySQL 8.0?",
    answer: "## Interview Answer\nFunctional key parts index expression values — e.g. INDEX((LOWER(email))). Enables index use on function-wrapped columns without app rewrite. Virtual generated columns can also be indexed for same effect with clearer schema.\n\n## Detailed Explanation\nCREATE INDEX idx ON t ((LOWER(email))); — double parens syntax.\nGenerated column STORED vs VIRTUAL indexed.\nFunctional index participates in unique constraints on expression.\nOptimizer must match expression exactly in query predicate.\nHidden generated column pattern cleaner for ORM compatibility.\n\n## Production Perspective\nPrefer generated column if ORM needs readable column. Test expression match case sensitivity. Online ADD INDEX on large table — monitor replication lag during build.\n\n## Common Follow-up Questions\n• STORED vs VIRTUAL generated column?\n• Functional index on JSON path?\n• Index on CAST datetime?\n\n## Common Mistakes\n• Query uses LOWER(email) but index on email raw.\n• STORED generated column doubling disk.\n• Functional index during unsupported ALGORITHM=INPLACE.\n\n## Senior Engineer Insights\nMigration path: add generated column + index online → dual-write app → switch query → drop old pattern.\n\n## Key Commands\n• CREATE INDEX idx_lower_email ON users ((LOWER(email)));\n• ALTER TABLE users ADD COLUMN email_lower VARCHAR(255) AS (LOWER(email)) VIRTUAL,\n  ADD INDEX idx_email_lower (email_lower);\n• EXPLAIN SELECT * FROM users WHERE LOWER(email)='test@x.com';\n\n## Best Practices\n• Match indexed expression exactly in SQL.\n• Consider virtual generated column for clarity.\n• Online index build with lag monitoring.\n• Document expression indexes for ORM teams.",
    command: "CREATE INDEX idx_lower_email ON users ((LOWER(email)));\nALTER TABLE users ADD COLUMN email_lower VARCHAR(255) AS (LOWER(email)) VIRTUAL,\n  ADD INDEX idx_email_lower (email_lower);\nEXPLAIN SELECT * FROM users WHERE LOWER(email)='test@x.com';",
    tags: ["mysql","Indexes","hard","common"],
  },
  {
    id: "mysql-idx-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "DBA",
    question: "When should you use FULLTEXT, SPATIAL, or regular B-tree indexes in MySQL?",
    answer: "## Interview Answer\nB-tree: equality/range on scalar columns — default choice. FULLTEXT: natural language search InnoDB FT index ngram for CJK. SPATIAL: R-tree geo queries with SRID. Wrong engine choice — FT on B-tree column with LIKE '%word%' scans entire table.\n\n## Detailed Explanation\nFULLTEXT: MATCH ... AGAINST IN BOOLEAN MODE.\nMinimal token length ft_min_word_len — config restart.\nngram_token_size for Chinese/Japanese without word boundaries.\nSPATIAL: POINT, ST_Distance; require NOT NULL geometry and SRID.\nCovering B-tree often beats FT for prefix match autocomplete with limit.\n\n## Production Perspective\nElasticsearch/OpenSearch for complex search; MySQL FT for simple catalog search co-located with transactional data. SPATIAL index build locks — plan maintenance. Test ngram parser token explosion on storage.\n\n## Common Follow-up Questions\n• FT vs Elasticsearch trade-off?\n• SPATIAL index limitations?\n• ngram vs mecab parser?\n\n## Common Mistakes\n• LIKE '%term%' expecting index use.\n• FULLTEXT on short codes SKU field.\n• Mixing SRID in spatial queries.\n\n## Senior Engineer Insights\nArchitecture split: OLTP in MySQL, search in dedicated engine unless search is simple — cost/complexity trade-off.\n\n## Key Commands\n• CREATE FULLTEXT INDEX ft_body ON articles(body);\n• SELECT * FROM articles WHERE MATCH(body) AGAINST('+mysql +innodb' IN BOOLEAN MODE);\n• CREATE SPATIAL INDEX idx_loc ON stores(location);\n\n## Best Practices\n• B-tree default; FT for real text search.\n• External search for advanced relevance.\n• Configure ngram for CJK.\n• Benchmark FT vs external before build.",
    command: "CREATE FULLTEXT INDEX ft_body ON articles(body);\nSELECT * FROM articles WHERE MATCH(body) AGAINST('+mysql +innodb' IN BOOLEAN MODE);\nCREATE SPATIAL INDEX idx_loc ON stores(location);",
    tags: ["mysql","Indexes","medium","rare"],
  },
  {
    id: "mysql-compidx-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "What is the leftmost prefix rule for composite indexes in MySQL?",
    answer: "## Interview Answer\nComposite index (a,b,c) can serve queries filtering on a; a,b; or a,b,c — not b alone or c alone (except index skip scan 8.0 edge cases). Order columns by equality filters first, then range, then ORDER BY columns.\n\n## Detailed Explanation\nkey_len in EXPLAIN shows prefix usage bytes.\nWHERE a=1 AND c=3 — uses a only from (a,b,c).\nReorder index to (a,c,b) if a and c equality, b range rare.\nDuplicate single-column indexes when composite exists — redundant.\nCardinality: most selective column not always leftmost — equality order matters more.\n\n## Production Perspective\nStandard pattern: (tenant_id, status, created_at) for SaaS multi-tenant lists. Review ORM-generated indexes against query patterns. Migration: ADD new composite, invisible old, cutover, DROP old.\n\n## Common Follow-up Questions\n• Index skip scan when?\n• How ORDER BY interacts with composite?\n• Three-column composite examples?\n\n## Common Mistakes\n• (created_at, tenant_id) when all queries filter tenant_id first.\n• Separate indexes on each column instead of one composite.\n• Ignoring sort direction in ORDER BY optimization.\n\n## Senior Engineer Insights\nTeach with concrete WHERE clause — interviewers want column ordering rationale not memorization.\n\n## Key Commands\n• CREATE INDEX idx_tenant_status_created ON orders(tenant_id, status, created_at);\n• EXPLAIN SELECT * FROM orders WHERE tenant_id=1 AND status='OPEN' ORDER BY created_at DESC;\n• SHOW INDEX FROM orders;\n\n## Best Practices\n• Equality columns leftmost.\n• Include ORDER BY columns in index.\n• One purposeful composite vs many singles.\n• Validate key_len in EXPLAIN.",
    command: "CREATE INDEX idx_tenant_status_created ON orders(tenant_id, status, created_at);\nEXPLAIN SELECT * FROM orders WHERE tenant_id=1 AND status='OPEN' ORDER BY created_at DESC;\nSHOW INDEX FROM orders;",
    tags: ["mysql","Composite Indexes","easy","very-common"],
  },
  {
    id: "mysql-compidx-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do you design composite indexes for mixed equality and range predicates?",
    answer: "## Interview Answer\nPlace equality columns first, range column last in composite — (status, created_at) for WHERE status='OPEN' AND created_at > ?. Multiple range columns — optimizer typically uses one range; put most selective equality filters leftmost.\n\n## Detailed Explanation\nRange stops index use for subsequent columns in traditional B-tree.\nIN clause treated as range or equality depending on version/optimizer.\nOR on different columns may prevent single composite use — UNION rewrite.\nPartition key should align with index prefix for pruning + index combo.\nConsider separate indexes + index merge vs one composite — EXPLAIN ANALYZE decides.\n\n## Production Perspective\nCheckout query: (user_id, product_id) equality, (created_at) range → index (user_id, product_id, created_at). Load test write amplification when adding wide composite on hot insert table.\n\n## Common Follow-up Questions\n• Two range columns in one query?\n• IN (1,2,3) index behavior?\n• Composite for JOIN + WHERE?\n\n## Common Mistakes\n• Range column in middle of composite.\n• Composite on 6 columns rarely used fully.\n• Not considering insert performance on wide index.\n\n## Senior Engineer Insights\nWrite amplification: every secondary index update on INSERT — composite count affects peak TPS ceiling.\n\n## Key Commands\n• EXPLAIN FORMAT=JSON\nSELECT * FROM events\nWHERE user_id=100 AND event_type IN ('click','view')\n  AND created_at > '2024-01-01';\n• CREATE INDEX idx_user_type_created ON events(user_id, event_type, created_at);\n\n## Best Practices\n• Equality before range in index definition.\n• Limit composite width to query needs.\n• Measure insert rate impact.\n• EXPLAIN ANALYZE mixed predicates.",
    command: "EXPLAIN FORMAT=JSON\nSELECT * FROM events\nWHERE user_id=100 AND event_type IN ('click','view')\n  AND created_at > '2024-01-01';\nCREATE INDEX idx_user_type_created ON events(user_id, event_type, created_at);",
    tags: ["mysql","Composite Indexes","medium","common"],
  },
  {
    id: "mysql-compidx-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "How do composite indexes affect INSERT, UPDATE, and DELETE performance?",
    answer: "## Interview Answer\nEach secondary index is B-tree updated per row change — more indexes = more write IO and lock duration. Wide composite indexes on high-insert tables (fact logs) hurt TPS. Keep indexes minimal on write-heavy tables; offload read patterns to replicas or summary tables.\n\n## Detailed Explanation\nUPDATE changing indexed column: delete+insert in each affected index tree.\nDELETE removes entries from all indexes.\nINSERT checks unique constraints on all unique indexes.\nFill factor not tunable InnoDB same as PostgreSQL — page splits on random PK.\nDelayed secondary index maintenance via change buffer — merges async.\n\n## Production Perspective\nFact table: only indexes required for purge job and critical lookup — not every report column. Batch inserts with ordered PK reduce page splits. Monitor history list if updates heavy on indexed columns.\n\n## Common Follow-up Questions\n• Change buffer when applies?\n• Insert order by PK benefit?\n• Drop index before bulk load?\n\n## Common Mistakes\n• Five composites on 50K inserts/sec table.\n• Updating indexed column frequently without need.\n• Bulk load without disabling unique checks strategy.\n\n## Senior Engineer Insights\nBulk ETL pattern: drop nonessential indexes → load → rebuild — maintenance window math for large tables.\n\n## Key Commands\n• SHOW GLOBAL STATUS LIKE 'Innodb_%insert%';\nSHOW GLOBAL STATUS LIKE 'Innodb_%delete%';\n• ALTER TABLE logs DROP INDEX idx_report, ALGORITHM=INPLACE;\n• -- bulk load then CREATE INDEX\n\n## Best Practices\n• Minimize indexes on write-heavy tables.\n• Ordered PK inserts for batch loads.\n• Rebuild indexes after large ETL.\n• Separate read replica indexes for reporting.",
    command: "SHOW GLOBAL STATUS LIKE 'Innodb_%insert%';\nSHOW GLOBAL STATUS LIKE 'Innodb_%delete%';\nALTER TABLE logs DROP INDEX idx_report, ALGORITHM=INPLACE;\n-- bulk load then CREATE INDEX",
    tags: ["mysql","Composite Indexes","medium","common"],
  },
  {
    id: "mysql-compidx-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Database Engineer",
    question: "Optimize composite indexing for a multi-tenant SaaS with 10K tenants and shared schema.",
    answer: "## Interview Answer\nLead with tenant_id as leftmost column on every composite — (tenant_id, ...). Avoid global indexes without tenant prefix causing full index scans across tenants. Consider partitioning by tenant hash/range for very large scale; connection routing per tenant shard at app layer for mega-tenants.\n\n## Detailed Explanation\nQuery always includes tenant_id from session context — composite (tenant_id, id) for PK lookups.\nList queries: (tenant_id, status, updated_at DESC).\nUnique constraints: UNIQUE(tenant_id, slug) not UNIQUE(slug) globally.\nCross-tenant admin reports: separate read replica or warehouse — not OLTP index.\nIndex size scales with total rows — monitor per-tenant row skew.\n\n## Production Perspective\nNoisy neighbor tenant with huge row count — partition or shard. ORM must enforce tenant_id in every query — static analysis in CI. Index cardinality fine per tenant but table huge — pruning via LIST partition on tenant_id bucket.\n\n## Common Follow-up Questions\n• Partition vs shard by tenant?\n• Global admin query strategy?\n• tenant_id UUID vs INT index size?\n\n## Common Mistakes\n• Missing tenant_id in index first column.\n• Global unique email without tenant scope wrong for B2B.\n• Cross-tenant scan on primary during billing job.\n\n## Senior Engineer Insights\nDraw tenant isolation as index design rule — security and performance aligned.\n\n## Key Commands\n• CREATE INDEX idx_tenant_status ON orders(tenant_id, status, updated_at);\n• EXPLAIN SELECT * FROM orders WHERE tenant_id=42 AND status='OPEN';\n• SELECT tenant_id, COUNT(*) FROM orders GROUP BY tenant_id ORDER BY 2 DESC LIMIT 10;\n\n## Best Practices\n• tenant_id leftmost always.\n• Unique constraints scoped to tenant.\n• Cross-tenant analytics off OLTP.\n• Monitor largest tenants for shard candidates.",
    command: "CREATE INDEX idx_tenant_status ON orders(tenant_id, status, updated_at);\nEXPLAIN SELECT * FROM orders WHERE tenant_id=42 AND status='OPEN';\nSELECT tenant_id, COUNT(*) FROM orders GROUP BY tenant_id ORDER BY 2 DESC LIMIT 10;",
    tags: ["mysql","Composite Indexes","hard","rare"],
  },
  {
    id: "mysql-compidx-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "DBA",
    question: "How do you add a composite index online on a billion-row table without breaking replication SLA?",
    answer: "## Interview Answer\nMySQL 8.0 INPLACE index build with LOCK=NONE allows concurrent DML — still IO heavy. Monitor replica lag during build; throttle via innodb_ddl_threads (8.0.27+) or run on replica first then switch. pt-osc rarely needed for index-only add; gh-ost for extreme caution environments.\n\n## Detailed Explanation\nALTER TABLE ADD INDEX ..., ALGORITHM=INPLACE, LOCK=NONE.\nProgress: performance_schema.events_stages_current for ALTER phase.\nReplica lag: single-threaded apply may lag on DDL event duration.\nSort buffer and disk temp during build — ensure free space 2× index size estimate.\nParallel DDL threads reduce duration but increase IO burst.\n\n## Production Perspective\nSchedule during low write window. Pre-notify replica lag alerts. Optional: build on fresh clone replica, swap via logical cutover extreme case. Document rollback DROP INDEX.\n\n## Common Follow-up Questions\n• ALGORITHM=COPY when forced?\n• DDL on primary vs replica first?\n• innodb_ddl_threads tuning?\n\n## Common Mistakes\n• LOCK=SHARED blocking writes unexpectedly on old version.\n• Disk full mid-build corrupting table.\n• No replica lag monitoring during DDL.\n\n## Senior Engineer Insights\nProvide duration estimate: row count × index width × IO speed — leadership expects timeline for index add on billion rows.\n\n## Key Commands\n• ALTER TABLE big_table ADD INDEX idx_composite (a,b,c), ALGORITHM=INPLACE, LOCK=NONE;\n• SELECT EVENT_NAME, WORK_COMPLETED, WORK_ESTIMATED\nFROM performance_schema.events_stages_current\nWHERE EVENT_NAME LIKE 'stage/innodb/alter%';\n• SHOW REPLICA STATUS\\G\n-- Seconds_Behind_Source during DDL\n\n## Best Practices\n• LOCK=NONE online add in 8.0.\n• Monitor replica lag continuously.\n• Ensure disk headroom before DDL.\n• Schedule IO-heavy DDL off-peak.",
    command: "ALTER TABLE big_table ADD INDEX idx_composite (a,b,c), ALGORITHM=INPLACE, LOCK=NONE;\nSELECT EVENT_NAME, WORK_COMPLETED, WORK_ESTIMATED\nFROM performance_schema.events_stages_current\nWHERE EVENT_NAME LIKE 'stage/innodb/alter%';\nSHOW REPLICA STATUS\\G\n-- Seconds_Behind_Source during DDL",
    tags: ["mysql","Composite Indexes","medium","rare"],
  },
  {
    id: "mysql-cover-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "What is a covering index and how does Using index in EXPLAIN indicate index-only access?",
    answer: "## Interview Answer\nCovering index includes all columns needed by query — SELECT, WHERE, ORDER BY — in index key. InnoDB secondary index leaves contain PK; including columns in index avoids lookup to clustered index. EXPLAIN Extra: Using index means index-only scan.\n\n## Detailed Explanation\nINCLUDE columns not in MySQL — add columns to index definition directly.\nSecondary index leaf: index cols + PK cols always present.\nSELECT id, status WHERE customer_id=1 covered by (customer_id, status, id).\nUsing index for SELECT * impossible unless all cols in index impractical.\nSmaller covering index — fewer columns — less write amplification.\n\n## Production Perspective\nHot list API: cover exactly projected columns not SELECT *. Verify rows_examined drop in EXPLAIN ANALYZE. Avoid duplicating wide VARCHAR in multiple covering indexes.\n\n## Common Follow-up Questions\n• Using index vs Using where?\n• PK columns in covering automatically?\n• Covering for COUNT(*)?\n\n## Common Mistakes\n• Covering index with 10 wide columns.\n• SELECT * expecting covering.\n• Duplicate index where covering supersedes.\n\n## Senior Engineer Insights\nBuffer pool efficiency: index-only scan reads narrower pages — less IO than random PK lookups scattered across table.\n\n## Key Commands\n• CREATE INDEX idx_cust_status_id ON orders(customer_id, status, id);\n• EXPLAIN SELECT id, status FROM orders WHERE customer_id=100;\n-- Using index\n• EXPLAIN ANALYZE SELECT id, status FROM orders WHERE customer_id=100;\n\n## Best Practices\n• Project only needed columns in API.\n• Design covering for top 5 queries.\n• Balance cover breadth vs write cost.\n• Verify Using index in EXPLAIN.",
    command: "CREATE INDEX idx_cust_status_id ON orders(customer_id, status, id);\nEXPLAIN SELECT id, status FROM orders WHERE customer_id=100;\n-- Using index\nEXPLAIN ANALYZE SELECT id, status FROM orders WHERE customer_id=100;",
    tags: ["mysql","Covering Indexes","easy","very-common"],
  },
  {
    id: "mysql-cover-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do you optimize COUNT queries and aggregations with covering indexes?",
    answer: "## Interview Answer\nCOUNT(*) with WHERE on indexed columns may use index scan smaller than table. COUNT(col) covering index on (filter_cols, col) avoids table access. For frequent COUNT on large table — summary table or counter cache maintained by trigger/application — index alone may not suffice.\n\n## Detailed Explanation\nEXPLAIN type=index scanning whole index still costly on wide index.\nCOUNT(DISTINCT col) benefits from index on col.\nPartial status counts: index (status, id) for COUNT WHERE status=X.\nApproximate counts: information_schema.table_rows rough for InnoDB.\nRollup tables refreshed by batch job for dashboard KPIs.\n\n## Production Perspective\nReal-time exact count on 500M rows — wrong architecture. Covering index for COUNT on subset with selective WHERE acceptable. Monitor if COUNT query still top digest after index — escalate to summary table.\n\n## Common Follow-up Questions\n• COUNT(*) vs COUNT(pk)?\n• Index-only COUNT on secondary?\n• When summary table required?\n\n## Common Mistakes\n• COUNT(*) on unfiltered huge table repeatedly.\n• Covering index for COUNT without selective WHERE.\n• Ignoring approximate counts UX option.\n\n## Senior Engineer Insights\nProduct decision: exact count pagination vs estimated — DBA proposes summary table when EXPLAIN shows index scan millions rows.\n\n## Key Commands\n• EXPLAIN SELECT COUNT(*) FROM orders WHERE status='OPEN';\n• CREATE INDEX idx_status_id ON orders(status, id);\n• SELECT status, COUNT(*) FROM orders GROUP BY status;  -- consider summary table\n\n## Best Practices\n• Covering index for selective COUNT.\n• Summary tables for global counts.\n• Cache counts in Redis for UI.\n• Avoid COUNT(*) pagination total on huge tables.",
    command: "EXPLAIN SELECT COUNT(*) FROM orders WHERE status='OPEN';\nCREATE INDEX idx_status_id ON orders(status, id);\nSELECT status, COUNT(*) FROM orders GROUP BY status;  -- consider summary table",
    tags: ["mysql","Covering Indexes","medium","common"],
  },
  {
    id: "mysql-cover-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "When does a query fail to use a covering index despite matching column list?",
    answer: "## Interview Answer\nOptimizer may choose PK scan if estimates favor it. VARCHAR prefix mismatch, hidden columns, or SELECT includes columns not in index. Random PK lookups after secondary index if not truly covering. UPDATE/DELETE rarely index-only. Collation mismatch prevents index use.\n\n## Detailed Explanation\nSELECT col not in index — back to PK lookup per row — Using index condition not Using index.\nImplicit conversion: WHERE varchar_idx = 123.\nOptimizer cost: full index scan vs range — may pick wrong.\nFORCE INDEX hints for validation not production permanent.\n8.0 multi-valued indexes different semantics.\n\n## Production Perspective\nEXPLAIN ANALYZE proves index-only vs not. Expand covering index only after confirming plan gap. Check if stats stale causing wrong choice — ANALYZE first.\n\n## Common Follow-up Questions\n• Using index vs Using index condition?\n• FORCE INDEX validation workflow?\n• Covering with JOIN two tables?\n\n## Common Mistakes\n• Adding columns to index without EXPLAIN verification.\n• FORCE INDEX in app permanently.\n• Missing PK in covering when secondary used.\n\n## Senior Engineer Insights\nSecondary index always includes PK at leaf — covering design must account for columns beyond PK explicitly in index definition.\n\n## Key Commands\n• EXPLAIN FORMAT=JSON SELECT a,b FROM t WHERE a=1;\n-- compare with SELECT a,b,c\n• ANALYZE TABLE t;\n• SELECT /*+ INDEX(t idx_cover) */ a,b FROM t WHERE a=1;\n\n## Best Practices\n• Verify Using index in EXPLAIN.\n• ANALYZE before index expansion.\n• Hints for diagnosis only.\n• Match types and collations in predicates.",
    command: "EXPLAIN FORMAT=JSON SELECT a,b FROM t WHERE a=1;\n-- compare with SELECT a,b,c\nANALYZE TABLE t;\nSELECT /*+ INDEX(t idx_cover) */ a,b FROM t WHERE a=1;",
    tags: ["mysql","Covering Indexes","medium","common"],
  },
  {
    id: "mysql-cover-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Database Engineer",
    question: "Design covering indexes for a high-QPS feed query without exploding write amplification.",
    answer: "## Interview Answer\nFeed query: WHERE user_id=? ORDER BY created_at DESC LIMIT 20 — covering index (user_id, created_at DESC, id, preview_cols limited). Avoid covering wide blob columns — fetch blobs by PK after feed IDs returned. Single composite serves filter+sort+cover for narrow columns only.\n\n## Detailed Explanation\nTwo-step pattern: index-only get ids+metadata, then PK lookup for blobs batch IN (...).\nDESC index (8.0): INDEX (user_id, created_at DESC) avoids filesort.\nWrite amp: every post INSERT updates feed index — monitor TPS ceiling.\nPartial feed archives: partition old data reduce index size.\nRead replica serves feed — primary write index unavoidable.\n\n## Production Perspective\nLoad test INSERT rate with new covering index before deploy. Cache feed in Redis for celebrities with fanout read problem — index alone insufficient. Denormalized fanout table pattern for Twitter-like feeds.\n\n## Common Follow-up Questions\n• Fanout on write vs read?\n• DESC index benefit?\n• Two-step blob fetch pattern?\n\n## Common Mistakes\n• Covering TEXT column in feed index.\n• Single global ORDER BY created_at index without user_id.\n• No load test on write path.\n\n## Senior Engineer Insights\nCelebrity post fanout — covering index on follower feed table per user_id is write-heavy — architectural pattern beats index tuning.\n\n## Key Commands\n• CREATE INDEX idx_feed ON feed_items(user_id, created_at DESC, item_id, title(100));\n• EXPLAIN ANALYZE SELECT item_id, title FROM feed_items WHERE user_id=1 ORDER BY created_at DESC LIMIT 20;\n• SELECT * FROM posts WHERE id IN (...);  -- second step for blobs\n\n## Best Practices\n• Narrow covering columns only.\n• DESC index for ORDER BY DESC.\n• Two-step fetch for wide rows.\n• Architecture review for fanout writes.",
    command: "CREATE INDEX idx_feed ON feed_items(user_id, created_at DESC, item_id, title(100));\nEXPLAIN ANALYZE SELECT item_id, title FROM feed_items WHERE user_id=1 ORDER BY created_at DESC LIMIT 20;\nSELECT * FROM posts WHERE id IN (...);  -- second step for blobs",
    tags: ["mysql","Covering Indexes","hard","rare"],
  },
  {
    id: "mysql-cover-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "DBA",
    question: "Compare covering index strategy in MySQL vs included columns in PostgreSQL.",
    answer: "## Interview Answer\nPostgreSQL INCLUDE adds non-key payload columns to index leaf without sort key participation. MySQL adds all columns to index key definition — functionally similar for read optimization but affects sort order and key length limits (3072 bytes InnoDB). MySQL has no separate INCLUDE syntax — design composite consciously.\n\n## Detailed Explanation\nMySQL key length limit: utf8mb4 char × 4 bytes counts toward limit.\nPrefix indexes in covering partial column storage.\nBoth achieve index-only scans when optimizer agrees.\nMySQL clustered PK in all secondary leaves — different from PG heap.\nMigration between DBs: rewrite covering definitions not copy verbatim.\n\n## Production Perspective\nWatch index key length on varchar(255) utf8mb4 composites — exceeds limit error on CREATE. Use prefix (title(100)) in covering carefully — may not cover full SELECT if need full title.\n\n## Common Follow-up Questions\n• Index key length limits?\n• Prefix in covering caveats?\n• PG INCLUDE migration to MySQL?\n\n## Common Mistakes\n• Copying PG INCLUDE list verbatim exceeding MySQL key limit.\n• Assuming identical optimizer covering decisions.\n• Full width varchar in composite hitting 3072 bytes.\n\n## Senior Engineer Insights\nCross-database interview question — demonstrate MySQL-specific key length and clustered index interaction.\n\n## Key Commands\n• SHOW VARIABLES LIKE 'innodb_%max%len%';\n• CREATE INDEX idx ON t (a, b, title(100));\n• EXPLAIN SELECT a, b, title FROM t WHERE a=1;\n\n## Best Practices\n• Calculate key byte length before CREATE.\n• Prefix columns knowingly in covering.\n• Test index-only EXPLAIN after create.\n• Document differences in migration guides.",
    command: "SHOW VARIABLES LIKE 'innodb_%max%len%';\nCREATE INDEX idx ON t (a, b, title(100));\nEXPLAIN SELECT a, b, title FROM t WHERE a=1;",
    tags: ["mysql","Covering Indexes","medium","rare"],
  },
  {
    id: "mysql-part-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Common",
    role: "Database Engineer",
    question: "What types of table partitioning does MySQL InnoDB support?",
    answer: "## Interview Answer\nInnoDB supports RANGE, LIST, HASH, and KEY partitioning. RANGE/LIST by column values — common for time-series monthly partitions. HASH/KEY for even spread. Partition pruning skips irrelevant partitions when WHERE matches partition key.\n\n## Detailed Explanation\nPARTITION BY RANGE (YEAR(created_at)) or RANGE COLUMNS (created_at).\nSubpartitioning: HASH under RANGE for large partitions.\nUnique keys must include partition key columns.\nMAXVALUE partition catch-all for RANGE.\nPartition-wise joins rare optimization — same partition key alignment needed.\n\n## Production Perspective\nMonthly RANGE on created_at for orders — DROP PARTITION for archival vs DELETE millions rows. Automate ADD PARTITION cron before month boundary. Query must include partition key in WHERE for prune — verify EXPLAIN partitions column.\n\n## Common Follow-up Questions\n• Partition key in unique index requirement?\n• RANGE vs LIST when?\n• How add new RANGE partition?\n\n## Common Mistakes\n• Queries without partition key — full scan all partitions.\n• Forgetting MAXVALUE partition overflow.\n• Unique on id alone without partition column — invalid.\n\n## Senior Engineer Insights\nPartition for manageability (drop old data) often beats query perf if prune fails — set expectations with app team on required filters.\n\n## Key Commands\n• CREATE TABLE orders (\n  id BIGINT, created_at DATE, ...\n) PARTITION BY RANGE COLUMNS(created_at) (\n  PARTITION p2024_01 VALUES LESS THAN ('2024-02-01'),\n  PARTITION pmax VALUES LESS THAN (MAXVALUE)\n);\n• EXPLAIN SELECT * FROM orders WHERE created_at='2024-06-15';\n• ALTER TABLE orders ADD PARTITION (PARTITION p2024_07 VALUES LESS THAN ('2024-08-01'));\n\n## Best Practices\n• Automate partition maintenance.\n• Always filter partition key in queries.\n• Plan MAXVALUE monitoring alerts.\n• DROP PARTITION for retention not DELETE.",
    command: "CREATE TABLE orders (\n  id BIGINT, created_at DATE, ...\n) PARTITION BY RANGE COLUMNS(created_at) (\n  PARTITION p2024_01 VALUES LESS THAN ('2024-02-01'),\n  PARTITION pmax VALUES LESS THAN (MAXVALUE)\n);\nEXPLAIN SELECT * FROM orders WHERE created_at='2024-06-15';\nALTER TABLE orders ADD PARTITION (PARTITION p2024_07 VALUES LESS THAN ('2024-08-01'));",
    tags: ["mysql","Partitioning","easy","common"],
  },
  {
    id: "mysql-part-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How does partition pruning work and how do you verify it in EXPLAIN?",
    answer: "## Interview Answer\nOptimizer eliminates partitions not matching WHERE on partition key — EXPLAIN partitions column lists p2024_06 only not all. Requires deterministic partition key expression match — functions on wrong column or OR across partitions may prune poorly.\n\n## Detailed Explanation\nEXPLAIN FORMAT=JSON: partitions_scanned vs total.\nRANGE COLUMNS allows multi-column range prune.\nCONSTANT fold: WHERE created_at BETWEEN x AND y in single partition.\nPartition selection wrong if timezone mismatch on DATE column.\nSubquery may prevent prune — rewrite to direct filter.\n\n## Production Perspective\nMonthly report without date filter scans all partitions — reject in query review. Staging EXPLAIN mandatory for new reporting queries. Alert on queries with partitions=ALL in audit log if implemented.\n\n## Common Follow-up Questions\n• Prune with OR on two months?\n• TO_DAYS vs RANGE COLUMNS?\n• Subpartition pruning?\n\n## Common Mistakes\n• YEAR(created_at)=2024 without matching partition expr.\n• Timezone shift pushing rows wrong partition.\n• Assuming partition replaces index — need both.\n\n## Senior Engineer Insights\nShow EXPLAIN partitions: p2024_01,p2024_02 vs p0-p11 all — visual proof of prune success.\n\n## Key Commands\n• EXPLAIN SELECT * FROM orders WHERE created_at >= '2024-06-01' AND created_at < '2024-07-01';\n-- partitions: p2024_06\n• EXPLAIN FORMAT=JSON SELECT * FROM orders WHERE id=100;  -- may not prune if id not partition key\n• SELECT PARTITION_NAME, TABLE_ROWS FROM information_schema.PARTITIONS WHERE TABLE_NAME='orders';\n\n## Best Practices\n• Verify partitions column in EXPLAIN.\n• Align app filters with partition key.\n• Use RANGE COLUMNS for dates.\n• Reject cross-partition scans in review.",
    command: "EXPLAIN SELECT * FROM orders WHERE created_at >= '2024-06-01' AND created_at < '2024-07-01';\n-- partitions: p2024_06\nEXPLAIN FORMAT=JSON SELECT * FROM orders WHERE id=100;  -- may not prune if id not partition key\nSELECT PARTITION_NAME, TABLE_ROWS FROM information_schema.PARTITIONS WHERE TABLE_NAME='orders';",
    tags: ["mysql","Partitioning","medium","common"],
  },
  {
    id: "mysql-part-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "Production Support",
    question: "What are operational challenges of managing partitioned tables in production?",
    answer: "## Interview Answer\nOperational wins: instant DROP PARTITION archival, smaller maintenance scope. Challenges: partition maintenance cron failures, MAXVALUE overflow, DDL slower on many partitions, backup/restore per partition complexity, foreign keys not supported with partitioning in MySQL.\n\n## Detailed Explanation\nADD PARTITION must be before MAXVALUE fill — automate ahead of calendar.\nREORGANIZE PARTITION splits or merges — locks involved read docs.\nToo many partitions (thousands) slows optimizer and DDL.\nFK workaround: no FK on partitioned table — enforce in app.\nReplication: DDL on partition metadata replicated — lag on REORGANIZE.\n\n## Production Perspective\nRunbook: monthly ADD PARTITION ticket automated. Monitor pmax row growth — alert if approaching capacity. Test RESTORE single partition restore procedure if using partial backup tools.\n\n## Common Follow-up Questions\n• FK alternatives with partitions?\n• How many partitions too many?\n• REORGANIZE vs ADD?\n\n## Common Mistakes\n• Manual partition add forgotten — inserts fail into pmax only.\n• Thousands of daily partitions.\n• Expecting FK on partitioned parent table.\n\n## Senior Engineer Insights\nPartition ops maturity: automation + alerts on pmax + EXPLAIN audit — three pillars.\n\n## Key Commands\n• ALTER TABLE orders REORGANIZE PARTITION pmax INTO (\n  PARTITION p2024_08 VALUES LESS THAN ('2024-09-01'),\n  PARTITION pmax VALUES LESS THAN (MAXVALUE)\n);\n• SELECT PARTITION_NAME, TABLE_ROWS FROM information_schema.PARTITIONS WHERE TABLE_NAME='orders' ORDER BY 1;\n\n## Best Practices\n• Automate ADD PARTITION.\n• Alert on pmax growth rate.\n• Limit partition count design.\n• Document no-FK constraint with app validation.",
    command: "ALTER TABLE orders REORGANIZE PARTITION pmax INTO (\n  PARTITION p2024_08 VALUES LESS THAN ('2024-09-01'),\n  PARTITION pmax VALUES LESS THAN (MAXVALUE)\n);\nSELECT PARTITION_NAME, TABLE_ROWS FROM information_schema.PARTITIONS WHERE TABLE_NAME='orders' ORDER BY 1;",
    tags: ["mysql","Partitioning","medium","rare"],
  },
  {
    id: "mysql-part-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Database Engineer",
    question: "When should you partition vs shard a large MySQL table?",
    answer: "## Interview Answer\nPartition: single instance manageability, prune, archival — limited by single-node IO/CPU ceiling. Shard: horizontal split across instances when single server maxed — Vitess, app-level shard key. Partition first for time-series retention; shard when connection count, write TPS, or storage exceeds one node.\n\n## Detailed Explanation\nPartition does not distribute load across servers — all on one mysqld.\nShard key irreversible without migration — choose carefully.\nCross-shard queries expensive — design bounded context per shard.\nVitess provides sharding with minimal app change for MySQL.\nCombined: sharded cluster each shard partitioned by time locally.\n\n## Production Perspective\nDecision metrics: CPU >70% sustained, disk >80%, write latency SLO miss after index/buffer tune — evaluate shard. Partition alone if problem is 500GB archival not 50K TPS.\n\n## Common Follow-up Questions\n• Vitess vs manual sharding?\n• Shard key selection?\n• Re-shard migration strategy?\n\n## Common Mistakes\n• Partition expecting 10× write scale-out.\n• Shard too early increasing ops burden.\n• Cross-shard JOIN in OLTP app.\n\n## Senior Engineer Insights\nCost curve: partition cheap ops; shard step-function ops complexity — justify with metrics not anticipation.\n\n## Key Commands\n• SELECT SUM(TABLE_ROWS), SUM(DATA_LENGTH)/1024/1024/1024 AS gb\nFROM information_schema.PARTITIONS WHERE TABLE_NAME='events';\n• SHOW GLOBAL STATUS LIKE 'Questions';\nSHOW GLOBAL STATUS LIKE 'Innodb_data_written';\n\n## Best Practices\n• Partition for retention first.\n• Shard on measured capacity limits.\n• Avoid cross-shard queries in OLTP.\n• Plan Vitess before manual shard sprawl.",
    command: "SELECT SUM(TABLE_ROWS), SUM(DATA_LENGTH)/1024/1024/1024 AS gb\nFROM information_schema.PARTITIONS WHERE TABLE_NAME='events';\nSHOW GLOBAL STATUS LIKE 'Questions';\nSHOW GLOBAL STATUS LIKE 'Innodb_data_written';",
    tags: ["mysql","Partitioning","hard","rare"],
  },
  {
    id: "mysql-part-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "DBA",
    question: "Lead migration from non-partitioned 2TB table to monthly RANGE partitions with minimal downtime.",
    answer: "## Interview Answer\nApproach: pt-osc or gh-ost copy to new partitioned table, cutover swap, or ALTER with native partition rebuild if downtime window allows. Validate prune on staging, automate ADD PARTITION, backfill historical partitions in REORGANIZE steps. Replication lag monitoring throughout.\n\n## Detailed Explanation\nNative ALTER PARTITION on huge table — long MDL lock unacceptable — use online copy tools.\nSteps: CREATE new partitioned empty schema → copy data by month batches → verify counts → RENAME swap.\nTriggers in pt-osc capture delta during copy.\nUnique PK must be (id, created_at) if partition on created_at.\nPost-cutover: EXPLAIN audit all queries for prune.\n\n## Production Perspective\nCommunicate read-only window if swap requires seconds of quiesce. Rollback: keep old table renamed _old 7 days. checksum tables before drop old.\n\n## Common Follow-up Questions\n• Change PK for partition requirement?\n• pt-osc on partitioned target?\n• Validate row counts per partition?\n\n## Common Mistakes\n• PK without partition column — migration blocked.\n• Cutover without checksum.\n• No query audit post migration — full scans hidden.\n\n## Senior Engineer Insights\nExecutive summary: 2TB DELETE archival becomes O(1) DROP PARTITION — business value of migration beyond query perf.\n\n## Key Commands\n• pt-online-schema-change --alter 'PARTITION BY RANGE COLUMNS(created_at) (...)' D=app,t=events --execute\n• SELECT PARTITION_NAME, COUNT(*) FROM events PARTITION (p2024_06);  -- verify\n• CHECKSUM TABLE events;\n\n## Best Practices\n• Online copy tool for large tables.\n• Checksum before cutover.\n• PK includes partition key.\n• Post-migration EXPLAIN audit.",
    command: "pt-online-schema-change --alter 'PARTITION BY RANGE COLUMNS(created_at) (...)' D=app,t=events --execute\nSELECT PARTITION_NAME, COUNT(*) FROM events PARTITION (p2024_06);  -- verify\nCHECKSUM TABLE events;",
    tags: ["mysql","Partitioning","hard","rare"],
  }
];
