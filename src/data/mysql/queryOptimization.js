// MySQL — queryOptimization
// Structured interview content — run: node scripts/generate-db-tracks.mjs
// 16 questions

export const questions = [
  {
    id: "mysql-qo-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "What is the MySQL query optimizer and what inputs does it use to choose a plan?",
    answer: "## Interview Answer\nThe cost-based optimizer picks join order, access methods (const, ref, range, ALL), and index usage using table/index statistics, index cardinality, and optimizer hints. Understanding its inputs explains most bad-plan incidents after data growth or stats drift.\n\n## Detailed Explanation\nStatistics: innodb_stats_persistent, ANALYZE TABLE refreshes.\nHistograms (8.0): bucket distribution for non-indexed predicates.\nCost model: server_cost, engine_cost tables tunable by experts.\nHints: INDEX, JOIN_ORDER, SET_VAR optimizer_switch.\nOptimizer trace (optimizer_trace) for deep dives — dev only.\n\n## Production Perspective\nSchedule ANALYZE after large bulk loads. Monitor queries switching from index to full scan when stats stale. Avoid hint sprawl — fix schema/stats first. Compare plans across minor version upgrades in staging.\n\n## Common Follow-up Questions\n• What is optimizer_switch?\n• How do histograms help?\n• When does optimizer pick filesort?\n\n## Common Mistakes\n• Adding hints instead of fixing missing index.\n• Never running ANALYZE on rapidly changing tables.\n• Assuming EXPLAIN rows equals actual without ANALYZE.\n\n## Senior Engineer Insights\nTrace a plan regression: deploy → stats stale → wrong join order → ANALYZE fixes — classic production story.\n\n## Key Commands\n• ANALYZE TABLE orders, order_items;\n• SELECT * FROM mysql.innodb_index_stats WHERE table_name='orders' LIMIT 10;\n• SET optimizer_trace='enabled=on'; SELECT ...; SELECT * FROM information_schema.optimizer_trace\\G\n\n## Best Practices\n• Automate ANALYZE on partition roll-offs.\n• Baseline EXPLAIN for critical queries in CI.\n• Use histograms for skewed columns.\n• Review optimizer trace in staging only.",
    command: "ANALYZE TABLE orders, order_items;\nSELECT * FROM mysql.innodb_index_stats WHERE table_name='orders' LIMIT 10;\nSET optimizer_trace='enabled=on'; SELECT ...; SELECT * FROM information_schema.optimizer_trace\\G",
    tags: ["mysql","Query Optimization","easy","very-common"],
  },
  {
    id: "mysql-qo-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "Production Support",
    question: "How do you identify and fix queries with high rows examined vs rows sent?",
    answer: "## Interview Answer\nLarge gap between rows examined and returned indicates inefficient filtering — missing index, wrong index, or function on indexed column. Use performance_schema summaries, slow log, and EXPLAIN FORMAT=JSON to find access type ALL or range with huge rows_examined.\n\n## Detailed Explanation\nRows_examined_per_scan in JSON EXPLAIN pinpoints index inefficiency.\nImplicit conversion: WHERE varchar_col = 123 prevents index use.\nOR conditions may merge multiple range scans or fail index merge limits.\nSubquery vs JOIN rewrite — optimizer may materialize derived table.\nCovering index eliminates table lookups reducing examined rows.\n\n## Production Perspective\nAlert on digest with rows_examined > 1M and latency SLO breach. pt-query-digest ranks by total rows examined. After index add, verify with EXPLAIN ANALYZE (8.0.18+) actual timings.\n\n## Common Follow-up Questions\n• What is index merge?\n• Impact of OR on index usage?\n• When rewrite subquery to JOIN?\n\n## Common Mistakes\n• Adding redundant single-column indexes without composite analysis.\n• SELECT * preventing covering index benefits.\n• Ignoring collation mismatch in JOIN keys.\n\n## Senior Engineer Insights\nQuantify savings: rows examined drop from 10M to 500 — tie to buffer pool IO and replica lag reduction.\n\n## Key Commands\n• SELECT SCHEMA_NAME, DIGEST_TEXT, COUNT_STAR, SUM_ROWS_EXAMINED, SUM_ROWS_SENT\nFROM performance_schema.events_statements_summary_by_digest\nORDER BY SUM_ROWS_EXAMINED DESC LIMIT 10;\n• EXPLAIN FORMAT=JSON SELECT * FROM orders WHERE YEAR(created_at)=2024;\n• CREATE INDEX idx_created ON orders(created_at);\n\n## Best Practices\n• Index columns in WHERE/JOIN unchanged.\n• Avoid functions on indexed columns in predicates.\n• Use composite indexes matching filter order.\n• Validate with EXPLAIN ANALYZE post-change.",
    command: "SELECT SCHEMA_NAME, DIGEST_TEXT, COUNT_STAR, SUM_ROWS_EXAMINED, SUM_ROWS_SENT\nFROM performance_schema.events_statements_summary_by_digest\nORDER BY SUM_ROWS_EXAMINED DESC LIMIT 10;\nEXPLAIN FORMAT=JSON SELECT * FROM orders WHERE YEAR(created_at)=2024;\nCREATE INDEX idx_created ON orders(created_at);",
    tags: ["mysql","Query Optimization","medium","very-common"],
  },
  {
    id: "mysql-qo-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do join order and join buffer settings affect query performance?",
    answer: "## Interview Answer\nMySQL optimizer orders joins to minimize intermediate row counts. Nested loop joins dominate; block nested loop uses join_buffer_size for index-less joins. Wrong order or missing index on join column causes explosive nested loop iterations.\n\n## Detailed Explanation\nEXPLAIN type=ALL on small table joined to large without index — disaster.\njoin_buffer_size helps Block Nested Loop / BNL — not substitute for index.hash join (8.0.18+) for equi-joins without useful index — memory bounded.\nOptimizer hints: JOIN_ORDER, BNL/BKA disable via optimizer_switch.\nSTRAIGHT_JOIN forces order — last resort.\n\n## Production Perspective\nIncrease join_buffer_size modestly if EXPLAIN shows Using join buffer; fix index first. Memory per connection × max_connections if global join_buffer bump. 8.0 hash join visible in EXPLAIN as Hash join.\n\n## Common Follow-up Questions\n• When hash join chosen over nested loop?\n• What is Batched Key Access?\n• Multi-column join cardinality estimation?\n\n## Common Mistakes\n• Global join_buffer_size=128M with 2000 connections — OOM risk.\n• STRAIGHT_JOIN everywhere freezing bad order permanently.\n• Ignoring join on different charsets.\n\n## Senior Engineer Insights\nDraw nested loop: outer 1000 rows × inner full scan 10M = 10B examinations — index on inner key is the fix, not buffer size.\n\n## Key Commands\n• EXPLAIN FORMAT=JSON\nSELECT * FROM customers c JOIN orders o ON c.id=o.customer_id WHERE c.region='EU';\n• SHOW VARIABLES LIKE 'join_buffer_size';\nSHOW VARIABLES LIKE 'optimizer_switch';\n• SET optimizer_switch='hash_join=on';\n\n## Best Practices\n• Index all join keys.\n• Fix join order via stats not STRAIGHT_JOIN.\n• Monitor memory per connection settings.\n• Upgrade 8.0 for hash join benefits on analytics.",
    command: "EXPLAIN FORMAT=JSON\nSELECT * FROM customers c JOIN orders o ON c.id=o.customer_id WHERE c.region='EU';\nSHOW VARIABLES LIKE 'join_buffer_size';\nSHOW VARIABLES LIKE 'optimizer_switch';\nSET optimizer_switch='hash_join=on';",
    tags: ["mysql","Query Optimization","medium","common"],
  },
  {
    id: "mysql-qo-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "Database Engineer",
    question: "How do you optimize pagination queries on large InnoDB tables?",
    answer: "## Interview Answer\nOFFSET/LIMIT deep pagination (OFFSET 1000000) scans and discards rows — O(n) cost. Fix with keyset pagination (WHERE id > last_seen ORDER BY id LIMIT 50), covering index on (sort_key, id), or deferred join pattern. Search-after cursor APIs for infinite scroll.\n\n## Detailed Explanation\nSELECT * FROM t ORDER BY id LIMIT 1000000, 20 — examines 1M+ rows.\nKeyset: WHERE (created_at, id) > (?, ?) ORDER BY created_at, id LIMIT 20.\nCovering index on filter+sort columns avoids back-and-forth PK lookups.\nCOUNT(*) for total pages expensive — approximate or cache totals.\nReverse ORDER BY id DESC pagination same pattern with < comparator.\n\n## Production Perspective\nAPI contract change requires app team — document cursor tokens. Monitor slow log for OFFSET patterns via pt-query-digest fingerprint. For admin UI export, use batch keyset not page numbers.\n\n## Common Follow-up Questions\n• Can deferred join help OFFSET?\n• Pagination on UUID PK?\n• How handle duplicate sort keys?\n\n## Common Mistakes\n• Increasing LIMIT to reduce page turns — worse.\n• OFFSET pagination on replica during batch job.\n• Missing composite index on (sort_col, id).\n\n## Senior Engineer Insights\nShow latency graph: OFFSET pagination linear growth vs flat keyset — compelling prod metric.\n\n## Key Commands\n• EXPLAIN SELECT * FROM orders ORDER BY id LIMIT 1000000, 20;\n• SELECT * FROM orders WHERE id > 1000000 ORDER BY id LIMIT 20;\n• CREATE INDEX idx_created_id ON orders(created_at, id);\n\n## Best Practices\n• Keyset pagination in public APIs.\n• Ban deep OFFSET in coding standards.\n• Covering index for list endpoints.\n• Cache approximate counts separately.",
    command: "EXPLAIN SELECT * FROM orders ORDER BY id LIMIT 1000000, 20;\nSELECT * FROM orders WHERE id > 1000000 ORDER BY id LIMIT 20;\nCREATE INDEX idx_created_id ON orders(created_at, id);",
    tags: ["mysql","Query Optimization","hard","common"],
  },
  {
    id: "mysql-qo-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Cloud Engineer",
    question: "What role do optimizer hints and query rewrite play when statistics are accurate but plan is still wrong?",
    answer: "## Interview Answer\nRare edge cases: optimizer bug, correlation not captured, derived table materialization choice. Use INDEX/NO_INDEX, JOIN_FIXED_ORDER hints temporarily; report bug with optimizer trace. Prefer schema fix, extended stats, or query rewrite over permanent hints.\n\n## Detailed Explanation\nOptimizer hints syntax /*+ INDEX(o idx) */ in 8.0 comment form.\nSET_VAR(max_execution_time) caps runaway queries.\nSubquery merge vs materialization — optimizer_switch derived_merge.\nCommon table expressions (8.0) merge behavior affects plans.\nVersion-specific regressions — check release notes before hint lock-in.\n\n## Production Perspective\nDocument every production hint with ticket and review date. Hints may break after upgrade — CI EXPLAIN snapshot tests. Contact Oracle support with optimizer_trace for confirmed bugs.\n\n## Common Follow-up Questions\n• Difference hint vs STRAIGHT_JOIN?\n• How extended statistics help correlated columns?\n• Query rewrite using UNION ALL?\n\n## Common Mistakes\n• Permanent hints without review calendar.\n• max_execution_time killing legit batch without app handling.\n• Disabling semijoin optimization globally.\n\n## Senior Engineer Insights\nSenior answer: hints are tech debt with expiry date — pair with upstream fix milestone.\n\n## Key Commands\n• SELECT /*+ INDEX(orders idx_status) */ * FROM orders WHERE status='PENDING';\n• INSERT INTO mysql.table_stats ...  -- extended stats example\n• SET SESSION optimizer_switch='derived_merge=off';  -- test only\n\n## Best Practices\n• Hints with expiration review.\n• Capture optimizer trace for support cases.\n• Test hints on minor upgrades.\n• Prefer rewrite over hint when possible.",
    command: "SELECT /*+ INDEX(orders idx_status) */ * FROM orders WHERE status='PENDING';\nINSERT INTO mysql.table_stats ...  -- extended stats example\nSET SESSION optimizer_switch='derived_merge=off';  -- test only",
    tags: ["mysql","Query Optimization","medium","common"],
  },
  {
    id: "mysql-explain-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Database Engineer",
    question: "How do you read a basic EXPLAIN output and what do key columns mean?",
    answer: "## Interview Answer\nEXPLAIN shows plan without executing (traditional). Key columns: type (access method best→worst: const, eq_ref, ref, range, index, ALL), key (index used), rows (estimate), Extra (Using index, Using filesort, Using temporary). ALL on large table is red flag.\n\n## Detailed Explanation\npossible_keys vs key — optimizer considered vs chose.\nkey_len — bytes of index prefix used — composite index partial usage.\nfiltered (5.7+) percentage of rows filtered by table condition.\nEXPLAIN FOR CONNECTION id shows running query plan.\nJSON format richer — query_block, nested loops structure.\n\n## Production Perspective\nStandardize on EXPLAIN FORMAT=JSON in runbooks. Store baseline plans for tier-1 queries. After migration, diff EXPLAIN not just latency — plan may regress silently on replica first.\n\n## Common Follow-up Questions\n• What is type=index vs ALL?\n• Explain key_len interpretation.\n• EXPLAIN vs EXPLAIN ANALYZE?\n\n## Common Mistakes\n• Trusting rows estimate without ANALYZE.\n• Ignoring Using temporary for GROUP BY.\n• Only EXPLAIN SELECT — forget UPDATE/DELETE plans.\n\n## Senior Engineer Insights\nWalk through one plan line-by-line in interview — examiners reward systematic reading not buzzwords.\n\n## Key Commands\n• EXPLAIN SELECT * FROM orders WHERE customer_id=100 AND status='OPEN';\n• EXPLAIN FORMAT=JSON SELECT * FROM orders WHERE customer_id=100\\G\n• EXPLAIN FOR CONNECTION 12345;\n\n## Best Practices\n• JSON EXPLAIN in documentation.\n• ANALYZE before trusting plan.\n• EXPLAIN ANALYZE for validation.\n• Monitor plan changes via digest.",
    command: "EXPLAIN SELECT * FROM orders WHERE customer_id=100 AND status='OPEN';\nEXPLAIN FORMAT=JSON SELECT * FROM orders WHERE customer_id=100\\G\nEXPLAIN FOR CONNECTION 12345;",
    tags: ["mysql","EXPLAIN Plan","easy","very-common"],
  },
  {
    id: "mysql-explain-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "DBA",
    question: "How do you use EXPLAIN FORMAT=JSON and EXPLAIN ANALYZE in MySQL 8.0?",
    answer: "## Interview Answer\nFORMAT=JSON exposes cost, rows_examined_per_scan, used_columns, attached conditions — machine-parseable. EXPLAIN ANALYZE (8.0.18+) executes query and shows actual time per iterator — bridges MySQL gap to PostgreSQL EXPLAIN ANALYZE workflow.\n\n## Detailed Explanation\nJSON: query_block.ordering, nested_loop, table.access_type.\nANALYZE: actual rows, loops, timing ms — compare estimated vs actual.\nEXPLAIN ANALYZE inserts rows into table — side effect awareness on prod.\nTree format EXPLAIN FORMAT=TREE human-readable hierarchy.\nVisualize with explain.depesz.com equivalent tools or PMM.\n\n## Production Perspective\nRun ANALYZE on representative prod clone not live peak — or use MAX_EXECUTION_TIME guard. Automate JSON plan diff in CI against staging data snapshot. PMM Query Analytics links digest to EXPLAIN.\n\n## Common Follow-up Questions\n• Does EXPLAIN ANALYZE lock tables?\n• How parse JSON in scripts?\n• Tree vs JSON format?\n\n## Common Mistakes\n• EXPLAIN ANALYZE on DELETE without WHERE audit in prod.\n• Ignoring actual_time >> estimated in ANALYZE output.\n• JSON plan too large — truncate nested blocks manually.\n\n## Senior Engineer Insights\nPresent before/after ANALYZE timing for index migration — actual ms proof beats rows estimate theory.\n\n## Key Commands\n• EXPLAIN ANALYZE SELECT o.* FROM orders o JOIN customers c ON c.id=o.customer_id WHERE c.region='US';\n• EXPLAIN FORMAT=JSON SELECT * FROM orders WHERE status='SHIPPED'\\G\n• EXPLAIN FORMAT=TREE SELECT * FROM orders WHERE id=1;\n\n## Best Practices\n• EXPLAIN ANALYZE on staging clone.\n• JSON plans in version control.\n• Compare estimated vs actual rows.\n• Tree format for incident war rooms.",
    command: "EXPLAIN ANALYZE SELECT o.* FROM orders o JOIN customers c ON c.id=o.customer_id WHERE c.region='US';\nEXPLAIN FORMAT=JSON SELECT * FROM orders WHERE status='SHIPPED'\\G\nEXPLAIN FORMAT=TREE SELECT * FROM orders WHERE id=1;",
    tags: ["mysql","EXPLAIN Plan","medium","very-common"],
  },
  {
    id: "mysql-explain-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Production Support",
    question: "What do Using filesort and Using temporary in EXPLAIN Extra mean and how do you eliminate them?",
    answer: "## Interview Answer\nUsing filesort: extra sort pass not satisfied by index order — costs CPU/memory, may use sort_buffer_size disk spill. Using temporary: internal temp table for GROUP BY/DISTINCT/UNION — memory or disk temp table. Fix with composite index matching WHERE+ORDER BY or rewrite query.\n\n## Detailed Explanation\nfilesort: ORDER BY columns not prefix of index after WHERE equality filters.\ntemporary: GROUP BY on non-indexed columns, DISTINCT wide rows.\nsort_buffer_size, tmp_table_size, max_heap_table_size bound memory before disk.\n8.0 hash join and improved sort algorithms reduce but not eliminate cost.\nEXPLAIN ANALYZE shows sort costs explicitly.\n\n## Production Perspective\nSpike in Created_tmp_disk_tables status — investigate digests. Add index (status, created_at) if query filters status ORDER BY created_at. Consider summary table for heavy GROUP BY dashboards.\n\n## Common Follow-up Questions\n• In-memory vs on-disk temp table?\n• Can covering index remove filesort?\n• Impact of ONLY_FULL_GROUP_BY?\n\n## Common Mistakes\n• Raising sort_buffer_size globally without limit.\n• Removing ORDER BY from app without index fix — wrong UX.\n• GROUP BY alias without functional dependency awareness.\n\n## Senior Engineer Insights\nfilesort on 10M row sort_buffer spill correlates with replica IO wait — tie EXPLAIN Extra to infrastructure metrics.\n\n## Key Commands\n• SHOW GLOBAL STATUS LIKE 'Created_tmp%';\nSHOW GLOBAL STATUS LIKE 'Sort_%';\n• EXPLAIN SELECT status, COUNT(*) FROM orders GROUP BY status;\n• CREATE INDEX idx_status_created ON orders(status, created_at);\n\n## Best Practices\n• Match index to WHERE+ORDER BY.\n• Monitor Created_tmp_disk_tables.\n• Preaggregate for BI queries.\n• Keep sort_buffer per-session not global max.",
    command: "SHOW GLOBAL STATUS LIKE 'Created_tmp%';\nSHOW GLOBAL STATUS LIKE 'Sort_%';\nEXPLAIN SELECT status, COUNT(*) FROM orders GROUP BY status;\nCREATE INDEX idx_status_created ON orders(status, created_at);",
    tags: ["mysql","EXPLAIN Plan","medium","common"],
  },
  {
    id: "mysql-explain-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "Database Engineer",
    question: "How do you diagnose index merge, index condition pushdown, and MRR in EXPLAIN plans?",
    answer: "## Interview Answer\nIndex merge combines multiple range scans (index_merge=on). ICP pushes filter conditions to storage engine reducing row lookups. MRR (Multi-Range Read) sorts PK lookups for sequential IO. JSON EXPLAIN shows using_index_condition, using_mrr; tune or disable via optimizer_switch when buggy.\n\n## Detailed Explanation\nindex_merge_union: OR on different indexes.\nindex_merge_intersect: AND on multiple indexes — rare beneficial.\nICP: Extra Using index condition — predicate in index but not in key.\nMRR: read_rnd_buffer_size affects batch size.\nSometimes merge slower than single composite index — compare ANALYZE timing.\n\n## Production Perspective\nIf merge on high-QPS query — benchmark composite index alternative. optimizer_switch='index_merge=off' A/B test in session. Version bugs documented — verify on current minor release.\n\n## Common Follow-up Questions\n• When disable index merge?\n• ICP vs covering index?\n• MRR benefit on SSD?\n\n## Common Mistakes\n• Assuming index_merge always optimal.\n• Ignoring composite index because merge \"works\".\n• read_rnd_buffer_size extreme tuning without measurement.\n\n## Senior Engineer Insights\nComposite index often obsoletes merge — explain cost of maintaining multiple indexes vs one purpose-built composite.\n\n## Key Commands\n• EXPLAIN FORMAT=JSON SELECT * FROM t WHERE a=1 OR b=2\\G\n-- look for index_merge\n• SHOW VARIABLES LIKE 'optimizer_switch';\n• SET optimizer_switch='mrr=on,mrr_cost_based=on';\n\n## Best Practices\n• Prefer composite over merge when possible.\n• Validate merge with EXPLAIN ANALYZE.\n• Document optimizer_switch exceptions.\n• Review merge on OR-heavy legacy queries.",
    command: "EXPLAIN FORMAT=JSON SELECT * FROM t WHERE a=1 OR b=2\\G\n-- look for index_merge\nSHOW VARIABLES LIKE 'optimizer_switch';\nSET optimizer_switch='mrr=on,mrr_cost_based=on';",
    tags: ["mysql","EXPLAIN Plan","hard","common"],
  },
  {
    id: "mysql-explain-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "DBA",
    question: "How do you use EXPLAIN FOR CONNECTION during a live production incident?",
    answer: "## Interview Answer\nEXPLAIN FOR CONNECTION processlist_id shows optimizer plan for currently running statement without re-executing — critical when query is slow but still running. Requires PROCESS privilege. Pair with SHOW PROCESSLIST and performance_schema threads for context.\n\n## Detailed Explanation\nId from SHOW FULL PROCESSLIST or performance_schema.threads.PROCESSLIST_ID.\nShows plan for active statement including locks held.\nDoes not work if connection idle — no current statement.\nFor completed queries use performance_schema.events_statements_history.\nReplica applier threads have connection ids too — debug lag apply.\n\n## Production Perspective\nWar room step: identify long runner → EXPLAIN FOR CONNECTION → missing index vs lock wait. Kill only after confirming not DDL migration. Capture plan to ticket before kill for postmortem.\n\n## Common Follow-up Questions\n• Difference from slow log?\n• How get plan for finished query?\n• Security of EXPLAIN FOR CONNECTION?\n\n## Common Mistakes\n• Killing connection before capturing EXPLAIN.\n• Wrong processlist id after connection pool reuse confusion.\n• EXPLAIN FOR CONNECTION on sleeping connection — empty.\n\n## Senior Engineer Insights\nContrast with PostgreSQL pg_stat_activity query plan — MySQL requires this separate command mid-flight.\n\n## Key Commands\n• SHOW FULL PROCESSLIST;\n• EXPLAIN FOR CONNECTION 4521\\G\n• SELECT THREAD_ID, PROCESSLIST_ID, SQL_TEXT FROM performance_schema.threads WHERE PROCESSLIST_TIME > 60;\n\n## Best Practices\n• Capture plan before kill in runbook.\n• Grant PROCESS to on-call role only.\n• Archive plans in incident tickets.\n• Use history tables for post-hoc plan analysis.",
    command: "SHOW FULL PROCESSLIST;\nEXPLAIN FOR CONNECTION 4521\\G\nSELECT THREAD_ID, PROCESSLIST_ID, SQL_TEXT FROM performance_schema.threads WHERE PROCESSLIST_TIME > 60;",
    tags: ["mysql","EXPLAIN Plan","medium","common"],
  },
  {
    id: "mysql-explain-006",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Rare",
    role: "Database Engineer",
    question: "Lead a plan regression investigation after a MySQL minor upgrade — what steps do you take?",
    answer: "## Interview Answer\nCompare EXPLAIN JSON before/after on affected digests; check optimizer_switch defaults, cost model changes, and histogram invalidation. Review release notes; test rollback in staging; fix via stats refresh, hint retirement, or index adjustment; engage vendor if bug confirmed.\n\n## Detailed Explanation\nCommon causes: derived merge default change, hash join eligibility, index merge heuristics.\nGather: optimizer_trace from staging, table sizes, index definitions unchanged.\nMitigate: pin old optimizer behavior via optimizer_switch session while preparing fix.\nLong-term: EXPLAIN snapshot tests in CI per release candidate.\nCommunicate to app teams if query rewrite required.\n\n## Production Perspective\nCanary upgrade one replica — compare PMM Query Analytics plans. Rollback primary if P95 latency breach. Document in ADR: upgrade blocked until plan parity.\n\n## Common Follow-up Questions\n• How cost model changes in 8.0?\n• Rollback optimizer via switch?\n• Impact on prepared statements?\n\n## Common Mistakes\n• Blaming app deploy when upgrade timed with regression.\n• Permanent optimizer_switch rollback without ticket.\n• Skipping histogram rebuild post-upgrade.\n\n## Senior Engineer Insights\nStructure executive summary: blast radius, customer impact, root cause (optimizer change X), fix validated, preventive CI gate added.\n\n## Key Commands\n• SELECT DIGEST_TEXT, SUM_TIMER_WAIT FROM performance_schema.events_statements_summary_by_digest ORDER BY SUM_TIMER_WAIT DESC LIMIT 5;\n• EXPLAIN FORMAT=JSON ... \\G  -- before/after diff\n• SELECT * FROM mysql.server_cost; SELECT * FROM mysql.engine_cost;\n\n## Best Practices\n• Canary minor upgrades on replica first.\n• CI EXPLAIN regression suite.\n• Read release notes optimizer section.\n• Histogram refresh post-upgrade.",
    command: "SELECT DIGEST_TEXT, SUM_TIMER_WAIT FROM performance_schema.events_statements_summary_by_digest ORDER BY SUM_TIMER_WAIT DESC LIMIT 5;\nEXPLAIN FORMAT=JSON ... \\G  -- before/after diff\nSELECT * FROM mysql.server_cost; SELECT * FROM mysql.engine_cost;",
    tags: ["mysql","EXPLAIN Plan","hard","rare"],
  },
  {
    id: "mysql-slow-001",
    technology: "mysql",
    category: "mysql",
    difficulty: "easy",
    frequency: "Very Common",
    role: "Production Support",
    question: "How do you enable and configure the MySQL slow query log?",
    answer: "## Interview Answer\nslow_query_log=ON logs queries exceeding long_query_time seconds. log_queries_not_using_indexes optional noise. Use log_output=FILE or TABLE; TABLE hits mysql.slow_log — rotate carefully. Prefer file + log aggregator (pt-query-digest, PMM) in production.\n\n## Detailed Explanation\nlong_query_time microsecond precision in 8.0 (slow_query_log_always_write_time).\nmin_examined_row_limit reduces log volume for low-row slow queries.\nlog_slow_extra (8.0) adds JSON metadata.\nReplica slow log separate — enable for applier debugging.\nPerformance impact modest at threshold 1s; aggressive 0.1s adds IO.\n\n## Production Perspective\nDynamic SET GLOBAL without restart. Ship logs to ELK/S3 via Fluent Bit. RDS: parameter group slow_query_log. Never leave log_queries_not_using_indexes=ON globally on busy OLTP — TB logs.\n\n## Common Follow-up Questions\n• slow log vs performance_schema?\n• How rotate slow log files?\n• log_output TABLE pros/cons?\n\n## Common Mistakes\n• long_query_time=0 on production primary.\n• Filling disk with slow log no rotation.\n• Logging not using indexes on replica during migration.\n\n## Senior Engineer Insights\nRecommend tiered approach: performance_schema always, slow log threshold 1s, temporarily 0.5s during incident.\n\n## Key Commands\n• SET GLOBAL slow_query_log=ON;\nSET GLOBAL long_query_time=1;\nSET GLOBAL log_slow_extra=ON;\n• SHOW VARIABLES LIKE 'slow_query%';\nSHOW VARIABLES LIKE 'long_query%';\n• SELECT * FROM mysql.slow_log ORDER BY start_time DESC LIMIT 5;\n\n## Best Practices\n• Automated log rotation and shipping.\n• Tune threshold per environment.\n• Disable not_using_indexes globally.\n• Correlate with digest stats.",
    command: "SET GLOBAL slow_query_log=ON;\nSET GLOBAL long_query_time=1;\nSET GLOBAL log_slow_extra=ON;\nSHOW VARIABLES LIKE 'slow_query%';\nSHOW VARIABLES LIKE 'long_query%';\nSELECT * FROM mysql.slow_log ORDER BY start_time DESC LIMIT 5;",
    tags: ["mysql","Slow Query Analysis","easy","very-common"],
  },
  {
    id: "mysql-slow-002",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Very Common",
    role: "DBA",
    question: "How do you use pt-query-digest to analyze slow query logs at scale?",
    answer: "## Interview Answer\npt-query-digest aggregates slow log or processlist snapshots into fingerprint groups ranked by total time, 95th percentile, rows examined. Identifies top offenders normalized by query shape — essential for weekly DBA review and capacity planning.\n\n## Detailed Explanation\nFingerprints replace literals with ? — group similar SQL.\nReview: Query time distribution, Lock time, Rows sent vs examined.\nSources: slow log file, tcpdump mysql traffic, performance_schema.\n--since and --until filter maintenance windows.\nOutput to file for trending week-over-week.\n\n## Production Perspective\nCron weekly digest email to team. Compare pre/post index deploy digest. Store historical digest reports in S3 for audit. Percona Monitoring integrates pt-query-digest patterns.\n\n## Common Follow-up Questions\n• Digest vs performance_schema digest?\n• How handle prepared statements?\n• pt-query-digest on RDS slow log export?\n\n## Common Mistakes\n• One-time digest without trending.\n• Ignoring Lock time dominance — not index issue.\n• Not normalizing same query with different comments.\n\n## Senior Engineer Insights\nPresent top 3 fingerprints = 80% total time — Pareto focus for leadership buy-in on index work.\n\n## Key Commands\n• pt-query-digest /var/log/mysql/slow.log --since '2024-06-14' --until '2024-06-15' > digest.txt\n• pt-query-digest --review h=127.0.0.1 --limit 10\n• mysqldumpslow -s t -t 10 /var/log/mysql/slow.log  -- built-in alternative\n\n## Best Practices\n• Weekly automated digest reports.\n• Track fingerprint IDs over time.\n• Include rows examined in review.\n• Action items per top fingerprint.",
    command: "pt-query-digest /var/log/mysql/slow.log --since '2024-06-14' --until '2024-06-15' > digest.txt\npt-query-digest --review h=127.0.0.1 --limit 10\nmysqldumpslow -s t -t 10 /var/log/mysql/slow.log  -- built-in alternative",
    tags: ["mysql","Slow Query Analysis","medium","very-common"],
  },
  {
    id: "mysql-slow-003",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Common",
    role: "Database Engineer",
    question: "How do you distinguish lock contention from poor query plans in slow query analysis?",
    answer: "## Interview Answer\nSlow log Lock_time high relative to Query_time indicates waiting on metadata or InnoDB row locks — not missing index. Check performance_schema.data_lock_waits, sys.innodb_lock_waits, and trx state. Low lock time with high rows examined — optimizer issue.\n\n## Detailed Explanation\nMDL locks: Waiting for table metadata lock in PROCESSLIST.\nInnoDB: lock wait timeout 12050, deadlock 1213 in error log.\nLong trx holding locks — find via information_schema.innodb_trx.\nPlan issue: rows_examined huge, lock_time minimal.\nReplica lag: applier lock conflict with read query on same row.\n\n## Production Perspective\nDashboard: lock wait count + slow query rate correlation. Kill idle in trx sessions after timeout policy. Schedule heavy DDL off-peak with pt-osc. Index fix won't help MDL blocked migration.\n\n## Common Follow-up Questions\n• What is metadata lock vs row lock?\n• How find blocking transaction?\n• Lock time in ROW binlog apply?\n\n## Common Mistakes\n• Adding index on lock-bound workload.\n• Killing blocker without identifying migration job.\n• Ignoring Lock_time in pt-query-digest output.\n\n## Senior Engineer Insights\nDecision matrix in interview: Lock_time > 50% Query_time → locks first; else EXPLAIN plan.\n\n## Key Commands\n• SELECT * FROM sys.innodb_lock_waits\\G\n• SELECT * FROM performance_schema.metadata_locks WHERE LOCK_STATUS='PENDING';\n• SELECT trx_id, trx_started, trx_rows_locked, trx_query FROM information_schema.innodb_trx;\n\n## Best Practices\n• Monitor lock waits separately from slow queries.\n• Policy for long transaction kill.\n• MDL monitoring during DDL.\n• Separate lock vs plan runbooks.",
    command: "SELECT * FROM sys.innodb_lock_waits\\G\nSELECT * FROM performance_schema.metadata_locks WHERE LOCK_STATUS='PENDING';\nSELECT trx_id, trx_started, trx_rows_locked, trx_query FROM information_schema.innodb_trx;",
    tags: ["mysql","Slow Query Analysis","medium","common"],
  },
  {
    id: "mysql-slow-004",
    technology: "mysql",
    category: "mysql",
    difficulty: "hard",
    frequency: "Common",
    role: "DBA",
    question: "How do you respond to a sudden slow query log spike after a deployment?",
    answer: "## Interview Answer\nTimeline: correlate deploy time with slow log rate. Compare pt-query-digest fingerprints new vs baseline. Check plan regressions (EXPLAIN), new queries N+1 from ORM, missing index on new feature, or connection pool misconfig. Rollback app or hotfix index/stats as needed.\n\n## Detailed Explanation\nNew fingerprint dominates — trace to release changelog.\nPlan change on existing fingerprint — stats or optimizer.\nTraffic shift: 10× QPS on existing slow query now visible.\nFeature flag rollback faster than index create if reversible.\nCapture samples: PROCESSLIST, digest, EXPLAIN before rollback loses evidence.\n\n## Production Perspective\nWar room bridge: DBA + app on-call. Temporary max_execution_time on new digest if identified. Post-incident: add load test covering new endpoints. Synthetic monitoring should have caught — gap analysis.\n\n## Common Follow-up Questions\n• How prove deploy caused spike?\n• Canary deploy query monitoring?\n• ORM N+1 detection?\n\n## Common Mistakes\n• Restarting MySQL clearing evidence.\n• Tuning global buffers for one bad query.\n• No baseline digest before deploy.\n\n## Senior Engineer Insights\nShow deploy correlation graph: slow log rate vs time with deploy marker — ops maturity signal.\n\n## Key Commands\n• pt-query-digest slow.log --since '2024-06-15 14:00' | head -80\n• SELECT SCHEMA_NAME, DIGEST_TEXT, COUNT_STAR, AVG_TIMER_WAIT/1e9 AS avg_sec\nFROM performance_schema.events_statements_summary_by_digest\nWHERE LAST_SEEN > NOW() - INTERVAL 1 HOUR\nORDER BY COUNT_STAR DESC;\n• EXPLAIN FORMAT=JSON SELECT ...  -- new hot query\n\n## Best Practices\n• Pre-deploy digest baseline.\n• Canary with query latency alerts.\n• Automated EXPLAIN on new migrations.\n• Runbook for deploy rollback criteria.",
    command: "pt-query-digest slow.log --since '2024-06-15 14:00' | head -80\nSELECT SCHEMA_NAME, DIGEST_TEXT, COUNT_STAR, AVG_TIMER_WAIT/1e9 AS avg_sec\nFROM performance_schema.events_statements_summary_by_digest\nWHERE LAST_SEEN > NOW() - INTERVAL 1 HOUR\nORDER BY COUNT_STAR DESC;\nEXPLAIN FORMAT=JSON SELECT ...  -- new hot query",
    tags: ["mysql","Slow Query Analysis","hard","common"],
  },
  {
    id: "mysql-slow-005",
    technology: "mysql",
    category: "mysql",
    difficulty: "medium",
    frequency: "Rare",
    role: "Cloud Engineer",
    question: "Compare slow query log, performance_schema, and cloud Performance Insights for troubleshooting.",
    answer: "## Interview Answer\nSlow log: historical complete SQL text, simple threshold trigger. performance_schema: aggregated digests, low overhead, no full text by default unless configured. Performance Insights (RDS/Aurora): visual wait event breakdown, easy top SQL — less flexible offline but faster MTTR for cloud teams.\n\n## Detailed Explanation\nEnable events_statements_history_long for text samples.\nDigest length limit truncates long SQL — use slow log for full text.\nPerformance Insights 7-day retention — export for long trends.\nCombine: PI finds wait event → digest → EXPLAIN → slow log sample.\nOn-prem: PMM bridges gap similar to PI.\n\n## Production Perspective\nStandard toolkit per platform documented. Don't enable redundant heavy instrumentation.all. Cost: PI priced per vCPU; performance_schema cheaper at scale.\n\n## Common Follow-up Questions\n• How enable full SQL in P_S?\n• PI db.load vs slow log?\n• PMM Query Analytics source?\n\n## Common Mistakes\n• Only PI without EXPLAIN validation.\n• performance_schema full SQL memory blowup.\n• Ignoring wait events pointing to locks not queries.\n\n## Senior Engineer Insights\nPick tool by question: who is slow (PI wait) vs why plan bad (EXPLAIN) vs exact SQL text (slow log).\n\n## Key Commands\n• UPDATE performance_schema.setup_consumers SET ENABLED='YES' WHERE NAME LIKE 'events_statements%';\n• SELECT * FROM sys.statement_analysis LIMIT 10;\n• -- AWS Performance Insights API or console top SQL\n\n## Best Practices\n• Layered observability stack documented.\n• Right-size instrumentation per env.\n• Export PI data for postmortems.\n• Train on-call on all three tools.",
    command: "UPDATE performance_schema.setup_consumers SET ENABLED='YES' WHERE NAME LIKE 'events_statements%';\nSELECT * FROM sys.statement_analysis LIMIT 10;\n-- AWS Performance Insights API or console top SQL",
    tags: ["mysql","Slow Query Analysis","medium","rare"],
  }
];
