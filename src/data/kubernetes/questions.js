// Auto-generated from scripts/content/*Content.js — Kubernetes
// Run: node scripts/generate-tracks.mjs
// 170 questions (150 bank + 20 production scenarios)

export const questions = [
  {
    id: "kubernetes-containers-easy-1",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a container and how does it differ from a virtual machine?",
    answer: "A container packages an application and its dependencies into an isolated process namespace sharing the host kernel. Unlike VMs, containers do not include a guest OS — they start in seconds, use less memory, and achieve isolation via cgroups and namespaces (PID, network, mount, IPC, UTS). VMs provide stronger hardware-level isolation via hypervisors but with higher overhead. In Kubernetes, containers are the unit of execution inside Pods.",
    command: "# Compare container vs host resource usage\ndocker stats --no-stream\nkubectl top pod -A\nkubectl get nodes -o custom-columns=NAME:.metadata.name,CPU:.status.capacity.cpu,MEM:.status.capacity.memory",
    tags: ["kubernetes","containers","easy"],
  },
  {
    id: "kubernetes-docker-easy-2",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is Docker and how does it relate to Kubernetes?",
    answer: "Docker popularized container tooling: docker CLI, image build (Dockerfile), and container runtime (containerd under the hood since Docker 20.10). Kubernetes orchestrates containers at scale via the CRI API — it does not require the Docker CLI. Modern clusters use containerd or CRI-O directly. Developers still use Docker locally to build images pushed to registries that Kubernetes pulls at deploy time.",
    command: "# Build locally, deploy to K8s\ndocker build -t myregistry.io/web:1.0 .\ndocker push myregistry.io/web:1.0\nkubectl create deployment web --image=myregistry.io/web:1.0\nkubectl rollout status deployment/web",
    tags: ["kubernetes","docker","easy"],
  },
  {
    id: "kubernetes-kubernetes-architecture-easy-3",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe the main components of a Kubernetes control plane.",
    answer: "The control plane manages cluster state: kube-apiserver (REST API front door, authn/authz, admission), etcd (consistent key-value store for all cluster data), kube-scheduler (assigns Pods to Nodes), kube-controller-manager (runs controllers like Deployment, Node, ServiceAccount), and cloud-controller-manager (cloud integration: LB, routes, volumes). All components are HA in production — typically 3+ control plane nodes with stacked etcd or external etcd cluster.",
    command: "kubectl get componentstatuses 2>/dev/null || kubectl get --raw='/healthz?verbose'\nkubectl -n kube-system get pods -l tier=control-plane\nkubectl get nodes -l node-role.kubernetes.io/control-plane",
    tags: ["kubernetes","kubernetes-architecture","easy"],
  },
  {
    id: "kubernetes-pods-easy-4",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a Pod in Kubernetes and why is it the smallest deployable unit?",
    answer: "A Pod wraps one or more containers that share network namespace (same IP, localhost communication), IPC, and optionally volumes. Sidecar pattern colocates helper containers (logging, proxy). Pods are ephemeral — controllers (Deployment, StatefulSet) create and replace them. You rarely create bare Pods in production; instead use controllers for self-healing and scaling.",
    command: "kubectl get pods -n prod\nkubectl describe pod api-7f8d9-abc12 -n prod\nkubectl get pod api-7f8d9-abc12 -n prod -o yaml | grep -A20 \"spec:\"",
    tags: ["kubernetes","pods","easy"],
  },
  {
    id: "kubernetes-deployments-easy-5",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a Deployment and how does it manage ReplicaSets?",
    answer: "A Deployment declaratively manages ReplicaSets to run stateless app replicas. It supports rolling updates, rollbacks, and scaling. When you change the pod template (e.g., new image), Deployment creates a new ReplicaSet and gradually shifts replicas. Old ReplicaSets are retained for rollback history (revisionHistoryLimit). kubectl rollout undo reverts to previous ReplicaSet.",
    command: "kubectl get deployment,rs,pods -n prod -l app=api\nkubectl describe deployment api -n prod | grep -A10 \"Replica Sets\"\nkubectl rollout history deployment/api -n prod",
    tags: ["kubernetes","deployments","easy"],
  },
  {
    id: "kubernetes-replicasets-easy-6",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a ReplicaSet and how does it differ from a Deployment?",
    answer: "ReplicaSet ensures a specified number of pod replicas with matching labels are running. It creates/deletes pods to match spec.replicas. Deployments manage ReplicaSets and add rolling updates/rollbacks — you should not manage ReplicaSets directly in production. Bare ReplicaSets only support recreate-style updates (delete all, create new).",
    command: "kubectl get rs -n prod -l app=web\nkubectl describe rs web-6d4f8b9c7 -n prod\nkubectl get rs -n prod -o custom-columns=NAME:.metadata.name,DESIRED:.spec.replicas,READY:.status.readyReplicas",
    tags: ["kubernetes","replicasets","easy"],
  },
  {
    id: "kubernetes-services-easy-7",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a Kubernetes Service and why do you need one?",
    answer: "Pods are ephemeral with changing IPs. A Service provides a stable virtual IP (ClusterIP) and DNS name (my-svc.namespace.svc.cluster.local) that load-balances traffic to matching pod endpoints via kube-proxy or dataplane (iptables/IPVS/eBPF). Types: ClusterIP (internal), NodePort, LoadBalancer (cloud LB), ExternalName (DNS CNAME).",
    command: "kubectl get svc -n prod\nkubectl describe svc api -n prod\nkubectl get endpoints api -n prod\nnslookup api.prod.svc.cluster.local",
    tags: ["kubernetes","services","easy"],
  },
  {
    id: "kubernetes-ingress-easy-8",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is an Ingress resource and how does it differ from a LoadBalancer Service?",
    answer: "Ingress defines HTTP/HTTPS routing rules (host, path) to backend Services — single entry point with TLS termination, path-based routing, and virtual hosts. Requires an Ingress Controller (nginx, traefik, AWS ALB) to implement rules. LoadBalancer Service creates one LB per Service — expensive at scale. Ingress consolidates many routes behind one LB.",
    command: "kubectl get ingress -n prod\nkubectl describe ingress api-ingress -n prod\nkubectl get svc -n ingress-nginx\nkubectl get pods -n ingress-nginx",
    tags: ["kubernetes","ingress","easy"],
  },
  {
    id: "kubernetes-configmaps-easy-9",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a ConfigMap and when should you use one?",
    answer: "ConfigMap stores non-sensitive configuration as key-value pairs or file content (up to 1MB per object). Mount as files in pod volumes or inject as environment variables. Use for app settings, feature flags, config files — not secrets (use Secret resource). Changes to mounted ConfigMaps propagate to pods asynchronously (kubelet sync ~60s) unless using subPath (no auto-update).",
    command: "kubectl create configmap app-config --from-literal=LOG_LEVEL=info --from-literal=ENV=prod -n prod\nkubectl get configmap app-config -n prod -o yaml\nkubectl describe configmap app-config -n prod",
    tags: ["kubernetes","configmaps","easy"],
  },
  {
    id: "kubernetes-secrets-easy-10",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a Kubernetes Secret and how does it differ from a ConfigMap?",
    answer: "Secrets store sensitive data (passwords, tokens, TLS keys) as base64-encoded values in etcd (encrypted at rest if encryption at rest enabled). Types: Opaque, kubernetes.io/tls, kubernetes.io/dockerconfigjson, bootstrap token. Same consumption patterns as ConfigMap (env/volume). Never commit plain secrets to git — use Sealed Secrets, External Secrets Operator, or cloud secret managers.",
    command: "kubectl create secret generic db-creds --from-literal=username=app --from-literal=password='s3cr3t' -n prod\nkubectl get secret db-creds -n prod\nkubectl describe secret db-creds -n prod  # values hidden",
    tags: ["kubernetes","secrets","easy"],
  },
  {
    id: "kubernetes-persistent-volumes-easy-11",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain the relationship between PersistentVolume, PersistentVolumeClaim, and Pods.",
    answer: "PV is cluster storage resource (admin or dynamic provisioner). PVC is user storage request (size, access mode, storageClass). Binding matches PVC to PV. Pod references PVC in volumes.persistentVolumeClaim. Dynamic provisioning creates PV automatically when PVC created with storageClassName. Access modes: RWO (single node), RWX (multi node read-write), ROX (read-only many).",
    command: "kubectl get pv,pvc -A\nkubectl describe pvc data-api-0 -n prod\nkubectl get pod db-0 -n prod -o jsonpath='{.spec.volumes[*].persistentVolumeClaim.claimName}'",
    tags: ["kubernetes","persistent-volumes","easy"],
  },
  {
    id: "kubernetes-storage-classes-easy-12",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a StorageClass and how does dynamic provisioning work?",
    answer: "StorageClass defines provisioner (ebs.csi.aws.com, pd.csi.storage.gke.io), parameters (type, iops), reclaimPolicy, allowVolumeExpansion, volumeBindingMode (Immediate vs WaitForFirstConsumer). PVC with storageClassName triggers provisioner to create PV automatically. Default SC annotated storageclass.kubernetes.io/is-default-class: true.",
    command: "kubectl get storageclass\nkubectl describe storageclass gp3\nkubectl get pvc -n prod -o custom-columns=NAME:.metadata.name,SC:.spec.storageClassName,SIZE:.spec.resources.requests.storage",
    tags: ["kubernetes","storage-classes","easy"],
  },
  {
    id: "kubernetes-namespaces-easy-13",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a Kubernetes namespace and why use multiple namespaces?",
    answer: "Namespaces provide virtual clusters within a physical cluster — scope for names, RBAC, ResourceQuotas, NetworkPolicies, and LimitRanges. Use cases: env separation (dev/staging/prod), team isolation, blast radius containment. Not a security boundary alone — combine with RBAC and NetworkPolicy. kube-system, kube-public, default are built-in.",
    command: "kubectl get namespaces\nkubectl create namespace staging\nkubectl get all -n prod\nkubectl config set-context --current --namespace=prod",
    tags: ["kubernetes","namespaces","easy"],
  },
  {
    id: "kubernetes-rbac-easy-14",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain Roles, ClusterRoles, RoleBindings, and ClusterRoleBindings.",
    answer: "Role: namespaced permissions (verbs on resources in namespace). ClusterRole: cluster-wide or aggregatable permissions. RoleBinding grants Role/ClusterRole to user/group/ServiceAccount within namespace. ClusterRoleBinding grants cluster-wide. Principle of least privilege: bind minimal verbs (get, list) not wildcard for app ServiceAccounts.",
    command: "kubectl get role,rolebinding -n prod\nkubectl get clusterrole,clusterrolebinding | head -20\nkubectl describe rolebinding dev-team -n prod",
    tags: ["kubernetes","rbac","easy"],
  },
  {
    id: "kubernetes-helm-easy-15",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is Helm and how does it simplify Kubernetes deployments?",
    answer: "Helm is package manager for Kubernetes — charts templatize manifests (Deployment, Service, Ingress, etc.) with values.yaml for customization. helm install/upgrade manages releases with revision history. helm rollback reverts bad deploys. Repositories share charts (Bitnami, official). Reduces YAML duplication across environments.",
    command: "helm list -A\nhelm search repo nginx\nhelm install my-nginx bitnami/nginx -n prod --set service.type=ClusterIP\nhelm status my-nginx -n prod",
    tags: ["kubernetes","helm","easy"],
  },
  {
    id: "kubernetes-monitoring-easy-16",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What metrics does metrics-server provide and how is it used?",
    answer: "metrics-server collects resource usage (CPU/memory) from kubelets via Summary API, serves metrics.k8s.io for kubectl top and HPA. Not for long-term storage — use Prometheus for that. Requires kubelet read-only port or authentication. One metrics-server deployment per cluster (typically kube-system).",
    command: "kubectl top nodes\nkubectl top pods -n prod --containers\nkubectl get apiservice v1beta1.metrics.k8s.io -o yaml\nkubectl get pods -n kube-system -l k8s-app=metrics-server",
    tags: ["kubernetes","monitoring","easy"],
  },
  {
    id: "kubernetes-scaling-easy-17",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is the Horizontal Pod Autoscaler (HPA) and how does it work?",
    answer: "HPA adjusts Deployment/StatefulSet replicas based on metrics (CPU, memory, custom, external). Compares current metric to target (e.g., 70% CPU), calculates desired replicas, patches scale subresource every sync period (~15s). Requires metrics-server for resource metrics. Respects minReplicas/maxReplicas bounds.",
    command: "kubectl autoscale deployment api -n prod --min=2 --max=20 --cpu-percent=70\nkubectl get hpa -n prod\nkubectl describe hpa api -n prod\nkubectl get deployment api -n prod -o jsonpath='{.spec.replicas}'",
    tags: ["kubernetes","scaling","easy"],
  },
  {
    id: "kubernetes-troubleshooting-easy-18",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is your systematic approach to troubleshooting a failing Pod?",
    answer: "1) kubectl get pod — check STATUS (CrashLoopBackOff, ImagePullBackOff, Pending). 2) kubectl describe pod — Events section is key. 3) kubectl logs pod [-c container] [--previous]. 4) kubectl exec for live debug. 5) Check Service endpoints, NetworkPolicy, resource limits. Follow detect → triage → mitigate → root cause → prevent.",
    command: "kubectl get pod failing -n prod\nkubectl describe pod failing -n prod\nkubectl logs failing -n prod --previous --tail=100\nkubectl exec -it failing -n prod -- sh",
    tags: ["kubernetes","troubleshooting","easy"],
  },
  {
    id: "kubernetes-containers-easy-19",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain the role of cgroups and namespaces in container isolation.",
    answer: "Linux namespaces isolate what a process can see: PID (process tree), NET (network stack), MNT (filesystem), IPC, UTS (hostname), and USER (UID mapping). cgroups limit and account for CPU, memory, I/O, and PIDs. Together they let multiple containers share one kernel safely. Kubernetes maps Pod resource requests/limits to container cgroups via the kubelet and container runtime (containerd/CRI-O).",
    command: "# Inspect cgroup limits on a node (containerd)\nsudo crictl inspect $(sudo crictl ps -q --name myapp | head -1) | jq '.info.runtimeSpec.linux.resources'\nkubectl describe pod myapp -n prod | grep -A5 \"Limits\\|Requests\"",
    tags: ["kubernetes","containers","easy"],
  },
  {
    id: "kubernetes-docker-easy-20",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain the purpose of a Dockerfile ENTRYPOINT vs CMD.",
    answer: "ENTRYPOINT defines the main executable that always runs; CMD provides default arguments overrideable at docker run. Combined form: ENTRYPOINT [\"python\"] + CMD [\"app.py\"] lets kubectl/docker run pass alternate args. In Kubernetes, command maps to ENTRYPOINT override and args maps to CMD override in container spec. Use exec form (JSON array) to avoid shell signal handling issues with PID 1.",
    command: "kubectl run debug --image=busybox --restart=Never --command -- sleep 3600\nkubectl get pod debug -o jsonpath='{.spec.containers[0].command}'\n# Override in deployment manifest:\n# command: [\"python\"]\n# args: [\"-m\", \"gunicorn\", \"app:application\"]",
    tags: ["kubernetes","docker","easy"],
  },
  {
    id: "kubernetes-kubernetes-architecture-easy-21",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is the role of the kubelet on worker nodes?",
    answer: "The kubelet is the node agent that registers the node with the API server, watches PodSpecs bound to its node, and ensures containers run via the CRI runtime. It executes liveness/readiness probes, reports node and pod status, mounts volumes, and enforces pod resources via cgroups. It does not manage containers not created by Kubernetes.",
    command: "kubectl describe node worker-1 | grep -A5 Conditions\nsudo systemctl status kubelet\nsudo journalctl -u kubelet -f --no-pager | tail -50\nkubectl get pods -n prod -o wide --field-selector spec.nodeName=worker-1",
    tags: ["kubernetes","kubernetes-architecture","easy"],
  },
  {
    id: "kubernetes-pods-easy-22",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain the Pod lifecycle phases: Pending, Running, Succeeded, Failed, Unknown.",
    answer: "Pending: accepted but not scheduled or images pulling. Running: at least one container started. Succeeded: all containers terminated exit 0 (Jobs). Failed: terminated with non-zero exit. Unknown: node communication lost. Conditions (PodScheduled, Initialized, ContainersReady, Ready) give finer detail. Use kubectl describe pod Events for transitions like Scheduled, Pulled, Created, Started.",
    command: "kubectl get pods -n prod -o custom-columns=NAME:.metadata.name,PHASE:.status.phase,READY:.status.conditions[?(@.type==\"Ready\")].status\nkubectl describe pod api-abc -n prod | grep -E \"Phase:|Conditions:|Events:\" -A15",
    tags: ["kubernetes","pods","easy"],
  },
  {
    id: "kubernetes-deployments-easy-23",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain rolling update strategy parameters maxSurge and maxUnavailable.",
    answer: "maxSurge: extra pods above desired count during update (absolute or percent). maxUnavailable: pods that can be down during update. Example: 10 replicas, maxSurge 25% (3), maxUnavailable 0 → always at least 10 ready; up to 13 total during rollout. Setting maxUnavailable 0 ensures no capacity drop — important for production traffic.",
    command: "kubectl get deployment api -n prod -o jsonpath='{.spec.strategy.rollingUpdate}'\nkubectl patch deployment api -n prod -p '{\"spec\":{\"strategy\":{\"rollingUpdate\":{\"maxSurge\":\"25%\",\"maxUnavailable\":0}}}}'\nkubectl rollout status deployment/api -n prod",
    tags: ["kubernetes","deployments","easy"],
  },
  {
    id: "kubernetes-replicasets-easy-24",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain how ReplicaSet selectors match pod labels.",
    answer: "ReplicaSet spec.selector.matchLabels must match template.metadata.labels. If labels on running pods do not match selector, ReplicaSet ignores them. Orphan pods (created manually with matching labels) are adopted if controllerRef not set. Immutable selector in apps/v1 ReplicaSet — changing it requires delete/recreate. Deployment owns ReplicaSets via ownerReferences.",
    command: "kubectl get rs web-6d4f8b9c7 -n prod -o jsonpath='{.spec.selector.matchLabels}'\nkubectl get pods -n prod -l app=web --show-labels\nkubectl get rs web-6d4f8b9c7 -n prod -o jsonpath='{.metadata.ownerReferences[0].kind}'",
    tags: ["kubernetes","replicasets","easy"],
  },
  {
    id: "kubernetes-services-easy-25",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain ClusterIP vs NodePort vs LoadBalancer Service types.",
    answer: "ClusterIP: internal VIP, default type, reachable only inside cluster. NodePort: exposes on each node high port (30000-32767), often fronted by external LB. LoadBalancer: provisions cloud LB (ELB, NLB, GCP LB) pointing to NodePort/ClusterIP — external access. ExternalName: maps service to external DNS. Production external traffic typically: Ingress → Service or LoadBalancer Service.",
    command: "kubectl expose deployment api -n prod --port=8080 --target-port=8080 --type=ClusterIP\nkubectl get svc api -n prod -o wide\n# LoadBalancer\nkubectl patch svc api -n prod -p '{\"spec\":{\"type\":\"LoadBalancer\"}}'",
    tags: ["kubernetes","services","easy"],
  },
  {
    id: "kubernetes-ingress-easy-26",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain how Ingress maps hostnames and paths to Services.",
    answer: "spec.rules[].host matches Host header (api.example.com). paths[].path with pathType Prefix/Exact/ImplementationSpecific route to backend service name and port number. Default backend catches unmatched requests. TLS section references Secret with cert/key for HTTPS. Controller watches Ingress and configures reverse proxy (nginx.conf, ALB listener rules).",
    command: "kubectl get ingress api -n prod -o yaml\nkubectl explain ingress.spec.rules\n# Test routing\ncurl -H \"Host: api.example.com\" http://<ingress-ip>/v1/health",
    tags: ["kubernetes","ingress","easy"],
  },
  {
    id: "kubernetes-configmaps-easy-27",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain mounting a ConfigMap as a volume vs environment variables.",
    answer: "Env vars: injected at pod start, good for simple key-value, requires pod restart to pick up changes. Volume mount: projects keys as files in directory, supports config files, auto-updates in place (apps must watch/reload). subPath mounts single file but does NOT update on ConfigMap change. Prefer volumes for file-based config with reload support.",
    command: "kubectl apply -f pod-with-configmap.yaml\nkubectl exec app-pod -n prod -- cat /etc/config/app.properties\nkubectl exec app-pod -n prod -- printenv LOG_LEVEL\nkubectl get pod app-pod -n prod -o jsonpath='{.spec.volumes[*].configMap.name}'",
    tags: ["kubernetes","configmaps","easy"],
  },
  {
    id: "kubernetes-secrets-easy-28",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain how to mount a TLS Secret for Ingress or application use.",
    answer: "Create kubernetes.io/tls Secret with tls.crt and tls.key. Reference in Ingress spec.tls[].secretName or mount as volume at /etc/tls in pod. cert-manager creates and renews these automatically. For mTLS, mount ca.crt alongside. Pods can read Secrets only if RBAC allows the ServiceAccount.",
    command: "kubectl create secret tls api-tls --cert=fullchain.pem --key=privkey.pem -n prod\nkubectl get secret api-tls -n prod -o jsonpath='{.type}'\nkubectl apply -f ingress-with-tls.yaml",
    tags: ["kubernetes","secrets","easy"],
  },
  {
    id: "kubernetes-persistent-volumes-easy-29",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What are PersistentVolume access modes and reclaim policies?",
    answer: "AccessModes: ReadWriteOnce (most block storage), ReadWriteMany (NFS/EFS), ReadOnlyMany. reclaimPolicy on PV: Retain (manual cleanup after PVC delete), Delete (cloud volume deleted with PVC), Recycle (deprecated). Production dynamic volumes typically use Delete with backup strategy for stateful data.",
    command: "kubectl get pv -o custom-columns=NAME:.metadata.name,ACCESS:.spec.accessModes,RECLAIM:.spec.persistentVolumeReclaimPolicy,STATUS:.status.phase\nkubectl explain persistentvolume.spec",
    tags: ["kubernetes","persistent-volumes","easy"],
  },
  {
    id: "kubernetes-storage-classes-easy-30",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain volumeBindingMode Immediate vs WaitForFirstConsumer.",
    answer: "Immediate: PV provisioned and bound when PVC created — may be in wrong zone vs pending pod. WaitForFirstConsumer: provisioning delayed until pod scheduled, ensuring volume topology matches pod node zone — recommended for zonal block storage. Topology-aware scheduling coordinates PVC binding with pod placement.",
    command: "kubectl get sc gp3 -o jsonpath='{.volumeBindingMode}'\nkubectl describe pvc zonal-vol -n prod | grep -A5 Events\nkubectl get pod app -n prod -o wide",
    tags: ["kubernetes","storage-classes","easy"],
  },
  {
    id: "kubernetes-namespaces-easy-31",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain ResourceQuota and LimitRange at namespace level.",
    answer: "ResourceQuota caps aggregate namespace consumption: total CPU/memory requests/limits, pod count, PVC count, LoadBalancer services. LimitRange sets defaults and min/max per container/PVC in namespace — prevents tiny or huge pods. Apply together: LimitRange defaults + ResourceQuota ceiling.",
    command: "kubectl describe resourcequota -n prod\nkubectl describe limitrange -n prod\nkubectl get pods -n prod -o json | jq '.items[].spec.containers[].resources'",
    tags: ["kubernetes","namespaces","easy"],
  },
  {
    id: "kubernetes-rbac-easy-32",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is a ServiceAccount and how does it relate to RBAC?",
    answer: "ServiceAccount provides identity for pods. Mounted token at /var/run/secrets/kubernetes.io/serviceaccount (or projected token with expiration). RBAC RoleBinding links SA to Role. Pods specify serviceAccountName. Default SA in namespace often too permissive — create dedicated SA per app with minimal permissions.",
    command: "kubectl get sa -n prod\nkubectl describe sa api -n prod\nkubectl get pod api-abc -n prod -o jsonpath='{.spec.serviceAccountName}'\nkubectl auth can-i list secrets --as=system:serviceaccount:prod:api -n prod",
    tags: ["kubernetes","rbac","easy"],
  },
  {
    id: "kubernetes-helm-easy-33",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain Helm chart structure: Chart.yaml, values.yaml, templates/.",
    answer: "Chart.yaml: metadata, version, appVersion. values.yaml: default parameters. templates/: Go-template YAML files (_helpers.tpl for shared snippets). charts/: subchart dependencies. helm template renders locally without install. Release name injects into resource names via {{ .Release.Name }}.",
    command: "helm pull bitnami/nginx --untar\ncat nginx/Chart.yaml\nhelm template my-nginx ./nginx --set replicaCount=3\nls nginx/templates/",
    tags: ["kubernetes","helm","easy"],
  },
  {
    id: "kubernetes-monitoring-easy-34",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain liveness vs readiness probes from a monitoring perspective.",
    answer: "Readiness failure removes pod from Service endpoints — metric: kube_pod_status_ready{condition=\"false\"}. Liveness failure triggers restart — track restart count and CrashLoopBackOff. Monitor probe failure events and latency. Alert on sustained unready pods or restart rate spikes. Probes are first-line health signals before deep APM.",
    command: "kubectl get pods -n prod -o json | jq '.items[] | {name: .metadata.name, ready: (.status.conditions[]|select(.type==\"Ready\")|.status), restarts: .status.containerStatuses[0].restartCount}'\nkubectl describe pod api-abc -n prod | grep -A5 \"Warning Unhealthy\"",
    tags: ["kubernetes","monitoring","easy"],
  },
  {
    id: "kubernetes-scaling-easy-35",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain Cluster Autoscaler and its relationship with HPA.",
    answer: "HPA adds pods; if no node has capacity, pods stay Pending. Cluster Autoscaler (CA) detects unschedulable pods and adds nodes to node groups/pools; removes underutilized nodes after cooldown. CA respects PodDisruptionBudgets, taints, and node selectors. Works with cloud auto-scaling groups (ASG, MIG, VMSS) or Karpenter for faster provisioning.",
    command: "kubectl get pods -A --field-selector status.phase=Pending\nkubectl logs -n kube-system deploy/cluster-autoscaler --tail=30\nkubectl get nodes\n# EKS: aws autostart autoscaling describe-auto-scaling-groups",
    tags: ["kubernetes","scaling","easy"],
  },
  {
    id: "kubernetes-troubleshooting-easy-36",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain common Pod status reasons: CrashLoopBackOff, ImagePullBackOff, ErrImagePull, OOMKilled.",
    answer: "CrashLoopBackOff: container exits repeatedly, kubelet backs off restarts — check logs and exit code. ImagePullBackOff/ErrImagePull: registry auth, wrong tag, network. OOMKilled: exceeded memory limit — increase limit or fix leak. CreateContainerConfigError: missing ConfigMap/Secret key. RunContainerError: bad command or volume mount.",
    command: "kubectl get pod -n prod -o wide\nkubectl describe pod crash -n prod | grep -E \"State:|Reason:|Exit Code:\"\nkubectl logs crash -n prod --previous\nkubectl get events -n prod --field-selector involvedObject.name=crash",
    tags: ["kubernetes","troubleshooting","easy"],
  },
  {
    id: "kubernetes-containers-easy-37",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "What is an OCI image and how is it structured?",
    answer: "An OCI (Open Container Initiative) image is a stack of read-only layers identified by SHA256 digests, described by an image manifest and config JSON. Each Dockerfile instruction adds a layer; layers are shared across images for efficient storage. The config specifies entrypoint, env vars, exposed ports, and default command. Runtimes pull manifests from registries, extract layers to overlay filesystems, and create a container from the top layer plus a writable container layer.",
    command: "# Inspect image layers locally\ndocker history myregistry.io/app:v1.2 --no-trunc\ndocker inspect myregistry.io/app:v1.2 --format='{{json .RootFS.Layers}}' | jq\nskopeo inspect docker://myregistry.io/app:v1.2",
    tags: ["kubernetes","containers","easy"],
  },
  {
    id: "kubernetes-docker-easy-38",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe a basic use case for Docker Compose vs Kubernetes.",
    answer: "Docker Compose defines multi-container apps on a single host for local dev and small deployments — one YAML file, docker compose up. Kubernetes targets production orchestration: multi-node scheduling, self-healing, service discovery, autoscaling, rolling updates, and RBAC. Typical flow: develop with Compose, build images in CI, deploy manifests/Helm charts to Kubernetes for staging and production.",
    command: "# Local dev\ndocker compose up -d\ndocker compose logs -f api\n# Production equivalent\nkubectl apply -f k8s/\nkubectl get pods,svc,ingress -n staging",
    tags: ["kubernetes","docker","easy"],
  },
  {
    id: "kubernetes-kubernetes-architecture-easy-39",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Explain how kubectl communicates with the cluster.",
    answer: "kubectl is a CLI client that sends HTTPS requests to kube-apiserver using kubeconfig (~/.kube/config) containing cluster URL, credentials (cert, token, or exec plugin for OIDC/cloud IAM), and context. It never talks directly to kubelets for management operations. Requests pass authentication, authorization (RBAC), admission controllers, then persist to etcd. kubectl get/describe reads from API server cache/watch streams.",
    command: "kubectl config view --minify\nkubectl config current-context\nkubectl get --raw /api/v1/namespaces/prod/pods --v=8 2>&1 | grep -E \"curl|Authorization\"\nkubectl auth can-i get pods -n prod",
    tags: ["kubernetes","kubernetes-architecture","easy"],
  },
  {
    id: "kubernetes-pods-easy-40",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe a basic use case for init containers in a Pod.",
    answer: "Init containers run sequentially before app containers start, completing setup tasks: wait for dependencies (DB migration service), download config, validate permissions, or delay startup until a service is reachable. They must exit successfully before main containers launch. Example: init container runs flyway migrate; app container starts only after schema is ready.",
    command: "kubectl apply -f - <<EOF\napiVersion: v1\nkind: Pod\nmetadata:\n  name: app-with-init\n  namespace: staging\nspec:\n  initContainers:\n  - name: wait-for-db\n    image: busybox:1.36\n    command: [\"sh\", \"-c\", \"until nc -z postgres 5432; do sleep 2; done\"]\n  containers:\n  - name: app\n    image: myapp:1.0\nEOF\nkubectl logs app-with-init -c wait-for-db -n staging",
    tags: ["kubernetes","pods","easy"],
  },
  {
    id: "kubernetes-deployments-easy-41",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe a basic use case for kubectl rollout undo.",
    answer: "After deploying a bad image causing 500 errors, quickly revert: kubectl rollout undo deployment/api rolls back to previous ReplicaSet revision without re-applying old YAML. Verify with rollout status and error rate dashboards. For GitOps, revert the git commit and sync instead to keep source of truth aligned.",
    command: "kubectl rollout undo deployment/api -n prod\nkubectl rollout status deployment/api -n prod\nkubectl rollout history deployment/api -n prod --revision=3\nkubectl get rs -n prod -l app=api --sort-by=.metadata.creationTimestamp",
    tags: ["kubernetes","deployments","easy"],
  },
  {
    id: "kubernetes-replicasets-easy-42",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe what happens when you scale a Deployment from 3 to 10 replicas.",
    answer: "Deployment updates its active ReplicaSet spec.replicas to 10. ReplicaSet controller creates 7 new pods; scheduler assigns nodes; kubelet starts containers. Deployment status shows updatedReplicas increasing until 10/10 ready. HPA may later adjust this count based on metrics. Scale-down removes pods gracefully respecting terminationGracePeriodSeconds and PDB.",
    command: "kubectl scale deployment web -n prod --replicas=10\nkubectl get pods -n prod -l app=web -w\nkubectl get deployment web -n prod -o jsonpath='{.status.replicas}{\" ready \"}{.status.readyReplicas}{\"\\n\"}'",
    tags: ["kubernetes","replicasets","easy"],
  },
  {
    id: "kubernetes-services-easy-43",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe how kube-proxy routes traffic to pod endpoints.",
    answer: "kube-proxy (or CNI replacement like Cilium) watches Services and Endpoints/EndpointSlices, programming rules on each node. iptables/IPVS modes DNAT ClusterIP:port to pod IP:port. Only Ready pods appear in endpoints (readiness probe governs). SessionAffinity: ClientIP sticks sessions to same pod for stateful interactions.",
    command: "kubectl get endpointslices -n prod -l kubernetes.io/service-name=api\nkubectl get pods -n kube-system -l k8s-app=kube-proxy\n# Cilium: cilium service list\nkubectl run curl --rm -it --image=curlimages/curl -n prod -- curl -v http://api:8080/health",
    tags: ["kubernetes","services","easy"],
  },
  {
    id: "kubernetes-ingress-easy-44",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe a basic TLS setup for Ingress.",
    answer: "Create kubernetes.io/tls Secret with tls.crt and tls.key (from cert-manager or manual). Reference in ingress.spec.tls[].secretName and hosts. Controller terminates TLS and forwards plain HTTP or re-encrypts to backends. cert-manager automates Let's Encrypt via ClusterIssuer — preferred for production cert lifecycle.",
    command: "kubectl create secret tls api-tls --cert=tls.crt --key=tls.key -n prod\nkubectl apply -f ingress-tls.yaml\nkubectl get certificate -n prod  # cert-manager\nkubectl describe certificate api-tls -n prod",
    tags: ["kubernetes","ingress","easy"],
  },
  {
    id: "kubernetes-configmaps-easy-45",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe a basic use case for immutable ConfigMaps.",
    answer: "immutable: true prevents updates/deletes — kubelet uses watch-based injection avoiding periodic API polls, improving control plane performance at scale. Trade-off: must create new ConfigMap name (app-config-v2) and update pod spec reference to change config — aligns with GitOps and versioned config. Recommended for large clusters with many ConfigMap-mounted pods.",
    command: "kubectl create configmap static-config --from-file=nginx.conf -n prod --dry-run=client -o yaml | kubectl apply -f -\nkubectl patch configmap static-config -n prod -p '{\"immutable\":true}'\n# To update: create static-config-v2 and update deployment volume reference",
    tags: ["kubernetes","configmaps","easy"],
  },
  {
    id: "kubernetes-secrets-easy-46",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe a basic use case for imagePullSecrets.",
    answer: "Private container registries require authentication. Create docker-registry Secret with registry credentials; reference in pod spec.imagePullSecrets or ServiceAccount.imagePullSecrets for automatic injection. EKS/GKE/ACR often use IAM/workload identity instead of long-lived docker config.",
    command: "kubectl create secret docker-registry regcred \\\n  --docker-server=myregistry.io \\\n  --docker-username=robot \\\n  --docker-password=token -n prod\nkubectl patch serviceaccount default -n prod -p '{\"imagePullSecrets\":[{\"name\":\"regcred\"}]}'",
    tags: ["kubernetes","secrets","easy"],
  },
  {
    id: "kubernetes-persistent-volumes-easy-47",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe a basic StatefulSet volumeClaimTemplate use case.",
    answer: "StatefulSet volumeClaimTemplates auto-create per-pod PVCs (data-db-0, data-db-1) with stable binding — pod-0 always reattaches same volume after reschedule. Used for databases, Kafka, Elasticsearch. Headless Service provides stable network ID. Ordered rollout: pod-1 starts after pod-0 ready.",
    command: "kubectl get statefulset db -n prod\nkubectl get pvc -n prod | grep data-db\nkubectl describe pod db-0 -n prod | grep -A5 \"Volumes:\"\nkubectl get statefulset db -n prod -o jsonpath='{.spec.volumeClaimTemplates[0].spec}' | jq",
    tags: ["kubernetes","persistent-volumes","easy"],
  },
  {
    id: "kubernetes-storage-classes-easy-48",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe a basic use case for multiple StorageClasses in one cluster.",
    answer: "Tiered storage: gp3 (default, general), io2 (high IOPS databases), st1/sc1 (throughput/cold), efs-sc (RWX shared). Apps request appropriate class via PVC storageClassName. Platform team manages SC definitions; app teams select in manifests. Cost optimization by matching workload I/O profile.",
    command: "kubectl get sc\nkubectl apply -f - <<EOF\napiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: fast-data\n  namespace: prod\nspec:\n  accessModes: [ReadWriteOnce]\n  storageClassName: io2\n  resources:\n    requests:\n      storage: 100Gi\nEOF",
    tags: ["kubernetes","storage-classes","easy"],
  },
  {
    id: "kubernetes-namespaces-easy-49",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe a basic use case for namespace labels and annotations.",
    answer: "Labels enable selection: environment=prod, team=platform for policy, billing, monitoring. Annotations store metadata: cost-center, owner email, runbook URL. Used by OPA/Gatekeeper (require labels), Prometheus service discovery, and GitOps ApplicationSet generators matching namespace labels.",
    command: "kubectl label namespace prod environment=production team=checkout --overwrite\nkubectl get namespace prod --show-labels\nkubectl annotate namespace prod runbook=https://wiki.example.com/prod-runbook",
    tags: ["kubernetes","namespaces","easy"],
  },
  {
    id: "kubernetes-rbac-easy-50",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "Describe how kubectl auth can-i helps verify permissions.",
    answer: "can-i checks authorization without performing action: kubectl auth can-i create deployments -n prod --as=user@corp.com. Useful for debugging Forbidden errors from CI/CD ServiceAccounts. --list shows all permissions. Impersonation requires cluster-admin or impersonate privilege.",
    command: "kubectl auth can-i create pods -n prod --as=system:serviceaccount:ci:deployer\nkubectl auth can-i --list --as=system:serviceaccount:prod:api -n prod\nkubectl auth can-i get secrets --as=developer -n prod",
    tags: ["kubernetes","rbac","easy"],
  },
  {
    id: "kubernetes-containers-medium-1",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement multi-stage Docker builds for production Kubernetes workloads?",
    answer: "Use a builder stage with full SDK/toolchain to compile artifacts, then copy only runtime artifacts into a minimal final stage (distroless, alpine, or scratch). This shrinks attack surface and image size, speeds pulls, and keeps secrets/build tools out of production images. Pin base image digests, run as non-root, and scan images in CI. In Kubernetes, smaller images reduce node disk pressure and ImagePull times during scale-out events.",
    command: "# Example multi-stage build + deploy\n# Dockerfile: FROM golang:1.22 AS build ... FROM gcr.io/distroless/static-debian12\n docker build -t myregistry.io/api:$(git rev-parse --short HEAD) .\n docker push myregistry.io/api:$(git rev-parse --short HEAD)\n kubectl set image deployment/api api=myregistry.io/api:$(git rev-parse --short HEAD) -n prod",
    tags: ["kubernetes","containers","medium"],
  },
  {
    id: "kubernetes-docker-medium-2",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement production-grade Docker builds in a Kubernetes CI/CD pipeline?",
    answer: "Use BuildKit/kaniko or Docker-in-Docker in CI runners to build without daemon on nodes. Multi-stage builds, non-root users, .dockerignore to exclude secrets, SBOM generation (syft), image scanning (Trivy, Grype), and sign with cosign. Push to private registry with IAM/RBAC auth. Kubernetes pulls via imagePullSecrets or workload identity (EKS pod identity, GKE workload identity). Tag with git SHA; update Deployment via GitOps or kubectl set image triggered by pipeline.",
    command: "# Kaniko in-cluster build\nkubectl apply -f kaniko-job.yaml\nkubectl logs job/kaniko-build -f\n# Scan before deploy\ntrivy image myregistry.io/api:abc123 --severity HIGH,CRITICAL --exit-code 1\ncosign sign --key cosign.key myregistry.io/api:abc123",
    tags: ["kubernetes","docker","medium"],
  },
  {
    id: "kubernetes-kubernetes-architecture-medium-3",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How does the Kubernetes scheduling pipeline work for a new Pod?",
    answer: "After admission and persistence to etcd, the scheduler performs filtering (nodeSelector, affinity, taints/tolerations, resource requests, PVC topology) then scoring (spread, preferred affinity, image locality). It binds the Pod to a node via API patch. The kubelet watches bound pods, pulls images, creates sandbox and containers via CRI, mounts volumes, and starts probes. Failed scheduling shows PodScheduled=False with reason; use describe pod for events.",
    command: "kubectl describe pod pending-pod -n prod | grep -A30 Events\nkubectl get events -n prod --field-selector reason=FailedScheduling\n# Simulate scheduling\nkubectl apply -f pod-with-affinity.yaml\nkubectl get pod pending-pod -o jsonpath='{.status.conditions[?(@.type==\"PodScheduled\")]}' | jq",
    tags: ["kubernetes","kubernetes-architecture","medium"],
  },
  {
    id: "kubernetes-pods-medium-4",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do liveness, readiness, and startup probes differ in production?",
    answer: "Readiness: pod receives Service traffic only when ready — remove from endpoints on failure (graceful during slow start). Liveness: failure restarts container — use carefully to avoid restart storms on slow dependencies. Startup: disables liveness/readiness until app finishes boot (JVM, large cache warm-up). Configure appropriate timeouts, failureThreshold, and httpGet/tcpSocket/exec handlers. Never point liveness at downstream dependencies.",
    command: "kubectl explain pod.spec.containers.livenessProbe\nkubectl get pod -l app=api -n prod -o json | jq '.items[0].spec.containers[0] | {readinessProbe, livenessProbe, startupProbe}'\nkubectl describe pod api-xyz -n prod | grep -A8 \"Liveness\\|Readiness\\|Startup\"",
    tags: ["kubernetes","pods","medium"],
  },
  {
    id: "kubernetes-deployments-medium-5",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement blue-green or canary deployments with Kubernetes Deployments?",
    answer: "Blue-green: two Deployments (api-blue, api-green) behind Service — switch selector or use Argo Rollouts/Flagger for automated traffic split. Canary: separate canary Deployment with fewer replicas; Ingress/service mesh weighted routes (5% → 25% → 100%). Monitor error rate and latency gates. Native Deployment rolling update is canary-lite; for fine traffic control use Istio VirtualService or Argo Rollouts analysis templates.",
    command: "# Argo Rollouts canary\nkubectl argo rollouts get rollout api -n prod\nkubectl argo rollouts promote api -n prod\n# Manual blue-green Service switch\nkubectl patch svc api -n prod -p '{\"spec\":{\"selector\":{\"version\":\"green\"}}}'",
    tags: ["kubernetes","deployments","medium"],
  },
  {
    id: "kubernetes-replicasets-medium-6",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do ReplicaSets handle node failures and pod evictions?",
    answer: "ReplicaSet is level-triggered: it continuously compares desired vs actual replicas. If node fails, pods enter Unknown/Failed; after nodeNotReady timeout (default 5m), pods marked for deletion; ReplicaSet creates replacements on healthy nodes. Faster recovery: reduce pod-eviction-timeout or use PodDisruptionBudgets with minAvailable during voluntary disruptions. DaemonSets differ — one pod per node regardless of ReplicaSet logic.",
    command: "kubectl get pods -n prod -o wide\nkubectl describe node failed-node | grep -A5 Conditions\nkubectl get rs web -n prod -w\nkubectl get events -n prod | grep -i \"node not ready\"",
    tags: ["kubernetes","replicasets","medium"],
  },
  {
    id: "kubernetes-services-medium-7",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement session affinity and external traffic policies in production?",
    answer: "sessionAffinity: ClientIP with timeoutSeconds for sticky sessions when app lacks shared session store — prefer external session store (Redis) instead. externalTrafficPolicy: Local preserves source IP on LoadBalancer/NodePort but may cause uneven load if pods not on all nodes — use topology-aware hints or ensure even pod distribution. Cluster (default) SNATs source IP at node.",
    command: "kubectl patch svc api -n prod -p '{\"spec\":{\"sessionAffinity\":\"ClientIP\",\"sessionAffinityConfig\":{\"clientIP\":{\"timeoutSeconds\":3600}}}}'\nkubectl get svc api -n prod -o jsonpath='{.spec.externalTrafficPolicy}'\nkubectl describe svc api -n prod | grep -A5 \"Session Affinity\"",
    tags: ["kubernetes","services","medium"],
  },
  {
    id: "kubernetes-ingress-medium-8",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement path-based routing and canary traffic with Ingress?",
    answer: "Native Ingress: multiple paths to different Services (/api → api-svc, /web → web-svc). Canary: nginx ingress annotations (canary, canary-weight), Traefik weighted services, or Istio/Gateway API HTTPRoute with weights. Gateway API (HTTPRoute) is the modern replacement with better role separation and traffic splitting.",
    command: "kubectl apply -f - <<EOF\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: api-canary\n  namespace: prod\n  annotations:\n    nginx.ingress.kubernetes.io/canary: \"true\"\n    nginx.ingress.kubernetes.io/canary-weight: \"10\"\nspec:\n  ingressClassName: nginx\n  rules:\n  - host: api.example.com\n    http:\n      paths:\n      - path: /\n        pathType: Prefix\n        backend:\n          service:\n            name: api-canary\n            port:\n              number: 8080\nEOF",
    tags: ["kubernetes","ingress","medium"],
  },
  {
    id: "kubernetes-configmaps-medium-9",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement hot-reload of ConfigMap changes without pod restart?",
    answer: "Mount ConfigMap as volume (not subPath, not env). Application watches file changes (inotify) or polls periodically — Spring @RefreshScope, nginx -s reload, custom SIGHUP handler. For Deployments, add checksum annotation of ConfigMap hash to pod template triggering rolling restart when config changes (reloader/stakater/Reloader operator automates this). Choose based on app reload capability.",
    command: "kubectl patch configmap app-config -n prod --patch '{\"data\":{\"LOG_LEVEL\":\"debug\"}}'\nkubectl exec app-pod -n prod -- ls -la /etc/config/\n# Stakater Reloader\nkubectl apply -f https://raw.githubusercontent.com/stakater/Reloader/master/deployments/kubernetes/reloader.yaml\nkubectl annotate deployment app -n prod reloader.stakater.com/auto=\"true\"",
    tags: ["kubernetes","configmaps","medium"],
  },
  {
    id: "kubernetes-secrets-medium-10",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement External Secrets Operator with AWS Secrets Manager?",
    answer: "Install ESO, create SecretStore/ClusterSecretStore with IAM role (IRSA on EKS). ExternalSecret defines remote key mapping to K8s Secret. ESO syncs on interval; target Secret owned by ESO. Rotate in AWS; ESO updates K8s Secret; Reloader restarts pods. Avoid storing long-lived creds in git; reference AWS ARN only.",
    command: "kubectl apply -f clustersecretstore-aws.yaml\nkubectl apply -f externalsecret-db.yaml\nkubectl get externalsecret -n prod\nkubectl get secret db-creds -n prod -o yaml | grep ownerReferences -A5",
    tags: ["kubernetes","secrets","medium"],
  },
  {
    id: "kubernetes-persistent-volumes-medium-11",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement backup and restore for PersistentVolumes?",
    answer: "Velero for cluster-level PV backup (snapshots via CSI or restic file backup). Cloud-native snapshots (EBS snapshot, GCP disk snapshot) via VolumeSnapshot CRD. Schedule backups, test restores quarterly. For RWO volumes, scale StatefulSet to 0 or use snapshot without quiesce for crash-consistent (prefer app-level backup for DBs). Document RPO/RTO per tier.",
    command: "kubectl get volumesnapshotclass\nvelero backup create prod-daily --include-namespaces prod --snapshot-volumes\nvelero restore create --from-backup prod-daily-20240619\nkubectl get volumesnapshot -n prod",
    tags: ["kubernetes","persistent-volumes","medium"],
  },
  {
    id: "kubernetes-storage-classes-medium-12",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you configure a StorageClass for production EBS gp3 with custom IOPS?",
    answer: "StorageClass parameters: type: gp3, iops: \"6000\", throughput: \"250\" (AWS EBS CSI). encrypted: \"true\", kmsKeyId optional. reclaimPolicy: Delete with backup strategy or Retain for critical data. allowVolumeExpansion: true. volumeBindingMode: WaitForFirstConsumer for multi-AZ clusters.",
    command: "kubectl apply -f - <<EOF\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: gp3-fast\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: gp3\n  iops: \"6000\"\n  throughput: \"250\"\n  encrypted: \"true\"\nvolumeBindingMode: WaitForFirstConsumer\nallowVolumeExpansion: true\nreclaimPolicy: Delete\nEOF",
    tags: ["kubernetes","storage-classes","medium"],
  },
  {
    id: "kubernetes-namespaces-medium-13",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement namespace-per-environment vs namespace-per-team models?",
    answer: "Per-environment: dev/staging/prod namespaces, shared cluster, RBAC separates teams within env. Per-team: team-a-prod, team-b-prod — stronger isolation, more namespaces to manage. Hybrid common: team namespaces within shared prod cluster + NetworkPolicy default-deny. Multi-cluster for hard prod/non-prod separation in regulated industries.",
    command: "kubectl get rolebinding -n prod --all-namespaces\nkubectl get networkpolicy -A\nkubectl auth can-i create deployments --as=system:serviceaccount:team-a:ci -n prod",
    tags: ["kubernetes","namespaces","medium"],
  },
  {
    id: "kubernetes-rbac-medium-14",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement least-privilege RBAC for CI/CD deployment pipelines?",
    answer: "Dedicated ServiceAccount per pipeline. Role with create/update/patch on deployments, services, configmaps in target namespace only — not secrets get unless needed. No cluster-admin. Use impersonation for cross-namespace deploys via CI orchestrator. Rotate SA tokens; prefer projected tokens with audience and expiration. Audit with can-i in pipeline dry-run stage.",
    command: "kubectl apply -f ci-deployer-role.yaml\nkubectl create rolebinding ci-deploy -n prod --role=deployer --serviceaccount=ci:deployer\nkubectl auth can-i delete nodes --as=system:serviceaccount:ci:deployer\nkubectl auth can-i patch deployments --as=system:serviceaccount:ci:deployer -n prod",
    tags: ["kubernetes","rbac","medium"],
  },
  {
    id: "kubernetes-helm-medium-15",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you manage Helm values across dev, staging, and production?",
    answer: "Layered values: values.yaml (defaults), values-staging.yaml, values-prod.yaml overrides. helm upgrade -f values.yaml -f values-prod.yaml (later files override). Or Kustomize helmChartInflationGenerator. CI promotes tested values files through git branches. Never edit live release with helm upgrade --set in prod without git commit.",
    command: "helm upgrade api ./charts/api -n prod \\\n  -f values.yaml -f values-prod.yaml \\\n  --dry-run --debug | less\nhelm diff upgrade api ./charts/api -n prod -f values-prod.yaml\nhelm get values api -n prod -o yaml > live-values.yaml",
    tags: ["kubernetes","helm","medium"],
  },
  {
    id: "kubernetes-monitoring-medium-16",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you implement custom application metrics for HPA and alerting?",
    answer: "Expose /metrics in Prometheus format from app (client_golang). ServiceMonitor selects Service with prometheus.io/scrape annotation or explicit selector. Prometheus adapter registers custom.metrics.k8s.io for HPA external metrics. Alert rules on error rate, latency p99. Validate metric cardinality — avoid unbounded labels.",
    command: "kubectl apply -f servicemonitor-api.yaml\nkubectl get --raw \"/apis/custom.metrics.k8s.io/v1beta1/namespaces/prod/services/*/http_requests_per_second\" | jq\nkubectl get hpa api -n prod -o yaml | grep -A10 metrics\nkubectl get prometheusrules -n monitoring",
    tags: ["kubernetes","monitoring","medium"],
  },
  {
    id: "kubernetes-scaling-medium-17",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you configure HPA with custom metrics from Prometheus?",
    answer: "Install prometheus-adapter mapping Prometheus queries to custom.metrics.k8s.io or external.metrics.k8s.io. HPA spec references metric name and target AverageValue or Value. Example: scale on http_requests_per_second or queue depth. Ensure metric reflects load accurately; tune target based on capacity testing.",
    command: "kubectl apply -f prometheus-adapter-config.yaml\nkubectl get --raw \"/apis/custom.metrics.k8s.io/v1beta1\" | jq .resources\nkubectl apply -f - <<EOF\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: api\n  namespace: prod\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: api\n  minReplicas: 3\n  maxReplicas: 50\n  metrics:\n  - type: Pods\n    pods:\n      metric:\n        name: http_requests_per_second\n      target:\n        type: AverageValue\n        averageValue: \"1000\"\nEOF",
    tags: ["kubernetes","scaling","medium"],
  },
  {
    id: "kubernetes-troubleshooting-medium-18",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "How do you troubleshoot network connectivity between Pods across namespaces?",
    answer: "Verify DNS resolution, Service endpoints, NetworkPolicy rules, and service mesh mTLS policies. Test path: curl from debug pod → ClusterIP → pod IP direct. Check kube-proxy/CNI health. For Ingress issues, test from inside cluster first to isolate L7 vs L4. tcpdump in netshoot on source and destination.",
    command: "kubectl run netshoot -n frontend --rm -it --image=nicolaka/netshoot -- bash\n# nslookup api.backend.svc.cluster.local\n# curl -v telnet://api.backend.svc.cluster.local:8080\nkubectl get networkpolicy -n backend -o yaml\nkubectl get endpoints api -n backend",
    tags: ["kubernetes","troubleshooting","medium"],
  },
  {
    id: "kubernetes-containers-medium-19",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls when running containers as root in Kubernetes?",
    answer: "Root containers can escape via kernel bugs, modify mounted volumes, bind privileged ports, and read host paths if misconfigured. Pitfalls include writable root filesystems, missing readOnlyRootFilesystem, excessive capabilities (NET_RAW, SYS_ADMIN), and hostPath mounts. Production best practice: runAsNonRoot, readOnlyRootFilesystem: true, drop ALL capabilities, use seccomp/AppArmor profiles, and avoid privileged: true unless absolutely required with strict Pod Security Standards enforcement.",
    command: "kubectl auth can-i --list --as=system:serviceaccount:prod:api\nkubectl get pod api-7d4f8 -n prod -o jsonpath='{.spec.securityContext}' | jq\nkubectl get pods -A -o json | jq '.items[] | select(.spec.containers[].securityContext.privileged==true) | .metadata.name'",
    tags: ["kubernetes","containers","medium"],
  },
  {
    id: "kubernetes-docker-medium-20",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with Docker layer caching in Kubernetes deployments?",
    answer: "Developers push :latest assuming K8s pulls fresh content, but imagePullPolicy: IfNotPresent skips re-pull on nodes with cached layers. Mitigation: use unique tags per build (git SHA), set imagePullPolicy: Always for dev, or use image digest in manifest. Another pitfall: COPY . . early in Dockerfile busts cache on any file change — order Dockerfile from least to most frequently changing layers. Stale cached images on nodes cause \"works in CI but not prod\" incidents.",
    command: "kubectl patch deployment api -n prod -p '{\"spec\":{\"template\":{\"spec\":{\"containers\":[{\"name\":\"api\",\"imagePullPolicy\":\"Always\"}]}}}}'\nkubectl rollout restart deployment/api -n prod\nkubectl get pod -o jsonpath='{range .items[*]}{.metadata.name}{\" imagePullPolicy=\"}{.spec.containers[0].imagePullPolicy}{\"\\n\"}{end}'",
    tags: ["kubernetes","docker","medium"],
  },
  {
    id: "kubernetes-kubernetes-architecture-medium-21",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with etcd in production Kubernetes?",
    answer: "etcd is the single source of truth — latency or corruption affects entire cluster. Pitfalls: undersized disks causing slow writes, defragmentation neglected, no regular snapshots/backups, running on oversubscribed control plane nodes, and exceeding recommended 8GB database size without compaction. Monitor etcd_disk_wal_fsync_duration_seconds, leader changes, and db size. Backup with etcdctl snapshot save before upgrades; test restore in staging.",
    command: "# On control plane node\nsudo ETCDCTL_API=3 etcdctl --endpoints=https://127.0.0.1:2379 \\\n  --cacert=/etc/kubernetes/pki/etcd/ca.crt \\\n  --cert=/etc/kubernetes/pki/etcd/server.crt \\\n  --key=/etc/kubernetes/pki/etcd/server.key \\\n  endpoint health\nsudo ETCDCTL_API=3 etcdctl snapshot save /backup/etcd-$(date +%F).db",
    tags: ["kubernetes","kubernetes-architecture","medium"],
  },
  {
    id: "kubernetes-pods-medium-22",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with Pod resource requests and limits?",
    answer: "Setting limits without requests causes unpredictable scheduling. Limits >> requests causes noisy neighbor issues if QoS is Burstable. No limits allows OOM on node affecting other pods. CPU limits cause throttling without OOM — latency spikes. Best practice: set requests to p95 usage, limits to peak for memory (requests=limits for Guaranteed QoS on critical tiers). Use Vertical Pod Autoscaler recommendations in staging.",
    command: "kubectl describe node worker-2 | grep -A15 \"Allocated resources\"\nkubectl get pod api-abc -n prod -o jsonpath='{.status.qosClass}'\nkubectl top pod -n prod --containers | sort -k3 -hr | head -20",
    tags: ["kubernetes","pods","medium"],
  },
  {
    id: "kubernetes-deployments-medium-23",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls during Deployment rollouts in production?",
    answer: "Missing readiness probes → traffic to unready pods. maxUnavailable too high → capacity drop. Config change without image change not triggering rollout (need checksum annotation on configmap). Resource quota blocking new pods. PDB blocking rollout when maxUnavailable conflicts. ImagePullBackOff on new version halting rollout mid-way leaving mixed versions. Always watch rollout status and set progressDeadlineSeconds.",
    command: "kubectl rollout status deployment/api -n prod --timeout=5m\nkubectl get rs -n prod -l app=api\nkubectl describe deployment api -n prod | grep -A5 Conditions\nkubectl get events -n prod --field-selector reason=FailedCreate",
    tags: ["kubernetes","deployments","medium"],
  },
  {
    id: "kubernetes-replicasets-medium-24",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with manual pod deletion and ReplicaSets?",
    answer: "Deleting a pod triggers immediate recreation — useful for restart but confusing if debugging on same pod name. kubectl delete pod without fixing root cause enters CrashLoopBackOff loop. For debugging, scale to 0 first or use --force only in emergencies. Deleting ReplicaSet directly (bypassing Deployment) causes Deployment to recreate it — always scale/delete Deployment instead.",
    command: "kubectl delete pod web-abc -n prod\nkubectl get pods -n prod -l app=web --watch\nkubectl scale deployment web -n prod --replicas=0\nkubectl scale deployment web -n prod --replicas=3",
    tags: ["kubernetes","replicasets","medium"],
  },
  {
    id: "kubernetes-services-medium-25",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with Service selectors and named ports?",
    answer: "Selector mismatch (app: api vs app: api-v2) → empty endpoints → connection refused. Port name typos in Ingress/monitoring break references. targetPort as string referencing container port name is safer than numeric when ports change. Forgetting readiness probe → not-ready pods excluded silently reducing capacity. Always verify endpoints before debugging app code.",
    command: "kubectl get svc api -n prod -o jsonpath='{.spec.selector}'\nkubectl get endpoints api -n prod\nkubectl get pods -n prod -l app=api --show-labels\nkubectl describe svc api -n prod | grep -A10 Endpoints",
    tags: ["kubernetes","services","medium"],
  },
  {
    id: "kubernetes-ingress-medium-26",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with Ingress annotations and controller-specific behavior?",
    answer: "Annotations differ per controller — nginx.ingress.kubernetes.io/* vs alb.ingress.kubernetes.io/*. Copy-paste from docs of wrong controller silently ignored. Large annotation sets not portable. Prefer Gateway API for portability. SSL redirect, body size, timeout annotations must match controller version. Test in staging with same controller version as prod.",
    command: "kubectl get ingress -n prod -o json | jq '.items[].metadata.annotations'\nkubectl exec -n ingress-nginx deploy/ingress-nginx-controller -- nginx -T | grep api.example.com -A20\nhelm list -n ingress-nginx",
    tags: ["kubernetes","ingress","medium"],
  },
  {
    id: "kubernetes-configmaps-medium-27",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls when ConfigMaps are not mounted correctly?",
    answer: "Wrong key name in volume items mapping. ConfigMap in different namespace (must be same ns as pod). Missing volumeMount path. subPath prevents updates. Env var from ConfigMap key missing → container fails CreateContainerConfigError. Deployment pod template not updated after ConfigMap change when using env (stale values until restart). Always kubectl describe pod for mount errors.",
    command: "kubectl describe pod app-pod -n prod | grep -A10 \"Mounts:\\|Volumes:\"\nkubectl get events -n prod | grep -i \"configmap\\|CreateContainerConfig\"\nkubectl get pod app-pod -n prod -o yaml | grep -A20 volumes",
    tags: ["kubernetes","configmaps","medium"],
  },
  {
    id: "kubernetes-secrets-medium-28",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with Secret rotation in running pods?",
    answer: "Mounted Secrets update on disk (~kubelet sync) but apps cache credentials at startup — DB connection pools hold old password after rotation. Mitigation: dual-credential window, app reload on file change, rolling restart after rotation, use dynamic credentials (Vault DB engine). For TLS, ensure apps reload certs or restart via Reloader.",
    command: "kubectl patch secret db-creds -n prod -p '{\"data\":{\"password\":\"'$(echo -n newpass | base64)'\"}}'\nkubectl exec app-pod -n prod -- cat /var/run/secrets/db/password\nkubectl rollout restart deployment/app -n prod\nkubectl logs app-pod -n prod | grep -i \"auth\\|connection refused\"",
    tags: ["kubernetes","secrets","medium"],
  },
  {
    id: "kubernetes-persistent-volumes-medium-29",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with zone-bound PersistentVolumes?",
    answer: "EBS/GCE PD volumes are zone-specific — pod rescheduling to another AZ fails with FailedAttachVolume. Mitigation: topology-aware provisioning, pod affinity to zone, or regional volumes (EFS, Filestore, Ceph). StatefulSet pod-0 in us-east-1a cannot move to 1b without volume migration. Check PV nodeAffinity for allowed topologies.",
    command: "kubectl describe pv pvc-abc -n prod | grep -A10 NodeAffinity\nkubectl get pv -o custom-columns=NAME:.metadata.name,ZONE:.metadata.labels.topology\\.kubernetes\\.io/zone\nkubectl describe pod db-0 -n prod | grep -i \"FailedAttach\\|volume\"",
    tags: ["kubernetes","persistent-volumes","medium"],
  },
  {
    id: "kubernetes-storage-classes-medium-30",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with default StorageClass changes?",
    answer: "Changing default SC does not migrate existing PVCs. New PVCs without storageClassName pick new default — unexpected cost/performance. Removing default breaks apps assuming implicit SC. Document default in runbooks; explicitly set storageClassName in all prod PVCs. Test Helm charts that create PVCs without class specified.",
    command: "kubectl annotate storageclass gp3 storageclass.kubernetes.io/is-default-class=true --overwrite\nkubectl annotate storageclass gp2 storageclass.kubernetes.io/is-default-class=false --overwrite\nkubectl get pvc -A -o json | jq '.items[] | select(.spec.storageClassName == null) | .metadata.name'",
    tags: ["kubernetes","storage-classes","medium"],
  },
  {
    id: "kubernetes-namespaces-medium-31",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with the default namespace?",
    answer: "Teams deploy to default without RBAC scoping — naming collisions, no ResourceQuota, harder multi-tenancy. ServiceAccounts share default SA with overly broad permissions. Production workloads should never use default namespace. Enforce via OPA/Gatekeeper deny deployments to default in prod clusters.",
    command: "kubectl get pods -n default\nkubectl get rolebinding -n default\nkubectl apply -f deny-default-namespace-policy.yaml\nkubectl auth can-i create pods -n default --as=system:serviceaccount:default:default",
    tags: ["kubernetes","namespaces","medium"],
  },
  {
    id: "kubernetes-rbac-medium-32",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with cluster-admin and wildcard RBAC?",
    answer: "Binding cluster-admin to developers or default SA — full cluster compromise if pod escaped. Wildcard verbs/resources in custom roles — audit with rbac-tool. Overly broad get/list secrets cluster-wide. Helm creating cluster-admin bindings. Regular RBAC audit: who can escalate (create rolebindings, bind cluster-admin).",
    command: "kubectl get clusterrolebinding -o json | jq '.items[] | select(.roleRef.name==\"cluster-admin\") | .subjects'\nrbac-tool audit cluster\nkubectl auth can-i create clusterrolebindings --as=developer",
    tags: ["kubernetes","rbac","medium"],
  },
  {
    id: "kubernetes-helm-medium-33",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with Helm hooks and ordering?",
    answer: "Hooks (pre-install, post-upgrade, test) run as Jobs/Pods with hook-weight ordering and hook-delete-policy. Pitfalls: hook Job fails blocking upgrade, hooks not cleaned up filling namespace, hook RBAC missing, pre-upgrade hook downtime if scales to zero. Test hooks in staging; use --no-hooks for emergency bypass only.",
    command: "helm upgrade api ./charts/api -n prod --dry-run | grep -A5 \"helm.sh/hook\"\nkubectl get pods -n prod -l helm.sh/hook\nhelm upgrade api ./charts/api -n prod --no-hooks  # emergency only\nkubectl logs job/api-pre-upgrade -n prod",
    tags: ["kubernetes","helm","medium"],
  },
  {
    id: "kubernetes-monitoring-medium-34",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls with monitoring DaemonSets and node-level metrics?",
    answer: "node-exporter needs hostPath mounts — security consideration. Duplicate metrics if multiple scrapers. Missing tolerations → no metrics from tainted nodes. High cardinality from cAdvisor container labels. Resource limits on monitoring agents causing OOM during spikes. Ensure Prometheus retention and storage sized for cluster growth.",
    command: "kubectl get ds -n monitoring\nkubectl describe ds node-exporter -n monitoring | grep -A10 Tolerations\nkubectl top pods -n monitoring\nkubectl get prometheus -n monitoring -o jsonpath='{.items[0].spec.retention}'",
    tags: ["kubernetes","monitoring","medium"],
  },
  {
    id: "kubernetes-scaling-medium-35",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls when HPA does not scale as expected?",
    answer: "Missing metrics-server or adapter. CPU target with missing resource requests (HPA needs requests set). Cooldown/stabilization windows delaying scale-down. maxReplicas too low. Metrics lag. PDB blocking scale-down. VPA conflict. Custom metric returning stale data. Pods Pending due to quota not CPU — HPA keeps adding. Always describe hpa for conditions.",
    command: "kubectl describe hpa api -n prod\nkubectl get apiservice v1beta1.metrics.k8s.io\nkubectl get pods -n prod -l app=api -o json | jq '.items[0].spec.containers[0].resources'\nkubectl get hpa api -n prod -o yaml | grep -A20 behavior",
    tags: ["kubernetes","scaling","medium"],
  },
  {
    id: "kubernetes-troubleshooting-medium-36",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "What are common pitfalls when using kubectl describe vs logs vs exec?",
    answer: "describe Events rotate quickly — capture early. logs missing if container never started (check describe). exec into CrashLoopBackOff pod may fail if crash is instant — use --previous logs. describe shows probe failures but not app stack traces. Multiple containers require -c flag. RBAC may block exec but allow logs.",
    command: "kubectl auth can-i exec pods --as=developer -n prod\nkubectl auth can-i get logs --as=developer -n prod\nkubectl describe pod multi -n prod | grep -A3 \"Containers:\"\nkubectl logs multi -n prod -c sidecar --previous",
    tags: ["kubernetes","troubleshooting","medium"],
  },
  {
    id: "kubernetes-containers-medium-37",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare container runtimes used in production Kubernetes clusters.",
    answer: "containerd is the default CRI runtime used by most managed K8s (EKS, GKE, AKS). CRI-O is common in OpenShift/RHEL environments — lightweight, OCI-native. Both implement the CRI gRPC API for kubelet. dockerd (legacy dockershim) is deprecated. For sandboxing, gVisor and Kata Containers add kernel-level isolation at latency cost. Choose based on compliance needs, node OS, and operational familiarity; containerd offers the broadest ecosystem support.",
    command: "# Verify runtime on nodes\nkubectl get nodes -o wide\nkubectl describe node worker-1 | grep -i \"container runtime\"\nsudo crictl info | jq '.runtimeHandlers'\n# EKS: kubectl get nodeclaim or describe node for AMI/runtime version",
    tags: ["kubernetes","containers","medium"],
  },
  {
    id: "kubernetes-docker-medium-38",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare Docker volume strategies with Kubernetes volume types.",
    answer: "Docker named volumes and bind mounts are node-local and not portable across hosts. Kubernetes abstracts storage via PV/PVC, ConfigMaps/Secrets as volumes, emptyDir for ephemeral scratch, and CSI drivers for cloud/network storage shared across nodes. StatefulSets use volumeClaimTemplates for stable per-pod storage. For logs, prefer stdout/stderr (collected by agents) over Docker log files on disk.",
    command: "kubectl explain pod.spec.volumes\nkubectl get pvc -n prod\nkubectl describe pod db-0 -n prod | grep -A15 \"Mounts:\"\n# Docker equivalent is node-local only:\n# docker volume create data && docker run -v data:/var/lib/mysql mysql:8",
    tags: ["kubernetes","docker","medium"],
  },
  {
    id: "kubernetes-kubernetes-architecture-medium-39",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare stacked etcd vs external etcd topologies.",
    answer: "Stacked: etcd runs on same nodes as control plane components — simpler for kubeadm/small clusters, but failure correlation if node dies. External: dedicated etcd cluster (3 or 5 nodes) — better isolation, required for large clusters and some enterprise setups. Managed K8s (EKS/GKE/AKS) hides etcd entirely with SLA-backed backups. Choose external when control plane scaling or etcd I/O isolation is critical.",
    command: "kubectl -n kube-system get pods -l component=etcd\n# kubeadm: cat /etc/kubernetes/manifests/etcd.yaml\n# External etcd health from admin host\nETCDCTL_API=3 etcdctl --endpoints=https://etcd-1:2379,https://etcd-2:2379,https://etcd-3:2379 endpoint status -w table",
    tags: ["kubernetes","kubernetes-architecture","medium"],
  },
  {
    id: "kubernetes-pods-medium-40",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare sidecar vs init container patterns at scale.",
    answer: "Init: sequential, terminates before app starts — good for one-time setup. Sidecar: runs alongside app for entire pod life — logging (Fluent Bit), service mesh (Envoy), config reload. Pitfall: sidecars inflate resource accounting and complicate termination (preStop ordering). Kubernetes 1.28+ native sidecar containers (restartPolicy: Always, initContainers with sidecar flag) improve lifecycle. Monitor sidecar CPU/memory separately.",
    command: "kubectl get pod -l app=orders -n prod -o jsonpath='{range .items[*]}{.metadata.name}{\": \"}{range .spec.containers[*]}{.name}{\" \"}{end}{\"\\n\"}{end}'\nkubectl logs orders-pod -c istio-proxy -n prod --tail=20\nkubectl exec orders-pod -n prod -c app -- curl -s localhost:15000/stats",
    tags: ["kubernetes","pods","medium"],
  },
  {
    id: "kubernetes-deployments-medium-41",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare Deployment vs StatefulSet for application workloads.",
    answer: "Deployment: stateless, random pod names, interchangeable replicas, RollingUpdate default — web APIs, workers. StatefulSet: stable network ID (pod-0, pod-1), ordered deploy/scale, per-pod PVCs — databases, Kafka, ZooKeeper. Do not run primary databases in Deployment unless using external managed DB. StatefulSet requires headless Service for stable DNS.",
    command: "kubectl explain statefulset.spec\nkubectl get statefulset,deployment -n prod\nkubectl get pod db-0 -n prod -o jsonpath='{.metadata.name}{\" \"}{.spec.hostname}{\"\\n\"}'\ndig db-0.db.prod.svc.cluster.local",
    tags: ["kubernetes","deployments","medium"],
  },
  {
    id: "kubernetes-replicasets-medium-42",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare ReplicaSet scaling with HorizontalPodAutoscaler.",
    answer: "ReplicaSet/Deployment spec.replicas is the desired count set manually or by HPA. HPA watches metrics (CPU, memory, custom) and patches Deployment replicas every sync period (default 15s). HPA respects min/max bounds. Without HPA, manual scaling or cluster autoscaler only adds nodes not pods. Combine HPA (pod count) with Cluster Autoscaler (node count) for full elasticity.",
    command: "kubectl autoscale deployment web -n prod --min=3 --max=20 --cpu-percent=75\nkubectl get hpa web -n prod -w\nkubectl describe hpa web -n prod | grep -A10 Conditions",
    tags: ["kubernetes","replicasets","medium"],
  },
  {
    id: "kubernetes-services-medium-43",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare ClusterIP Service discovery with environment variables vs DNS.",
    answer: "Legacy: Kubernetes injects env vars (API_SERVICE_HOST/PORT) for Services existing at pod creation — does not update dynamically, not recommended. DNS (CoreDNS): always current, standard approach — short name within namespace, FQDN cross-namespace. CoreDNS stub domains and ndots affect resolution. Debug with nslookup/dig from debug pod.",
    command: "kubectl run debug --rm -it --image=busybox:1.36 -n prod -- sh\n# nslookup api\n# nslookup api.default.svc.cluster.local\nkubectl get pods -n kube-system -l k8s-app=kube-dns\nkubectl logs -n kube-system -l k8s-app=kube-dns --tail=30",
    tags: ["kubernetes","services","medium"],
  },
  {
    id: "kubernetes-ingress-medium-44",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare Ingress vs Gateway API for new projects.",
    answer: "Ingress: mature, wide support, annotation-heavy, limited TCP/UDP, no role separation. Gateway API: expressive (HTTPRoute, GRPCRoute, TCPRoute), role-oriented (platform vs app teams), standard traffic splitting, better extensibility. Migration path: install Gateway controller (nginx, Istio, Cilium), create Gateway (infra) + HTTPRoute (app teams).",
    command: "kubectl get gatewayclass\nkubectl get gateway -A\nkubectl get httproute -n prod\nkubectl describe httproute api-route -n prod",
    tags: ["kubernetes","ingress","medium"],
  },
  {
    id: "kubernetes-configmaps-medium-45",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare ConfigMaps with external configuration stores (Consul, Vault, AWS AppConfig).",
    answer: "ConfigMaps: native, GitOps-friendly, no extra infra, limited size, no encryption at rest by default. External stores: dynamic config, audit, encryption, fine-grained ACL — add dependency and latency. Hybrid: bootstrap from ConfigMap, runtime from external via sidecar (consul-template) or CSI Secret Store driver. Choose native for static config; external for frequently changing or compliance-heavy config.",
    command: "kubectl get pods -n prod -l app=vault-agent\nkubectl exec app-pod -n prod -c vault-agent -- cat /vault/secrets/config\n# CSI: kubectl get secretproviderclass\nhelm list -n kube-system | grep secrets-store",
    tags: ["kubernetes","configmaps","medium"],
  },
  {
    id: "kubernetes-secrets-medium-46",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare Sealed Secrets vs External Secrets vs Vault Agent sidecar.",
    answer: "Sealed Secrets: encrypt Secret manifest for git storage, cluster-specific sealing key, good for GitOps static secrets. External Secrets: pull from cloud SM at runtime, central rotation. Vault Agent: dynamic secrets, sidecar injects files, best for short-lived creds, more operational complexity. Choose based on secret lifecycle and compliance requirements.",
    command: "kubeseal --format yaml < secret.yaml > sealedsecret.yaml\nkubectl apply -f sealedsecret.yaml\nkubectl get sealedsecret -n prod\nkubectl get secret -n prod -l sealedsecrets.bitnami.com/sealed-secrets-manager",
    tags: ["kubernetes","secrets","medium"],
  },
  {
    id: "kubernetes-persistent-volumes-medium-47",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare block storage (RWO) vs shared filesystem (RWX) for Kubernetes workloads.",
    answer: "RWO block (EBS, PD): high IOPS, single writer, databases, etcd. RWX NFS/EFS: multiple pods read-write same data, content management, shared config — lower IOPS, higher latency. ROX for read-heavy shared data. Match access mode to app architecture; do not force RWX on block-only storage classes.",
    command: "kubectl get storageclass\nkubectl get pvc -n prod -o custom-columns=NAME:.metadata.name,ACCESS:.spec.accessModes,CLASS:.spec.storageClassName\nkubectl describe sc gp3 | grep -A5 Parameters",
    tags: ["kubernetes","persistent-volumes","medium"],
  },
  {
    id: "kubernetes-storage-classes-medium-48",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare local storage vs network-attached StorageClasses.",
    answer: "Local PV (local-ssd): lowest latency, tied to specific node — pod cannot reschedule without data loss unless replicated app-side. Network (EBS/NFS): portable across nodes in zone, slightly higher latency. Use local for high-perf caching layers with replication (Cassandra, Kafka); network for general persistence and databases with external backup.",
    command: "kubectl get pv -l type=local\nkubectl describe pod latency-app -n prod | grep -A5 \"Node-Selectors\\|Tolerations\"\nkubectl get storageclass local-ssd -o yaml",
    tags: ["kubernetes","storage-classes","medium"],
  },
  {
    id: "kubernetes-namespaces-medium-49",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare namespace termination and finalizers behavior.",
    answer: "Deleting namespace sets phase Terminating; API server removes resources in namespace. Stuck Terminating: resources with finalizers (custom operators, PV Retain) block completion. Debug: kubectl get namespace ns -o json | jq .spec.finalizers, find remaining resources kubectl api-resources --verbs=list --namespaced -o name | xargs -n1 kubectl get -n ns. Remove finalizers only as last resort.",
    command: "kubectl delete namespace stuck-ns\nkubectl get namespace stuck-ns -o json | jq .status\nkubectl get all -n stuck-ns\nkubectl api-resources --verbs=list --namespaced -o name | xargs -n1 -I{} sh -c 'kubectl get {} -n stuck-ns 2>/dev/null | tail -n +2 | grep . && echo {}'",
    tags: ["kubernetes","namespaces","medium"],
  },
  {
    id: "kubernetes-rbac-medium-50",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "Compare user authentication methods: certs, OIDC, and cloud IAM.",
    answer: "X509 client certs: kubeadm default, hard to rotate at scale. OIDC: integrate corporate IdP (Okta, Azure AD) via --oidc-* apiserver flags, groups claim maps to RBAC. Cloud: aws eks get-token, gcloud container clusters get-credentials — IAM to RBAC via aws-auth ConfigMap or EKS access entries. Prefer OIDC/IAM over static certs.",
    command: "kubectl config view --raw | grep -A5 users\nkubectl get configmap aws-auth -n kube-system -o yaml  # EKS\naws eks create-access-entry --cluster-name prod --principal-arn arn:aws:iam::123:role/DevOps",
    tags: ["kubernetes","rbac","medium"],
  },
  {
    id: "kubernetes-containers-hard-1",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a highly available containerized stateless API on Kubernetes with zero-downtime deploys.",
    answer: "Deploy behind a Deployment with minReadySeconds, readiness probes gating Service endpoints, and PodDisruptionBudgets (minAvailable: 80%). Use rollingUpdate maxSurge: 25%, maxUnavailable: 0. Spread across zones via topologySpreadConstraints and podAntiAffinity. HorizontalPodAutoscaler on CPU and custom latency metrics. Ingress with health-checked backends. PreStop hook with sleep for connection draining. Image pinned by digest; GitOps (Argo CD/Flux) for declarative rollouts with automated rollback on failed readiness.",
    command: "kubectl apply -f - <<EOF\napiVersion: policy/v1\nkind: PodDisruptionBudget\nmetadata:\n  name: api-pdb\n  namespace: prod\nspec:\n  minAvailable: 80%\n  selector:\n    matchLabels:\n      app: api\nEOF\nkubectl rollout status deployment/api -n prod\nkubectl get pdb -n prod",
    tags: ["kubernetes","containers","hard"],
  },
  {
    id: "kubernetes-docker-hard-2",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a secure container supply chain from Docker build to Kubernetes deployment.",
    answer: "Pipeline: lint Dockerfile (hadolint) → BuildKit build with pinned bases → SBOM (syft) → scan (Trivy) → sign (cosign keyless via OIDC) → push to registry with immutable tags → admission controller (Kyverno/Cosign) verifies signature at deploy → deploy via GitOps. Runtime: Pod Security restricted, read-only root FS, NetworkPolicy default-deny. Audit trail links git commit → image digest → deployed ReplicaSet revision.",
    command: "# Verify signature at deploy time (Kyverno policy excerpt)\n# kubectl apply -f verify-image-signature-policy.yaml\ncosign verify --certificate-identity=... --certificate-oidc-issuer=... myregistry.io/api:sha-abc123\nkubectl annotate deployment api -n prod image.digest=sha256:abc123 --overwrite",
    tags: ["kubernetes","docker","hard"],
  },
  {
    id: "kubernetes-kubernetes-architecture-hard-3",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a highly available multi-AZ Kubernetes cluster architecture for a financial services workload.",
    answer: "3+ control plane nodes across AZs (or managed control plane with 99.95% SLA). Worker node pools per AZ with topologySpreadConstraints on critical apps. External etcd or managed backend with cross-AZ replication. Ingress/load balancer multi-AZ. PodDisruptionBudgets, anti-affinity for stateless tiers, StatefulSets with zone-aware PVCs. NetworkPolicies default-deny. Separate etcd/control plane from worker blast radius. DR: etcd snapshots + Velero cluster backups, RTO/RPO defined, quarterly restore drills.",
    command: "kubectl get nodes -L topology.kubernetes.io/zone\nkubectl get pdb -A\nkubectl apply -f - <<EOF\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: payments\n  namespace: prod\nspec:\n  replicas: 6\n  template:\n    spec:\n      topologySpreadConstraints:\n      - maxSkew: 1\n        topologyKey: topology.kubernetes.io/zone\n        whenUnsatisfiable: DoNotSchedule\n        labelSelector:\n          matchLabels:\n            app: payments\nEOF",
    tags: ["kubernetes","kubernetes-architecture","hard"],
  },
  {
    id: "kubernetes-pods-hard-4",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a Pod spec for a latency-sensitive Java service with graceful shutdown.",
    answer: "Guaranteed QoS: requests=limits for CPU/memory. startupProbe with long failureThreshold for JVM boot. readinessProbe on /ready after warmup. livenessProbe on lightweight /health. terminationGracePeriodSeconds: 60 with preStop hook sleeping 15s for LB deregistration. lifecycle.preStop: exec sleep. PodDisruptionBudget minAvailable. Spread across nodes/AZs. Avoid CPU limits if latency-critical (use dedicated nodes instead).",
    command: "kubectl apply -f java-deployment.yaml\nkubectl delete pod -l app=payments -n prod --grace-period=60\nkubectl get events -n prod --field-selector involvedObject.name=payments-abc | grep -i Killing",
    tags: ["kubernetes","pods","hard"],
  },
  {
    id: "kubernetes-deployments-hard-5",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a zero-downtime global Deployment strategy across multiple clusters.",
    answer: "GitOps with Argo CD ApplicationSet per cluster/region. Sequential promotion: canary cluster → 10% traffic regions → full. Each cluster: Deployment with maxUnavailable 0, PDB, HPA headroom. Global load balancer health checks per region. Feature flags decouple code deploy from exposure. Automated rollback on cross-cluster SLO breach via Argo Rollouts or custom pipeline. Consistent image digest across clusters; config via Kustomize overlays per region.",
    command: "argocd app list | grep api\nkubectl config use-context prod-us-east\nkubectl rollout status deployment/api -n prod\nkubectl config use-context prod-eu-west\nkubectl get deployment api -n prod -o jsonpath='{.spec.template.spec.containers[0].image}'",
    tags: ["kubernetes","deployments","hard"],
  },
  {
    id: "kubernetes-replicasets-hard-6",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a multi-tier application with independent ReplicaSet scaling per tier.",
    answer: "Separate Deployments per tier (frontend, api, worker) with independent HPAs — frontend scales on RPS via custom metrics, api on CPU, worker on queue depth (KEDA). Each Deployment owns its ReplicaSet. NetworkPolicies enforce tier boundaries. Shared config via ConfigMaps. Avoid single Deployment with mixed containers — prevents independent scaling and complicates rollouts.",
    command: "kubectl apply -f frontend-deployment.yaml -f api-deployment.yaml -f worker-deployment.yaml\nkubectl get hpa -n prod\nkubectl get deploy -n prod\n# KEDA ScaledObject for worker\nkubectl get scaledobject -n prod",
    tags: ["kubernetes","replicasets","hard"],
  },
  {
    id: "kubernetes-services-hard-7",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a multi-cluster Service discovery strategy for disaster recovery.",
    answer: "Options: global load balancer with health checks per cluster, Submariner/Liqo multi-cluster Services, service mesh (Istio multi-primary), or DNS failover (Route53). Active-active requires data layer replication and conflict handling. Active-passive simpler: standby cluster warm with scaled-to-zero or minimal replicas, failover updates DNS/LB. Avoid split-brain with leader election at data tier.",
    command: "# Submariner export\nsubctl export service api -n prod\nsubctl show connections\n# Route53 failover\naws route53 change-resource-record-sets --hosted-zone-id Z123 --change-batch file://failover.json",
    tags: ["kubernetes","services","hard"],
  },
  {
    id: "kubernetes-ingress-hard-8",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a highly available Ingress architecture handling 100k RPS.",
    answer: "Multiple controller replicas with PodAntiAffinity across AZs, HPA on CPU/connections. External LB (NLB/GLB) with cross-zone load balancing. Separate internal/external Ingress tiers. WAF at edge (Cloudflare, AWS WAF). TLS at LB or controller with session ticket optimization. Backend keepalive, appropriate worker_connections. Rate limiting annotations or WAF rules. CDN for static assets.",
    command: "kubectl get deploy -n ingress-nginx -o wide\nkubectl get hpa -n ingress-nginx\nkubectl describe svc ingress-nginx-controller -n ingress-nginx | grep -A5 \"Load Balancer\"\n# NLB cross-zone\nkubectl annotate svc ingress-nginx-controller -n ingress-nginx service.beta.kubernetes.io/aws-load-balancer-cross-zone-load-balancing-enabled=true",
    tags: ["kubernetes","ingress","hard"],
  },
  {
    id: "kubernetes-configmaps-hard-9",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a configuration management strategy for 100 microservices across environments.",
    answer: "Kustomize overlays per env (base + dev/staging/prod patches). ConfigMaps generated from env files in git. Sealed Secrets or External Secrets for sensitive overlap. Reloader triggers rollouts on config change. Naming convention: {service}-config-{env}. CI validates config schema (JSON Schema/cue). No manual kubectl edit in prod — GitOps only. Feature flags in dedicated service (LaunchDarkly) for dynamic toggles.",
    command: "kustomize build overlays/prod | kubectl apply --dry-run=client -f -\nkubectl get configmap -n prod -l app.kubernetes.io/part-of=platform\nargocd app sync prod-services --dry-run\nkubectl diff -k overlays/prod",
    tags: ["kubernetes","configmaps","hard"],
  },
  {
    id: "kubernetes-secrets-hard-10",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a zero-trust secrets architecture for a multi-tenant Kubernetes platform.",
    answer: "Per-tenant namespace isolation with NetworkPolicy. External Secrets from tenant-specific vault paths. IRSA/workload identity — no static cloud keys. Encryption at rest with per-cluster KMS. Secret rotation automation with Reloader. Admission policy denying Opaque secrets without external-secrets owner. Audit all secret reads. Break-glass access via PAM with MFA and session recording.",
    command: "kubectl get namespaces -L tenant\nkubectl get networkpolicy -A\nkubectl apply -f deny-manual-secrets-policy.yaml\naws kms describe-key --key-id alias/k8s-etcd-encryption",
    tags: ["kubernetes","secrets","hard"],
  },
  {
    id: "kubernetes-persistent-volumes-hard-11",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design storage architecture for a PostgreSQL HA cluster on Kubernetes.",
    answer: "Prefer managed RDS/Cloud SQL for production Postgres; if in-cluster: CloudNativePG or Crunchy Operator with RWO gp3/io2, synchronous replication, Velero backups, PDB. Separate WAL and data volumes. node affinity for IO-heavy nodes. Monitor disk latency and replication lag. Avoid NFS for Postgres data dir. Test failover and restore regularly.",
    command: "kubectl get cluster -n prod  # CloudNativePG\nkubectl cnpg status pg-prod -n prod\nkubectl get pvc -n prod -l cnpg.io/cluster=pg-prod\nvelero schedule create pg-backup --schedule=\"0 2 * * *\" --include-namespaces prod",
    tags: ["kubernetes","persistent-volumes","hard"],
  },
  {
    id: "kubernetes-storage-classes-hard-12",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a multi-tenant StorageClass strategy with quotas and isolation.",
    answer: "Per-tenant StorageClasses with different parameters and allowed topologies. ResourceQuota limits PVC count and total storage per namespace. RBAC restricts SC usage via admission (only platform SCs allowed). Separate cloud accounts/subscriptions for large tenants. Encrypt with tenant-specific KMS keys. Chargeback via labels on PVCs.",
    command: "kubectl apply -f - <<EOF\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: storage-quota\n  namespace: tenant-a\nspec:\n  hard:\n    persistentvolumeclaims: \"20\"\n    requests.storage: 500Gi\nEOF\nkubectl describe resourcequota storage-quota -n tenant-a",
    tags: ["kubernetes","storage-classes","hard"],
  },
  {
    id: "kubernetes-namespaces-hard-13",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a multi-tenant namespace architecture for a shared Kubernetes platform.",
    answer: "Namespace per tenant with: ResourceQuota, LimitRange, NetworkPolicy default-deny, dedicated ServiceAccount, Pod Security restricted, separate Ingress subdomain, External Secrets per tenant, HPA limits in quota. Platform namespace for shared ingress controller, monitoring agents. Admission webhook validates required labels/annotations on namespace create. No cluster-admin for tenants.",
    command: "kubectl get ns -l type=tenant\nkubectl describe resourcequota -n tenant-a\nkubectl get networkpolicy -n tenant-a\nkubectl get rolebinding -n tenant-a",
    tags: ["kubernetes","namespaces","hard"],
  },
  {
    id: "kubernetes-rbac-hard-14",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design RBAC model for platform team, app teams, and read-only auditors.",
    answer: "ClusterRoles: platform-admin (nodes, PV, CRDs), app-admin (all namespaced resources in owned namespaces via RoleBinding per ns), developer (get/list/watch, port-forward), auditor (get/list cluster-wide, no secrets). ClusterRoleBinding for platform/auditor; RoleBinding per namespace for app teams. Automate via Terraform/Helm on namespace onboarding. No shared cluster-admin.",
    command: "kubectl get clusterrole platform-admin -o yaml\nkubectl get rolebinding -n team-checkout\nkubectl auth can-i list nodes --as=auditor@corp.com\nkubectl auth can-i get secrets --as=auditor@corp.com -n prod",
    tags: ["kubernetes","rbac","hard"],
  },
  {
    id: "kubernetes-helm-hard-15",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a GitOps workflow with Helm charts and Argo CD.",
    answer: "Charts in git repo; environment values in overlays. Argo CD Application points to chart path + valueFiles. Sync policy: manual for prod, automated for dev. helm dependency update in CI. Chart version pinned; appVersion tracks image. Pre-sync hooks for DB migration Jobs. Rollback via Argo CD history or git revert + sync.",
    command: "argocd app create api-prod --repo https://git.example.com/charts --path api --dest-namespace prod --helm-set-file values=values-prod.yaml\nargocd app sync api-prod\nargocd app history api-prod\nhelm dependency update ./charts/api",
    tags: ["kubernetes","helm","hard"],
  },
  {
    id: "kubernetes-monitoring-hard-16",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design an observability platform for a 500-node multi-tenant Kubernetes cluster.",
    answer: "Tiered: metrics (Prometheus federation or Thanos/Mimir for long-term), logs (Loki with tenant labels), traces (Tempo/Jaeger via OpenTelemetry Collector DaemonSet). Per-tenant billing via label cardinality controls. HA Prometheus pairs per region, Alertmanager clustering. Grafana with RBAC orgs per team. SLO dashboards per service. On-call rotation integrated with Alertmanager.",
    command: "helm install thanos bitnami/thanos -n monitoring -f thanos-values.yaml\nkubectl get pods -n monitoring -l app.kubernetes.io/name=otel-collector\nkubectl apply -f opentelemetry-instrumentation.yaml\nkubectl get servicemonitor -A | wc -l",
    tags: ["kubernetes","monitoring","hard"],
  },
  {
    id: "kubernetes-scaling-hard-17",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design autoscaling strategy for a flash-sale e-commerce platform on Kubernetes.",
    answer: "Pre-warm: scale Deployment minReplicas 2h before event via CronJob/KEDA cron trigger. HPA on RPS custom metric + CPU with aggressive scale-up (0s stabilization), conservative scale-down (300s). Over-provisioned node pool or Karpenter NodePool with high limits. CDN for static. PDB minAvailable 80%. Load test to calibrate targets. Cluster Autoscaler/Karpenter headroom. Circuit breakers in app.",
    command: "kubectl patch hpa api -n prod --type=merge -p '{\"spec\":{\"behavior\":{\"scaleUp\":{\"stabilizationWindowSeconds\":0,\"policies\":[{\"type\":\"Percent\",\"value\":100,\"periodSeconds\":15}]}}}}'\nkubectl apply -f keda-scaledobject-cron.yaml\nkubectl get nodes -l node.kubernetes.io/instance-type\nkubectl top pods -n prod",
    tags: ["kubernetes","scaling","hard"],
  },
  {
    id: "kubernetes-troubleshooting-hard-18",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Design a production troubleshooting runbook framework for Kubernetes on-call.",
    answer: "Tiered runbooks per alert with: symptom, blast radius checklist, diagnostic commands, mitigation steps, escalation path, comms template. Integrate runbook links in Alertmanager annotations. Post-incident: update runbook. Categories: pod lifecycle, networking, storage, control plane, security. Monthly game days validate runbooks. Avoid single-person tribal knowledge.",
    command: "# Runbook example commands section:\nkubectl get pods -n $NS -l app=$APP\nkubectl describe pod $POD -n $NS\nkubectl logs $POD -n $NS --previous\n# Link: https://runbooks.example.com/k8s/crashloop",
    tags: ["kubernetes","troubleshooting","hard"],
  },
  {
    id: "kubernetes-containers-hard-19",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize container image pull performance during peak autoscaling?",
    answer: "Reduce image size (multi-stage, distroless), use regional registry mirrors or pull-through caches (Harbor, ECR pull-through, GCR remote repos), pre-pull images via DaemonSet or node image cache on warm node pools, and set imagePullPolicy: IfNotPresent for tagged releases. For bursty workloads, maintain over-provisioned node pools or use Karpenter/Cluster Autoscaler with pre-warmed AMIs containing common base layers. Monitor image pull duration via kubelet metrics and kube_pod_container_status_waiting_reason{reason=\"ImagePullBackOff\"}.",
    command: "# Pre-pull critical images on all nodes\nkubectl apply -f image-prepull-daemonset.yaml\n# Check pull backoff\nkubectl get pods -n prod | grep -E \"ImagePull|ErrImage\"\nkubectl describe pod checkout-abc -n prod | grep -A10 Events\n# ECR: aws ecr create-pull-through-cache-rule --upstream-registry-url public.ecr.aws",
    tags: ["kubernetes","containers","hard"],
  },
  {
    id: "kubernetes-docker-hard-20",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize Docker-to-Kubernetes workflow for a monorepo with 50 microservices?",
    answer: "Use Bazel or buildx bake for selective builds based on changed paths. Shared base images cached in registry. Matrix CI builds only affected services. Helm umbrella chart or individual charts per service with shared library templates. Remote cache (ECR/GCR) and parallel kaniko jobs in K8s CI namespace. Argo CD ApplicationSet for per-service sync. Standardize Dockerfile patterns, health probes, and resource templates to reduce drift across 50 services.",
    command: "# Build only changed services\ngit diff --name-only HEAD~1 | grep ^services/ | cut -d/ -f2 | sort -u > changed.txt\nxargs -a changed.txt -I{} docker buildx build --cache-from type=registry,ref=myregistry.io/{}:cache --push -t myregistry.io/{}:${GIT_SHA} services/{}\nargocd app sync $(cat changed.txt | sed 's/.*/&-prod/')",
    tags: ["kubernetes","docker","hard"],
  },
  {
    id: "kubernetes-kubernetes-architecture-hard-21",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize API server performance under 10,000+ Pod churn per hour?",
    answer: "Increase apiserver --max-requests-inflight, tune etcd quota and defrag schedule, use API Priority and Fairness (APF) flowschemas for critical controllers. Reduce list/watch pressure: avoid cluster-wide unfiltered watches in controllers, use informer resync wisely, shard operators by namespace. Tune kube-controller-manager QPS/burst. Consider event aggregation, limit kubectl get pods -A in CI. Monitor etcd db size and apiserver watch count; horizontal apiserver scaling where supported.",
    command: "kubectl get flowschema -o wide\nkubectl get prioritylevelconfiguration\nkubectl get --raw /metrics | grep etcd_db_total_size_in_bytes\nkubectl top pods -n kube-system | grep -E \"kube-apiserver|etcd\"",
    tags: ["kubernetes","kubernetes-architecture","hard"],
  },
  {
    id: "kubernetes-pods-hard-22",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize pod density on cost-constrained node pools?",
    answer: "Right-size requests using VPA/historical metrics. Use LimitRanges to cap per-pod max. Bin-pack with descheduler for rebalancing. Separate bursty workloads from steady-state via taints/tolerations and dedicated pools. Enable CPU manager static policy for pinned workloads if needed. Monitor node allocatable vs requested; target 70-80% utilization with headroom for bursts and system pods.",
    command: "kubectl describe nodes | grep -E \"Name:|cpu|memory\" -A2\nkubectl get pods -A -o json | jq '[.items[].spec.containers[].resources.requests.cpu // \"0\"] | length'\nkubectl top nodes\n# Descheduler: kubectl apply -f descheduler-policy-configmap.yaml",
    tags: ["kubernetes","pods","hard"],
  },
  {
    id: "kubernetes-deployments-hard-23",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize Deployment rollout speed for a 200-replica service?",
    answer: "Increase maxSurge (50%) for faster parallel pod creation if cluster capacity allows. Pre-warm nodes via over-provisioned node pool or Karpenter. Reduce image size and use regional registry. readinessProbe initialDelaySeconds tuned to actual startup p99. Pod topology spread may slow scheduling — relax for rollout window. progressDeadlineSeconds aligned with expected duration. Consider surge node pool that scales during deploys.",
    command: "kubectl patch deployment api -n prod -p '{\"spec\":{\"strategy\":{\"rollingUpdate\":{\"maxSurge\":\"50%\",\"maxUnavailable\":\"5%\"}}}}'\nkubectl get pods -n prod -l app=api --no-headers | wc -l\nkubectl top nodes\nkubectl get events -n prod --sort-by=.lastTimestamp | grep -i \"SuccessfulCreate\" | tail -10",
    tags: ["kubernetes","deployments","hard"],
  },
  {
    id: "kubernetes-replicasets-hard-24",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize ReplicaSet controller behavior during large-scale node loss?",
    answer: "Sudden loss of many nodes causes simultaneous pod recreation — API server thundering herd. Mitigation: pod topology spread avoids single-AZ concentration; over-provision nodes; rate-limit via multiple smaller Deployments; priority classes ensure critical pods schedule first. Cluster Autoscaler adds nodes but lag minutes — maintain buffer capacity. Monitor scheduling queue depth and pending pod count.",
    command: "kubectl get pods -A --field-selector status.phase=Pending | wc -l\nkubectl get events -A --field-selector reason=FailedScheduling | tail -20\nkubectl describe node | grep -E \"Name:|Allocatable\" -A3",
    tags: ["kubernetes","replicasets","hard"],
  },
  {
    id: "kubernetes-services-hard-25",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize Service load balancing for gRPC long-lived connections?",
    answer: "kube-proxy round-robin does not rebalance existing TCP/gRPC connections — leads to hot pods. Solutions: client-side load balancing with headless Service + gRPC resolver, service mesh (Envoy LB), or proxy layer (nginx with upstream keepalive and least_conn). Set appropriate connection limits and HPA on custom metrics (active streams).",
    command: "kubectl get svc api -n prod -o yaml | grep clusterIP\n# Headless for gRPC\nkubectl patch svc api -n prod -p '{\"spec\":{\"clusterIP\":\"None\"}}'\n# Istio DestinationRule\nkubectl apply -f api-destinationrule.yaml",
    tags: ["kubernetes","services","hard"],
  },
  {
    id: "kubernetes-ingress-hard-26",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you implement automatic TLS certificate rotation with cert-manager?",
    answer: "Install cert-manager, create ClusterIssuer (Let's Encrypt prod with DNS-01 for wildcards or HTTP-01 for host routes). Certificate resource or ingress annotation cert-manager.io/cluster-issuer triggers ACME order. cert-manager renews at 2/3 lifetime. Monitor Certificate Ready=False, cert expiry metrics. Use separate issuers for staging vs prod. Backup account key.",
    command: "kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.14.4/cert-manager.yaml\nkubectl get clusterissuer\nkubectl describe certificate api-tls -n prod\nkubectl get certificaterequest -n prod",
    tags: ["kubernetes","ingress","hard"],
  },
  {
    id: "kubernetes-configmaps-hard-27",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize ConfigMap volume propagation at scale (5000+ pods)?",
    answer: "Use immutable ConfigMaps to enable kubelet watch-based updates vs polling. Reduce number of distinct ConfigMaps — shared base config with app-specific overlays. Avoid mounting huge ConfigMaps — use init container or artifact server. Split clusters by blast radius. Monitor apiserver LIST configmaps QPS. Consider node-local cache (Konfig) patterns for read-heavy static config.",
    command: "kubectl get configmaps -A --no-headers | wc -l\nkubectl get --raw /metrics | grep apiserver_request_total | grep configmaps\nkubectl get pods -A -o json | jq '[.items[].spec.volumes[]? | select(.configMap)] | length'",
    tags: ["kubernetes","configmaps","hard"],
  },
  {
    id: "kubernetes-secrets-hard-28",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you handle etcd encryption at rest key rotation?",
    answer: "Configure EncryptionConfiguration with identity + aescbc/kms provider. Rotate: add new key as first in sequence, run kube-apiserver with updated config, run encryption-provider-config-rotation.sh (or manual re-encrypt all secrets via kubectl get/apply). Remove old key after all objects re-encrypted. Test restore from backup after rotation in staging.",
    command: "kubectl get secrets -A -o json | kubectl replace -f -  # triggers re-encrypt\n# Verify encryption\nkubectl get --raw /metrics | grep apiserver_storage_data_key\n# EKS: aws eks associate-encryption-config",
    tags: ["kubernetes","secrets","hard"],
  },
  {
    id: "kubernetes-persistent-volumes-hard-29",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you migrate PersistentVolumes between clusters or storage classes?",
    answer: "Velero backup/restore cross-cluster with matching StorageClass mapping. Or: snapshot PV → create new PVC from VolumeSnapshot in target SC → rsync data → switch app. For live migration, use vendor tools (Portworx, Rook migration). Plan downtime or dual-write window. Validate checksums post-migration.",
    command: "kubectl create volumesnapshot data-snap --source-persistent-volume-claim-name=data-db-0 -n prod\nkubectl get volumesnapshot data-snap -n prod\nvelero restore create migrate --from-backup source-cluster-backup --storage-class-mappings old-sc:new-sc",
    tags: ["kubernetes","persistent-volumes","hard"],
  },
  {
    id: "kubernetes-storage-classes-hard-30",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you implement cross-AZ resilient storage for ReadWriteMany workloads?",
    answer: "Use regional shared filesystem: AWS EFS, Azure Files, GCP Filestore, or Ceph/Rook CephFS. StorageClass with RWX access mode. Accept latency trade-off vs block. For read-heavy, replicate with CDN front. Avoid block RWO stretched across AZs. Test failover: kill all pods in one AZ, verify remount on survivors.",
    command: "kubectl get sc efs-sc -o yaml\nkubectl apply -f pvc-rwx.yaml\nkubectl run test -n prod --image=nginx --overrides='{\"spec\":{\"volumes\":[{\"name\":\"data\",\"persistentVolumeClaim\":{\"claimName\":\"shared-data\"}}],\"containers\":[{\"name\":\"nginx\",\"volumeMounts\":[{\"name\":\"data\",\"mountPath\":\"/usr/share/nginx/html\"}]}]}}'",
    tags: ["kubernetes","storage-classes","hard"],
  },
  {
    id: "kubernetes-namespaces-hard-31",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you automate namespace provisioning via GitOps?",
    answer: "Argo CD ApplicationSet with git generator: directories per tenant/env create Namespace + Quota + RBAC + NetworkPolicy from kustomize overlay. PR review for new tenant onboarding. Sync wave ordering: namespace first, then policies, then apps. Track namespace inventory in git as source of truth.",
    command: "kubectl apply -f applicationset-tenants.yaml\nargocd app list | grep tenant\nkubectl get applications -n argocd\nkustomize build tenants/tenant-a | kubectl apply --dry-run=client -f -",
    tags: ["kubernetes","namespaces","hard"],
  },
  {
    id: "kubernetes-rbac-hard-32",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you implement just-in-time elevated access for production incidents?",
    answer: "Break-glass ClusterRoleBinding created via automation on PagerDuty ack, auto-expires in 1h (CronJob removes binding or use impersonation token). All actions audit logged. Alternative: Teleport/kubernetes access proxy with session recording. Never permanent cluster-admin for on-call. Post-incident review of break-glass usage.",
    command: "kubectl create clusterrolebinding breakglass-jane --clusterrole=cluster-admin --user=jane@corp.com\n# Auto-remove via CronJob or:\nkubectl delete clusterrolebinding breakglass-jane\nkubectl get events -A | grep breakglass",
    tags: ["kubernetes","rbac","hard"],
  },
  {
    id: "kubernetes-helm-hard-33",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you implement Helm chart testing and validation in CI?",
    answer: "helm lint chart/, kubeconform or helm template | kubectl apply --dry-run=server, chart-testing (ct lint-and-install) in kind cluster, helm unittest plugin for template logic tests. Policy check rendered YAML with conftest/kyverno. Pin chart and dependency versions. SBOM and sign charts with cosign for supply chain.",
    command: "helm lint ./charts/api\nhelm template api ./charts/api -f values-prod.yaml | kubeconform -strict -summary\nct lint-and-install --chart-dirs charts/ --all\nhelm unittest ./charts/api",
    tags: ["kubernetes","helm","hard"],
  },
  {
    id: "kubernetes-monitoring-hard-34",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize Prometheus cardinality and scrape performance at scale?",
    answer: "Drop high-cardinality labels via relabel_configs, avoid pod name in alert labels, use recording rules for expensive queries, federation for cross-cluster, sharding Prometheus by namespace/team. Tune scrape intervals (30s default → 60s for non-critical). Limit targets per Prometheus (~10k). Use adaptive metrics (Grafana Cloud) or Mimir for horizontal scale.",
    command: "kubectl exec -n monitoring prometheus-prometheus-0 -- promtool tsdb analyze /prometheus | head -50\nkubectl get prometheus -n monitoring -o yaml | grep -A20 retention\n# Check series count\ncurl -s http://prometheus:9090/api/v1/status/tsdb | jq .data.seriesCountByMetricName | head",
    tags: ["kubernetes","monitoring","hard"],
  },
  {
    id: "kubernetes-scaling-hard-35",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you optimize scale-down behavior to protect SLAs during traffic dips?",
    answer: "HPA behavior.scaleDown.stabilizationWindowSeconds (default 300) prevents flapping. scaleDown policies limit percent/pods removed per period. PDB ensures minimum availability. Pod terminationGracePeriod for connection drain. For CA/Karpenter, delay node removal until pods rescheduled. Monitor scale events correlation with error rate — tune if premature scale-down causes latency spikes.",
    command: "kubectl get hpa api -n prod -o jsonpath='{.spec.behavior.scaleDown}' | jq\nkubectl get pdb -n prod\nkubectl get events -n prod | grep -i Scale\nkubectl describe hpa api -n prod | grep -A15 Conditions",
    tags: ["kubernetes","scaling","hard"],
  },
  {
    id: "kubernetes-troubleshooting-hard-36",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "How would you diagnose intermittent failures that leave no obvious pod status errors?",
    answer: "Intermittent: correlate with deployments, node events, spot interruptions, GC pauses, DNS timeouts. Enable distributed tracing (OpenTelemetry). Capture metrics during failure window (latency histograms, tcp retransmits). Use kubectl debug with tcpdump. Check apiserver audit for admission webhook latency spikes. Chaos engineering to reproduce.",
    command: "kubectl get events -A --sort-by=.lastTimestamp | grep -i \"spot\\|preempt\\|evict\"\nkubectl top pod -n prod -l app=api\nkubectl run tcpdump --rm -it --image=nicolaka/netshoot -n prod -- tcpdump -i any host api.prod.svc.cluster.local\n# Jaeger: search traces with error=true during incident window",
    tags: ["kubernetes","troubleshooting","hard"],
  },
  {
    id: "kubernetes-containers-hard-37",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain container escape scenarios and how Kubernetes mitigates them.",
    answer: "Escapes exploit kernel bugs, misconfigured privileged containers, hostPath mounts to sensitive paths (/var/run/docker.sock, /), excessive capabilities, or shared PID namespace with host. Mitigations: Pod Security Standards (restricted), seccomp RuntimeDefault, AppArmor/SELinux, no privileged/hostNetwork/hostPID, NetworkPolicies limiting lateral movement, regular node patching, and runtime sandboxing (gVisor) for untrusted workloads. Audit with kube-bench, Polaris, and OPA Gatekeeper policies denying risky pod specs at admission.",
    command: "kubectl label namespace prod pod-security.kubernetes.io/enforce=restricted\nkubectl get pods -A -o json | jq '.items[] | select(.spec.hostPID or .spec.hostNetwork or (.spec.containers[].securityContext.privileged)) | {ns: .metadata.namespace, name: .metadata.name}'\n# Run kube-bench on node\nkubectl apply -f https://raw.githubusercontent.com/aquasecurity/kube-bench/main/job.yaml",
    tags: ["kubernetes","containers","hard"],
  },
  {
    id: "kubernetes-docker-hard-38",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain failure scenarios when migrating from Docker Compose to Kubernetes.",
    answer: "Common failures: hardcoded localhost service URLs (must use K8s DNS), missing health probes causing premature traffic, stateful data on local volumes without PVC migration, depends_on semantics absent (use init containers or readiness), single-replica assumptions without PodDisruptionBudgets, and env files becoming ConfigMaps/Secrets. Network: Compose bridge != ClusterIP — verify NetworkPolicies do not block migrated traffic. Plan blue/green deploy with parallel run and traffic cutover.",
    command: "kubectl run curl --rm -it --image=curlimages/curl -- curl -s http://api.prod.svc.cluster.local:8080/health\nkubectl get endpoints api -n prod\nkompose convert -f docker-compose.yml  # starting point, requires manual fixes\nkubectl diff -f k8s/ | less",
    tags: ["kubernetes","docker","hard"],
  },
  {
    id: "kubernetes-kubernetes-architecture-hard-39",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain failure scenarios during a Kubernetes control plane upgrade.",
    answer: "Risks: etcd schema/version skew if nodes upgraded out of order, admission webhook timeouts blocking all creates, API aggregation layer incompatibility, CRD version deprecation breaking operators, and kubelet/API version skew beyond supported N-2 window. Mitigation: read release notes, upgrade etcd first (if external), then control plane, then nodes one AZ at a time. Maintain PodDisruptionBudgets; verify /healthz after each step. Rollback plan: restore etcd snapshot or revert control plane manifests.",
    command: "kubectl version\nkubeadm upgrade plan\nkubectl get nodes -o custom-columns=NAME:.metadata.name,VERSION:.status.nodeInfo.kubeletVersion\nkubectl drain cp-node-1 --ignore-daemonsets --delete-emptydir-data",
    tags: ["kubernetes","kubernetes-architecture","hard"],
  },
  {
    id: "kubernetes-pods-hard-40",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain pod eviction scenarios: node pressure, preemption, and API eviction.",
    answer: "Node pressure: kubelet evicts BestEffort then Burstable exceeding requests when memory/disk/PID pressure (see describe node Conditions). API eviction: kubectl drain/eviction subresource respects PDBs. Preemption: scheduler removes lower-priority pods to schedule pending high-priority pods. Mitigation: set appropriate QoS, PDBs, priorityClasses, and monitor eviction events. Critical apps use PriorityClass system-cluster-critical with caution.",
    command: "kubectl describe node worker-1 | grep -A10 Conditions\nkubectl get events -A | grep Evicted\nkubectl get priorityclass\nkubectl drain worker-1 --ignore-daemonsets --delete-emptydir-data --grace-period=30",
    tags: ["kubernetes","pods","hard"],
  },
  {
    id: "kubernetes-deployments-hard-41",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain failure scenarios when Deployment and HPA interact during rollouts.",
    answer: "HPA may scale up old ReplicaSet during rollout if metrics spike from mixed-version latency. New pods not ready → HPA scales more → resource exhaustion. Mitigation: pause HPA during deploy (remove autoscaler temporarily), use Argo Rollouts with HPA integration, or ensure maxSurge provides enough headroom. PDB minAvailable can block both rollout and scale-down. Test rollouts under load in staging with HPA enabled.",
    command: "kubectl get hpa -n prod\nkubectl describe hpa api -n prod\nkubectl autoscale deployment api -n prod --min=10 --max=50 --cpu-percent=70\n# Pause: kubectl patch hpa api -n prod -p '{\"spec\":{\"minReplicas\":30,\"maxReplicas\":30}}'",
    tags: ["kubernetes","deployments","hard"],
  },
  {
    id: "kubernetes-replicasets-hard-42",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain revision history and orphaned ReplicaSets after failed rollouts.",
    answer: "Deployment keeps old ReplicaSets scaled to 0 for rollback (revisionHistoryLimit). Failed rollout may leave new RS with 0 ready and old RS serving traffic — Deployment condition Progressing=False. Manual intervention: rollout undo or fix new template. Orphan RS (deployment deleted with orphanDependents) continue running unmanaged pods — dangerous drift. Always manage via Deployment; audit with kubectl get rs showing DESIRED > 0 but no owner Deployment.",
    command: "kubectl rollout history deployment/api -n prod\nkubectl get rs -n prod -l app=api\nkubectl get rs -n prod -o json | jq '.items[] | select(.spec.replicas > 0 and (.metadata.ownerReferences | length == 0)) | .metadata.name'\nkubectl rollout undo deployment/api -n prod --to-revision=2",
    tags: ["kubernetes","replicasets","hard"],
  },
  {
    id: "kubernetes-services-hard-43",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain kube-proxy modes and when IPVS or eBPF dataplanes outperform iptables.",
    answer: "iptables: default, O(n) rules per service causing latency at thousands of services. IPVS: hash-based, better performance at scale, supports more LB algorithms. eBPF (Cilium): bypass kube-proxy, direct pod-to-pod with policy enforcement, lower latency. Migration: test rule sync during upgrades, verify health check node ports on LoadBalancer with externalTrafficPolicy Local.",
    command: "kubectl get configmap kube-proxy -n kube-system -o yaml | grep mode\nkubectl exec -n kube-system ds/kube-proxy -- ipvsadm -Ln 2>/dev/null || echo \"iptables mode\"\ncilium status | grep KubeProxyReplacement",
    tags: ["kubernetes","services","hard"],
  },
  {
    id: "kubernetes-ingress-hard-44",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain failure scenarios when Ingress backend Service has no ready endpoints.",
    answer: "Controller returns 502/503 to clients. Causes: all pods failing readiness, selector mismatch, wrong service port, NetworkPolicy blocking controller→pod. Debug: kubectl get endpoints, describe ingress backend, curl from controller pod to service ClusterIP. During rollouts, brief 502s if maxUnavailable > 0 without enough ready replicas — tune rollout and probes.",
    command: "kubectl get endpoints api -n prod\nkubectl exec -n ingress-nginx deploy/ingress-nginx-controller -- curl -sv http://api.prod.svc.cluster.local:8080/health\nkubectl describe ingress api -n prod | grep -A10 \"Backends\"",
    tags: ["kubernetes","ingress","hard"],
  },
  {
    id: "kubernetes-configmaps-hard-45",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain failure scenarios when ConfigMap and Deployment get out of sync.",
    answer: "Operator updates ConfigMap but Deployment unchanged — pods run stale env vars until manual restart. Rolling update mid-config change causes mixed config versions — brief inconsistent behavior. Mitigation: checksum annotation on pod template, atomic config releases (new ConfigMap name + deployment update in single transaction), integration tests verifying config version endpoint. Document config version in /health response.",
    command: "kubectl get deployment app -n prod -o jsonpath='{.spec.template.metadata.annotations}' | jq\nkubectl exec app-pod-1 -n prod -- printenv CONFIG_VERSION\nkubectl exec app-pod-2 -n prod -- printenv CONFIG_VERSION\nkubectl rollout restart deployment/app -n prod",
    tags: ["kubernetes","configmaps","hard"],
  },
  {
    id: "kubernetes-secrets-hard-46",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain failure scenarios when Secret sync fails silently.",
    answer: "ExternalSecret status Degraded but Deployment still references stale K8s Secret — app runs with expired creds until auth failure. ESO IAM misconfiguration stops sync; no alert. Mitigation: monitor externalsecret_status_condition, alert on Secret age > rotation interval, liveness that validates credential validity, integration test post-rotation.",
    command: "kubectl describe externalsecret db-creds -n prod\nkubectl get secret db-creds -n prod -o jsonpath='{.metadata.creationTimestamp}'\nkubectl logs -n external-secrets deploy/external-secrets --tail=30",
    tags: ["kubernetes","secrets","hard"],
  },
  {
    id: "kubernetes-persistent-volumes-hard-47",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain volume expansion and its limitations.",
    answer: "PVC spec.resources.requests.storage can increase if StorageClass allowVolumeExpansion: true and CSI driver supports it. Patch PVC size; controller expands volume and filesystem (if supported). Cannot shrink. Some file systems require pod restart for FS expand. RWX expansion behavior varies by provider. Test expansion in staging; monitor resize conditions.",
    command: "kubectl patch pvc data-db-0 -n prod -p '{\"spec\":{\"resources\":{\"requests\":{\"storage\":\"200Gi\"}}}}'\nkubectl get pvc data-db-0 -n prod -o jsonpath='{.status.conditions}' | jq\nkubectl describe pvc data-db-0 -n prod | grep -i \"resiz\\|expand\"",
    tags: ["kubernetes","persistent-volumes","hard"],
  },
  {
    id: "kubernetes-storage-classes-hard-48",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain CSI snapshot and clone workflows for disaster recovery.",
    answer: "VolumeSnapshotClass defines snapshot driver. Create VolumeSnapshot from PVC; restore to new PVC via dataSource snapshot. Clone PVC with dataSource pvc (same namespace). Snapshots crash-consistent unless app quiesce hook. Automate with Velero or scheduled VolumeSnapshot CRs. Cross-region copy via cloud replication on snapshot.",
    command: "kubectl get volumesnapshotclass\nkubectl apply -f snapshot.yaml\nkubectl get volumesnapshot -n prod\nkubectl apply -f restore-pvc-from-snapshot.yaml",
    tags: ["kubernetes","storage-classes","hard"],
  },
  {
    id: "kubernetes-namespaces-hard-49",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain namespace-scoped vs cluster-scoped resources for platform operators.",
    answer: "Cluster-scoped: Node, PV, StorageClass, ClusterRole, CRD, Namespace — platform team manages. Namespace-scoped: Pod, Service, Deployment, ConfigMap — tenant manages within bounds. Operators may use cluster-scoped CRDs with namespace-scoped custom resources. RBAC separation: tenant admin Role vs platform ClusterRole.",
    command: "kubectl api-resources --namespaced=true -o name | head -20\nkubectl api-resources --namespaced=false -o name | head -20\nkubectl get clusterrolebinding | grep platform\nkubectl get role -n tenant-a",
    tags: ["kubernetes","namespaces","hard"],
  },
  {
    id: "kubernetes-rbac-hard-50",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "Explain privilege escalation paths and how to audit them.",
    answer: "Escalation vectors: create pods with privileged SA, bind cluster-admin RoleBinding, exec into privileged pod, modify kube-system, create ValidatingWebhook that always allows, access secrets with cloud credentials. Audit: rbac-tool viz, who-can --list, restrict escalate verb, Pod Security Standards, deny exec to sensitive namespaces.",
    command: "rbac-tool viz --out-dir /tmp/rbac\nkubectl auth can-i create pods --as=developer -n prod\nkubectl auth can-i create rolebindings --as=developer -n prod\nkubectl get clusterrole -o json | jq '.items[] | select(.rules[]?.verbs[]? == \"escalate\") | .metadata.name'",
    tags: ["kubernetes","rbac","hard"],
  },
  {
    id: "kubernetes-scenario-1",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "[Production Scenario] Pod CrashLoopBackOff: What is your troubleshooting approach?",
    answer: "Detect: alert on pod restart rate or CrashLoopBackOff status. Triage: kubectl get pods, describe pod for exit code and Events, logs --previous for stack trace. Common causes: misconfig (missing env), app bug, failed migrations, probe killing during slow start (check if liveness too aggressive). Mitigate: rollback deployment if post-release, scale unaffected replicas, fix config. Verify: new pods reach Running with restarts=0 for 10min. Prevent: staging soak test, startupProbe for slow apps, CI smoke tests.",
    command: "kubectl get pods -n prod -l app=checkout\nkubectl describe pod checkout-7f8d9-xyz -n prod | grep -A30 Events\nkubectl logs checkout-7f8d9-xyz -n prod --previous --tail=200\nkubectl rollout undo deployment/checkout -n prod  # if post-deploy",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-2",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "[Production Scenario] Node Failure During Peak Traffic: What is your troubleshooting approach?",
    answer: "Detect: node NotReady alert, pod rescheduling spike, error rate increase. Triage: kubectl get nodes, describe failed node (Conditions, Events), check cloud provider instance status. Pods on failed node rescheduled after pod-eviction-timeout (~5m). Mitigate: cordon bad node, verify replacements scheduling, ensure Cluster Autoscaler adds capacity, confirm PDB not blocking. Communicate status page if SLO impacted. Prevent: multi-AZ spread, PDB, over-provisioned node pools, spot diversification.",
    command: "kubectl get nodes\nkubectl describe node ip-10-0-1-45.ec2.internal | grep -A10 Conditions\nkubectl get pods -A -o wide | grep -v Running\nkubectl get events -A | grep -i \"node not ready\\|evict\"\naws ec2 describe-instance-status --instance-ids i-0abc123",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-3",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "[Production Scenario] Service Connectivity Issues: What is your troubleshooting approach?",
    answer: "Detect: 502/503 from Ingress, connection refused errors in app logs. Triage: verify Service endpoints non-empty (kubectl get endpoints), pods Ready, selector labels match, NetworkPolicy allows traffic, DNS resolves (nslookup from client pod). Test ClusterIP directly bypassing Ingress. Mitigate: fix selector mismatch, restart misconfigured pods, adjust NetworkPolicy. Verify: curl from client namespace to service FQDN succeeds.",
    command: "kubectl get svc api -n prod\nkubectl get endpoints api -n prod\nkubectl get pods -n prod -l app=api --show-labels\nkubectl run curl -n frontend --rm -it --image=curlimages/curl -- curl -sv http://api.prod.svc.cluster.local:8080/health\nkubectl get networkpolicy -n prod",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-4",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "[Production Scenario] Ingress TLS Certificate Expiry: What is your troubleshooting approach?",
    answer: "Detect: browser TLS warnings, cert-manager Certificate NotReady alert, ssl_cert_not_after metric < 7 days. Triage: kubectl get certificate, describe for ACME challenge failures, check cert-manager logs, verify DNS/HTTP-01 reachability. Mitigate: manual cert secret update as hotfix, fix ClusterIssuer/Issuer config, renew via cert-manager. Verify: openssl s_client -connect api.example.com:443 | openssl x509 -dates. Prevent: alert at 30/14/7 days, automated renewal monitoring, staging issuer validation.",
    command: "kubectl get certificate -n prod\nkubectl describe certificate api-tls -n prod\nkubectl logs -n cert-manager deploy/cert-manager --tail=50\nkubectl get certificaterequest,order,challenge -n prod\necho | openssl s_client -connect api.example.com:443 2>/dev/null | openssl x509 -noout -dates",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-5",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "[Production Scenario] Persistent Volume Mount Failure: What is your troubleshooting approach?",
    answer: "Detect: pod stuck ContainerCreating, FailedMount events. Triage: describe pod Events (volume attachment, permission, wrong fs type), describe PVC (Bound?), PV nodeAffinity for zone match, CSI driver pod health. Common: volume attached to dead node, SELinux context, subPath directory missing. Mitigate: force detach volume (cloud console or CSI), delete pod to retry mount, fix fsGroup. Verify: pod Running with df showing mount.",
    command: "kubectl describe pod db-0 -n prod | grep -A20 Events\nkubectl get pvc,pv -n prod\nkubectl get volumeattachment\nkubectl logs -n kube-system -l app=ebs-csi-node --tail=30\nkubectl exec db-0 -n prod -- df -h /var/lib/postgresql",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-6",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "[Production Scenario] HPA Not Scaling: What is your troubleshooting approach?",
    answer: "Detect: sustained high CPU/latency but replica count flat. Triage: kubectl describe hpa for conditions (AbleToScale, ScalingActive), verify metrics-server running, pods have resource requests set, current metric vs target. Check maxReplicas not already reached, PDB blocking scale-down not up. Mitigate: fix missing requests, restart metrics-server, adjust target or maxReplicas. Verify: HPA scales within 2 sync periods under load test.",
    command: "kubectl describe hpa api -n prod\nkubectl get apiservice v1beta1.metrics.k8s.io\nkubectl top pods -n prod -l app=api\nkubectl get hpa api -n prod -o yaml | grep -A15 metrics\nkubectl get pods -n prod -l app=api -o json | jq '.items[0].spec.containers[0].resources'",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-7",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "easy",
    question: "[Production Scenario] ConfigMap Not Mounted: What is your troubleshooting approach?",
    answer: "Detect: app using defaults/wrong config, CreateContainerConfigError, missing file in container. Triage: verify ConfigMap exists in same namespace, key names match volume items, volumeMount path correct, describe pod Mounts/Volumes section. Env-based config requires pod restart after ConfigMap change. Mitigate: fix ConfigMap keys, apply corrected deployment, rollout restart. Verify: kubectl exec cat /etc/config/app.yaml shows expected content.",
    command: "kubectl get configmap app-config -n prod -o yaml\nkubectl describe pod app-abc -n prod | grep -A15 \"Mounts:\\|Volumes:\"\nkubectl exec app-abc -n prod -- ls -la /etc/config/\nkubectl get events -n prod | grep -i configmap\nkubectl rollout restart deployment/app -n prod",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-8",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "[Production Scenario] Secret Rotation Breaks App: What is your troubleshooting approach?",
    answer: "Detect: auth errors spike after secret update timestamp. Triage: compare Secret resourceVersion vs pod start time, check if app reloads mounted secrets or caches at startup, ESO sync status. Mitigate: immediate rollout restart to pick up new secret, temporarily restore dual-valid credentials at DB level, rollback secret if bad value. Verify: app connects with new credentials, no auth errors for 15min. Prevent: Reloader annotation, app SIGHUP reload, rotation runbook with overlap window.",
    command: "kubectl get secret db-creds -n prod -o jsonpath='{.metadata.resourceVersion}{\" \"}{.metadata.creationTimestamp}{\"\\n\"}'\nkubectl get pods -n prod -l app=api -o jsonpath='{range .items[*]}{.metadata.name}{\" started \"}{.status.startTime}{\"\\n\"}{end}'\nkubectl rollout restart deployment/api -n prod\nkubectl logs -l app=api -n prod | grep -i \"auth\\|password\\|denied\" | tail -20",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-9",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "[Production Scenario] NetworkPolicy Blocking Traffic: What is your troubleshooting approach?",
    answer: "Detect: connection timeouts between known services after policy deploy. Triage: list NetworkPolicies in source and dest namespaces, identify default-deny policies, check if egress/ingress rules allow required ports and namespace/pod selectors, DNS (kube-system port 53 UDP/TCP). Mitigate: add explicit allow rule for required flow, or temporarily remove policy (emergency). Verify: netshoot curl between namespaces. Prevent: policy testing in staging, network policy simulator (cilium hubble, konfigure).",
    command: "kubectl get networkpolicy -n prod\nkubectl get networkpolicy -n frontend -o yaml\nkubectl run test -n frontend --rm -it --image=nicolaka/netshoot -- curl -sv --max-time 5 http://api.backend.svc.cluster.local:8080\nkubectl describe networkpolicy default-deny -n prod\n# Cilium: cilium hubble observe --namespace prod",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-10",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "[Production Scenario] Deployment Rollout Stuck: What is your troubleshooting approach?",
    answer: "Detect: kubectl rollout status timeout, mixed ReplicaSet replica counts, ProgressDeadlineExceeded condition. Triage: get rs showing new RS with 0 ready, describe deployment, check new pod failures (image, probes, quota), PDB minAvailable blocking. Mitigate: fix failing pod issue or rollout undo, temporarily adjust maxUnavailable, resolve quota. Verify: all replicas on new RS, rollout status success. Prevent: --atomic helm upgrades, progressDeadlineSeconds alerting, pre-deploy resource checks.",
    command: "kubectl rollout status deployment/api -n prod --timeout=2m\nkubectl get rs -n prod -l app=api\nkubectl describe deployment api -n prod | grep -A10 Conditions\nkubectl get pods -n prod -l app=api | grep -v \"Running\\|Completed\"\nkubectl rollout undo deployment/api -n prod  # if needed",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-11",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "[Production Scenario] StatefulSet Pod Ordering Failure: What is your troubleshooting approach?",
    answer: "Detect: pod-N stuck Pending/NotReady blocking pod-N+1 creation. Triage: describe failing pod-N (scheduling, PVC binding, readiness), check orderedReady policy, headless Service exists, PVC status for data-pod-N. Mitigate: fix pod-0 issue first (database bootstrap, PVC), never skip ordering by manual pod create. Verify: pods 0..N all Running and Ready sequentially. Prevent: robust readiness on pod-0, adequate zone/storage for PVCs.",
    command: "kubectl get statefulset db -n prod\nkubectl get pods -n prod -l app=db --sort-by=.metadata.name\nkubectl describe pod db-0 -n prod\nkubectl get pvc -n prod | grep data-db\nkubectl logs db-0 -n prod --tail=50",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-12",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "[Production Scenario] Resource Quota Exceeded: What is your troubleshooting approach?",
    answer: "Detect: FailedCreate events, pods Pending, HPA unable to scale, deploy pipeline failures. Triage: kubectl describe resourcequota showing USED vs HARD limits (pods, cpu, memory, pvc count). Identify resource hog (batch job, runaway HPA). Mitigate: delete unnecessary resources, temporary quota increase via platform team, optimize pod requests. Verify: new pods schedule successfully. Prevent: quota alerts at 80%, LimitRange defaults, regular capacity review.",
    command: "kubectl describe resourcequota -n prod\nkubectl get events -n prod | grep -i \"quota\\|exceeded\\|Forbidden\"\nkubectl top pods -n prod --sort-by=memory | head -15\nkubectl get pods -n prod --field-selector status.phase=Pending",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-13",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "[Production Scenario] ImagePullBackOff in Production: What is your troubleshooting approach?",
    answer: "Detect: deployment rollout stuck, pods ImagePullBackOff/ErrImagePull. Triage: describe pod Events for 401/404/rate limit, verify image tag exists in registry, imagePullSecrets on SA/pod, registry reachable from nodes. Mitigate: fix tag/digest, update pull secret, rollback to known-good image, use cached digest on nodes. Verify: pods Pulled and Running. Prevent: deploy by digest, registry mirror, CI verify image exists before deploy.",
    command: "kubectl describe pod api-new-xyz -n prod | grep -A15 Events\nkubectl get sa default -n prod -o yaml | grep imagePullSecrets\nkubectl run pull-test --rm -it --image=alpine --overrides='{\"spec\":{\"containers\":[{\"name\":\"t\",\"image\":\"myregistry.io/api:bad-tag\"}]}}' 2>&1 | head\nkubectl set image deployment/api api=myregistry.io/api@sha256:knowngood -n prod",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-14",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "medium",
    question: "[Production Scenario] DNS Resolution Failure Inside Cluster: What is your troubleshooting approach?",
    answer: "Detect: NXDOMAIN or timeout errors in app logs for *.svc.cluster.local names. Triage: check CoreDNS pods Running in kube-system, kubectl logs on coredns, test nslookup from netshoot pod, verify Service exists, check ndots and search path in pod resolv.conf, NetworkPolicy blocking UDP/TCP 53 to kube-dns. Mitigate: restart CoreDNS, scale CoreDNS replicas, fix upstream forward config, add DNS allow NetworkPolicy. Verify: nslookup resolves all critical services.",
    command: "kubectl get pods -n kube-system -l k8s-app=kube-dns\nkubectl logs -n kube-system -l k8s-app=kube-dns --tail=30\nkubectl run dns-test -n prod --rm -it --image=busybox:1.36 -- nslookup api.prod.svc.cluster.local\nkubectl exec app-pod -n prod -- cat /etc/resolv.conf\nkubectl get svc kube-dns -n kube-system",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-15",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "[Production Scenario] Helm Release Upgrade Failure: What is your troubleshooting approach?",
    answer: "Detect: helm upgrade fails, release status pending-upgrade/failed, partial resource updates. Triage: helm status/history, --debug dry-run for template errors, hook Job failures, resource conflicts, CRD version mismatch, admission webhook rejections. Mitigate: helm rollback to last good revision, fix chart values, --force only if safe. Verify: helm status deployed, all pods ready, smoke tests pass. Prevent: helm diff in CI, --atomic --wait in prod, chart lint and kubeconform.",
    command: "helm status api -n prod\nhelm history api -n prod\nhelm upgrade api ./charts/api -n prod -f values-prod.yaml --debug --dry-run 2>&1 | tail -50\nkubectl get jobs -n prod -l helm.sh/hook\nhelm rollback api -n prod",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-16",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "[Production Scenario] Node Disk Pressure Evictions: What is your troubleshooting approach?",
    answer: "Detect: Evicted pods, node DiskPressure=True, imagefs/containerfs threshold exceeded alerts. Triage: describe node Conditions, identify disk usage (images, logs, emptyDir), kubectl get pods --field-selector status.phase=Failed. Mitigate: clean unused images (crictl rmi), increase disk or add nodes, reduce log volume, fix app writing unbounded emptyDir. Verify: node condition clears, evicted pods rescheduled. Prevent: monitoring node disk, log rotation, image GC tuning, ephemeral storage limits on pods.",
    command: "kubectl describe node worker-2 | grep -A10 Conditions\nkubectl get pods -A --field-selector status.reason=Evicted | wc -l\nkubectl get events -A | grep -i \"disk pressure\\|evict\"\n# On node: sudo crictl images | wc -l && sudo crictl rmi --prune\nkubectl get pods -n prod -o json | jq '.items[] | select(.spec.containers[].resources.limits.\"ephemeral-storage\" == null) | .metadata.name'",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-17",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "[Production Scenario] Liveness Probe Killing Pods: What is your troubleshooting approach?",
    answer: "Detect: restart count climbing without deploy changes, Unhealthy liveness probe events, correlation with load spikes or GC pauses. Triage: describe pod probe config (timeout, periodSeconds, failureThreshold), check if /health checks downstream deps, compare with startup/readiness probes. Mitigate: fix probe endpoint to lightweight check, increase timeout/threshold, add startupProbe, rollout fixed deployment. Verify: restarts stable under peak load test. Prevent: probe standards in platform docs, CI lint rejecting dependency checks in liveness.",
    command: "kubectl describe pod api-xyz -n prod | grep -A15 \"Liveness\\|Events\"\nkubectl get pods -n prod -l app=api -o json | jq '.items[] | {name: .metadata.name, restarts: .status.containerStatuses[0].restartCount}'\nkubectl logs api-xyz -n prod --previous | tail -30\nkubectl patch deployment api -n prod --type=json -p='[{\"op\":\"replace\",\"path\":\"/spec/template/spec/containers/0/livenessProbe/timeoutSeconds\",\"value\":5}]'",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-18",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "[Production Scenario] RBAC Forbidden for CI ServiceAccount: What is your troubleshooting approach?",
    answer: "Detect: CI/CD pipeline fails with \"Forbidden\" creating/updating resources. Triage: reproduce with kubectl auth can-i --as=system:serviceaccount:ci:deployer, review RoleBinding and Role rules for missing verbs/resources/apiGroups, check if deploying to wrong namespace. Mitigate: add required permissions to Role (not cluster-admin), bind SA to Role in target namespace. Verify: pipeline dry-run succeeds, can-i returns yes. Prevent: RBAC-as-code in git, least privilege review in PR, periodic rbac-tool audit.",
    command: "kubectl auth can-i create deployments --as=system:serviceaccount:ci:deployer -n prod\nkubectl auth can-i patch deployments --as=system:serviceaccount:ci:deployer -n prod\nkubectl describe rolebinding ci-deploy -n prod\nkubectl get role deployer -n prod -o yaml\nkubectl auth can-i --list --as=system:serviceaccount:ci:deployer -n prod",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-19",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "[Production Scenario] PVC Binding Stuck Pending: What is your troubleshooting approach?",
    answer: "Detect: pod Pending, PVC Pending, provisioning timeout alerts. Triage: describe PVC Events (no StorageClass, provisioner error, zone mismatch with WaitForFirstConsumer, quota, IAM permission for CSI). Check StorageClass exists, CSI controller logs, cloud volume limits. Mitigate: fix SC reference, correct IAM/IRSA for EBS CSI, increase quota, manually create PV for static provisioning. Verify: PVC Bound, pod schedules. Prevent: SC validation in CI, CSI health monitoring, default SC documentation.",
    command: "kubectl describe pvc data-app-0 -n prod\nkubectl get storageclass\nkubectl logs -n kube-system deploy/ebs-csi-controller --tail=40\nkubectl get events -n prod | grep -i \"provisioning\\|FailedBinding\"\naws iam simulate-principal-policy --policy-source-arn arn:aws:iam::123:role/ebs-csi --action-names ec2:CreateVolume",
    tags: ["kubernetes","production-scenario","scenario"],
  },
  {
    id: "kubernetes-scenario-20",
    technology: "kubernetes",
    category: "kubernetes",
    difficulty: "hard",
    question: "[Production Scenario] Cluster Autoscaler Not Adding Nodes: What is your troubleshooting approach?",
    answer: "Detect: pods Pending FailedScheduling insufficient resources, node count static during load. Triage: CA logs (scale-up unneeded, max nodes reached, ASG at max, taints/tolerations mismatch, pod too large for any instance type, unschedulable due to affinity not autoscaler issue). Check node group max size, IAM for CA, expander config. Mitigate: increase max nodes, add instance types, fix pod resource requests, manual node group scale. Verify: CA log \"Scale-up\" and new node Ready, pods scheduled. Prevent: regular load tests, CA metrics dashboard, headroom node pool.",
    command: "kubectl get pods -A --field-selector status.phase=Pending\nkubectl logs -n kube-system deploy/cluster-autoscaler --tail=50\nkubectl describe pod pending-large -n prod | grep -A10 Events\nkubectl get nodes -o custom-columns=NAME:.metadata.name,TAINTS:.spec.taints\naws autoscaling describe-auto-scaling-groups --auto-scaling-group-names eks-prod-ng --query \"AutoScalingGroups[0].[MinSize,MaxSize,DesiredCapacity]\"",
    tags: ["kubernetes","production-scenario","scenario"],
  }
];
